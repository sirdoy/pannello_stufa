---
phase: 86-netatmo-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/netatmoProxy.ts
  - .env.local
  - .env.example
autonomous: true
requirements: [API-07, API-08]
must_haves:
  truths:
    - "All 20+ convenience wrappers in netatmoProxy.ts call haGet/haPost instead of netatmoProxyGet/netatmoProxyPost"
    - "Binary endpoint getProxyCameraEventSnapshot reads HA_API_URL and HA_API_KEY instead of NETATMO_PROXY_URL and NETATMO_PROXY_API_KEY"
    - "No reference to NETATMO_PROXY_URL or NETATMO_PROXY_API_KEY exists in netatmoProxy.ts"
    - "All endpoint paths include /api/v1/netatmo/ prefix"
    - "HA_API_URL and HA_API_KEY are present in .env.local"
  artifacts:
    - path: "lib/netatmoProxy.ts"
      provides: "Migrated Netatmo proxy client using haGet/haPost transport"
      contains: "import { haGet, haPost } from"
    - path: ".env.local"
      provides: "HA_API_URL and HA_API_KEY env vars"
      contains: "HA_API_URL="
  key_links:
    - from: "lib/netatmoProxy.ts"
      to: "lib/haClient.ts"
      via: "import { haGet, haPost }"
      pattern: "haGet<.*>\\('/api/v1/netatmo/"
---

<objective>
Migrate netatmoProxy.ts transport layer from NETATMO_PROXY_URL/NETATMO_PROXY_API_KEY to shared haGet/haPost from haClient.ts, and add HA_API_URL/HA_API_KEY env vars to .env.local.

Purpose: Replace duplicated transport logic with the shared HA client (Phase 84), matching the Fritz!Box migration pattern (Phase 85). All convenience wrappers remain the public API for routes.
Output: Migrated netatmoProxy.ts, updated .env.local and .env.example.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-netatmo-migration/86-CONTEXT.md
@.planning/phases/86-netatmo-migration/86-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/haClient.ts:
```typescript
export async function haGet<T>(endpoint: string, options: HaRequestOptions = {}): Promise<T>;
export async function haPost<T>(endpoint: string, body: Record<string, unknown>, options: HaRequestOptions = {}): Promise<T>;
```

From lib/fritzbox/fritzboxClient.ts (reference pattern):
```typescript
import { haGet } from '@/lib/haClient';
// Each function calls haGet directly with full path:
async function getDevices() { return haGet<...>('/api/v1/devices'); }
```

From types/netatmoProxy.ts (types to keep importing):
```typescript
NetatmoProxyHomestatusResponse, NetatmoProxyHomesdataResponse,
SetRoomThermpointRequest, SetThermmodeRequest, SetThermmodeResponse,
SwitchHomeScheduleRequest, ProxyControlResponse,
CameraStatusResponse, CameraStreamResponse, CameraSnapshotUrlResponse,
SetMonitoringRequest, SetMonitoringResponse, CameraEventsResponse,
ValveStatusResponse, CalibrateBatchResponse, NetatmoHealthResponse,
RoomMeasureResponse
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate netatmoProxy.ts transport to haGet/haPost</name>
  <files>lib/netatmoProxy.ts</files>
  <read_first>
    - lib/netatmoProxy.ts (current implementation — 451 lines with transport + 20 wrappers + binary endpoint)
    - lib/haClient.ts (target transport — haGet/haPost signatures)
    - lib/fritzbox/fritzboxClient.ts (reference pattern — thin wrappers calling haGet directly)
    - types/netatmoProxy.ts (type imports to preserve)
  </read_first>
  <action>
Rewrite `lib/netatmoProxy.ts` with these exact changes:

1. **Replace imports:** Remove `RFC9457ProblemDetail` from the `@/types/netatmoProxy` import. Add `import { haGet, haPost } from '@/lib/haClient';`. Keep all other type imports from `@/types/netatmoProxy` unchanged. Remove the `ApiError, ERROR_CODES, HTTP_STATUS` import ONLY if no longer used (keep it for the binary endpoint which still needs it).

2. **Delete `netatmoProxyGet` function entirely** (lines 56-141). Delete `netatmoProxyPost` function entirely (lines 152-243). Delete the `DEFAULT_TIMEOUT_MS` constant ONLY if not used by the binary endpoint (it IS used — keep it).

3. **Update all JSON convenience wrappers** to call `haGet`/`haPost` directly with the `/api/v1/netatmo/` prefix. The old `NETATMO_PROXY_URL` was `https://pdupun8zpr7exw43.myfritz.net/api/v1/netatmo` so wrappers called e.g. `/homestatus`. The new `HA_API_URL` is `https://pdupun8zpr7exw43.myfritz.net` (root), so wrappers must call `/api/v1/netatmo/homestatus`. Exact mappings:

   - `getProxyHomestatus()` → `haGet<NetatmoProxyHomestatusResponse>('/api/v1/netatmo/homestatus')`
   - `getProxyHomesdata()` → `haGet<NetatmoProxyHomesdataResponse>('/api/v1/netatmo/homesdata')`
   - `proxySetRoomThermpoint(body)` → `haPost<ProxyControlResponse>('/api/v1/netatmo/setroomthermpoint', body as unknown as Record<string, unknown>)`
   - `proxySetThermMode(body)` → `haPost<SetThermmodeResponse>('/api/v1/netatmo/setthermmode', body as unknown as Record<string, unknown>)`
   - `proxySwitchHomeSchedule(body)` → `haPost<ProxyControlResponse>('/api/v1/netatmo/switchhomeschedule', body as unknown as Record<string, unknown>)`
   - `proxySyncHomeSchedule(body)` → `haPost<ProxyControlResponse>('/api/v1/netatmo/synchomeschedule', body)`
   - `proxyCreateNewHomeSchedule(body)` → `haPost<ProxyControlResponse>('/api/v1/netatmo/createnewhomeschedule', body)`
   - `getProxyCameraStatus()` → `haGet<CameraStatusResponse>('/api/v1/netatmo/camera/status')`
   - `getProxyCameraStream(cameraId)` → `haGet<CameraStreamResponse>('/api/v1/netatmo/camera/${cameraId}/stream')`  (template literal)
   - `getProxyCameraSnapshot(cameraId)` → `haGet<CameraSnapshotUrlResponse>('/api/v1/netatmo/camera/${cameraId}/snapshot')` (template literal)
   - `proxySetCameraMonitoring(cameraId, body)` → `haPost<SetMonitoringResponse>('/api/v1/netatmo/camera/${cameraId}/monitoring', body as unknown as Record<string, unknown>)` (template literal)
   - `getProxyCameraEvents(hours?)` → `haGet<CameraEventsResponse>(hours !== undefined ? '/api/v1/netatmo/camera/events?hours=${hours}' : '/api/v1/netatmo/camera/events')` (template literal for hours variant)
   - `getProxyValves()` → `haGet<ValveStatusResponse>('/api/v1/netatmo/valves')`
   - `proxyCalibrateValves()` → `haPost<CalibrateBatchResponse>('/api/v1/netatmo/valves/calibrate', {})`
   - `getProxyHealth()` → `haGet<NetatmoHealthResponse>('/api/v1/netatmo/health')`

4. **Update `getProxyCameraEventSnapshot` binary endpoint** (lines 411-450):
   - Change `process.env.NETATMO_PROXY_URL` → `process.env.HA_API_URL`
   - Change `process.env.NETATMO_PROXY_API_KEY` → `process.env.HA_API_KEY`
   - Change error message from `'Netatmo proxy not configured: missing ${missing}'` where missing references `'NETATMO_PROXY_URL'` / `'NETATMO_PROXY_API_KEY'` → `'HA_API_URL'` / `'HA_API_KEY'`
   - Change the URL from `` `${baseUrl}/camera/events/${eventId}/snapshot` `` → `` `${baseUrl}/api/v1/netatmo/camera/events/${eventId}/snapshot` ``
   - Change error messages from `'Netatmo proxy timeout'` → `'HA proxy timeout'` and `'Netatmo proxy request failed'` → `'HA proxy request failed'`
   - Keep the `ApiError, ERROR_CODES, HTTP_STATUS` import and `DEFAULT_TIMEOUT_MS` constant for this function.

5. **Add a new convenience wrapper** for getroommeasure (used by one route that currently imports `netatmoProxyGet` directly):
   ```typescript
   export async function getProxyRoomMeasure(params: URLSearchParams): Promise<RoomMeasureResponse> {
     return haGet<RoomMeasureResponse>(`/api/v1/netatmo/getroommeasure?${params.toString()}`);
   }
   ```
   Add `RoomMeasureResponse` to the type import from `@/types/netatmoProxy`.

6. **Update the module JSDoc comment** at the top: change "NETATMO_PROXY_URL" and "NETATMO_PROXY_API_KEY" references to note this module now uses haGet/haPost from haClient.ts. Keep it brief.

7. **Do NOT export `netatmoProxyGet` or `netatmoProxyPost`** — they are deleted. All routes import convenience wrappers, not transport functions.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -c 'NETATMO_PROXY_URL\|NETATMO_PROXY_API_KEY' lib/netatmoProxy.ts | grep '^0$' && grep -c "haGet\|haPost" lib/netatmoProxy.ts | grep -v '^0$' && grep -c '/api/v1/netatmo/' lib/netatmoProxy.ts | grep -v '^0$' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/netatmoProxy.ts` contains `import { haGet, haPost } from '@/lib/haClient'`
    - `lib/netatmoProxy.ts` does NOT contain `NETATMO_PROXY_URL` or `NETATMO_PROXY_API_KEY` anywhere
    - `lib/netatmoProxy.ts` does NOT contain `export async function netatmoProxyGet` or `export async function netatmoProxyPost`
    - `lib/netatmoProxy.ts` contains `'/api/v1/netatmo/homestatus'` in getProxyHomestatus
    - `lib/netatmoProxy.ts` contains `'/api/v1/netatmo/camera/events/${eventId}/snapshot'` in getProxyCameraEventSnapshot
    - `lib/netatmoProxy.ts` contains `process.env.HA_API_URL` and `process.env.HA_API_KEY` in getProxyCameraEventSnapshot
    - `lib/netatmoProxy.ts` exports `getProxyRoomMeasure` function
    - `lib/netatmoProxy.ts` still imports `RoomMeasureResponse` from `@/types/netatmoProxy`
    - `lib/netatmoProxy.ts` does NOT import `RFC9457ProblemDetail` from types
    - All 20+ convenience wrapper exports are preserved (getProxyHomestatus, getProxyHomesdata, proxySetRoomThermpoint, etc.)
  </acceptance_criteria>
  <done>All convenience wrappers call haGet/haPost with /api/v1/netatmo/ prefix. Binary endpoint uses HA_API_URL/HA_API_KEY. netatmoProxyGet/netatmoProxyPost deleted. New getProxyRoomMeasure wrapper added.</done>
</task>

<task type="auto">
  <name>Task 2: Add HA env vars to .env.local and update .env.example</name>
  <files>.env.local, .env.example</files>
  <read_first>
    - .env.local (current env vars — confirm NETATMO_PROXY_URL and NETATMO_PROXY_API_KEY exist, confirm HA_API_URL/HA_API_KEY do NOT exist)
    - .env.example (template — has NETATMO_PROXY_URL and NETATMO_PROXY_API_KEY placeholders)
  </read_first>
  <action>
1. **Edit `.env.local`:**
   - Add these two lines (same API key since they share the same proxy, base URL is root without provider prefix):
     ```
     HA_API_URL=https://pdupun8zpr7exw43.myfritz.net
     HA_API_KEY=_yNsO3kyqxO47_Nj5H-ADJEglQ3Bk88LmceVHDepz3A
     ```
   - Remove these two lines:
     ```
     NETATMO_PROXY_URL="https://pdupun8zpr7exw43.myfritz.net/api/v1/netatmo"
     NETATMO_PROXY_API_KEY="_yNsO3kyqxO47_Nj5H-ADJEglQ3Bk88LmceVHDepz3A"
     ```
   - Place the HA_API_URL/HA_API_KEY lines where the old NETATMO_PROXY lines were, or near other API configuration vars.

2. **Edit `.env.example`:**
   - Replace the two Netatmo proxy placeholder lines:
     ```
     NETATMO_PROXY_URL=http://your-proxy-host:port/api/v1/netatmo
     NETATMO_PROXY_API_KEY=your-proxy-api-key
     ```
     with:
     ```
     HA_API_URL=https://your-ha-proxy-host
     HA_API_KEY=your-ha-api-key
     ```
   - If HA_API_URL/HA_API_KEY already exist elsewhere in .env.example, just remove the old NETATMO lines.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -q 'HA_API_URL=' .env.local && grep -q 'HA_API_KEY=' .env.local && ! grep -q 'NETATMO_PROXY_URL' .env.local && ! grep -q 'NETATMO_PROXY_API_KEY' .env.local && grep -q 'HA_API_URL=' .env.example && ! grep -q 'NETATMO_PROXY_URL' .env.example && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.local` contains `HA_API_URL=https://pdupun8zpr7exw43.myfritz.net`
    - `.env.local` contains `HA_API_KEY=_yNsO3kyqxO47_Nj5H-ADJEglQ3Bk88LmceVHDepz3A`
    - `.env.local` does NOT contain `NETATMO_PROXY_URL` or `NETATMO_PROXY_API_KEY`
    - `.env.example` contains `HA_API_URL=` placeholder
    - `.env.example` contains `HA_API_KEY=` placeholder
    - `.env.example` does NOT contain `NETATMO_PROXY_URL` or `NETATMO_PROXY_API_KEY`
  </acceptance_criteria>
  <done>HA_API_URL and HA_API_KEY are present in .env.local with correct values. Old NETATMO_PROXY vars removed from both .env.local and .env.example.</done>
</task>

</tasks>

<verification>
- `grep -c 'NETATMO_PROXY' lib/netatmoProxy.ts` returns 0
- `grep -c 'haGet\|haPost' lib/netatmoProxy.ts` returns 15+ (one per wrapper)
- `grep -c '/api/v1/netatmo/' lib/netatmoProxy.ts` returns 15+ (one per endpoint)
- `grep 'HA_API_URL' .env.local` returns the correct URL
- All convenience wrappers remain exported from netatmoProxy.ts
</verification>

<success_criteria>
- netatmoProxy.ts uses haGet/haPost for all JSON endpoints and HA_API_URL/HA_API_KEY for binary endpoint
- No reference to NETATMO_PROXY_URL or NETATMO_PROXY_API_KEY in netatmoProxy.ts
- .env.local has HA_API_URL and HA_API_KEY, no old NETATMO vars
- .env.example updated with HA placeholders
</success_criteria>

<output>
After completion, create `.planning/phases/86-netatmo-migration/86-01-SUMMARY.md`
</output>
