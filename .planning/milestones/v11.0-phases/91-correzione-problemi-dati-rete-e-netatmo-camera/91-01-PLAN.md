---
phase: 91-correzione-problemi-dati-rete-e-netatmo-camera
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/debug/camera-snapshot-live-broken.md
  - .planning/debug/topology-not-ready-schedules.md
  - .planning/phases/91-correzione-problemi-dati-rete-e-netatmo-camera/91-01-SUMMARY.md
autonomous: false
requirements:
  - CAM-01
  - CAM-02
  - CAM-03
  - SCHED-01
  - ROOM-01

must_haves:
  truths:
    - "All 72 camera tests pass (snapshot redirect, stream states, error handlers)"
    - "All 14 hook tests pass (useScheduleData retry, useRoomStatus retry)"
    - "Debug files are marked as resolved (not awaiting_human_verify)"
    - "Camera snapshot loads in browser via 302 redirect"
    - "Schedule page handles 503 gracefully during proxy warm-up"
  artifacts:
    - path: ".planning/phases/91-correzione-problemi-dati-rete-e-netatmo-camera/91-01-SUMMARY.md"
      provides: "Formal documentation of debug fixes"
      contains: "d33d210"
    - path: ".planning/debug/camera-snapshot-live-broken.md"
      provides: "Debug session closure"
      contains: "status: resolved"
    - path: ".planning/debug/topology-not-ready-schedules.md"
      provides: "Debug session closure"
      contains: "status: resolved"
  key_links:
    - from: "app/api/netatmo/camera/snapshot/route.ts"
      to: "Netatmo CDN"
      via: "302 redirect"
      pattern: "NextResponse\\.redirect"
    - from: "lib/hooks/useScheduleData.ts"
      to: "/api/netatmo/schedules"
      via: "503 retry logic"
      pattern: "SERVICE_UNAVAILABLE"
---

<objective>
Formalize and verify the bug fixes from commit d33d210 (camera snapshot/live + schedule/room 503 retry).

Purpose: All code fixes are already committed. This plan documents the work formally, runs test verification, updates debug file statuses to resolved, and includes a human verification checkpoint for browser-level confirmation.

Output: SUMMARY.md documenting the fixes, debug files marked resolved, human confirmation of browser behavior.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-correzione-problemi-dati-rete-e-netatmo-camera/91-RESEARCH.md
@.planning/debug/camera-snapshot-live-broken.md
@.planning/debug/topology-not-ready-schedules.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify tests and document fixes formally</name>
  <files>
    .planning/debug/camera-snapshot-live-broken.md,
    .planning/debug/topology-not-ready-schedules.md,
    .planning/phases/91-correzione-problemi-dati-rete-e-netatmo-camera/91-01-SUMMARY.md
  </files>
  <read_first>
    .planning/debug/camera-snapshot-live-broken.md,
    .planning/debug/topology-not-ready-schedules.md,
    .planning/phases/91-correzione-problemi-dati-rete-e-netatmo-camera/91-RESEARCH.md,
    app/api/netatmo/camera/snapshot/route.ts,
    lib/hooks/useScheduleData.ts,
    lib/hooks/useRoomStatus.ts
  </read_first>
  <action>
1. Run the targeted test suite to confirm all 86 tests pass:
   ```bash
   npx jest --testPathPattern="camera|useScheduleData|useRoomStatus" --no-coverage
   ```

2. Update `.planning/debug/camera-snapshot-live-broken.md`:
   - Change frontmatter `status: awaiting_human_verify` to `status: resolved`
   - Add `resolved: 2026-03-18` to frontmatter

3. Update `.planning/debug/topology-not-ready-schedules.md`:
   - Change frontmatter `status: awaiting_human_verify` to `status: resolved`
   - Add `resolved: 2026-03-18` to frontmatter

4. Create `.planning/phases/91-correzione-problemi-dati-rete-e-netatmo-camera/91-01-SUMMARY.md` with:
   - Title: "Phase 91 Plan 01: Verify and Document Debug Fixes"
   - Reference commit d33d210 as the implementation commit
   - List all 11 files changed across both debug sessions:
     - app/api/netatmo/camera/snapshot/route.ts (302 redirect instead of binary proxy)
     - app/api/netatmo/camera/route.ts (DELETED — Turbopack alias conflict)
     - app/components/devices/camera/CameraCard.tsx (streamLoading + streamError states)
     - app/(pages)/camera/CameraDashboard.tsx (snapshotErrors + stream loading/error)
     - __tests__/app/api/netatmo/camera/snapshot.test.ts (rewritten for redirect)
     - __tests__/app/api/netatmo/camera/stream.test.ts (new test file)
     - lib/hooks/useScheduleData.ts (503 retry: MAX_RETRIES=5, RETRY_DELAY_MS=3000)
     - lib/hooks/useRoomStatus.ts (503 retry: same pattern)
     - lib/hooks/__tests__/useScheduleData.test.ts (7 tests)
     - lib/hooks/__tests__/useRoomStatus.test.ts (7 tests)
     - docs/camera-proxy-requirements.md (proxy-side issues documented)
   - Document test results: 72 camera + 14 hook = 86 tests passing
   - Note remaining proxy-side issues (NOT Next.js bugs):
     - Camera live snapshot: proxy returns 503 when vpn_url unavailable
     - Camera event snapshots: SAS tokens expire ~5 min after proxy caches
     - These require HA proxy changes, documented in docs/camera-proxy-requirements.md
  </action>
  <verify>
    <automated>npx jest --testPathPattern="camera|useScheduleData|useRoomStatus" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - grep "status: resolved" .planning/debug/camera-snapshot-live-broken.md returns a match
    - grep "status: resolved" .planning/debug/topology-not-ready-schedules.md returns a match
    - .planning/phases/91-correzione-problemi-dati-rete-e-netatmo-camera/91-01-SUMMARY.md exists and contains "d33d210"
    - 91-01-SUMMARY.md contains "302 redirect"
    - 91-01-SUMMARY.md contains "SERVICE_UNAVAILABLE"
    - 91-01-SUMMARY.md contains "86 tests" or lists "72 camera" and "14 hook"
    - All targeted tests pass (exit code 0)
  </acceptance_criteria>
  <done>Debug files marked resolved, SUMMARY.md documents all fixes from d33d210 with test counts, all 86 tests confirmed green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification of camera and schedule fixes</name>
  <files>none (verification only)</files>
  <action>
Present the user with browser verification steps for the two debug fixes committed at d33d210.
No code changes — this is a human verification checkpoint.

Camera fixes to verify:
- Snapshot route issues 302 redirect to Netatmo CDN (was server-side binary proxy)
- CameraCard shows loading spinner during stream fetch, error message on failure
- CameraDashboard shows "N/D" fallback on snapshot errors, stream loading/error states

Schedule/thermostat fixes to verify:
- useScheduleData retries up to 5 times on 503 SERVICE_UNAVAILABLE (3s delay)
- useRoomStatus has identical retry logic
- UI stays in loading state during retries (no error flash)

Verification steps:
1. Navigate to /camera — thumbnails load or show "Snapshot non disponibile" (not broken image icon)
2. Click "Live" on a camera — loading spinner appears, then stream or error message
3. Navigate to /schedule — loading state during proxy warm-up, schedules load when ready
4. Navigate to /network — bandwidth charts, WAN status, device list all render
  </action>
  <verify>
    <automated>echo "Human verification — no automated check"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms camera snapshots load or show graceful fallback
    - User confirms live stream shows loading/error states
    - User confirms schedule page handles 503 without error flash
    - User confirms network dashboard renders correctly
  </acceptance_criteria>
  <done>User has approved browser behavior for camera, schedule, and network pages</done>
</task>

</tasks>

<verification>
- All 86 targeted tests pass (72 camera + 14 hooks)
- Debug files updated from awaiting_human_verify to resolved
- SUMMARY.md created documenting commit d33d210 fixes
- Human confirms browser behavior for camera, schedule, and network pages
</verification>

<success_criteria>
- Tests green: npx jest --testPathPattern="camera|useScheduleData|useRoomStatus" --no-coverage exits 0
- Debug files resolved: both contain "status: resolved"
- Documentation complete: SUMMARY.md exists with fix details
- Human approved: browser verification confirms fixes work in practice
</success_criteria>

<output>
After completion, create `.planning/phases/91-correzione-problemi-dati-rete-e-netatmo-camera/91-01-SUMMARY.md`
</output>
