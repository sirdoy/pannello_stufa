---
phase: 93-api-infrastructure-test-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/stoveApi.ts
  - lib/maintenanceService.ts
  - lib/schedulerService.ts
  - lib/healthDeadManSwitch.ts
autonomous: true
requirements:
  - TFIX-03
  - TFIX-04
  - TFIX-05
  - TFIX-06

must_haves:
  truths:
    - "fetchWithRetry logs timeout messages during retry attempts"
    - "trackUsageHours logs when maintenance threshold is reached"
    - "Scheduler CRUD functions log their operations on success"
    - "alertDeadManSwitch logs when ADMIN_USER_ID is not configured"
  artifacts:
    - path: "lib/stoveApi.ts"
      provides: "Retry logging in fetchWithRetry empty if-blocks"
      contains: "Timeout on attempt"
    - path: "lib/maintenanceService.ts"
      provides: "Threshold-reached log in trackUsageHours"
      contains: "Maintenance threshold reached"
    - path: "lib/schedulerService.ts"
      provides: "Success logs in saveSchedule, setSchedulerMode, setSemiManualMode, clearSemiManualMode"
      contains: "Scheduler salvato per"
    - path: "lib/healthDeadManSwitch.ts"
      provides: "Missing ADMIN_USER_ID log in alertDeadManSwitch"
      contains: "ADMIN_USER_ID not configured"
  key_links:
    - from: "lib/__tests__/stoveApi.test.ts"
      to: "lib/stoveApi.ts"
      via: "console.log spy for retry messages"
      pattern: "stringContaining.*Timeout on attempt"
    - from: "lib/__tests__/schedulerService.test.ts"
      to: "lib/schedulerService.ts"
      via: "console.log spy for CRUD operations"
      pattern: "console\\.log.*Scheduler salvato"
---

<objective>
Fix stoveApi.test.ts (TFIX-03), maintenanceService.test.ts (TFIX-04), schedulerService.test.ts (TFIX-05), and healthDeadManSwitch.test.ts (TFIX-06) by adding missing console.log calls to source files.

Purpose: All four suites fail because source functions are missing console.log statements that tests assert on. These are one-line additions per function — no logic changes.
Output: 1 + 1 + 5 + 1 = 8 tests passing across 4 suites
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/93-api-infrastructure-test-fixes/93-CONTEXT.md
@.planning/phases/93-api-infrastructure-test-fixes/93-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing logs to stoveApi + maintenanceService + healthDeadManSwitch (TFIX-03, TFIX-04, TFIX-06)</name>
  <files>lib/stoveApi.ts, lib/maintenanceService.ts, lib/healthDeadManSwitch.ts</files>
  <read_first>
    - lib/stoveApi.ts (see empty if-blocks at lines 99-100 and 118-119 inside fetchWithRetry)
    - lib/__tests__/stoveApi.test.ts (see assertion at line 273: `expect(console.log).toHaveBeenCalledWith(expect.stringContaining('Timeout on attempt'))`)
    - lib/maintenanceService.ts (see line 198-200 where needsCleaning is set but no log)
    - lib/__tests__/maintenanceService.test.ts (see assertion at line 451: `expect(console.log).toHaveBeenCalledWith(expect.stringContaining('Maintenance threshold reached'))`)
    - lib/healthDeadManSwitch.ts (see lines 106-108 where early return has no log)
    - __tests__/lib/healthDeadManSwitch.test.ts (see assertion at line 207: `expect(console.log).toHaveBeenCalledWith(expect.stringContaining('ADMIN_USER_ID not configured'))`)
  </read_first>
  <action>
Three source files need one console.log each:

**1. lib/stoveApi.ts — fetchWithRetry retry logging (TFIX-03)**

Inside the `fetchWithRetry` function, find the empty `if (attempt < maxRetries) {}` block (around line 118-119) inside the catch block. Add:
```typescript
if (attempt < maxRetries) {
  console.log(`Timeout on attempt ${attempt + 1}/${maxRetries + 1}. Retrying...`);
}
```

The test asserts `expect(console.log).toHaveBeenCalledWith(expect.stringContaining('Timeout on attempt'))` so the exact string must contain "Timeout on attempt".

**2. lib/maintenanceService.ts — threshold reached log (TFIX-04)**

Inside `trackUsageHours`, immediately after `currentData.needsCleaning = true;` (around line 199), add:
```typescript
console.log(`⚠️ Maintenance threshold reached: ${newCurrentHours.toFixed(2)}h / ${currentData.targetHours}h`);
```

The test asserts `expect(console.log).toHaveBeenCalledWith(expect.stringContaining('Maintenance threshold reached'))`.

**3. lib/healthDeadManSwitch.ts — missing ADMIN_USER_ID log (TFIX-06)**

Inside `alertDeadManSwitch`, before the `return;` at line 107 (inside the `if (!adminUserId)` block), add:
```typescript
console.log('[DeadManSwitch] ADMIN_USER_ID not configured, skipping alert');
```

The test asserts `expect(console.log).toHaveBeenCalledWith(expect.stringContaining('ADMIN_USER_ID not configured'))`.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npx jest lib/__tests__/stoveApi.test.ts lib/__tests__/maintenanceService.test.ts __tests__/lib/healthDeadManSwitch.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/stoveApi.ts contains `console.log(`Timeout on attempt ${attempt + 1}` inside the `if (attempt < maxRetries)` block of fetchWithRetry
    - lib/maintenanceService.ts contains `console.log(` with string containing `Maintenance threshold reached` immediately after `currentData.needsCleaning = true`
    - lib/healthDeadManSwitch.ts contains `console.log(` with string containing `ADMIN_USER_ID not configured` before the early return in the `if (!adminUserId)` block
    - `npx jest lib/__tests__/stoveApi.test.ts --no-coverage` exits 0 (TFIX-03)
    - `npx jest lib/__tests__/maintenanceService.test.ts --no-coverage` exits 0 (TFIX-04)
    - `npx jest __tests__/lib/healthDeadManSwitch.test.ts --no-coverage` exits 0 (TFIX-06)
  </acceptance_criteria>
  <done>stoveApi retry log test passes, maintenanceService threshold test passes, healthDeadManSwitch ADMIN_USER_ID test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing logs to schedulerService (TFIX-05)</name>
  <files>lib/schedulerService.ts</files>
  <read_first>
    - lib/schedulerService.ts (see saveSchedule at line 292, setSchedulerMode at line 335, setSemiManualMode at line 374, clearSemiManualMode at line 389)
    - lib/__tests__/schedulerService.test.ts (see assertions at lines 310, 449, 464, 617, 664)
  </read_first>
  <action>
In `lib/schedulerService.ts`, add 5 console.log calls to 4 functions. Each goes at the end of the `try` block, after the Firebase `set()` call succeeds, before the `} catch`:

**1. saveSchedule** (after the `await set(ref(db, ...updatedAt...), ...)` line, around line 298):
```typescript
console.log(`Scheduler salvato per ${day}`);
```

**2. setSchedulerMode** (after the `await set(ref(db, 'schedules-v2/mode'), {...})` call, around line 340):
```typescript
console.log(`Modalità scheduler impostata su: ${enabled ? 'attiva' : 'disattiva'}`);
```

This single line covers both test assertions:
- `'Modalità scheduler impostata su: attiva'` when `enabled=true`
- `'Modalità scheduler impostata su: disattiva'` when `enabled=false`

**3. setSemiManualMode** (after the `await set(ref(db, 'schedules-v2/mode'), {...})` call, around line 383):
```typescript
console.log(`Modalità semi-manuale attivata. Ritorno automatico previsto: ${nextScheduledChange}`);
```

**4. clearSemiManualMode** (after the `await set(ref(db, 'schedules-v2/mode'), {...})` call, around line 395):
```typescript
console.log('Modalità semi-manuale disattivata. Ritorno in automatico.');
```
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npx jest lib/__tests__/schedulerService.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/schedulerService.ts `saveSchedule` contains `console.log(`Scheduler salvato per ${day}`)`
    - lib/schedulerService.ts `setSchedulerMode` contains `console.log(`Modalità scheduler impostata su: ${enabled ? 'attiva' : 'disattiva'}`)`
    - lib/schedulerService.ts `setSemiManualMode` contains `console.log(`Modalità semi-manuale attivata. Ritorno automatico previsto: ${nextScheduledChange}`)`
    - lib/schedulerService.ts `clearSemiManualMode` contains `console.log('Modalità semi-manuale disattivata. Ritorno in automatico.')`
    - `npx jest lib/__tests__/schedulerService.test.ts --no-coverage` exits with code 0
    - All 5 previously failing scheduler tests pass: saveSchedule, setSchedulerMode (enabled/disabled), setSemiManualMode, clearSemiManualMode
  </acceptance_criteria>
  <done>All 5 scheduler tests pass. All 4 scheduler CRUD functions emit expected log messages.</done>
</task>

</tasks>

<verification>
Run all 4 suites together:
```bash
npx jest lib/__tests__/stoveApi.test.ts lib/__tests__/maintenanceService.test.ts lib/__tests__/schedulerService.test.ts __tests__/lib/healthDeadManSwitch.test.ts --no-coverage
```
All tests pass with 0 failures.
</verification>

<success_criteria>
- TFIX-03: fetchWithRetry retry logging test passes in stoveApi.test.ts
- TFIX-04: needsCleaning threshold test passes in maintenanceService.test.ts
- TFIX-05: All 5 save/set/clear schedule tests pass in schedulerService.test.ts
- TFIX-06: ADMIN_USER_ID skip test passes in healthDeadManSwitch.test.ts
- No other tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/93-api-infrastructure-test-fixes/93-02-SUMMARY.md`
</output>
