---
phase: 104-fix-command-body-key-mismatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/devices/stove/hooks/useStoveCommands.ts
  - __tests__/components/devices/stove/hooks/useStoveCommands.test.ts
autonomous: true
requirements: [CMD-03, CMD-04, UI-03]
gap_closure: true

must_haves:
  truths:
    - "handleFanChange sends { value: N, source: 'manual' } in the POST body to /api/stove/setFan"
    - "handlePowerChange sends { value: N, source: 'manual' } in the POST body to /api/stove/setPower"
    - "Fan and power values reach the proxy as numbers, not undefined"
  artifacts:
    - path: "app/components/devices/stove/hooks/useStoveCommands.ts"
      provides: "Corrected body key for fan and power commands"
      contains: "value: level, source: 'manual'"
    - path: "__tests__/components/devices/stove/hooks/useStoveCommands.test.ts"
      provides: "Correct assertions matching the fixed body shape"
      contains: "value: 4, source: 'manual'"
  key_links:
    - from: "app/components/devices/stove/hooks/useStoveCommands.ts"
      to: "app/api/stove/setFan/route.ts"
      via: "fetch POST with JSON body"
      pattern: "value.*level.*source.*manual"
    - from: "app/components/devices/stove/hooks/useStoveCommands.ts"
      to: "app/api/stove/setPower/route.ts"
      via: "fetch POST with JSON body"
      pattern: "value.*level.*source.*manual"
---

<objective>
Fix the body key mismatch between useStoveCommands and the setPower/setFan API routes so fan and power level commands actually reach the proxy with the correct numeric value.

Purpose: useStoveCommands sends `{ level: N }` but routes extract `body['value']` -- the numeric value arrives as undefined, silently breaking fan/power control from the UI.
Output: Two lines fixed in hook, two test assertions updated, all useStoveCommands tests green.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-fix-command-body-key-mismatch/104-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix body key from level to value in useStoveCommands and update test assertions</name>
  <files>app/components/devices/stove/hooks/useStoveCommands.ts, __tests__/components/devices/stove/hooks/useStoveCommands.test.ts</files>
  <read_first>
    - app/components/devices/stove/hooks/useStoveCommands.ts (see lines 145-190 for handleFanChange and handlePowerChange)
    - __tests__/components/devices/stove/hooks/useStoveCommands.test.ts (see lines 283-289 for fan assertion, lines 354-360 for power assertion)
    - app/api/stove/setFan/route.ts (confirms route reads body['value'] -- do NOT modify this file)
    - app/api/stove/setPower/route.ts (confirms route reads body['value'] -- do NOT modify this file)
  </read_first>
  <behavior>
    - RED: Update test assertions FIRST. Change line ~287 from `body: JSON.stringify({ level: 4, source: 'manual' })` to `body: JSON.stringify({ value: 4, source: 'manual' })`. Change line ~358 from `body: JSON.stringify({ level: 3, source: 'manual' })` to `body: JSON.stringify({ value: 3, source: 'manual' })`. Run tests -- the two updated assertions MUST fail (confirming the hook still sends `level`).
    - GREEN: Fix useStoveCommands.ts. Change line 152 from `body: JSON.stringify({ level, source: 'manual' })` to `body: JSON.stringify({ value: level, source: 'manual' })`. Change line 178 from `body: JSON.stringify({ level, source: 'manual' })` to `body: JSON.stringify({ value: level, source: 'manual' })`. Run tests -- all assertions MUST pass.
  </behavior>
  <action>
    Step 1 (RED): In `__tests__/components/devices/stove/hooks/useStoveCommands.test.ts`:
    - Find the test "handleFanChange calls execute with correct level" (around line 260). In its assertion block (around line 287), change:
      `body: JSON.stringify({ level: 4, source: 'manual' }),`
      to:
      `body: JSON.stringify({ value: 4, source: 'manual' }),`
    - Find the test "handlePowerChange calls execute with correct level" (around line 325). In its assertion block (around line 358), change:
      `body: JSON.stringify({ level: 3, source: 'manual' }),`
      to:
      `body: JSON.stringify({ value: 3, source: 'manual' }),`
    - Run `npm test -- --testPathPattern="useStoveCommands" --no-coverage` and confirm these 2 tests FAIL.

    Step 2 (GREEN): In `app/components/devices/stove/hooks/useStoveCommands.ts`:
    - In `handleFanChange` (line 152), change:
      `body: JSON.stringify({ level, source: 'manual' }),`
      to:
      `body: JSON.stringify({ value: level, source: 'manual' }),`
    - In `handlePowerChange` (line 178), change:
      `body: JSON.stringify({ level, source: 'manual' }),`
      to:
      `body: JSON.stringify({ value: level, source: 'manual' }),`
    - Run `npm test -- --testPathPattern="useStoveCommands" --no-coverage` and confirm ALL tests pass.

    IMPORTANT: Do NOT modify setFan/route.ts or setPower/route.ts. Do NOT remove `source: 'manual'` from the body. Do NOT change any other test assertions.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="useStoveCommands" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - useStoveCommands.ts line 152 contains `{ value: level, source: 'manual' }` (not `{ level, source: 'manual' }`)
    - useStoveCommands.ts line 178 contains `{ value: level, source: 'manual' }` (not `{ level, source: 'manual' }`)
    - useStoveCommands.test.ts contains `body: JSON.stringify({ value: 4, source: 'manual' })` in handleFanChange test
    - useStoveCommands.test.ts contains `body: JSON.stringify({ value: 3, source: 'manual' })` in handlePowerChange test
    - `npm test -- --testPathPattern="useStoveCommands" --no-coverage` exits 0 with all tests passing
    - No changes to any route files (setFan/route.ts, setPower/route.ts remain untouched)
  </acceptance_criteria>
  <done>handleFanChange and handlePowerChange send { value: N, source: 'manual' } matching route expectations. Tests verify the correct body shape. Full useStoveCommands test suite passes.</done>
</task>

</tasks>

<verification>
1. `grep -n "value: level, source: 'manual'" app/components/devices/stove/hooks/useStoveCommands.ts` returns exactly 2 matches (lines 152 and 178)
2. `grep -c "level, source: 'manual'" app/components/devices/stove/hooks/useStoveCommands.ts` returns 0 (old pattern gone)
3. `npm test -- --testPathPattern="useStoveCommands" --no-coverage` exits 0
</verification>

<success_criteria>
- Fan and power commands from the UI send `{ value: N }` matching the route extraction `body['value']`
- No undefined values reach the proxy for fan/power settings
- All useStoveCommands unit tests pass with correct body assertions
</success_criteria>

<output>
After completion, create `.planning/phases/104-fix-command-body-key-mismatch/104-01-SUMMARY.md`
</output>
