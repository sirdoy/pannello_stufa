---
phase: 109-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/hue/hueApi.ts
  - lib/hue/hueRemoteApi.ts
  - lib/hue/hueConnectionStrategy.ts
  - lib/hue/hueRemoteTokenHelper.ts
  - lib/hue/hueLocalHelper.ts
  - lib/hue/__tests__/hueApiScenes.test.ts
  - lib/hue/__tests__/hueRemoteTokenHelper.test.ts
  - lib/hue/__tests__/hueLocalHelper.test.ts
  - app/api/hue/discover/route.ts
  - app/api/hue/discover/__tests__/route.test.ts
  - app/api/hue/pair/route.ts
  - app/api/hue/disconnect/route.ts
  - app/api/hue/remote/authorize/route.ts
  - app/api/hue/remote/callback/route.ts
  - app/api/hue/remote/pair/route.ts
  - app/api/hue/remote/disconnect/route.ts
  - app/api/hue/callback/route.ts
  - app/api/hue/callback/route.js.disabled
  - app/api/hue/test/route.ts
  - app/api/hue/scenes/create/route.ts
  - app/api/hue/scenes/[id]/route.ts
  - app/api/hue/scenes/[id]/activate/route.ts
  - app/api/scheduler/check/route.ts
  - app/api/scheduler/check/__tests__/route.test.ts
  - app/lights/page.tsx
  - .env.example
  - lib/core/middleware.ts
  - lib/core/apiErrors.ts
  - lib/core/apiResponse.ts
  - lib/core/index.ts
  - lib/core/__tests__/apiErrors.test.ts
  - lib/core/__tests__/apiResponse.test.ts
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05
  - CLEAN-06
  - CLEAN-07

must_haves:
  truths:
    - "hueApi.ts, hueRemoteApi.ts, hueConnectionStrategy.ts, hueRemoteTokenHelper.ts, hueLocalHelper.ts do not exist in the codebase"
    - "No bridge discovery, pair, disconnect, remote/*, callback, or test routes exist under app/api/hue/"
    - "Scene CRUD routes (create, [id], [id]/activate) do not exist — only scenes/route.ts (GET) remains"
    - "Scheduler check route has no import of hueRemoteTokenHelper and no proactiveTokenRefresh call"
    - "No surviving .ts/.tsx file imports from any deleted module"
    - ".env.example contains no HUE_CLIENT_SECRET, NEXT_PUBLIC_HUE_CLIENT_ID, or NEXT_PUBLIC_HUE_APP_ID"
    - "app/lights/page.tsx has no NEXT_PUBLIC_HUE_CLIENT_ID reference"
    - "withHueHandler, hueNotConnected, hueNotOnLocalNetwork are not exported from lib/core/"
  artifacts:
    - path: "lib/hue/hueProxy.ts"
      provides: "Surviving proxy client (must NOT be deleted)"
    - path: "lib/hue/colorUtils.ts"
      provides: "Surviving color utils (must NOT be deleted)"
    - path: "lib/hue/__tests__/hueProxy.test.ts"
      provides: "Surviving proxy tests (must NOT be deleted)"
    - path: "lib/hue/__tests__/colorUtils.test.ts"
      provides: "Surviving color utils tests (must NOT be deleted)"
    - path: "app/api/hue/scenes/route.ts"
      provides: "Surviving GET scenes route (must NOT be deleted)"
  key_links:
    - from: "app/api/scheduler/check/route.ts"
      to: "lib/hue/hueProxy.ts"
      via: "Only proxy imports remain, no legacy imports"
      pattern: "no.*hueRemoteTokenHelper"
    - from: "lib/core/index.ts"
      to: "lib/core/apiResponse.ts"
      via: "hueNotConnected and hueNotOnLocalNetwork exports removed"
      pattern: "no.*hueNotConnected"
---

<objective>
Delete all legacy Hue infrastructure files and clean up surviving import references.

Purpose: Complete the Hue proxy migration by removing dead code — CLIP v2 local client, v1 remote client, connection strategy, OAuth, bridge discovery/pairing routes, scene CRUD routes (deferred feature), Firebase bridge credentials, Hue-specific env vars, and dead lib/core Hue helpers.
Output: Clean codebase with only proxy-based Hue code remaining. All tests pass, TypeScript compiles clean.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-cleanup/109-CONTEXT.md
@.planning/phases/109-cleanup/109-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all legacy Hue lib modules, route directories, and test files</name>
  <files>
    lib/hue/hueApi.ts
    lib/hue/hueRemoteApi.ts
    lib/hue/hueConnectionStrategy.ts
    lib/hue/hueRemoteTokenHelper.ts
    lib/hue/hueLocalHelper.ts
    lib/hue/__tests__/hueApiScenes.test.ts
    lib/hue/__tests__/hueRemoteTokenHelper.test.ts
    lib/hue/__tests__/hueLocalHelper.test.ts
    app/api/hue/discover/ (entire directory)
    app/api/hue/pair/route.ts
    app/api/hue/disconnect/route.ts
    app/api/hue/remote/ (entire directory — authorize, callback, pair, disconnect)
    app/api/hue/callback/route.ts
    app/api/hue/callback/route.js.disabled
    app/api/hue/test/route.ts
    app/api/hue/scenes/create/route.ts
    app/api/hue/scenes/[id]/route.ts
    app/api/hue/scenes/[id]/activate/route.ts
  </files>
  <read_first>
    lib/hue/hueApi.ts
    lib/hue/hueRemoteApi.ts
    app/api/hue/scenes/route.ts
    app/api/hue/scenes/__tests__/route.test.ts
  </read_first>
  <action>
Delete the following files and directories using `rm` or `rm -rf`:

**lib/hue/ — 5 legacy modules:**
- `rm lib/hue/hueApi.ts`
- `rm lib/hue/hueRemoteApi.ts`
- `rm lib/hue/hueConnectionStrategy.ts`
- `rm lib/hue/hueRemoteTokenHelper.ts`
- `rm lib/hue/hueLocalHelper.ts`

**lib/hue/__tests__/ — 3 test files:**
- `rm lib/hue/__tests__/hueApiScenes.test.ts`
- `rm lib/hue/__tests__/hueRemoteTokenHelper.test.ts`
- `rm lib/hue/__tests__/hueLocalHelper.test.ts`

**KEEP** `lib/hue/__tests__/colorUtils.test.ts` and `lib/hue/__tests__/hueProxy.test.ts` — these test surviving modules.

**app/api/hue/ — legacy route directories:**
- `rm -rf app/api/hue/discover/` (includes route.ts + __tests__/route.test.ts)
- `rm app/api/hue/pair/route.ts` then `rmdir app/api/hue/pair/`
- `rm app/api/hue/disconnect/route.ts` then `rmdir app/api/hue/disconnect/`
- `rm -rf app/api/hue/remote/` (includes authorize/, callback/, pair/, disconnect/)
- `rm app/api/hue/callback/route.ts` and `rm app/api/hue/callback/route.js.disabled` then `rmdir app/api/hue/callback/`
- `rm app/api/hue/test/route.ts` then `rmdir app/api/hue/test/`

**app/api/hue/scenes/ — scene CRUD routes (deferred feature, import deleted modules):**
- `rm app/api/hue/scenes/create/route.ts` then `rmdir app/api/hue/scenes/create/`
- `rm app/api/hue/scenes/[id]/activate/route.ts` then `rmdir app/api/hue/scenes/[id]/activate/`
- `rm app/api/hue/scenes/[id]/route.ts` then `rmdir app/api/hue/scenes/[id]/`

**KEEP** `app/api/hue/scenes/route.ts` (GET endpoint — already uses hueProxy.ts) and `app/api/hue/scenes/__tests__/route.test.ts`.

After all deletions, verify the remaining structure with `ls -R app/api/hue/` and `ls lib/hue/`.
  </action>
  <verify>
    <automated>test ! -f lib/hue/hueApi.ts && test ! -f lib/hue/hueRemoteApi.ts && test ! -f lib/hue/hueConnectionStrategy.ts && test ! -f lib/hue/hueRemoteTokenHelper.ts && test ! -f lib/hue/hueLocalHelper.ts && test ! -d app/api/hue/discover && test ! -d app/api/hue/remote && test ! -d app/api/hue/pair && test ! -f app/api/hue/callback/route.ts && test -f lib/hue/hueProxy.ts && test -f app/api/hue/scenes/route.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/hue/` contains only: hueProxy.ts, colorUtils.ts, __tests__/hueProxy.test.ts, __tests__/colorUtils.test.ts
    - `app/api/hue/discover/`, `app/api/hue/pair/`, `app/api/hue/disconnect/`, `app/api/hue/remote/`, `app/api/hue/callback/`, `app/api/hue/test/` directories do not exist
    - `app/api/hue/scenes/create/`, `app/api/hue/scenes/[id]/` directories do not exist
    - `app/api/hue/scenes/route.ts` still exists (GET endpoint using hueProxy)
  </acceptance_criteria>
  <done>All 22+ legacy files/directories deleted. Only proxy-based Hue code survives in lib/hue/ and app/api/hue/.</done>
</task>

<task type="auto">
  <name>Task 2: Clean surviving file imports, env vars, and lib/core dead Hue helpers</name>
  <files>
    app/api/scheduler/check/route.ts
    app/api/scheduler/check/__tests__/route.test.ts
    app/lights/page.tsx
    .env.example
    lib/core/middleware.ts
    lib/core/apiErrors.ts
    lib/core/apiResponse.ts
    lib/core/index.ts
    lib/core/__tests__/apiErrors.test.ts
    lib/core/__tests__/apiResponse.test.ts
  </files>
  <read_first>
    app/api/scheduler/check/route.ts
    app/api/scheduler/check/__tests__/route.test.ts
    app/lights/page.tsx
    .env.example
    lib/core/middleware.ts
    lib/core/apiErrors.ts
    lib/core/apiResponse.ts
    lib/core/index.ts
    lib/core/__tests__/apiErrors.test.ts
    lib/core/__tests__/apiResponse.test.ts
  </read_first>
  <action>
**1. app/api/scheduler/check/route.ts:**
- Remove line 43: `import { proactiveTokenRefresh } from '@/lib/hue/hueRemoteTokenHelper';`
- Remove lines 850-855 (the proactiveTokenRefresh fire-and-forget block):
  ```
  // Proactive Hue token refresh (async, don't wait)
  // Refreshes token if expiring within 24 hours to avoid manual reconnection
  proactiveTokenRefresh().then((result) => {
    if (result.refreshed) {
    }
  }).catch(err => console.error('... Hue token refresh error:', err.message));
  ```

**2. app/api/scheduler/check/__tests__/route.test.ts:**
- Remove line 34: `jest.mock('@/lib/hue/hueRemoteTokenHelper');`
- Remove line 73: `import { proactiveTokenRefresh } from '@/lib/hue/hueRemoteTokenHelper';`
- Remove line 96: `const mockProactiveTokenRefresh = jest.mocked(proactiveTokenRefresh);`
- Remove lines 616-626 (the full test block):
  ```
  it('calls proactiveTokenRefresh as fire-and-forget', async () => {
    setupSchedulerMocks({
      mode: { enabled: true, semiManual: false },
      intervals: [],
    });
    const request = createMockRequest();
    await GET(request);
    expect(mockProactiveTokenRefresh).toHaveBeenCalled();
  });
  ```

**3. app/lights/page.tsx:**
- Remove line 575: `const remoteApiAvailable = !!process.env.NEXT_PUBLIC_HUE_CLIENT_ID;`
- The broader pairing-state UI logic using `remoteApiAvailable` can remain — the deleted routes mean it renders a dead branch anyway. Just remove the env var reference line itself.

**4. .env.example:**
- Remove lines 35-40 (comment block + 3 env vars):
  ```
  # Philips Hue Remote API (OAuth 2.0) - Optional for cloud access
  # Register app at: https://developers.meethue.com/my-apps/
  # Required only if you want remote access (outside local network)
  NEXT_PUBLIC_HUE_APP_ID=your-hue-app-id
  NEXT_PUBLIC_HUE_CLIENT_ID=your-hue-client-id
  HUE_CLIENT_SECRET=your-hue-client-secret
  ```

**5. lib/core/middleware.ts:**
- Remove the entire `withHueHandler` function (lines 210-247) including the JSDoc comment above it.

**6. lib/core/apiErrors.ts:**
- Remove lines in `ERROR_CODES` constant: `HUE_NOT_CONNECTED: 'HUE_NOT_CONNECTED',` (line 77) and `HUE_NOT_ON_LOCAL_NETWORK: 'HUE_NOT_ON_LOCAL_NETWORK',` (line 80)
- Remove the matching `ERROR_MESSAGES` entries: `[ERROR_CODES.HUE_NOT_CONNECTED]: 'Philips Hue non connesso',` (line 144) and `[ERROR_CODES.HUE_NOT_ON_LOCAL_NETWORK]: 'Non sei sulla rete locale del bridge Hue',` (line 147)
- Remove static methods `hueNotConnected()` (lines 296-303) and `hueNotOnLocalNetwork()` (lines 305-312)
- Remove the two `if` blocks in `fromLegacyError` that map to these methods: the `HUE_NOT_CONNECTED` check (lines 356-358) and the `NETWORK_TIMEOUT` check (lines 359-361)

**7. lib/core/apiResponse.ts:**
- Remove functions `hueNotConnected()` (lines 292-302) and `hueNotOnLocalNetwork()` (lines 305-315)
- Remove references to `HUE_NOT_CONNECTED` and `HUE_NOT_ON_LOCAL_NETWORK` in the `mapErrorToResponse` function (around lines 157 and 164-167)

**8. lib/core/index.ts:**
- Remove `hueNotConnected,` (line 52) and `hueNotOnLocalNetwork,` (line 53) from the apiResponse re-exports
- Remove `withHueHandler,` (line 72) from the middleware re-exports

**9. lib/core/__tests__/apiErrors.test.ts:**
- Remove the `HUE_NOT_CONNECTED` constant assertion (line 42): `expect(ERROR_CODES.HUE_NOT_CONNECTED).toBe('HUE_NOT_CONNECTED');`
- Remove the `hueNotConnected` static method test block (lines 173-177)
- Remove the `HUE_NOT_CONNECTED` error mapping test (lines 192-196): `it('should map HUE_NOT_CONNECTED error', ...)`
- Remove the `NETWORK_TIMEOUT` error mapping test (lines ~199-204): `const error = ApiError.fromLegacyError(new Error('NETWORK_TIMEOUT'));` + assertion

**10. lib/core/__tests__/apiResponse.test.ts:**
- Remove the `hueNotConnected` import (line 21)
- Remove the `describe('hueNotConnected', ...)` test block (lines 193-200)
- Remove the `HUE_NOT_ON_LOCAL_NETWORK` test (around line 235)
- Remove the `HUE_NOT_CONNECTED error` mapping test (lines 238-244)

After all edits, run `grep -r "hueRemoteTokenHelper\|hueApi\b\|hueConnectionStrategy\|hueLocalHelper\|hueRemoteApi\|withHueHandler\|hueNotConnected\|hueNotOnLocalNetwork\|HUE_CLIENT_SECRET\|NEXT_PUBLIC_HUE_CLIENT_ID\|NEXT_PUBLIC_HUE_APP_ID\|HUE_NOT_CONNECTED\|HUE_NOT_ON_LOCAL_NETWORK" app lib .env.example --include="*.ts" --include="*.tsx" --include=".env.example"` to verify no surviving references (lib/version.ts changelog string is acceptable).

Then run `npm test -- --testPathPattern="scheduler/check|lib/core|lib/hue"` followed by full `npm test` to confirm nothing is broken.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="scheduler/check|lib/core|lib/hue"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "hueRemoteTokenHelper" app/api/scheduler/` returns no matches
    - `grep -r "proactiveTokenRefresh" app/api/scheduler/` returns no matches
    - `grep "NEXT_PUBLIC_HUE_CLIENT_ID" app/lights/page.tsx` returns no matches
    - `grep "HUE_CLIENT_SECRET\|NEXT_PUBLIC_HUE_CLIENT_ID\|NEXT_PUBLIC_HUE_APP_ID" .env.example` returns no matches
    - `grep "withHueHandler" lib/core/middleware.ts` returns no matches
    - `grep "withHueHandler" lib/core/index.ts` returns no matches
    - `grep "hueNotConnected\|hueNotOnLocalNetwork" lib/core/index.ts` returns no matches
    - `grep "HUE_NOT_CONNECTED\|HUE_NOT_ON_LOCAL_NETWORK" lib/core/apiErrors.ts` returns no matches
    - `grep "hueNotConnected\|hueNotOnLocalNetwork" lib/core/apiResponse.ts` returns no matches
    - `npm test -- --testPathPattern="scheduler/check|lib/core|lib/hue"` passes
    - `npm test` full suite passes
  </acceptance_criteria>
  <done>All surviving files cleaned of legacy Hue imports and references. Scheduler, lib/core, and env vars all updated. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `ls lib/hue/` shows only: hueProxy.ts, colorUtils.ts, __tests__/
2. `ls lib/hue/__tests__/` shows only: hueProxy.test.ts, colorUtils.test.ts
3. `ls app/api/hue/` shows only: lights/, rooms/, scenes/, groups/, status/, history/ (no discover/, pair/, disconnect/, remote/, callback/, test/)
4. `grep -r "hueRemoteTokenHelper\|hueApi\b\|hueConnectionStrategy\|hueLocalHelper\|hueRemoteApi\|withHueHandler\|hueNotConnected\|hueNotOnLocalNetwork\|HUE_CLIENT_SECRET\|NEXT_PUBLIC_HUE_CLIENT_ID\|NEXT_PUBLIC_HUE_APP_ID" app lib .env.example --include="*.ts" --include="*.tsx" --include=".env.example"` returns only lib/version.ts changelog string (acceptable)
5. `npm test` — full suite green
</verification>

<success_criteria>
- Zero legacy Hue files remain in the codebase
- Zero surviving imports reference deleted modules
- .env.example has no Hue OAuth env vars
- lib/core has no Hue-specific error helpers or middleware
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/109-cleanup/109-01-SUMMARY.md`
</output>
