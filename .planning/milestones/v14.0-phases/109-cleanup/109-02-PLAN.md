---
phase: 109-cleanup
plan: 02
type: execute
wave: 2
depends_on: [109-01]
files_modified:
  - docs/setup/hue-setup.md
  - docs/api/hue.md
  - docs/api/README.md
autonomous: true
requirements:
  - CLEAN-04
  - CLEAN-07

must_haves:
  truths:
    - "docs/setup/hue-setup.md documents proxy-based setup only — no OAuth, no bridge pairing from the frontend, no HueConnectionStrategy"
    - "docs/api/hue.md has no references to deleted scene CRUD routes or legacy bridge pairing frontend flow"
    - "docs/api/hue.md Quick Reference table contains no planned scene CRUD endpoints"
  artifacts:
    - path: "docs/setup/hue-setup.md"
      provides: "Updated Hue setup guide for proxy architecture"
      contains: "HA_BASE_URL"
    - path: "docs/api/hue.md"
      provides: "Updated API reference without deleted endpoints"
  key_links:
    - from: "docs/setup/hue-setup.md"
      to: "lib/hue/hueProxy.ts"
      via: "Documents the proxy-only setup flow"
      pattern: "HA_BASE_URL.*HA_API_KEY"
---

<objective>
Update Hue documentation to reflect the proxy-only architecture — remove all legacy setup instructions and delete references to removed scene CRUD endpoints.

Purpose: Documentation accuracy after code deletion. New developers should see only proxy-based setup, not the deleted OAuth/bridge pairing flow.
Output: Updated docs/setup/hue-setup.md and docs/api/hue.md.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-cleanup/109-CONTEXT.md
@.planning/phases/109-cleanup/109-RESEARCH.md
@.planning/phases/109-cleanup/109-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/setup/hue-setup.md for proxy-only setup</name>
  <files>docs/setup/hue-setup.md</files>
  <read_first>
    docs/setup/hue-setup.md
    lib/hue/hueProxy.ts
  </read_first>
  <action>
Rewrite `docs/setup/hue-setup.md` to document proxy-based setup only. Replace the entire file content.

The new guide should contain:

**Title:** `# Philips Hue Setup Guide`

**Overview section:**
- Hue integration communicates through the HomeAssistant proxy running on the Raspberry Pi
- The proxy handles Bridge connectivity, polling, and caching
- The Next.js app only needs two env vars: `HA_BASE_URL` and `HA_API_KEY`
- No OAuth, no bridge discovery from the browser, no HueConnectionStrategy

**Prerequisites:**
- Philips Hue Bridge connected to local LAN
- HomeAssistant proxy running on Raspberry Pi (`192.168.178.50`)
- Bridge paired with the proxy (HUE_USERNAME in `.secrets.toml` on the Pi)

**Env vars section:**
- `HA_BASE_URL` — already configured in `.env.local` for all proxy integrations
- `HA_API_KEY` — already configured in `.env.local` for all proxy integrations
- Note: No Hue-specific env vars needed in the Next.js app

**Bridge provisioning on the Pi (if bridge is new or re-paired):**
Keep the existing Step 1 (curl to obtain HUE_USERNAME via link button), Step 2 (configure .secrets.toml), Step 3 (restart homeassistant.service), and Step 4 (verify via /api/v1/hue/health) — these are still accurate for the proxy architecture.

**Troubleshooting:** Keep the existing FAQ (Bridge not found, Link button timeout, Invalid username) as they apply to proxy provisioning.

**Remove entirely:**
- All references to Local API / Remote API / Hybrid Mode
- All references to `HueConnectionStrategy`
- All references to OAuth 2.0, Hue Developer Account, HUE_CLIENT_SECRET, NEXT_PUBLIC_HUE_CLIENT_ID, NEXT_PUBLIC_HUE_APP_ID
- All references to "Remote API (Ovunque)" or cloud access
- The "Pairing" section that describes pressing a button in the browser UI
- Any reference to `hueApi.ts`, `hueRemoteApi.ts`, `hueRemoteTokenHelper.ts`, `hueLocalHelper.ts`

Write in Italian where the original used Italian (match existing doc style), English for technical terms.
  </action>
  <verify>
    <automated>grep -c "OAuth\|HUE_CLIENT_SECRET\|NEXT_PUBLIC_HUE_CLIENT_ID\|HueConnectionStrategy\|Remote API\|hueRemoteApi\|hueApi\.ts" docs/setup/hue-setup.md | grep "^0$" && grep "HA_BASE_URL" docs/setup/hue-setup.md && grep "HA_API_KEY" docs/setup/hue-setup.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "OAuth" docs/setup/hue-setup.md` returns no matches
    - `grep "HUE_CLIENT_SECRET\|NEXT_PUBLIC_HUE_CLIENT_ID\|NEXT_PUBLIC_HUE_APP_ID" docs/setup/hue-setup.md` returns no matches
    - `grep "HueConnectionStrategy" docs/setup/hue-setup.md` returns no matches
    - `grep "Remote API" docs/setup/hue-setup.md` returns no matches
    - `grep "HA_BASE_URL" docs/setup/hue-setup.md` returns at least 1 match
    - `grep "HA_API_KEY" docs/setup/hue-setup.md` returns at least 1 match
    - `grep "homeassistant" docs/setup/hue-setup.md` returns at least 1 match
    - File contains Bridge provisioning steps (curl command, .secrets.toml, systemctl restart)
  </acceptance_criteria>
  <done>Hue setup guide documents proxy-only architecture. No OAuth, no frontend pairing, no legacy env vars.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/api/hue.md — remove deleted endpoints and legacy bridge pairing section</name>
  <files>docs/api/hue.md, docs/api/README.md</files>
  <read_first>
    docs/api/hue.md
    docs/api/README.md
  </read_first>
  <action>
Edit `docs/api/hue.md` to remove references to deleted routes and endpoints:

**1. Quick Reference table (around lines 22-24):** Remove these three rows:
```
| `POST` | `/api/v1/hue/scenes` | Create a new scene (planned) |
| `PUT` | `/api/v1/hue/scenes/{scene_id}` | Update an existing scene (planned) |
| `DELETE` | `/api/v1/hue/scenes/{scene_id}` | Delete a scene (planned) |
```

**2. Table of Contents (around lines 43-45):** Remove these three entries:
```
  - [POST /scenes (planned)](#post-scenes)
  - [PUT /scenes/{scene_id} (planned)](#put-scenesscene_id)
  - [DELETE /scenes/{scene_id} (planned)](#delete-scenesscene_id)
```

**3. Scenes section:** Find and remove the full documentation blocks for the three planned scene CRUD endpoints:
- `### POST /scenes` (planned) — the entire section
- `### PUT /scenes/{scene_id}` (planned) — the entire section
- `### DELETE /scenes/{scene_id}` (planned) — the entire section

**4. Bridge Setup Guide section (line 1013+):** The existing Bridge Setup Guide (lines 1013-1099) actually already documents the proxy provisioning flow (curl to Bridge, .secrets.toml, systemctl restart homeassistant). Update only:
- Change the introductory line at 1015 to point to `docs/setup/hue-setup.md` (not `../HUE_SETUP.md` which may not exist): `> **Authoritative guide:** For the complete step-by-step provisioning procedure, see [Hue Setup Guide](../setup/hue-setup.md). The section below is a quick reference.`
- The rest of the section is accurate for proxy provisioning — keep it as-is.

**5. Introduction paragraph (line 5):** Remove "JWT Bearer token or" from the authentication description (line 7). The proxy uses only `X-API-Key` header now. Update to: `All endpoints require authentication via API Key (\`X-API-Key\` header). See [Authentication](./README.md#authentication) for details.`

**6. Also check `docs/api/README.md`** — if it lists the deleted Hue endpoints (discover, pair, disconnect, remote/*, callback, test, scenes/create, scenes/[id]) in any endpoint table, remove those entries. If it references JWT auth for Hue specifically, update to X-API-Key only.
  </action>
  <verify>
    <automated>grep -c "POST.*scenes.*planned\|PUT.*scenes.*planned\|DELETE.*scenes.*planned" docs/api/hue.md | grep "^0$" && grep -c "HUE_SETUP.md" docs/api/hue.md | grep "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "POST.*scenes.*planned" docs/api/hue.md` returns no matches
    - `grep "PUT.*scenes.*planned" docs/api/hue.md` returns no matches
    - `grep "DELETE.*scenes.*planned" docs/api/hue.md` returns no matches
    - `grep "HUE_SETUP.md" docs/api/hue.md` returns no matches (updated to hue-setup.md)
    - `grep "hue-setup.md" docs/api/hue.md` returns at least 1 match
    - Quick Reference table has 10 rows (7 read + 3 control, no planned endpoints)
    - `grep "JWT Bearer" docs/api/hue.md` returns no matches (updated to X-API-Key only)
  </acceptance_criteria>
  <done>API docs reflect actual endpoint inventory. No deleted or planned endpoints referenced. Bridge setup link corrected.</done>
</task>

</tasks>

<verification>
1. `grep -r "OAuth\|HUE_CLIENT_SECRET\|HueConnectionStrategy\|hueRemoteApi\|hueApi\.ts" docs/setup/hue-setup.md docs/api/hue.md` returns no matches
2. `grep "planned" docs/api/hue.md` returns no matches for scene CRUD
3. Both doc files reference proxy-based architecture (HA_BASE_URL, X-API-Key)
</verification>

<success_criteria>
- docs/setup/hue-setup.md documents only proxy-based setup (HA_BASE_URL + HA_API_KEY)
- docs/api/hue.md has no references to deleted scene CRUD or legacy auth
- Documentation matches the actual codebase state after Plan 01 deletions
</success_criteria>

<output>
After completion, create `.planning/phases/109-cleanup/109-02-SUMMARY.md`
</output>
