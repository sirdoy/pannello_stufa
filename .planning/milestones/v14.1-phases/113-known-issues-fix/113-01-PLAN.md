---
phase: 113-known-issues-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/debug/components/tabs/HueTab.tsx
  - app/debug/api/components/tabs/HueTab.tsx
  - app/components/devices/stove/components/StoveStatus.tsx
  - app/components/devices/stove/hooks/useStoveData.ts
  - app/network/components/CopyableIp.tsx
  - app/components/ui/__tests__/FormModal.test.tsx
autonomous: true
requirements:
  - ISSUE-01
  - ISSUE-02
  - ISSUE-03
  - ISSUE-04
  - ISSUE-05
  - ISSUE-06

must_haves:
  truths:
    - "Debug panel HueTab displays correct field name 'connected' from HueBridgeHealth interface"
    - "Debug panel HueTab form param name is 'bri' matching the actual PUT body key"
    - "StoveStatus staleness display uses simplified Date construction without instanceof guard"
    - "UseStoveDataReturn.status is typed as StoveState union — no string widening anywhere"
    - "CopyableIp renders design system Button component — no raw <button> element"
    - "FormModal test suite passes in isolation without ordering dependencies"
  artifacts:
    - path: "app/debug/components/tabs/HueTab.tsx"
      provides: "Primary debug HueTab with correct field names"
      contains: "data.connected"
    - path: "app/debug/api/components/tabs/HueTab.tsx"
      provides: "API debug HueTab with correct field names"
      contains: "data.connected"
    - path: "app/components/devices/stove/components/StoveStatus.tsx"
      provides: "Stove staleness display without instanceof guard"
      contains: "new Date(staleness.cachedAt)"
    - path: "app/network/components/CopyableIp.tsx"
      provides: "Design system compliant copy button"
      contains: "import { Button }"
    - path: "app/components/ui/__tests__/FormModal.test.tsx"
      provides: "Isolation-stable FormModal tests"
      contains: "jest.useRealTimers()"
  key_links:
    - from: "app/debug/components/tabs/HueTab.tsx"
      to: "types/hueProxy.ts"
      via: "HueBridgeHealth interface field names"
      pattern: "data\\.connected"
    - from: "app/network/components/CopyableIp.tsx"
      to: "app/components/ui/Button.tsx"
      via: "Button import with iconOnly prop"
      pattern: "import.*Button.*from.*ui/Button"
---

<objective>
Fix all 6 known issues from the v14.0 audit: debug panel HueTab field name mismatches (ISSUE-01, ISSUE-02), stove staleness dead code and type verification (ISSUE-03, ISSUE-04), CopyableIp design system compliance (ISSUE-05), and FormModal test isolation flake (ISSUE-06).

Purpose: Close the v14.0 audit backlog so the codebase has zero known issues before starting type safety work.
Output: 6 files corrected, all tests green, tsc passes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-known-issues-fix/113-CONTEXT.md
@.planning/phases/113-known-issues-fix/113-RESEARCH.md

@types/hueProxy.ts
@app/components/ui/Button.tsx
@app/components/devices/thermostat/ThermostatCard.tsx (line 721 — reference pattern for staleness.cachedAt)
@lib/pwa/stalenessDetector.ts (StalenessInfo type)

<interfaces>
<!-- Key types and contracts the executor needs -->

From types/hueProxy.ts (lines 120-128):
```typescript
export interface HueBridgeHealth {
  connected: boolean;       // NOT bridgeConnected
  firmware_version: string | null;
  // ...
}
```

From app/components/ui/Button.tsx:
```typescript
export const buttonVariants = cva(/* ... */, {
  variants: {
    variant: { ember, subtle, ghost, success, danger, outline },
    size: { sm, md, lg },
    iconOnly: { true: 'gap-0', false: '' },
    // compound: iconOnly + size => proper padding
  }
});

// Button accepts: variant, size, iconOnly, fullWidth, colorScheme + standard button props
// ButtonIcon accepts: icon: string (emoji/text) — NOT suitable for Lucide JSX icons
```

From lib/pwa/stalenessDetector.ts:
```typescript
export interface StalenessInfo {
  cachedAt: Date | null;  // Always Date | null, never string
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix debug panel HueTab field names and form params (ISSUE-01, ISSUE-02)</name>
  <files>app/debug/components/tabs/HueTab.tsx, app/debug/api/components/tabs/HueTab.tsx</files>
  <read_first>
    - app/debug/components/tabs/HueTab.tsx (full file — see current bridgeConnected and brightness references)
    - app/debug/api/components/tabs/HueTab.tsx (full file — identical changes needed)
    - types/hueProxy.ts (HueBridgeHealth interface — source of truth for field names)
  </read_first>
  <action>
    In BOTH `app/debug/components/tabs/HueTab.tsx` AND `app/debug/api/components/tabs/HueTab.tsx`, make the following changes:

    **ISSUE-01 — bridgeConnected to connected (per D-01):**
    Line 43: Change `data.bridgeConnected !== undefined` to `data.connected !== undefined`
    Line 44: Change `data.bridgeConnected ? 'connected' : 'disconnected'` to `data.connected ? 'connected' : 'disconnected'`

    **ISSUE-02 — brightness param name to bri (per D-02):**
    Line 213 (Control Light params): Change `{ name: 'brightness', label: 'Brightness (0-100)', ...}` to `{ name: 'bri', label: 'Brightness (0-100)', ...}` — keep label unchanged
    Line 221 (Control Light onExecute): Change `Number(values.brightness)` to `Number(values.bri)`
    Line 235 (Control Room params): Change `{ name: 'brightness', label: 'Brightness (0-100)', ...}` to `{ name: 'bri', label: 'Brightness (0-100)', ...}` — keep label unchanged
    Line 243 (Control Room onExecute): Change `Number(values.brightness)` to `Number(values.bri)`

    Both files have identical structure — apply the same 6 changes to each file.
  </action>
  <verify>
    <automated>grep -n "bridgeConnected" app/debug/components/tabs/HueTab.tsx app/debug/api/components/tabs/HueTab.tsx | wc -l | grep "^0$" && grep -c "data.connected" app/debug/components/tabs/HueTab.tsx | grep "^2$" && grep -c "values.bri" app/debug/components/tabs/HueTab.tsx | grep "^2$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "bridgeConnected" app/debug/components/tabs/HueTab.tsx` returns no matches
    - `grep "bridgeConnected" app/debug/api/components/tabs/HueTab.tsx` returns no matches
    - `grep "data.connected" app/debug/components/tabs/HueTab.tsx` returns 2 matches (lines 43-44)
    - `grep "data.connected" app/debug/api/components/tabs/HueTab.tsx` returns 2 matches
    - `grep "name: 'brightness'" app/debug/components/tabs/HueTab.tsx` returns no matches
    - `grep "name: 'bri'" app/debug/components/tabs/HueTab.tsx` returns 2 matches
    - `grep "values.bri" app/debug/components/tabs/HueTab.tsx` returns 2 matches
    - `grep "values.bri" app/debug/api/components/tabs/HueTab.tsx` returns 2 matches
    - `grep "label: 'Brightness" app/debug/components/tabs/HueTab.tsx` returns 2 matches (label unchanged)
    - `npx tsc --noEmit` passes with no errors in these files
  </acceptance_criteria>
  <done>Both HueTab files use `data.connected` (not `bridgeConnected`) and form param `name: 'bri'` (not `brightness`), with `values.bri` in onExecute handlers. Labels remain human-readable.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stove staleness display, verify status type, and replace CopyableIp button (ISSUE-03, ISSUE-04, ISSUE-05)</name>
  <files>app/components/devices/stove/components/StoveStatus.tsx, app/network/components/CopyableIp.tsx</files>
  <read_first>
    - app/components/devices/stove/components/StoveStatus.tsx (full file — see instanceof guard at line 129)
    - app/components/devices/stove/hooks/useStoveData.ts (lines 35-50 for UseStoveDataReturn type, line 102 for useState)
    - app/components/devices/thermostat/ThermostatCard.tsx (line 721 — reference pattern for cachedAt)
    - app/network/components/CopyableIp.tsx (full file — raw button to replace)
    - app/components/ui/Button.tsx (Button component props and variants)
    - lib/pwa/stalenessDetector.ts (StalenessInfo type — cachedAt is Date | null)
  </read_first>
  <action>
    **ISSUE-03/04 — Stove staleness and type verification (per D-03, D-04, D-05):**

    1. In `app/components/devices/stove/components/StoveStatus.tsx` line 129, replace:
       ```
       staleness.cachedAt instanceof Date ? staleness.cachedAt : new Date(staleness.cachedAt)
       ```
       with:
       ```
       new Date(staleness.cachedAt)
       ```
       This matches the ThermostatCard.tsx pattern at line 721. The `instanceof Date` branch is unnecessary because `StalenessInfo.cachedAt` is `Date | null` and the outer `staleness?.cachedAt` already guards null.

    2. For ISSUE-03 verification: The hook at useStoveData.ts line 182 populates `lastPollAt` from `last_poll_at` when the proxy returns it. The staleness display code is NOT dead — it renders when the proxy returns a non-null `last_poll_at`. Keep the display block. Add a brief comment above the staleness block:
       ```typescript
       {/* last_poll_at populated by proxy when available — see useStoveData.ts */}
       ```

    3. For ISSUE-04 verification: Confirm `UseStoveDataReturn.status` is typed `StoveState` at line 41 and `useState<StoveState>('off')` at line 102. Run grep to verify no other file widens it to `string`. No code change needed — issue is already resolved.

    **ISSUE-05 — CopyableIp design system compliance (per D-06, D-07):**

    4. In `app/network/components/CopyableIp.tsx`:
       - Add import: `import { Button } from '@/app/components/ui/Button';`
       - Replace the raw `<button>` element (lines 32-42) with:
         ```tsx
         <Button
           variant="ghost"
           size="sm"
           iconOnly
           onClick={handleCopy}
           aria-label={copied ? 'IP copiato' : 'Copia IP'}
         >
           {copied ? (
             <Check className="w-4 h-4 text-sage-400" />
           ) : (
             <Copy className="w-4 h-4" />
           )}
         </Button>
         ```
       - Remove ALL inline Tailwind classes from the button (`min-h-[44px] min-w-[44px] px-4 py-2.5 text-sm bg-transparent...`). The `buttonVariants` ghost+sm compound with `iconOnly` handles sizing, padding, hover states, and focus ring.
  </action>
  <verify>
    <automated>grep -c "instanceof Date" app/components/devices/stove/components/StoveStatus.tsx | grep "^0$" && grep -c "new Date(staleness.cachedAt)" app/components/devices/stove/components/StoveStatus.tsx | grep "^1$" && grep -c "<button" app/network/components/CopyableIp.tsx | grep "^0$" && grep -c "import { Button }" app/network/components/CopyableIp.tsx | grep "^1$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "instanceof Date" app/components/devices/stove/components/StoveStatus.tsx` returns no matches
    - `grep "new Date(staleness.cachedAt)" app/components/devices/stove/components/StoveStatus.tsx` returns 1 match
    - StoveStatus.tsx staleness block (lines 126-132) still present (NOT removed)
    - `grep "status: StoveState" app/components/devices/stove/hooks/useStoveData.ts` returns 1 match (confirming ISSUE-04 already fixed)
    - `grep "<button" app/network/components/CopyableIp.tsx` returns no matches (raw button gone)
    - `grep "import { Button }" app/network/components/CopyableIp.tsx` returns 1 match
    - `grep "variant=\"ghost\"" app/network/components/CopyableIp.tsx` returns 1 match
    - `grep "iconOnly" app/network/components/CopyableIp.tsx` returns 1 match
    - `grep "aria-label" app/network/components/CopyableIp.tsx` returns 1 match (accessibility preserved)
    - `npx tsc --noEmit` passes with no errors in modified files
  </acceptance_criteria>
  <done>StoveStatus uses simplified `new Date(staleness.cachedAt)` without instanceof guard. Staleness display block retained (not dead code). StoveState typing confirmed correct. CopyableIp uses design system `Button` with ghost variant, sm size, iconOnly — no raw button or inline Tailwind.</done>
</task>

<task type="auto">
  <name>Task 3: Fix FormModal test isolation flake (ISSUE-06)</name>
  <files>app/components/ui/__tests__/FormModal.test.tsx</files>
  <read_first>
    - app/components/ui/__tests__/FormModal.test.tsx (full file — see Success State describe at line 359, fake timers at line 389, mockClear workaround at line 430-431)
  </read_first>
  <action>
    **ISSUE-06 — FormModal timer leak fix (per D-08, D-09):**

    1. In the `describe('Success State', ...)` block (starting at line 359), add an `afterEach` teardown immediately after the opening brace:
       ```typescript
       describe('Success State', () => {
         afterEach(() => {
           jest.useRealTimers();
         });
       ```
       This ensures fake timers are cleaned up even if the test throws before reaching `jest.useRealTimers()` at line 422.

    2. Keep the existing `jest.useRealTimers()` at line 422 inside the test body for explicitness — the `afterEach` is a safety net.

    3. In the `describe('Cancel Behavior', ...)` block (line 426), REMOVE the manual workaround at lines 430-431:
       ```typescript
       // Clear any prior calls from full suite test interference BEFORE render
       mockOnClose.mockClear();
       ```
       Delete both the comment (line 430) and the `mockOnClose.mockClear()` call (line 431). With the `afterEach` teardown in Success State, the leaked timer no longer fires `onClose` during cleanup, so this workaround is unnecessary.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npx jest --testPathPattern="FormModal" --no-cache 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "afterEach" app/components/ui/__tests__/FormModal.test.tsx` includes `jest.useRealTimers()`
    - `grep "mockOnClose.mockClear" app/components/ui/__tests__/FormModal.test.tsx` returns no matches
    - `grep "prior calls from full suite" app/components/ui/__tests__/FormModal.test.tsx` returns no matches (comment removed)
    - `npx jest --testPathPattern="FormModal" --no-cache` passes all tests
    - Running the test 3 times consecutively produces 3 passes (no flake)
  </acceptance_criteria>
  <done>FormModal test suite passes in isolation and in full suite runs. afterEach teardown prevents timer leak. Manual mockClear workaround removed.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero type errors across all modified files
2. `npx jest --testPathPattern="FormModal" --no-cache` — all FormModal tests pass
3. `grep -rn "bridgeConnected" app/debug/` — returns zero matches
4. `grep -rn "name: 'brightness'" app/debug/` — returns zero matches
5. `grep -rn "instanceof Date" app/components/devices/stove/` — returns zero matches
6. `grep -rn "<button" app/network/components/CopyableIp.tsx` — returns zero matches
7. `grep -rn "mockOnClose.mockClear" app/components/ui/__tests__/FormModal.test.tsx` — returns zero matches
</verification>

<success_criteria>
- All 6 ISSUE requirements resolved
- ISSUE-01: `data.connected` in both HueTab files
- ISSUE-02: `name: 'bri'` and `values.bri` in both HueTab files
- ISSUE-03: Staleness display retained with simplified Date construction, instanceof guard removed
- ISSUE-04: Confirmed StoveState typing correct — no code change needed
- ISSUE-05: CopyableIp uses design system Button with ghost/sm/iconOnly
- ISSUE-06: FormModal afterEach teardown, mockClear workaround removed, tests pass in isolation
- `npx tsc --noEmit` passes
- `npm test` full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/113-known-issues-fix/113-01-SUMMARY.md`
</output>
