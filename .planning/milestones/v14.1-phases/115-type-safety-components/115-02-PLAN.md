---
phase: 115-type-safety-components
plan: 02
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - app/components/devices/camera/HlsPlayer.tsx
  - app/components/devices/camera/EventPreviewModal.tsx
  - app/components/devices/camera/CameraCard.tsx
  - app/components/devices/stove/StoveCard.tsx
  - app/components/devices/lights/LightsCard.tsx
  - app/components/devices/lights/components/LightsRoomControl.tsx
  - app/components/devices/thermostat/ThermostatCard.tsx
  - app/components/devices/thermostat/components/WeeklyTimeline.tsx
  - app/components/devices/weather/WeatherCardWrapper.tsx
  - app/components/devices/thermostat/components/ManualOverrideSheet.tsx
  - app/components/devices/thermostat/components/ScheduleSelector.tsx
  - app/components/thermostat/RoomCard.tsx
  - app/components/ui/DataTable.tsx
  - app/components/ui/SmartHomeCard.tsx
autonomous: true
requirements:
  - TYPE-07
  - TYPE-08
  - TYPE-09
  - TYPE-10

must_haves:
  truths:
    - "Icon JSX casts (<X /> as any) removed from all 6 camera/thermostat/device files"
    - "StoveCard statusDisplay variant/health are typed union literals, not string"
    - "LightsCard footerActions and statusBadge use exported DeviceCard interfaces without casts"
    - "LightsRoomControl Slider callbacks and adaptive.buttonVariant are typed"
    - "ThermostatCard schedule ID access and NETATMO_ROUTES access are typed"
    - "WeatherCardWrapper weatherData is properly typed"
    - "DataTable label element replaces Text as='label' cast"
    - "RoomCard BatteryBadge rendered as JSX not function call"
    - "SmartHomeCard Banner children passed explicitly"
  artifacts:
    - path: "app/components/devices/stove/StoveCard.tsx"
      provides: "Typed StatusDisplay interface"
      contains: "interface StatusDisplay"
    - path: "app/components/devices/lights/LightsCard.tsx"
      provides: "Typed footerActions using exported FooterAction"
      contains: "import type"
    - path: "app/components/devices/thermostat/ThermostatCard.tsx"
      provides: "Typed ScheduleItem interface"
      contains: "interface ScheduleItem"
  key_links:
    - from: "app/components/devices/lights/LightsCard.tsx"
      to: "app/components/ui/DeviceCard.tsx"
      via: "import FooterAction, StatusBadge"
      pattern: "import.*FooterAction.*DeviceCard"
    - from: "app/components/devices/stove/StoveCard.tsx"
      to: "app/components/ui/Badge.tsx"
      via: "StatusDisplay.variant matches Badge variant union"
      pattern: "variant.*ember.*ocean"
---

<objective>
Remove all `as any` casts from device component consumers: 6 icon prop casts (camera, thermostat components), variant/health casts in StoveCard, footerActions/statusBadge casts in LightsCard, Slider callback casts in LightsRoomControl, schedule/route casts in ThermostatCard, weather data cast in WeatherCardWrapper, Text-as-label cast in DataTable, BatteryBadge function call in RoomCard, and Banner children spread in SmartHomeCard.

Purpose: With Plan 01's foundation types in place (widened icon props, exported DeviceCard interfaces), these consumer files can now remove their casts and reference the proper types.
Output: 14 consumer component files with zero `as any` casts remaining.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-type-safety-components/115-CONTEXT.md
@.planning/phases/115-type-safety-components/115-RESEARCH.md
@.planning/phases/115-type-safety-components/115-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, these interfaces are available -->

From app/components/ui/Button.tsx (after Plan 01):
```typescript
export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement>, VariantProps<typeof buttonVariants> {
  children?: React.ReactNode;
  loading?: boolean;
  icon?: string | React.ReactNode;  // WIDENED
  iconPosition?: 'left' | 'right';
  haptic?: boolean;
  hapticPattern?: 'short' | 'success' | 'warning' | 'error';
}
```

From app/components/ui/DeviceCard.tsx (after Plan 01):
```typescript
export interface StatusBadge { label: string; color: string; icon?: ReactNode; }
export interface BannerItem extends BannerProps {}
export interface FooterAction extends Omit<ButtonProps, 'children'> { label: string; }
export interface ToastNotification extends ToastProps { show: boolean; }
```

From app/components/ui/LoadingOverlay.tsx (after Plan 01):
```typescript
export interface LoadingOverlayProps {
  show?: boolean; message?: string; icon?: string | React.ReactNode; liquid?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix icon casts + StoveCard variants + LightsCard/RoomControl types + DataTable label</name>
  <files>
    app/components/devices/camera/HlsPlayer.tsx,
    app/components/devices/camera/EventPreviewModal.tsx,
    app/components/devices/camera/CameraCard.tsx,
    app/components/devices/thermostat/components/ManualOverrideSheet.tsx,
    app/components/devices/thermostat/components/ScheduleSelector.tsx,
    app/components/devices/stove/StoveCard.tsx,
    app/components/devices/lights/LightsCard.tsx,
    app/components/devices/lights/components/LightsRoomControl.tsx,
    app/components/ui/DataTable.tsx
  </files>
  <read_first>
    app/components/devices/camera/HlsPlayer.tsx,
    app/components/devices/camera/EventPreviewModal.tsx,
    app/components/devices/camera/CameraCard.tsx,
    app/components/devices/thermostat/components/ManualOverrideSheet.tsx,
    app/components/devices/thermostat/components/ScheduleSelector.tsx,
    app/components/devices/stove/StoveCard.tsx,
    app/components/devices/lights/LightsCard.tsx,
    app/components/devices/lights/components/LightsRoomControl.tsx,
    app/components/ui/DataTable.tsx,
    app/components/ui/Badge.tsx,
    app/components/ui/HealthIndicator.tsx,
    app/components/ui/ControlButton.tsx,
    app/components/ui/Slider.tsx,
    app/components/ui/DeviceCard.tsx
  </read_first>
  <action>
**Icon cast removals (D-02, TYPE-07):** In each of these 5 files, Button.icon now accepts `string | React.ReactNode`, so simply remove the `as any` cast:
- `HlsPlayer.tsx:291` — change `icon={<SomeIcon /> as any}` to `icon={<SomeIcon />}`
- `EventPreviewModal.tsx:65` — same pattern, remove `as any`
- `CameraCard.tsx:359` — same pattern, remove `as any`
- `ManualOverrideSheet.tsx:181` — same pattern, remove `as any`
- `ScheduleSelector.tsx:96` — same pattern, remove `as any`

**StoveCard.tsx (D-11, D-12, TYPE-09):** The `statusDisplay` object has `variant` and `health` typed as `string`. Fix by:
1. Read Badge.tsx to find the exact variant union. Create a local interface:
```typescript
interface StatusDisplay {
  variant: NonNullable<VariantProps<typeof badgeVariants>['variant']>;
  health: 'ok' | 'warning' | 'error' | 'critical';
  label: string;
  pulse?: boolean;
}
```
Import `badgeVariants` from Badge.tsx if exported, OR manually list the union from the CVA definition (e.g., `'ember' | 'ocean' | 'sage' | 'warning' | 'danger' | 'neutral'`).
2. At line 111, `statusDisplay.variant` no longer needs cast. At line 118, `statusDisplay.health` no longer needs cast.
3. Ensure the function/object that creates `statusDisplay` values uses the typed interface.

**LightsCard.tsx (D-14, D-17, TYPE-10):**
1. Import `FooterAction` and `StatusBadge` from DeviceCard: `import type { FooterAction, StatusBadge } from '@/app/components/ui/DeviceCard';`
2. Line 63: `'outline' as any` in footerActions — FooterAction now extends ButtonProps which includes `'outline'` in its variant union. Remove the `as any`.
3. Lines 88, 92: `statusBadge` and `footerActions` arrays should be typed as `StatusBadge` and `FooterAction[]` respectively. Remove the `as any` casts.

**LightsRoomControl.tsx (D-15, D-16, TYPE-09):**
1. Lines 257, 269: `adaptive.buttonVariant` is `string`. Read ControlButton.tsx to find its variant union. Type the adaptive config object:
```typescript
interface AdaptiveConfig {
  buttonVariant: 'ember' | 'ocean' | 'sage' | 'warning' | 'danger' | 'subtle' | 'ghost';
  // ... other fields
}
```
Derive exact union from ControlButton's CVA variants.
2. Lines 233, 241: Slider `onValueChange` and `onValueCommit` callbacks cast. Read Slider.tsx to find the actual callback signature. It likely accepts `(value: number[]) => void` or `(value: number | number[]) => void`. Type the callbacks to match, removing `as any`.

**DataTable.tsx (D-13, TYPE-09):** At line 659, `<Text {...({ variant: "secondary", size: "sm", as: "label", htmlFor: "page-size" } as any)}>` — replace with a plain `<label>` element:
```tsx
<label
  htmlFor="page-size"
  className="text-sm text-slate-300 [html:not(.dark)_&]:text-slate-600"
>
```
Read the surrounding context to preserve the same text content as children.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -rn "as any" app/components/devices/camera/HlsPlayer.tsx app/components/devices/camera/EventPreviewModal.tsx app/components/devices/camera/CameraCard.tsx app/components/devices/thermostat/components/ManualOverrideSheet.tsx app/components/devices/thermostat/components/ScheduleSelector.tsx app/components/devices/stove/StoveCard.tsx app/components/devices/lights/LightsCard.tsx app/components/devices/lights/components/LightsRoomControl.tsx app/components/ui/DataTable.tsx | grep -v ".test." || echo "PASS: no as any found"</automated>
  </verify>
  <acceptance_criteria>
    - HlsPlayer.tsx, EventPreviewModal.tsx, CameraCard.tsx, ManualOverrideSheet.tsx, ScheduleSelector.tsx each contain zero `as any`
    - StoveCard.tsx contains `interface StatusDisplay` with typed variant union (not `string`)
    - StoveCard.tsx contains zero `as any`
    - LightsCard.tsx contains `import type` referencing DeviceCard exports
    - LightsCard.tsx contains zero `as any`
    - LightsRoomControl.tsx contains zero `as any`
    - DataTable.tsx contains `<label htmlFor="page-size"` instead of `<Text` with `as any`
    - DataTable.tsx contains zero `as any`
  </acceptance_criteria>
  <done>All 9 device/UI component files have zero as any casts. Icon props pass ReactNode directly, StoveCard variants are typed unions, LightsCard uses exported DeviceCard interfaces, Slider callbacks typed, DataTable uses native label element.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ThermostatCard + WeatherCardWrapper + RoomCard + SmartHomeCard + WeeklyTimeline casts</name>
  <files>
    app/components/devices/thermostat/ThermostatCard.tsx,
    app/components/devices/thermostat/components/WeeklyTimeline.tsx,
    app/components/devices/weather/WeatherCardWrapper.tsx,
    app/components/thermostat/RoomCard.tsx,
    app/components/ui/SmartHomeCard.tsx
  </files>
  <read_first>
    app/components/devices/thermostat/ThermostatCard.tsx,
    app/components/devices/thermostat/components/WeeklyTimeline.tsx,
    app/components/devices/weather/WeatherCardWrapper.tsx,
    app/components/devices/weather/WeatherCard.tsx,
    app/components/thermostat/RoomCard.tsx,
    app/components/ui/SmartHomeCard.tsx,
    app/components/ui/Banner.tsx,
    lib/routes.ts,
    lib/hooks/useScheduleData.ts,
    app/components/devices/thermostat/components/BatteryBadge.tsx
  </read_first>
  <action>
**ThermostatCard.tsx (D-22, D-23):**
1. Lines 74-75: `(activeSchedule as any).id` — define a local interface:
```typescript
interface ScheduleItem {
  id: string;
  name: string;
  selected?: boolean;
}
```
If `typedActiveSchedule` already exists as a cast to `ScheduleItem`, use it consistently. If not, cast once at the top: `const typedSchedule = activeSchedule as unknown as ScheduleItem | undefined;` and use `typedSchedule?.id` at lines 74-75. Remove the `(activeSchedule as any).id` casts.

2. Line 340: `(NETATMO_ROUTES as any).setRoomThermpoint` — read `lib/routes.ts` to confirm `NETATMO_ROUTES` is typed with `as const` and includes `setRoomThermpoint`. If it does, simply remove the cast: `NETATMO_ROUTES.setRoomThermpoint`. If `NETATMO_ROUTES` has a type that doesn't include this key, add it to the routes const.

**WeeklyTimeline.tsx (D-26):** Line 50: `schedule as any` passed to `parseTimelineSlots`. Read `parseTimelineSlots` function signature to determine what type it expects. Type the `schedule` parameter/variable to match. If `parseTimelineSlots` accepts `Schedule`, ensure the local variable is typed as `Schedule`. If it accepts a different shape, create or import the matching interface.

**WeatherCardWrapper.tsx (D-24):** Line 128: `weatherData as any` passed to WeatherCard. Read WeatherCard.tsx to find its `WeatherData` type. Either:
- Export `WeatherData` from WeatherCard.tsx and import it in WeatherCardWrapper.tsx
- Or align WeatherCardWrapper's local `WeatherData` interface to match WeatherCard's exactly
Then remove the `as any` cast at line 128.

**RoomCard.tsx (D-21):** Line 436: `(BatteryBadge as any)({ batteryState: module.battery_state, showLabel: true })` — render as JSX instead:
```tsx
<BatteryBadge batteryState={module.battery_state} showLabel />
```
Verify BatteryBadge accepts `batteryState` and `showLabel` props by reading its component definition.

**SmartHomeCard.tsx (D-18):** Line 214: Banner spread with children prop. Read the exact line to understand the pattern. Fix by passing `children` as an explicit prop to Banner rather than spreading. Banner already accepts `children` via `BannerProps`. Remove the `as any` cast.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -rn "as any" app/components/devices/thermostat/ThermostatCard.tsx app/components/devices/thermostat/components/WeeklyTimeline.tsx app/components/devices/weather/WeatherCardWrapper.tsx app/components/thermostat/RoomCard.tsx app/components/ui/SmartHomeCard.tsx | grep -v ".test." || echo "PASS: no as any found"</automated>
  </verify>
  <acceptance_criteria>
    - ThermostatCard.tsx contains `interface ScheduleItem` with `id: string`
    - ThermostatCard.tsx does NOT contain `(activeSchedule as any)` or `(NETATMO_ROUTES as any)`
    - ThermostatCard.tsx contains zero `as any`
    - WeeklyTimeline.tsx contains zero `as any`
    - WeatherCardWrapper.tsx contains zero `as any`
    - RoomCard.tsx contains `<BatteryBadge` rendered as JSX (not function call)
    - RoomCard.tsx contains zero `as any`
    - SmartHomeCard.tsx contains zero `as any`
    - `npm test -- --testPathPattern="ThermostatCard|WeatherCard|RoomCard|SmartHomeCard" --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>All 5 remaining component files have zero as any casts. ThermostatCard schedule is typed with ScheduleItem, NETATMO_ROUTES accessed directly, WeatherCardWrapper uses aligned WeatherData type, RoomCard renders BatteryBadge as JSX, SmartHomeCard passes Banner children explicitly.</done>
</task>

</tasks>

<verification>
```bash
# All 14 consumer files should have zero as any:
grep -rn "as any" \
  app/components/devices/camera/HlsPlayer.tsx \
  app/components/devices/camera/EventPreviewModal.tsx \
  app/components/devices/camera/CameraCard.tsx \
  app/components/devices/stove/StoveCard.tsx \
  app/components/devices/lights/LightsCard.tsx \
  app/components/devices/lights/components/LightsRoomControl.tsx \
  app/components/devices/thermostat/ThermostatCard.tsx \
  app/components/devices/thermostat/components/WeeklyTimeline.tsx \
  app/components/devices/thermostat/components/ManualOverrideSheet.tsx \
  app/components/devices/thermostat/components/ScheduleSelector.tsx \
  app/components/devices/weather/WeatherCardWrapper.tsx \
  app/components/thermostat/RoomCard.tsx \
  app/components/ui/DataTable.tsx \
  app/components/ui/SmartHomeCard.tsx \
  | grep -v ".test." | wc -l
# Expected: 0

# Full test suite passes:
npm test
```
</verification>

<success_criteria>
- Zero `as any` casts in all 14 consumer component files
- All device cards construct DeviceCard props using exported typed interfaces
- Variant props use typed union literals matching CVA definitions
- BatteryBadge rendered as JSX
- DataTable uses native label element
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/115-type-safety-components/115-02-SUMMARY.md`
</output>
