---
phase: 125-navigation-menu-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/devices/deviceTypes.ts
  - app/components/Navbar.tsx
  - lib/devices/__tests__/deviceRegistry.test.ts
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "User can reach /registry/types from the app navigation menu"
    - "User can reach /rooms from the app navigation menu"
    - "Navigation items render in hamburger menu following Ember Noir design"
  artifacts:
    - path: "lib/devices/deviceTypes.ts"
      provides: "REGISTRO and STANZE entries in GLOBAL_SECTIONS"
      contains: "REGISTRO"
    - path: "app/components/Navbar.tsx"
      provides: "Icon mapping for /registry and /rooms paths"
      contains: "registry"
    - path: "lib/devices/__tests__/deviceRegistry.test.ts"
      provides: "Test coverage for global nav items"
      contains: "getGlobalNavItems"
  key_links:
    - from: "lib/devices/deviceTypes.ts"
      to: "lib/devices/deviceRegistry.ts"
      via: "GLOBAL_SECTIONS import"
      pattern: "GLOBAL_SECTIONS"
    - from: "lib/devices/deviceRegistry.ts"
      to: "app/components/Navbar.tsx"
      via: "getNavigationStructureWithPreferences() → navStructure.global"
      pattern: "navStructure\\.global"
---

<objective>
Add Registro and Stanze navigation entries to the app hamburger menu so users can reach v15.0 pages (/registry/types, /registry/devices, /rooms, /rooms/status) without manually typing URLs.

Purpose: Gap closure — v15.0 pages were built without navbar entry points.
Output: Two new GLOBAL_SECTIONS entries + icon mapping + test coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-navigation-menu-links/125-RESEARCH.md

@lib/devices/deviceTypes.ts
@lib/devices/deviceRegistry.ts
@app/components/Navbar.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/devices/deviceTypes.ts (lines 271-289):
```typescript
interface GlobalSection {
  id: string;
  name: string;
  icon: string;
  route: string;
}

export const GLOBAL_SECTIONS: Record<string, GlobalSection> = {
  MONITORING: {
    id: 'monitoring',
    name: 'Monitoring',
    icon: '📊',
    route: '/monitoring',
  },
};
```

From lib/devices/deviceRegistry.ts (lines 9-14, 141-146):
```typescript
interface NavItem {
  label: string;
  route: string;
  icon?: string;
}

function getGlobalNavItems(): NavItem[] {
  return Object.values(GLOBAL_SECTIONS).map(section => ({
    label: section.name,
    route: section.route,
    icon: section.icon,
  }));
}
```

From app/components/Navbar.tsx (lines 171-177):
```typescript
const getIconForPath = (path: string) => {
  if (path === '/') return <Home className="w-5 h-5" />;
  if (path.includes('scheduler')) return <Calendar className="w-5 h-5" />;
  if (path.includes('errors')) return <AlertCircle className="w-5 h-5" />;
  if (path.includes('log')) return <Clock className="w-5 h-5" />;
  return null;
};
```

Lucide imports in Navbar.tsx (line 7):
```typescript
import { Home, Calendar, AlertCircle, Clock, Settings, User, LogOut, Menu, X, ChevronDown, Lightbulb } from 'lucide-react';
```

MenuItem icon prop (from navigation/DropdownComponents.tsx line 142):
```typescript
icon?: string | ReactNode;  // null is safe
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add REGISTRO and STANZE to GLOBAL_SECTIONS + extend getIconForPath</name>
  <files>lib/devices/deviceTypes.ts, app/components/Navbar.tsx</files>
  <read_first>
    - lib/devices/deviceTypes.ts (lines 270-290 for GLOBAL_SECTIONS)
    - app/components/Navbar.tsx (line 7 for Lucide imports, lines 171-177 for getIconForPath)
    - lib/devices/deviceRegistry.ts (lines 141-146 for getGlobalNavItems — no changes needed, just verify data flow)
  </read_first>
  <action>
    1. In `lib/devices/deviceTypes.ts`, add two entries to the `GLOBAL_SECTIONS` object after the existing MONITORING entry:

    ```typescript
    REGISTRO: {
      id: 'registry',
      name: 'Registro',
      icon: '📋',
      route: '/registry/types',
    },
    STANZE: {
      id: 'rooms',
      name: 'Stanze',
      icon: '🏠',
      route: '/rooms',
    },
    ```

    2. In `app/components/Navbar.tsx`, add two Lucide imports to the existing import on line 7: `ClipboardList` and `DoorOpen`.

    Updated import:
    ```typescript
    import { Home, Calendar, AlertCircle, Clock, Settings, User, LogOut, Menu, X, ChevronDown, Lightbulb, ClipboardList, DoorOpen } from 'lucide-react';
    ```

    3. In `app/components/Navbar.tsx`, extend `getIconForPath` function (around line 175, before `return null`) with two new cases:

    ```typescript
    if (path.includes('registry')) return <ClipboardList className="w-5 h-5" />;
    if (path.includes('rooms')) return <DoorOpen className="w-5 h-5" />;
    ```

    Insert these BEFORE the `return null;` line.

    Note: The stale MONITORING entry (pointing to deleted /monitoring page) is out of scope — do not remove it.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="Navbar" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/devices/deviceTypes.ts contains `REGISTRO:` inside GLOBAL_SECTIONS
    - lib/devices/deviceTypes.ts contains `STANZE:` inside GLOBAL_SECTIONS
    - lib/devices/deviceTypes.ts contains `route: '/registry/types'`
    - lib/devices/deviceTypes.ts contains `route: '/rooms'`
    - app/components/Navbar.tsx contains `ClipboardList` in Lucide import
    - app/components/Navbar.tsx contains `DoorOpen` in Lucide import
    - app/components/Navbar.tsx getIconForPath contains `path.includes('registry')`
    - app/components/Navbar.tsx getIconForPath contains `path.includes('rooms')`
    - Existing Navbar tests pass: `npm test -- --testPathPattern="Navbar"` exits 0
  </acceptance_criteria>
  <done>GLOBAL_SECTIONS has REGISTRO and STANZE entries, getIconForPath returns Lucide icons for registry and rooms paths, existing tests green</done>
</task>

<task type="auto">
  <name>Task 2: Add unit test for getGlobalNavItems covering new entries</name>
  <files>lib/devices/__tests__/deviceRegistry.test.ts</files>
  <read_first>
    - lib/devices/deviceRegistry.ts (full file — need to understand exports and getGlobalNavItems)
    - lib/devices/deviceTypes.ts (lines 282-299 for final GLOBAL_SECTIONS after Task 1)
  </read_first>
  <action>
    Create a new test file `lib/devices/__tests__/deviceRegistry.test.ts`.

    The file must import `getGlobalNavItems` from `@/lib/devices/deviceRegistry` (or relative `../deviceRegistry`). Note: `getGlobalNavItems` is a module-level function — check if it's exported. If not exported, test indirectly via `getNavigationStructure` or `getNavigationStructureWithPreferences` which ARE exported and include `global: NavItem[]` in their return.

    Test cases:
    1. "getNavigationStructure returns global nav items including Registro" — assert that the `global` array contains an item with `{ label: 'Registro', route: '/registry/types' }`
    2. "getNavigationStructure returns global nav items including Stanze" — assert that the `global` array contains an item with `{ label: 'Stanze', route: '/rooms' }`
    3. "global nav items have at least 3 entries" — assert `global.length >= 3` (MONITORING + REGISTRO + STANZE)

    Use `expect.arrayContaining` and `expect.objectContaining` for flexible matching:
    ```typescript
    import { getNavigationStructure } from '../deviceRegistry';

    describe('getNavigationStructure - global nav items', () => {
      it('includes Registro entry pointing to /registry/types', () => {
        const nav = getNavigationStructure();
        expect(nav.global).toEqual(
          expect.arrayContaining([
            expect.objectContaining({ label: 'Registro', route: '/registry/types' }),
          ])
        );
      });

      it('includes Stanze entry pointing to /rooms', () => {
        const nav = getNavigationStructure();
        expect(nav.global).toEqual(
          expect.arrayContaining([
            expect.objectContaining({ label: 'Stanze', route: '/rooms' }),
          ])
        );
      });

      it('has at least 3 global entries', () => {
        const nav = getNavigationStructure();
        expect(nav.global.length).toBeGreaterThanOrEqual(3);
      });
    });
    ```

    If `getNavigationStructure` is not directly exported (check the file), use `getNavigationStructureWithPreferences` instead — it takes no required args if preferences default, or pass an empty object. Adjust import accordingly.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="deviceRegistry" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File lib/devices/__tests__/deviceRegistry.test.ts exists
    - Test file contains assertion for `label: 'Registro'` and `route: '/registry/types'`
    - Test file contains assertion for `label: 'Stanze'` and `route: '/rooms'`
    - `npm test -- --testPathPattern="deviceRegistry"` exits 0
    - `npm test -- --testPathPattern="Navbar"` still exits 0
  </acceptance_criteria>
  <done>Unit tests verify that getNavigationStructure().global contains Registro and Stanze entries, all tests pass</done>
</task>

</tasks>

<verification>
1. `npm test -- --testPathPattern="deviceRegistry"` — new tests pass
2. `npm test -- --testPathPattern="Navbar"` — existing tests unbroken
3. `npm test` — full suite green
4. grep confirms REGISTRO and STANZE in GLOBAL_SECTIONS
5. grep confirms getIconForPath handles registry and rooms paths
</verification>

<success_criteria>
- GLOBAL_SECTIONS contains REGISTRO (route: /registry/types) and STANZE (route: /rooms)
- getIconForPath returns Lucide icons for /registry and /rooms paths
- Unit test exists and passes proving global nav items include both new entries
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/125-navigation-menu-links/125-01-SUMMARY.md`
</output>
