---
phase: 148-tuya-frontend
plan: 03
type: execute
wave: 2
depends_on: ["148-01"]
files_modified:
  - app/components/devices/tuya/components/TuyaPlugCard.tsx
  - app/components/devices/tuya/components/TuyaEnergyChart.tsx
  - app/components/devices/tuya/components/TuyaEnergyChartInner.tsx
  - app/components/devices/tuya/hooks/useTuyaHistory.ts
  - app/tuya/page.tsx
  - app/components/devices/tuya/components/__tests__/TuyaEnergyChart.test.tsx
  - app/tuya/__tests__/page.test.tsx
autonomous: true
requirements:
  - TUYA-12
  - TUYA-14

must_haves:
  truths:
    - "User can see all Tuya plugs in a responsive grid on /tuya page"
    - "User can toggle each plug on/off from the /tuya page"
    - "User can set and cancel timers on each plug"
    - "User can view energy history chart with period selector (24h/7d/30d)"
    - "Chart shows correct metric based on auto-granularity (power_w for raw, avg_power_w for hourly/daily)"
  artifacts:
    - path: "app/tuya/page.tsx"
      provides: "/tuya page orchestrator with plug grid"
      exports: ["default"]
      min_lines: 40
    - path: "app/components/devices/tuya/components/TuyaPlugCard.tsx"
      provides: "Per-plug card with toggle, timer, power, freshness"
      exports: ["TuyaPlugCard"]
    - path: "app/components/devices/tuya/components/TuyaEnergyChart.tsx"
      provides: "Lazy-loaded chart wrapper via next/dynamic"
      exports: ["default"]
    - path: "app/components/devices/tuya/components/TuyaEnergyChartInner.tsx"
      provides: "Recharts AreaChart with granularity branching"
      exports: ["default"]
    - path: "app/components/devices/tuya/hooks/useTuyaHistory.ts"
      provides: "History data fetching hook with period param"
      exports: ["useTuyaHistory"]
  key_links:
    - from: "app/tuya/page.tsx"
      to: "app/components/devices/tuya/hooks/useTuyaData.ts"
      via: "useTuyaData() for live plug list"
      pattern: "useTuyaData"
    - from: "app/tuya/page.tsx"
      to: "app/components/devices/tuya/hooks/useTuyaCommands.ts"
      via: "useTuyaCommands() for toggle/timer"
      pattern: "useTuyaCommands"
    - from: "app/components/devices/tuya/components/TuyaPlugCard.tsx"
      to: "app/components/devices/tuya/components/TuyaEnergyChart.tsx"
      via: "Expandable section shows chart on click"
      pattern: "TuyaEnergyChart"
    - from: "app/components/devices/tuya/hooks/useTuyaHistory.ts"
      to: "/api/tuya/plugs/[device_id]/history"
      via: "fetch GET with period query param"
      pattern: "api/tuya/plugs.*history"
---

<objective>
Create the /tuya page with multi-plug card grid, on/off toggles, timer controls, and energy history charts.

Purpose: Users can monitor and control all Tuya smart plugs from a dedicated page with per-plug detail (D-03, D-04, D-05, D-06, D-07, D-08).
Output: /tuya page, TuyaPlugCard, TuyaEnergyChart (lazy-loaded), useTuyaHistory hook, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-tuya-frontend/148-CONTEXT.md
@.planning/phases/148-tuya-frontend/148-RESEARCH.md
@.planning/phases/148-tuya-frontend/148-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From app/components/devices/tuya/hooks/useTuyaData.ts:
```typescript
export interface UseTuyaDataReturn {
  plugs: TuyaPlug[] | null;
  loading: boolean;
  error: string | null;
  stale: boolean;
  lastUpdatedAt: number | null;
}
export function useTuyaData(): UseTuyaDataReturn;
```

From app/components/devices/tuya/hooks/useTuyaCommands.ts:
```typescript
export interface UseTuyaCommandsReturn {
  togglePlug: (deviceId: string, currentState: boolean) => Promise<TuyaPlugMutation | null>;
  setTimer: (deviceId: string, seconds: number) => Promise<TuyaPlugMutation | null>;
  cancelTimer: (deviceId: string) => Promise<TuyaPlugMutation | null>;
}
export function useTuyaCommands(): UseTuyaCommandsReturn;
```

From types/tuyaProxy.ts:
```typescript
export interface TuyaPlug {
  device_id: string;
  switch_on: boolean | null;
  power_w: number | null;
  voltage_v: number | null;
  current_ma: number | null;
  energy_kwh: number | null;
  countdown_s: number | null;
  data_freshness: 'LIVE' | 'STALE' | 'UNREACHABLE';
  last_polled_at: number | null;
  custom_name: string | null;
  device_type: string | null;
}

export interface TuyaHistoryItem {
  timestamp: number;
  device_id: string;
  granularity: 'raw' | 'hourly' | 'daily';
  power_w?: number | null;           // non-null for raw
  avg_power_w?: number | null;       // non-null for hourly/daily
  energy_kwh_delta?: number | null;  // non-null for hourly/daily
  // ... other fields
}

export interface TuyaHistoryResponse {
  device_id: string;
  granularity: 'raw' | 'hourly' | 'daily';
  period: { from: number; to: number };
  page: number;
  page_size: number;
  total: number;
  items: TuyaHistoryItem[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useTuyaHistory hook and TuyaEnergyChart components</name>
  <files>
    app/components/devices/tuya/hooks/useTuyaHistory.ts,
    app/components/devices/tuya/components/TuyaEnergyChart.tsx,
    app/components/devices/tuya/components/TuyaEnergyChartInner.tsx,
    app/components/devices/tuya/components/__tests__/TuyaEnergyChart.test.tsx
  </files>
  <read_first>
    app/components/devices/sonos/components/SonosHistoryChart.tsx,
    app/network/components/BudgetStatsCard.tsx,
    types/tuyaProxy.ts,
    docs/api/tuya.md
  </read_first>
  <action>
    1. Create `app/components/devices/tuya/hooks/useTuyaHistory.ts`:
       - 'use client' directive
       - Export function useTuyaHistory(deviceId: string | null, period: '24h' | '7d' | '30d')
       - Return type: { data: TuyaHistoryResponse | null; loading: boolean; error: string | null }
       - useEffect fetches GET `/api/tuya/plugs/${deviceId}/history?period=${period}&page_size=500` when deviceId is non-null
       - Re-fetches when deviceId or period changes (dependency array)
       - Parse response as TuyaHistoryResponse
       - On error: set error='Storico non disponibile', data stays null
       - When deviceId is null: skip fetch, return null data

    2. Create `app/components/devices/tuya/components/TuyaEnergyChart.tsx` (lazy wrapper per D-06):
       - 'use client' directive
       - Import dynamic from 'next/dynamic'
       - `const TuyaEnergyChartInner = dynamic(() => import('./TuyaEnergyChartInner'), { ssr: false, loading: () => <div className="h-[200px] rounded-xl bg-slate-700/30 animate-pulse" /> });`
       - Export default component that accepts props: { deviceId: string }
       - Manages period state: useState<'24h' | '7d' | '30d'>('24h')
       - Calls useTuyaHistory(deviceId, period)
       - Renders period selector buttons: "24h", "7g", "30g" (Italian: giorni). Active button has amber/warning highlight
       - If loading: show loading placeholder
       - If error: show error text
       - If data: render <TuyaEnergyChartInner items={data.items} granularity={data.granularity} />

    3. Create `app/components/devices/tuya/components/TuyaEnergyChartInner.tsx` (actual Recharts per D-05/D-06):
       - 'use client' directive
       - Import { AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid } from 'recharts'
       - Import { format } from 'date-fns'
       - Props: { items: TuyaHistoryItem[]; granularity: 'raw' | 'hourly' | 'daily' }
       - Branch on granularity for dataKeys:
         - granularity === 'raw': primary dataKey = 'power_w' (label: "Potenza (W)")
         - granularity === 'hourly' or 'daily': primary = 'avg_power_w' (label: "Potenza media (W)"), secondary = 'energy_kwh_delta' (label: "Energia (kWh)") on right Y axis
       - XAxis: dataKey="timestamp", tickFormatter uses format(new Date(ts * 1000), granularity === 'raw' ? 'HH:mm' : granularity === 'hourly' ? 'HH:mm' : 'dd/MM')
       - Primary Area: fill amber gradient (warning palette colors), stroke warning-500
       - Secondary Area (if hourly/daily): fill ocean/blue gradient, stroke ocean-500, yAxisId="right"
       - Tooltip: custom formatter showing value with unit
       - ResponsiveContainer width="100%" height={200}
       - Dark-first styling: CartesianGrid stroke="#334155" (slate-700)

    4. Create `app/components/devices/tuya/components/__tests__/TuyaEnergyChart.test.tsx`:
       - Mock next/dynamic to render children directly
       - Mock useTuyaHistory
       - Test: "renders period selector with 3 buttons" (24h, 7g, 30g)
       - Test: "shows loading state when fetching"
       - Test: "renders chart with raw granularity dataKey power_w"
       - Test: "renders chart with hourly granularity dataKey avg_power_w"
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="TuyaEnergyChart" --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - app/components/devices/tuya/hooks/useTuyaHistory.ts contains "export function useTuyaHistory"
    - app/components/devices/tuya/hooks/useTuyaHistory.ts contains "api/tuya/plugs" and "history" and "period"
    - app/components/devices/tuya/components/TuyaEnergyChart.tsx contains "dynamic(" and "ssr: false"
    - app/components/devices/tuya/components/TuyaEnergyChart.tsx contains "'24h'" and "'7d'" and "'30d'"
    - app/components/devices/tuya/components/TuyaEnergyChartInner.tsx contains "AreaChart" and "ResponsiveContainer"
    - app/components/devices/tuya/components/TuyaEnergyChartInner.tsx contains "power_w" and "avg_power_w" and "energy_kwh_delta"
    - app/components/devices/tuya/components/TuyaEnergyChartInner.tsx contains "granularity" branching logic
    - TuyaEnergyChart test passes
  </acceptance_criteria>
  <done>useTuyaHistory fetches history data, TuyaEnergyChart lazy-loads Recharts via next/dynamic, TuyaEnergyChartInner branches on granularity for correct dataKeys (D-05/D-06), period selector shows Italian labels</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TuyaPlugCard and /tuya page with tests</name>
  <files>
    app/components/devices/tuya/components/TuyaPlugCard.tsx,
    app/tuya/page.tsx,
    app/tuya/__tests__/page.test.tsx
  </files>
  <read_first>
    app/raspi/page.tsx,
    app/components/devices/tuya/hooks/useTuyaData.ts,
    app/components/devices/tuya/hooks/useTuyaCommands.ts,
    app/components/devices/tuya/components/TuyaEnergyChart.tsx,
    types/tuyaProxy.ts
  </read_first>
  <behavior>
    TuyaPlugCard:
    - Renders plug name (custom_name ?? device_id) and on/off toggle
    - Shows power_w value when available, "--" when null/UNREACHABLE
    - Shows data_freshness badge (LIVE=green, STALE=amber, UNREACHABLE=red)
    - Timer section: when countdown_s > 0, shows mm:ss countdown + "Annulla" button; when 0, shows minutes input + "Imposta" button
    - Countdown decrements every second client-side, resets when WS pushes new countdown_s value
    - Expandable: click to show/hide TuyaEnergyChart for this plug's device_id

    /tuya page:
    - Renders heading "Tuya Smart Plug"
    - Uses useTuyaData() and useTuyaCommands()
    - Loading: shows spinner or skeleton grid
    - Error with no data: shows Banner
    - Data: responsive grid of TuyaPlugCard (1-col mobile, 2-col md, 3-col lg per D-03)
    - Passes toggle/timer handlers from useTuyaCommands to each TuyaPlugCard
    - When toggle returns null (data_confirmed=false), schedules re-fetch via setTimeout 2s
  </behavior>
  <action>
    1. Create `app/components/devices/tuya/components/TuyaPlugCard.tsx`:
       - 'use client' directive
       - Props: { plug: TuyaPlug; onToggle: (deviceId: string, currentState: boolean) => void; onSetTimer: (deviceId: string, seconds: number) => void; onCancelTimer: (deviceId: string) => void; }
       - State: expanded (boolean, for chart), timerMinutes (string, input value), remaining (number, countdown)
       - Display: card div with rounded-xl bg-slate-800/50 border border-slate-700 p-4
         - Header row: plug name (custom_name ?? device_id), data_freshness badge (colored dot + text)
         - Toggle: button or div styled as toggle switch, calls onToggle(plug.device_id, plug.switch_on ?? false). Disabled when data_freshness === 'UNREACHABLE'
         - Power: "{plug.power_w?.toFixed(1) ?? '--'} W" in prominent text
         - Voltage/current: smaller text "{plug.voltage_v?.toFixed(0) ?? '--'} V / {plug.current_ma?.toFixed(0) ?? '--'} mA"
         - Timer section per D-07/D-08:
           - If plug.countdown_s && plug.countdown_s > 0: show formatted countdown (mm:ss) via formatCountdown helper + "Annulla" button calling onCancelTimer(plug.device_id)
           - Else: number input for minutes + "Imposta" button. On click: parse int, validate > 0, call onSetTimer(plug.device_id, minutes * 60), clear input
         - Countdown effect: useEffect with setInterval(1000) that decrements `remaining`. Reset remaining when plug.countdown_s changes (from WS). When reaches 0, clear interval.
         - Expand button: "Storico energia" text, toggles expanded state
         - If expanded: render <TuyaEnergyChart deviceId={plug.device_id} />
       - formatCountdown helper: `const m = Math.floor(s/60).toString().padStart(2,'0'); const sec = (s%60).toString().padStart(2,'0'); return \`\${m}:\${sec}\`;`

    2. Create `app/tuya/page.tsx`:
       - 'use client' directive
       - Default export TuyaPage
       - Call useTuyaData() and useTuyaCommands()
       - Handler: handleToggle = async (deviceId, currentState) => { const result = await togglePlug(deviceId, currentState); if (!result) setTimeout(() => { /* WS will push update */ }, 2000); }
       - Handler: handleSetTimer = async (deviceId, seconds) => { await setTimer(deviceId, seconds); }
       - Handler: handleCancelTimer = async (deviceId) => { await cancelTimer(deviceId); }
       - Layout:
         - Page header: h1 "Tuya Smart Plug" with amber icon
         - Loading: centered spinner or skeleton placeholders
         - Error + no data: Banner variant="warning" "Tuya non raggiungibile"
         - Data: `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">` per D-03
         - Map plugs to TuyaPlugCard components with key={plug.device_id}
         - Pass onToggle, onSetTimer, onCancelTimer handlers

    3. Create `app/tuya/__tests__/page.test.tsx` (smoke test):
       - Mock useTuyaData, useTuyaCommands
       - Test: "renders page heading" — expect "Tuya Smart Plug" text
       - Test: "renders plug cards when data available" — mock 2 plugs, expect 2 cards
       - Test: "renders error when no data" — mock error + null plugs
       - Test: "grid has responsive classes" — expect "grid-cols-1" and "md:grid-cols-2" and "lg:grid-cols-3"
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="tuya.*(page|PlugCard)" --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - app/components/devices/tuya/components/TuyaPlugCard.tsx contains "export" and "TuyaPlugCard"
    - app/components/devices/tuya/components/TuyaPlugCard.tsx contains "onToggle" and "onSetTimer" and "onCancelTimer"
    - app/components/devices/tuya/components/TuyaPlugCard.tsx contains "countdown" and "formatCountdown"
    - app/components/devices/tuya/components/TuyaPlugCard.tsx contains "Imposta" and "Annulla" (Italian per D-07)
    - app/components/devices/tuya/components/TuyaPlugCard.tsx contains "TuyaEnergyChart" (expandable chart)
    - app/tuya/page.tsx contains "grid-cols-1" and "md:grid-cols-2" and "lg:grid-cols-3" (D-03 responsive grid)
    - app/tuya/page.tsx contains "useTuyaData" and "useTuyaCommands"
    - app/tuya/page.tsx contains "TuyaPlugCard"
    - app/tuya/__tests__/page.test.tsx exits 0
  </acceptance_criteria>
  <done>TuyaPlugCard shows toggle, power, timer controls (D-04/D-07/D-08), countdown, expandable chart. /tuya page renders responsive plug grid (D-03) with all handlers wired. Smoke tests pass.</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="tuya" --watchAll=false` passes (all Tuya tests)
- /tuya page renders responsive grid per D-03
- Each plug card has toggle, power, timer, freshness badge per D-04
- Timer input sends correct seconds per D-07/D-08
- Energy chart lazy-loads via next/dynamic per D-06
- Chart branches on granularity for correct dataKeys per D-05
</verification>

<success_criteria>
- /tuya page displays all plugs in responsive grid (1/2/3 cols per D-03)
- Each plug card shows: toggle, name, power, timer, freshness badge (D-04)
- Timer controls: "Imposta" with minutes input, "Annulla" with countdown (D-07/D-08)
- Energy chart: lazy-loaded Recharts AreaChart with period selector 24h/7g/30g (D-05/D-06)
- Chart shows power_w for raw, avg_power_w + energy_kwh_delta for aggregated (D-05/D-06)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/148-tuya-frontend/148-03-SUMMARY.md`
</output>
