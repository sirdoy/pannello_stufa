---
phase: 158-automations-module
plan: 02
type: execute
wave: 2
depends_on: ["158-01"]
files_modified:
  - lib/devices/deviceTypes.ts
  - app/components/Navbar.tsx
  - app/automations/page.tsx
  - app/automations/[rule_id]/page.tsx
autonomous: false
requirements:
  - AUTO-01
  - AUTO-02
  - AUTO-03
  - AUTO-04
  - AUTO-05
  - AUTO-06

must_haves:
  truths:
    - "User sees Automazioni in the navigation menu"
    - "User sees paginated list of automation rules at /automations"
    - "User can create a new rule via FormModal"
    - "User can edit an existing rule via FormModal"
    - "User can delete a rule with ConfirmationDialog"
    - "User can navigate to /automations/[rule_id] to see rule detail and execution history"
  artifacts:
    - path: "lib/devices/deviceTypes.ts"
      provides: "AUTOMAZIONI entry in GLOBAL_SECTIONS"
      contains: "AUTOMAZIONI"
    - path: "app/components/Navbar.tsx"
      provides: "Zap icon mapping for automations path"
      contains: "automations"
    - path: "app/automations/page.tsx"
      provides: "Rules list page with DataTable + FormModal + ConfirmationDialog"
      min_lines: 150
    - path: "app/automations/[rule_id]/page.tsx"
      provides: "Rule detail + execution history page"
      min_lines: 80
  key_links:
    - from: "app/automations/page.tsx"
      to: "/api/v1/automations"
      via: "fetch in useAutomations hook"
      pattern: "fetch.*api/v1/automations"
    - from: "app/automations/[rule_id]/page.tsx"
      to: "/api/v1/automations/${ruleId}"
      via: "fetch in useAutomationDetail and useExecutions hooks"
      pattern: "fetch.*api/v1/automations/"
    - from: "lib/devices/deviceTypes.ts"
      to: "app/automations/page.tsx"
      via: "GLOBAL_SECTIONS.AUTOMAZIONI.route = '/automations'"
      pattern: "route.*automations"
---

<objective>
Build the complete UI layer for the Automations module: navigation entry, rules list page with full CRUD, and rule detail page with execution history.

Purpose: Surfaces the automation CRUD API (Plan 01) through the established DataTable + FormModal + ConfirmationDialog pattern, consistent with existing Registro and Stanze pages.
Output: 4 files — nav config changes (2 files), rules list page, rule detail page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-automations-module/158-CONTEXT.md
@.planning/phases/158-automations-module/158-RESEARCH.md
@.planning/phases/158-automations-module/158-UI-SPEC.md
@.planning/phases/158-automations-module/158-01-SUMMARY.md

<interfaces>
<!-- Types created in Plan 01 -->
From types/automations.ts:
```typescript
export interface AutomationRule {
  id: string;
  name: string;
  description?: string | null;
  enabled: boolean;
  last_execution_at?: string | null;
  created_at?: string;
  [key: string]: unknown;
}

export interface AutomationCreate {
  name: string;
  description?: string | null;
  enabled?: boolean;
}

export interface AutomationUpdate {
  name?: string;
  description?: string | null;
  enabled?: boolean;
}

export interface AutomationExecution {
  id: string;
  rule_id: string;
  status: 'success' | 'failure' | 'running';
  started_at: string;
  duration_ms?: number | null;
  error_message?: string | null;
}
```

From types/common.ts:
```typescript
export interface PaginatedResponse<T> {
  items: T[];
  total_count: number;
  limit: number;
  offset: number;
}
```

From lib/devices/deviceTypes.ts GLOBAL_SECTIONS (current state):
```typescript
export const GLOBAL_SECTIONS: Record<string, GlobalSection> = {
  REGISTRO: { id: 'registry', name: 'Registro', icon: '📋', route: '/registry/types', items: [...] },
  STANZE: { id: 'rooms', name: 'Stanze', icon: '🏠', route: '/rooms', items: [...] },
};
```

From app/components/Navbar.tsx getIconForPath:
```typescript
const getIconForPath = (path: string) => {
  if (path === '/') return <Home className="w-5 h-5" />;
  if (path.includes('scheduler')) return <Calendar className="w-5 h-5" />;
  if (path.includes('errors')) return <AlertCircle className="w-5 h-5" />;
  if (path.includes('log')) return <Clock className="w-5 h-5" />;
  if (path.includes('registry')) return <ClipboardList className="w-5 h-5" />;
  if (path.includes('rooms')) return <DoorOpen className="w-5 h-5" />;
  return null;
};
```

From app/rooms/page.tsx (canonical CRUD page pattern):
- 'use client' directive
- Inline useRooms() hook with useState/useEffect/useCallback
- Zod schema + type inference
- Controller + Control<T> from react-hook-form
- ColumnDef from @tanstack/react-table
- DataTable, FormModal, ConfirmationDialog, Button, Badge, Banner, Skeleton, Card, Input, useToast imports
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Automazioni nav entry</name>
  <files>lib/devices/deviceTypes.ts, app/components/Navbar.tsx</files>
  <read_first>
    - lib/devices/deviceTypes.ts (full file — find GLOBAL_SECTIONS around line 305, understand GlobalSection interface)
    - app/components/Navbar.tsx (full file — find getIconForPath around line 179, find lucide-react import at line 7)
  </read_first>
  <action>
**1. In `lib/devices/deviceTypes.ts`**, add AUTOMAZIONI entry to GLOBAL_SECTIONS object, after the STANZE entry:

```typescript
AUTOMAZIONI: {
  id: 'automations',
  name: 'Automazioni',
  icon: '⚡',
  route: '/automations',
},
```

No `items` array needed — single route, no dropdown sub-items.

**2. In `app/components/Navbar.tsx`**, two changes:

a) Add `Zap` to the lucide-react import at line 7:
```typescript
import { Home, Calendar, AlertCircle, Clock, Settings, User, LogOut, Menu, X, ChevronDown, Lightbulb, ClipboardList, DoorOpen, Zap } from 'lucide-react';
```

b) Add automations branch in `getIconForPath` function, before the final `return null`:
```typescript
if (path.includes('automations')) return <Zap className="w-5 h-5" />;
```

Insert it after the `rooms` line and before the `return null` line.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -n "AUTOMAZIONI" lib/devices/deviceTypes.ts && grep -n "automations" app/components/Navbar.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/devices/deviceTypes.ts contains `AUTOMAZIONI:` inside GLOBAL_SECTIONS
    - lib/devices/deviceTypes.ts AUTOMAZIONI entry has `route: '/automations'`
    - lib/devices/deviceTypes.ts AUTOMAZIONI entry has `name: 'Automazioni'`
    - app/components/Navbar.tsx imports `Zap` from 'lucide-react'
    - app/components/Navbar.tsx getIconForPath contains `if (path.includes('automations')) return <Zap`
  </acceptance_criteria>
  <done>Automazioni appears in navigation (both desktop and mobile) with Zap icon, route points to /automations</done>
</task>

<task type="auto">
  <name>Task 2: Rules list page with CRUD (per D-01, D-02, D-05, D-06)</name>
  <files>app/automations/page.tsx</files>
  <read_first>
    - app/rooms/page.tsx (canonical CRUD page — full file, study useRooms hook, Zod schema, columns, FormModal render-prop, ConfirmationDialog, key prop on edit modal)
    - app/registry/devices/page.tsx (paginated list with PaginatedResponse — study useRegistryDevices hook with page/offset/limit state, pagination buttons)
    - app/components/ui/DataTable.tsx (understand DataTable props)
    - app/components/ui/FormModal.tsx (understand FormModal props including validationSchema, renderFields)
    - app/components/ui/ConfirmationDialog.tsx (understand props)
    - app/components/ui/Badge.tsx (understand variant prop values)
    - app/components/ui/Switch.tsx (understand Switch props for enabled toggle)
    - .planning/phases/158-automations-module/158-UI-SPEC.md (full copywriting contract)
  </read_first>
  <action>
Create `app/automations/page.tsx` as a 'use client' component following the rooms/page.tsx CRUD pattern exactly.

**Structure:**

1. **Imports:** Same set as rooms/page.tsx — useState, useEffect, useCallback, useRouter, z, Controller/Control, ColumnDef, all UI components, useToast. Add Switch import. Add AutomationRule/AutomationCreate/AutomationUpdate from types/automations. Add PaginatedResponse from types/common.

2. **Zod schema:**
```typescript
const automationSchema = z.object({
  name: z.string().min(1, 'Nome obbligatorio').max(128, 'Max 128 caratteri'),
  description: z.string().max(500, 'Max 500 caratteri').nullable().optional(),
  enabled: z.boolean().default(true),
});
type AutomationFormData = z.infer<typeof automationSchema>;
```

3. **useAutomations hook** (inline, following devices/page.tsx paginated pattern):
- State: `rules: AutomationRule[]`, `totalCount: number`, `loading: boolean`, `error: string | null`, `page: number` (starting at 0)
- `PAGE_SIZE = 20` constant
- `refetch` callback: `fetch('/api/v1/automations?limit=${PAGE_SIZE}&offset=${page * PAGE_SIZE}')` -> parse as `PaginatedResponse<AutomationRule>` -> `setRules(data.items)`, `setTotalCount(data.total_count)`
- Error text: `'Errore nel caricamento delle automazioni'`
- `useEffect(() => { void refetch(); }, [refetch]);`
- Return: `{ rules, totalCount, loading, error, refetch, page, setPage }`

4. **Columns definition** (`ColumnDef<AutomationRule>[]`):
- **Nome:** `accessorKey: 'name'`, cell renders rule name as `<Text>` + optional description below as `<Text variant="secondary" size="sm">`. Clickable — `router.push(\`/automations/${row.original.id}\`)`
- **Stato:** `accessorKey: 'enabled'`, cell renders `<Badge variant={row.original.enabled ? 'ember' : 'neutral'} pulse={row.original.enabled}>{row.original.enabled ? 'Attiva' : 'Disattiva'}</Badge>`
- **Ultima esecuzione:** `accessorKey: 'last_execution_at'`, cell renders formatted date or '—'
- **Azioni:** `id: 'actions'`, cell renders edit `<Button.Icon>` + delete `<Button.Icon>`, 44px touch targets

5. **Component state:**
- `showCreateModal: boolean`
- `ruleToEdit: AutomationRule | null`
- `ruleToDelete: AutomationRule | null`
- `submitting: boolean`

6. **CRUD handlers:**
- `handleCreate(data: AutomationFormData)`: POST to `/api/v1/automations`, success toast `'Regola creata con successo'`, error toast `'Operazione non riuscita. Riprova.'`, refetch
- `handleUpdate(data: AutomationFormData)`: PATCH to `/api/v1/automations/${ruleToEdit.id}`, success toast `'Regola aggiornata con successo'`, error toast, refetch
- `handleDelete()`: DELETE to `/api/v1/automations/${ruleToDelete.id}`, success toast `'Regola eliminata'`, error toast, refetch

7. **FormModal render-prop fields** (using `Control<AutomationFormData>`):
- `<Controller name="name" control={control} render={...}><Input label="Nome" /></Controller>`
- `<Controller name="description" control={control} render={...}><Input label="Descrizione" /></Controller>`
- `<Controller name="enabled" control={control} render={...}><Switch label="Abilitata" /></Controller>`

8. **FormModal key prop:** `key={ruleToEdit?.id ?? 'create'}` to force remount on edit target change

9. **Page layout** (per UI-SPEC):
```tsx
<SettingsLayout>
  <PageLayout.Header
    title={<Heading level={1} variant="ember">Automazioni</Heading>}
    description={<Text variant="secondary">Gestisci le regole di automazione del sistema</Text>}
    actions={<Button variant="ember" onClick={() => setShowCreateModal(true)}>Nuova Regola</Button>}
  />
  {error && <Banner variant="danger">{error}</Banner>}
  {loading ? <Skeleton rows={5} /> : (
    rules.length === 0 ? <EmptyState heading="Nessuna automazione configurata" body="Crea la tua prima regola..." cta="Nuova Regola" onCtaClick={() => setShowCreateModal(true)} /> : <DataTable columns={columns} data={rules} />
  )}
  {/* Pagination buttons if totalCount > PAGE_SIZE */}
</SettingsLayout>
```

10. **ConfirmationDialog:** title `'Elimina automazione'`, body `Sei sicuro di voler eliminare la regola "${ruleToDelete?.name}"? L'operazione non può essere annullata.`, confirm button text `'Elimina'`, variant danger.

Adapt the exact PageLayout.Header/SettingsLayout/EmptyState usage from rooms/page.tsx — check exact prop names and adjust.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && test -f app/automations/page.tsx && grep -c "useCallback\|useState\|FormModal\|DataTable\|ConfirmationDialog" app/automations/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/automations/page.tsx starts with `'use client'`
    - app/automations/page.tsx contains `const automationSchema = z.object`
    - app/automations/page.tsx contains `fetch('/api/v1/automations` (not `/api/automations`)
    - app/automations/page.tsx contains `<FormModal` with `key={ruleToEdit?.id`
    - app/automations/page.tsx contains `<ConfirmationDialog`
    - app/automations/page.tsx contains `'Regola creata con successo'`
    - app/automations/page.tsx contains `'Regola aggiornata con successo'`
    - app/automations/page.tsx contains `'Regola eliminata'`
    - app/automations/page.tsx contains `variant="ember"` for enabled badge
    - app/automations/page.tsx contains `variant="neutral"` for disabled badge
    - app/automations/page.tsx contains `method: 'PATCH'` for update
    - app/automations/page.tsx contains `method: 'DELETE'` for delete
    - app/automations/page.tsx contains `<Switch` for enabled toggle
  </acceptance_criteria>
  <done>Rules list page renders at /automations with paginated DataTable, create/edit FormModal with Zod validation, delete ConfirmationDialog, all Italian copy per UI-SPEC</done>
</task>

<task type="auto">
  <name>Task 3: Rule detail + execution history page (per D-03, D-04)</name>
  <files>app/automations/[rule_id]/page.tsx</files>
  <read_first>
    - app/automations/page.tsx (just created in Task 2 — understand patterns used)
    - app/rooms/[room_id]/page.tsx (detail page pattern with dynamic params, if exists — otherwise use app/registry/devices/page.tsx as paginated reference)
    - .planning/phases/158-automations-module/158-UI-SPEC.md (detail page layout contract + execution history columns)
    - types/automations.ts (AutomationRule, AutomationExecution types)
    - types/common.ts (PaginatedResponse<T>)
  </read_first>
  <action>
Create `app/automations/[rule_id]/page.tsx` as a 'use client' component.

**Structure:**

1. **Imports:** useState, useEffect, useCallback, useParams, useRouter from next/navigation. UI: SettingsLayout, Card, DataTable, Badge, Button, Skeleton, Banner, Text, Heading from design system. Types: AutomationRule, AutomationExecution from types/automations, PaginatedResponse from types/common. ArrowLeft from lucide-react.

2. **useAutomationDetail hook** (inline):
- State: `rule: AutomationRule | null`, `loading: boolean`, `error: string | null`
- Fetch: `fetch(\`/api/v1/automations/${ruleId}\`)`
- Error text: `'Errore nel caricamento della regola'`

3. **useExecutions hook** (inline):
- State: `executions: AutomationExecution[]`, `totalCount: number`, `loading: boolean`, `error: string | null`, `page: number`
- `PAGE_SIZE = 20`
- Fetch: `fetch(\`/api/v1/automations/${ruleId}/executions?limit=${PAGE_SIZE}&offset=${page * PAGE_SIZE}\`)`
- Error text: `'Errore nel caricamento dello storico esecuzioni'`

4. **Status badge mapping function:**
```typescript
function getExecutionBadge(status: string) {
  switch (status) {
    case 'success': return <Badge variant="sage">Completata</Badge>;
    case 'failure': return <Badge variant="danger">Fallita</Badge>;
    case 'running': return <Badge variant="warning">In esecuzione</Badge>;
    default: return <Badge variant="neutral">{status}</Badge>;
  }
}
```

5. **Execution columns** (`ColumnDef<AutomationExecution>[]`):
- **Data:** `accessorKey: 'started_at'`, cell formats ISO string to locale datetime `new Date(value).toLocaleString('it-IT')`
- **Stato:** `accessorKey: 'status'`, cell renders `getExecutionBadge(value)`
- **Durata:** `accessorKey: 'duration_ms'`, cell renders `${value}ms` or `'—'`
- **Dettaglio:** `accessorKey: 'error_message'`, cell renders `<Text variant="secondary" size="sm">{value || '—'}</Text>`

6. **Page layout** (per UI-SPEC):
```tsx
<SettingsLayout>
  {/* Header with back button */}
  <div className="flex items-center gap-4 mb-6">
    <Button variant="subtle" onClick={() => router.push('/automations')}>
      <ArrowLeft className="w-4 h-4" />
    </Button>
    <div>
      <Heading level={1}>{rule.name}</Heading>
      <Text variant="secondary">{rule.description || 'Nessuna descrizione'}</Text>
    </div>
  </div>

  {/* Rule metadata card */}
  <Card variant="elevated" className="mb-6">
    <Card.Content>
      <div className="grid grid-cols-3 gap-4">
        <div>
          <Text variant="secondary" size="sm">Stato</Text>
          <Badge variant={rule.enabled ? 'ember' : 'neutral'}>
            {rule.enabled ? 'Attiva' : 'Disattiva'}
          </Badge>
        </div>
        <div>
          <Text variant="secondary" size="sm">Creata il</Text>
          <Text>{rule.created_at ? new Date(rule.created_at).toLocaleDateString('it-IT') : '—'}</Text>
        </div>
        <div>
          <Text variant="secondary" size="sm">ID</Text>
          <Text variant="secondary" size="sm">{rule.id}</Text>
        </div>
      </div>
    </Card.Content>
  </Card>

  {/* Execution history section */}
  <Heading level={2} className="mb-4">Storico Esecuzioni</Heading>
  {executionsLoading ? <Skeleton rows={3} /> : (
    executions.length === 0 ? (
      <Card variant="elevated">
        <Card.Content>
          <Text variant="secondary">Nessuna esecuzione registrata</Text>
          <Text variant="secondary" size="sm">Questa regola non ha ancora eseguito nessuna azione.</Text>
        </Card.Content>
      </Card>
    ) : <DataTable columns={executionColumns} data={executions} />
  )}
  {/* Pagination for executions if totalCount > PAGE_SIZE */}
</SettingsLayout>
```

7. **Loading state:** When `ruleLoading` is true, show Skeleton for metadata + Skeleton for history.

8. **Error state:** When `ruleError`, show `<Banner variant="danger">{ruleError}</Banner>`.

Adapt exact component prop names from the rooms/page.tsx reference and the actual design system components.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && test -f app/automations/\[rule_id\]/page.tsx && grep -c "useParams\|DataTable\|AutomationExecution\|Completata\|Fallita" app/automations/\[rule_id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/automations/[rule_id]/page.tsx starts with `'use client'`
    - app/automations/[rule_id]/page.tsx contains `useParams` for extracting rule_id
    - app/automations/[rule_id]/page.tsx fetches from `/api/v1/automations/${ruleId}`
    - app/automations/[rule_id]/page.tsx fetches from `/api/v1/automations/${ruleId}/executions`
    - app/automations/[rule_id]/page.tsx contains `variant="sage"` with text `Completata`
    - app/automations/[rule_id]/page.tsx contains `variant="danger"` with text `Fallita`
    - app/automations/[rule_id]/page.tsx contains `variant="warning"` with text `In esecuzione`
    - app/automations/[rule_id]/page.tsx contains `Storico Esecuzioni` heading
    - app/automations/[rule_id]/page.tsx contains `Nessuna esecuzione registrata` empty state
    - app/automations/[rule_id]/page.tsx contains `toLocaleString('it-IT')` or `toLocaleDateString('it-IT')`
  </acceptance_criteria>
  <done>Rule detail page renders at /automations/[rule_id] with metadata card and paginated execution history DataTable, all Italian copy per UI-SPEC</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of Automations module</name>
  <files>app/automations/page.tsx, app/automations/[rule_id]/page.tsx</files>
  <action>
Human verifies the complete Automations module visually:
1. Run `npm run dev` and open http://localhost:3000
2. Verify "Automazioni" appears in the navigation menu with a Zap icon
3. Click "Automazioni" — verify it navigates to /automations
4. Verify the page shows "Automazioni" title, description, and "Nuova Regola" button
5. Click "Nuova Regola" — verify FormModal opens with Nome, Descrizione, Abilitata fields
6. Check form validation: submit empty form should show "Nome obbligatorio" error
7. If HA backend is available: create a rule, verify it appears in the list
8. If HA backend is available: click on a rule name to navigate to detail page, verify execution history table
9. Verify delete button shows "Elimina automazione" confirmation dialog
  </action>
  <verify>Manual visual inspection by user</verify>
  <done>User approves the Automations module UI or provides feedback for fixes</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> FormModal | Untrusted user text enters form fields |
| Client-side fetch -> API routes | Browser requests to /api/v1/automations/* |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-158-05 | Tampering | FormModal input (rule name/description) | mitigate | Zod validation (min 1, max 128 for name; max 500 for description) prevents oversized input |
| T-158-06 | Tampering | XSS via rule name in DataTable | accept | React JSX auto-escapes all rendered text — no dangerouslySetInnerHTML used |
| T-158-07 | Information Disclosure | Rule detail page | accept | Page fetches via auth-protected API route; no direct HA backend exposure |
</threat_model>

<verification>
1. `npm run dev` — both pages render without console errors
2. `grep "AUTOMAZIONI" lib/devices/deviceTypes.ts` — nav entry exists
3. `grep "Zap" app/components/Navbar.tsx` — icon mapping exists
4. `grep "api/v1/automations" app/automations/page.tsx` — correct API path used
5. `grep "api/v1/automations" app/automations/\[rule_id\]/page.tsx` — correct API path used
</verification>

<success_criteria>
- Automazioni nav entry visible in both desktop and mobile navigation
- /automations page renders with DataTable, FormModal for create/edit, ConfirmationDialog for delete
- /automations/[rule_id] page renders rule metadata and execution history DataTable
- All copy matches UI-SPEC Italian text exactly
- Form has Zod validation with error messages
- FormModal uses key prop to prevent stale data on edit
</success_criteria>

<output>
After completion, create `.planning/phases/158-automations-module/158-02-SUMMARY.md`
</output>
