---
phase: 160-sonos-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/v1/sonos/zones/[groupId]/playback/route.ts
  - app/api/v1/sonos/zones/[groupId]/playback/__tests__/route.test.ts
  - app/api/v1/sonos/zones/[groupId]/play/route.ts
  - app/api/v1/sonos/zones/[groupId]/play/__tests__/route.test.ts
  - app/api/v1/sonos/zones/[groupId]/pause/route.ts
  - app/api/v1/sonos/zones/[groupId]/pause/__tests__/route.test.ts
  - app/api/v1/sonos/zones/[groupId]/stop/route.ts
  - app/api/v1/sonos/zones/[groupId]/stop/__tests__/route.test.ts
  - app/api/v1/sonos/zones/[groupId]/next/route.ts
  - app/api/v1/sonos/zones/[groupId]/next/__tests__/route.test.ts
  - app/api/v1/sonos/zones/[groupId]/previous/route.ts
  - app/api/v1/sonos/zones/[groupId]/previous/__tests__/route.test.ts
autonomous: true
requirements:
  - SONOS-01
  - SONOS-02
  - SONOS-03
  - SONOS-04
  - SONOS-05
  - SONOS-06

must_haves:
  truths:
    - "GET /api/v1/sonos/zones/{groupId}/playback returns 200 with playback data"
    - "POST /api/v1/sonos/zones/{groupId}/play returns 202 Accepted"
    - "POST /api/v1/sonos/zones/{groupId}/pause returns 202 Accepted"
    - "POST /api/v1/sonos/zones/{groupId}/stop returns 202 Accepted"
    - "POST /api/v1/sonos/zones/{groupId}/next returns 202 Accepted"
    - "POST /api/v1/sonos/zones/{groupId}/previous returns 202 Accepted"
    - "All 6 routes return 401 when unauthenticated"
  artifacts:
    - path: "app/api/v1/sonos/zones/[groupId]/playback/route.ts"
      provides: "GET playback state"
      exports: ["GET"]
    - path: "app/api/v1/sonos/zones/[groupId]/play/route.ts"
      provides: "POST play command"
      exports: ["POST"]
    - path: "app/api/v1/sonos/zones/[groupId]/pause/route.ts"
      provides: "POST pause command"
      exports: ["POST"]
    - path: "app/api/v1/sonos/zones/[groupId]/stop/route.ts"
      provides: "POST stop command"
      exports: ["POST"]
    - path: "app/api/v1/sonos/zones/[groupId]/next/route.ts"
      provides: "POST next command"
      exports: ["POST"]
    - path: "app/api/v1/sonos/zones/[groupId]/previous/route.ts"
      provides: "POST previous command"
      exports: ["POST"]
  key_links:
    - from: "app/api/v1/sonos/zones/[groupId]/play/route.ts"
      to: "@/lib/sonos/sonosProxy"
      via: "import { play }"
      pattern: "import.*play.*from.*sonosProxy"
---

<objective>
Create v1 Sonos zone routes for playback state (GET) and 5 transport commands (play, pause, stop, next, previous — POST). Each route is a thin wrapper around existing proxy functions in `lib/sonos/sonosProxy.ts`, following the established `withAuthAndErrorHandler` pattern per D-01 and D-02.

Purpose: API alignment — canonical v1 paths for Sonos zone-level playback and transport endpoints per D-03.
Output: 6 route files + 6 test files under `app/api/v1/sonos/zones/[groupId]/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-sonos-gap-closure/160-CONTEXT.md
@.planning/phases/160-sonos-gap-closure/160-RESEARCH.md

<interfaces>
<!-- Existing proxy functions to call — executor must NOT create new proxy code -->

From lib/sonos/sonosProxy.ts:
- getPlayback(groupId: string): Promise<SonosPlaybackState>
- play(groupId: string): Promise<SonosCommandResponse>
- pause(groupId: string): Promise<SonosCommandResponse>
- stop(groupId: string): Promise<SonosCommandResponse>
- next(groupId: string): Promise<SonosCommandResponse>
- previous(groupId: string): Promise<SonosCommandResponse>

From lib/core:
- withAuthAndErrorHandler(handler, tag): wraps route with auth + error handling
- success(data, message?, status?): builds JSON response
- getPathParam(context, key): extracts dynamic route param (returns Promise)
- HTTP_STATUS.ACCEPTED: 202
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create playback GET route + test (SONOS-01)</name>
  <files>
    app/api/v1/sonos/zones/[groupId]/playback/route.ts
    app/api/v1/sonos/zones/[groupId]/playback/__tests__/route.test.ts
  </files>
  <read_first>
    app/api/sonos/zones/[groupId]/playback/route.ts
    app/api/v1/hue/lights/[lightId]/route.ts
    app/api/v1/hue/lights/[lightId]/__tests__/route.test.ts
  </read_first>
  <action>
Create `app/api/v1/sonos/zones/[groupId]/playback/route.ts` — an exact copy of the old-path route `app/api/sonos/zones/[groupId]/playback/route.ts` with the JSDoc updated to say `/api/v1/sonos/zones/[groupId]/playback`. Per D-02, the file must have:

1. `export const dynamic = 'force-dynamic'` as the first non-import export
2. `import { withAuthAndErrorHandler, success, getPathParam } from '@/lib/core'`
3. `import { getPlayback } from '@/lib/sonos/sonosProxy'`
4. `export const GET = withAuthAndErrorHandler(async (_request, context) => { ... }, 'Sonos/Zones/Playback')`
5. Inside handler: `const groupId = await getPathParam(context, 'groupId')`, then `const data = await getPlayback(groupId)`, then `return success(data as unknown as Record<string, unknown>)`

Create `app/api/v1/sonos/zones/[groupId]/playback/__tests__/route.test.ts` following the Phase 159 Hue test pattern:

1. `jest.mock('@/lib/sonos/sonosProxy')` and `jest.mock('@/lib/auth0', () => ({ auth0: { getSession: jest.fn() } }))` BEFORE imports
2. Import `GET` from `../route`, `* as sonosProxy` from `@/lib/sonos/sonosProxy`, `auth0` from `@/lib/auth0`
3. `mockContext = { params: Promise.resolve({ groupId: 'RINCON_123' }) }` — use Promise.resolve per Next.js 15
4. Test 1: 401 when `mockGetSession.mockResolvedValue(null)` — assert `response.status` is 401, `data.code` is `'UNAUTHORIZED'`
5. Test 2: 200 with playback data — mock `getPlayback` with `{ group_id: 'RINCON_123', transport_state: 'PLAYING' }`, assert `response.status` is 200, `data.success` is true, `mockGetPlayback.toHaveBeenCalledWith('RINCON_123')`
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npm test -- --testPathPattern="app/api/v1/sonos/zones/\\[groupId\\]/playback" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/api/v1/sonos/zones/[groupId]/playback/route.ts contains `export const GET`
    - app/api/v1/sonos/zones/[groupId]/playback/route.ts contains `export const dynamic = 'force-dynamic'`
    - app/api/v1/sonos/zones/[groupId]/playback/route.ts contains `getPlayback(groupId)`
    - app/api/v1/sonos/zones/[groupId]/playback/route.ts contains `'Sonos/Zones/Playback'`
    - app/api/v1/sonos/zones/[groupId]/playback/__tests__/route.test.ts contains `expect(response.status).toBe(200)`
    - app/api/v1/sonos/zones/[groupId]/playback/__tests__/route.test.ts contains `expect(response.status).toBe(401)`
    - Test file passes with 0 failures
  </acceptance_criteria>
  <done>GET /api/v1/sonos/zones/{groupId}/playback route exists, returns 200 with playback data, rejects 401 without auth. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create 5 transport POST command routes + tests (SONOS-02 through SONOS-06)</name>
  <files>
    app/api/v1/sonos/zones/[groupId]/play/route.ts
    app/api/v1/sonos/zones/[groupId]/play/__tests__/route.test.ts
    app/api/v1/sonos/zones/[groupId]/pause/route.ts
    app/api/v1/sonos/zones/[groupId]/pause/__tests__/route.test.ts
    app/api/v1/sonos/zones/[groupId]/stop/route.ts
    app/api/v1/sonos/zones/[groupId]/stop/__tests__/route.test.ts
    app/api/v1/sonos/zones/[groupId]/next/route.ts
    app/api/v1/sonos/zones/[groupId]/next/__tests__/route.test.ts
    app/api/v1/sonos/zones/[groupId]/previous/route.ts
    app/api/v1/sonos/zones/[groupId]/previous/__tests__/route.test.ts
  </files>
  <read_first>
    app/api/sonos/zones/[groupId]/play/route.ts
    app/api/v1/hue/health/__tests__/route.test.ts
  </read_first>
  <action>
Create 5 POST command routes under `app/api/v1/sonos/zones/[groupId]/`. Each is an exact copy of the corresponding old-path route with updated JSDoc path. Per D-03, all return 202 Accepted with `suggested_poll_delay_s: 1`. Per D-06, response shapes are identical to old routes.

For each of play, pause, stop, next, previous — create `route.ts`:

1. `import { withAuthAndErrorHandler, success, getPathParam, HTTP_STATUS } from '@/lib/core'`
2. `import { FUNCTION } from '@/lib/sonos/sonosProxy'` where FUNCTION is play/pause/stop/next/previous
3. `export const dynamic = 'force-dynamic'`
4. `export const POST = withAuthAndErrorHandler(async (_request, context) => { ... }, 'Sonos/Zones/TAG')` where TAG is Play/Pause/Stop/Next/Previous
5. Inside: `const groupId = await getPathParam(context, 'groupId')`, `const data = await FUNCTION(groupId)`, `return success({ ...data, suggested_poll_delay_s: 1 } as unknown as Record<string, unknown>, null, HTTP_STATUS.ACCEPTED)`

For each — create `__tests__/route.test.ts`:

1. `jest.mock('@/lib/sonos/sonosProxy')` and auth0 mock BEFORE imports
2. Import `POST` from `../route`
3. `mockContext = { params: Promise.resolve({ groupId: 'RINCON_123' }) }`
4. Test 1: 401 when not authenticated
5. Test 2: 202 with command response — mock the proxy function with `{ success: true }`, assert `response.status` is 202, `data.success` is true, `data.suggested_poll_delay_s` is 1, proxy function called with `'RINCON_123'`

Log tags per Claude's discretion:
- play: `'Sonos/Zones/Play'`
- pause: `'Sonos/Zones/Pause'`
- stop: `'Sonos/Zones/Stop'`
- next: `'Sonos/Zones/Next'`
- previous: `'Sonos/Zones/Previous'`
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npm test -- --testPathPattern="app/api/v1/sonos/zones/\\[groupId\\]/(play|pause|stop|next|previous)/" --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - app/api/v1/sonos/zones/[groupId]/play/route.ts contains `export const POST`
    - app/api/v1/sonos/zones/[groupId]/pause/route.ts contains `export const POST`
    - app/api/v1/sonos/zones/[groupId]/stop/route.ts contains `export const POST`
    - app/api/v1/sonos/zones/[groupId]/next/route.ts contains `export const POST`
    - app/api/v1/sonos/zones/[groupId]/previous/route.ts contains `export const POST`
    - All 5 route files contain `HTTP_STATUS.ACCEPTED`
    - All 5 route files contain `suggested_poll_delay_s: 1`
    - All 5 test files contain `expect(response.status).toBe(202)`
    - All 5 test files contain `expect(response.status).toBe(401)`
    - All 10 tests pass with 0 failures
  </acceptance_criteria>
  <done>All 5 transport POST routes exist at v1 paths, each returns 202 Accepted with suggested_poll_delay_s, rejects 401 without auth. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API route | Unauthenticated requests must be rejected with 401 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-01 | S (Spoofing) | All 6 route handlers | mitigate | `withAuthAndErrorHandler` rejects null sessions with 401 — verified in every test file |
| T-160-02 | T (Tampering) | POST body (none for these routes) | accept | Transport commands take no body — no input to tamper |
| T-160-03 | I (Information Disclosure) | Error messages | mitigate | `withAuthAndErrorHandler` catches errors and returns generic messages, no stack traces |
</threat_model>

<verification>
All 6 routes respond correctly:
- Playback GET: 200 with data
- 5 POST commands: 202 Accepted with suggested_poll_delay_s
- All reject with 401 when unauthenticated

```bash
npm test -- --testPathPattern="app/api/v1/sonos/zones" --no-coverage
```
</verification>

<success_criteria>
- 6 new route.ts files exist under app/api/v1/sonos/zones/[groupId]/
- 6 new test files exist with 100% pass rate
- GET playback returns 200, 5 POST commands return 202
- All routes use withAuthAndErrorHandler and return 401 without auth
- No changes to old-path routes or frontend hooks (per D-01 and D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/160-sonos-gap-closure/160-01-SUMMARY.md`
</output>
