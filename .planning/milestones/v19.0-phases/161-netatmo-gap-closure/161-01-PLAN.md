---
phase: 161-netatmo-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/netatmo/netatmoProxy.ts
  - types/netatmoProxy.ts
  - app/api/v1/netatmo/health/route.ts
  - app/api/v1/netatmo/health/__tests__/route.test.ts
  - app/api/v1/netatmo/homesdata/route.ts
  - app/api/v1/netatmo/homesdata/__tests__/route.test.ts
  - app/api/v1/netatmo/homestatus/route.ts
  - app/api/v1/netatmo/homestatus/__tests__/route.test.ts
  - app/api/v1/netatmo/getthermstate/route.ts
  - app/api/v1/netatmo/getthermstate/__tests__/route.test.ts
  - app/api/v1/netatmo/getroommeasure/route.ts
  - app/api/v1/netatmo/getroommeasure/__tests__/route.test.ts
  - app/api/v1/netatmo/gethomedata/route.ts
  - app/api/v1/netatmo/gethomedata/__tests__/route.test.ts
  - app/api/v1/netatmo/setroomthermpoint/route.ts
  - app/api/v1/netatmo/setroomthermpoint/__tests__/route.test.ts
  - app/api/v1/netatmo/setthermmode/route.ts
  - app/api/v1/netatmo/setthermmode/__tests__/route.test.ts
  - app/api/v1/netatmo/switchhomeschedule/route.ts
  - app/api/v1/netatmo/switchhomeschedule/__tests__/route.test.ts
  - app/api/v1/netatmo/synchomeschedule/route.ts
  - app/api/v1/netatmo/synchomeschedule/__tests__/route.test.ts
  - app/api/v1/netatmo/createnewhomeschedule/route.ts
  - app/api/v1/netatmo/createnewhomeschedule/__tests__/route.test.ts
  - app/api/v1/netatmo/renamehome/route.ts
  - app/api/v1/netatmo/renamehome/__tests__/route.test.ts
  - app/api/v1/netatmo/valves/route.ts
  - app/api/v1/netatmo/valves/__tests__/route.test.ts
  - app/api/v1/netatmo/valves/calibrate/route.ts
  - app/api/v1/netatmo/valves/calibrate/__tests__/route.test.ts
  - app/api/v1/netatmo/valves/[moduleId]/calibrate/route.ts
  - app/api/v1/netatmo/valves/[moduleId]/calibrate/__tests__/route.test.ts
autonomous: true
requirements:
  - NETA-01
  - NETA-02
  - NETA-03
  - NETA-08
  - NETA-09

must_haves:
  truths:
    - "GET /api/v1/netatmo/getthermstate returns 200 with thermostat state data"
    - "GET /api/v1/netatmo/gethomedata returns 200 with home data"
    - "POST /api/v1/netatmo/renamehome returns 202 Accepted"
    - "POST /api/v1/netatmo/valves/calibrate returns 202 Accepted"
    - "POST /api/v1/netatmo/valves/{moduleId}/calibrate returns 202 Accepted"
    - "All 13 routes return 401 when unauthenticated"
    - "GET routes for health, homesdata, homestatus, getroommeasure return 200"
    - "POST routes for setroomthermpoint, setthermmode, switchhomeschedule, synchomeschedule, createnewhomeschedule return 202"
  artifacts:
    - path: "lib/netatmo/netatmoProxy.ts"
      provides: "4 new proxy functions: getProxyThermState, proxyCalibrateValve, proxyRenameHome, getProxyHomeData"
    - path: "types/netatmoProxy.ts"
      provides: "4 new types: NetatmoThermstateResponse, CalibrateValveResponse, RenameHomeRequest, NetatmoHomedataResponse"
    - path: "app/api/v1/netatmo/getthermstate/route.ts"
      provides: "GET thermostat state"
      exports: ["GET"]
    - path: "app/api/v1/netatmo/gethomedata/route.ts"
      provides: "GET home data"
      exports: ["GET"]
    - path: "app/api/v1/netatmo/renamehome/route.ts"
      provides: "POST rename home"
      exports: ["POST"]
    - path: "app/api/v1/netatmo/valves/[moduleId]/calibrate/route.ts"
      provides: "POST calibrate single valve"
      exports: ["POST"]
  key_links:
    - from: "app/api/v1/netatmo/getthermstate/route.ts"
      to: "@/lib/netatmo/netatmoProxy"
      via: "import { getProxyThermState }"
      pattern: "import.*getProxyThermState.*from.*netatmoProxy"
    - from: "app/api/v1/netatmo/renamehome/route.ts"
      to: "@/lib/netatmo/netatmoProxy"
      via: "import { proxyRenameHome }"
      pattern: "import.*proxyRenameHome.*from.*netatmoProxy"
---

<objective>
Create 4 new proxy functions + types and 13 v1 Netatmo route wrappers covering thermostat, home management, schedules, and valve endpoints. Each route is a thin wrapper around proxy functions in `lib/netatmo/netatmoProxy.ts`, following the established `withAuthAndErrorHandler` pattern per D-01 and D-02.

Purpose: API alignment — canonical v1 paths for Netatmo thermostat, home, schedule, and valve endpoints per D-05.
Output: 4 new proxy functions, 4 new types, 13 route files + 13 test files under `app/api/v1/netatmo/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-netatmo-gap-closure/161-CONTEXT.md
@.planning/phases/161-netatmo-gap-closure/161-RESEARCH.md

<interfaces>
<!-- Existing proxy functions — executor uses these directly for route-only endpoints -->

From lib/netatmo/netatmoProxy.ts (existing, DO NOT recreate):
- getProxyHealth(): Promise<NetatmoHealthResponse>
- getProxyHomesdata(): Promise<NetatmoProxyHomesdataResponse>
- getProxyHomestatus(): Promise<NetatmoProxyHomestatusResponse>
- getProxyRoomMeasure(params: URLSearchParams): Promise<RoomMeasureResponse>
- proxySetRoomThermpoint(body: SetRoomThermpointRequest): Promise<ProxyControlResponse>
- proxySetThermMode(body: SetThermmodeRequest): Promise<SetThermmodeResponse>
- proxySwitchHomeSchedule(body: SwitchHomeScheduleRequest): Promise<ProxyControlResponse>
- proxySyncHomeSchedule(body: Record<string, unknown>): Promise<ProxyControlResponse>
- proxyCreateNewHomeSchedule(body: Record<string, unknown>): Promise<ProxyControlResponse>
- getProxyValves(): Promise<ValveStatusResponse>
- proxyCalibrateValves(): Promise<CalibrateBatchResponse>

From lib/core:
- withAuthAndErrorHandler(handler, tag): wraps route with auth + error handling
- success(data, message?, status?): builds JSON response
- getPathParam(context, key): extracts dynamic route param (returns Promise)
- parseJson(request): parses JSON body
- HTTP_STATUS.ACCEPTED: 202

From types/netatmoProxy.ts (existing):
- SetRoomThermpointRequest, SetThermmodeRequest, SwitchHomeScheduleRequest
- ProxyControlResponse, SetThermmodeResponse, ValveStatusResponse, CalibrateBatchResponse
- NetatmoHealthResponse, NetatmoProxyHomestatusResponse, NetatmoProxyHomesdataResponse, RoomMeasureResponse
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 new proxy functions + 4 new types, create 6 GET route wrappers + tests (NETA-01, NETA-09)</name>
  <files>
    lib/netatmo/netatmoProxy.ts
    types/netatmoProxy.ts
    app/api/v1/netatmo/health/route.ts
    app/api/v1/netatmo/health/__tests__/route.test.ts
    app/api/v1/netatmo/homesdata/route.ts
    app/api/v1/netatmo/homesdata/__tests__/route.test.ts
    app/api/v1/netatmo/homestatus/route.ts
    app/api/v1/netatmo/homestatus/__tests__/route.test.ts
    app/api/v1/netatmo/getthermstate/route.ts
    app/api/v1/netatmo/getthermstate/__tests__/route.test.ts
    app/api/v1/netatmo/getroommeasure/route.ts
    app/api/v1/netatmo/getroommeasure/__tests__/route.test.ts
    app/api/v1/netatmo/gethomedata/route.ts
    app/api/v1/netatmo/gethomedata/__tests__/route.test.ts
  </files>
  <read_first>
    lib/netatmo/netatmoProxy.ts
    types/netatmoProxy.ts
    app/api/v1/thermorossi/status/route.ts
    app/api/v1/sonos/zones/[groupId]/playback/route.ts
    app/api/v1/sonos/zones/[groupId]/playback/__tests__/route.test.ts
    app/api/netatmo/getroommeasure/route.ts
    docs/api/netatmo.md
  </read_first>
  <action>
**Step A: Add 4 new types to `types/netatmoProxy.ts`** (per D-04)

Append these interfaces at the end of the file:

```typescript
/** GET /getthermstate response */
interface NetatmoSetpoint {
  setpoint_mode: string;
  setpoint_temp: number | null;
  setpoint_endtime: number | null;
}
interface NetatmoThermProgram {
  program_id: string;
  name: string;
  selected: number;
  timetable: Record<string, unknown>[];
}
export interface NetatmoThermstateResponse {
  body: {
    status: string;
    setpoint: NetatmoSetpoint;
    therm_program_list: NetatmoThermProgram[];
    device_id: string;
  };
  status: string;
  time_exec: number;
  time_server: number;
}

/** POST /renamehome request body */
export interface RenameHomeRequest {
  home_id: string;
  name: string;
}

/** GET /gethomedata response */
export interface NetatmoHomedataResponse {
  body: {
    homes: Array<{
      id: string;
      cameras: Record<string, unknown>[];
      smokedetectors: Record<string, unknown>[];
      persons: Record<string, unknown>[];
    }>;
    global_info: Record<string, unknown>;
  };
  status: string;
  time_exec: number;
  time_server: number;
}

/** POST /valves/{module_id}/calibrate response */
export interface CalibrateValveResponse {
  status: 'accepted';
  module_id: string;
  poll_endpoint: string;
}
```

**Step B: Add 4 new proxy functions to `lib/netatmo/netatmoProxy.ts`** (per D-03)

Add imports for the new types at the top, then append functions at the end:

```typescript
import type { NetatmoThermstateResponse, RenameHomeRequest, NetatmoHomedataResponse, CalibrateValveResponse } from '@/types/netatmoProxy';

export async function getProxyThermState(params: URLSearchParams): Promise<NetatmoThermstateResponse> {
  return haGet<NetatmoThermstateResponse>(`/api/v1/netatmo/getthermstate?${params.toString()}`);
}

export async function proxyCalibrateValve(moduleId: string): Promise<CalibrateValveResponse> {
  return haPost<CalibrateValveResponse>(`/api/v1/netatmo/valves/${moduleId}/calibrate`, {});
}

export async function proxyRenameHome(body: RenameHomeRequest): Promise<ProxyControlResponse> {
  return haPost<ProxyControlResponse>('/api/v1/netatmo/renamehome', body as unknown as Record<string, unknown>);
}

export async function getProxyHomeData(): Promise<NetatmoHomedataResponse> {
  return haGet<NetatmoHomedataResponse>('/api/v1/netatmo/gethomedata');
}
```

**Step C: Create 6 GET route files** (per D-02, D-05)

All follow this exact pattern — `export const dynamic = 'force-dynamic'`, `withAuthAndErrorHandler` wrapper, proxy function delegation, `success()` 200 response.

**health/route.ts** — tag `'Netatmo/Health'`:
```typescript
import { withAuthAndErrorHandler, success } from '@/lib/core';
import { getProxyHealth } from '@/lib/netatmo/netatmoProxy';
export const dynamic = 'force-dynamic';
export const GET = withAuthAndErrorHandler(async () => {
  const data = await getProxyHealth();
  return success(data as unknown as Record<string, unknown>);
}, 'Netatmo/Health');
```

**homesdata/route.ts** — tag `'Netatmo/Homesdata'`, calls `getProxyHomesdata()`.

**homestatus/route.ts** — tag `'Netatmo/Homestatus'`, calls `getProxyHomestatus()`.

**gethomedata/route.ts** — tag `'Netatmo/GetHomeData'`, calls `getProxyHomeData()`. (NETA-09)

**getthermstate/route.ts** — tag `'Netatmo/GetThermState'` (NETA-01). This route MUST read query params from request:
```typescript
import { withAuthAndErrorHandler, success } from '@/lib/core';
import { getProxyThermState } from '@/lib/netatmo/netatmoProxy';
import type { NextRequest } from 'next/server';
export const dynamic = 'force-dynamic';
export const GET = withAuthAndErrorHandler(async (request: NextRequest) => {
  const { searchParams } = new URL(request.url);
  const data = await getProxyThermState(searchParams);
  return success(data as unknown as Record<string, unknown>);
}, 'Netatmo/GetThermState');
```

**getroommeasure/route.ts** — tag `'Netatmo/GetRoomMeasure'`. Same query-param-forwarding pattern as getthermstate:
```typescript
import { withAuthAndErrorHandler, success } from '@/lib/core';
import { getProxyRoomMeasure } from '@/lib/netatmo/netatmoProxy';
import type { NextRequest } from 'next/server';
export const dynamic = 'force-dynamic';
export const GET = withAuthAndErrorHandler(async (request: NextRequest) => {
  const { searchParams } = new URL(request.url);
  const data = await getProxyRoomMeasure(searchParams);
  return success(data as unknown as Record<string, unknown>);
}, 'Netatmo/GetRoomMeasure');
```

**Step D: Create 6 test files** (per D-08)

Each test file follows this exact structure:

```typescript
jest.mock('@/lib/netatmo/netatmoProxy');
jest.mock('@/lib/auth0', () => ({
  auth0: { getSession: jest.fn() },
}));

import { GET } from '../route';
import * as netatmoProxy from '@/lib/netatmo/netatmoProxy';
import { auth0 } from '@/lib/auth0';

const mockGetSession = jest.mocked(auth0.getSession);
const mockProxyFn = jest.mocked(netatmoProxy.FUNCTION_NAME);
const mockSession = { user: { sub: 'auth0|123', email: 'test@test.com' } };

describe('GET /api/v1/netatmo/ENDPOINT', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockGetSession.mockResolvedValue(mockSession as any);
    jest.spyOn(console, 'error').mockImplementation(() => {});
    jest.spyOn(console, 'warn').mockImplementation(() => {});
  });

  it('should return 401 when not authenticated', async () => {
    mockGetSession.mockResolvedValue(null);
    const request = new Request('http://localhost:3000/api/v1/netatmo/ENDPOINT');
    const response = await GET(request as any, {} as any);
    const data = await response.json();
    expect(response.status).toBe(401);
    expect(data.code).toBe('UNAUTHORIZED');
  });

  it('should return 200 with data', async () => {
    mockProxyFn.mockResolvedValue({ MOCK_DATA } as any);
    const request = new Request('http://localhost:3000/api/v1/netatmo/ENDPOINT');
    const response = await GET(request as any, {} as any);
    const data = await response.json();
    expect(response.status).toBe(200);
    expect(data.success).toBe(true);
    expect(mockProxyFn).toHaveBeenCalled();
  });
});
```

Mock data per route:
- **health**: `{ status: 'ok', provider: 'netatmo' }`
- **homesdata**: `{ homes: [] }`
- **homestatus**: `{ body: { home: {} } }`
- **gethomedata**: `{ body: { homes: [], global_info: {} } }`
- **getthermstate**: `{ body: { status: 'ok', device_id: '09:00:00:aa:bb:cc' } }` — request URL must include `?device_id=09:00:00:aa:bb:cc`, assert `mockProxyFn.toHaveBeenCalledWith(expect.any(URLSearchParams))`
- **getroommeasure**: `{ body: [{ beg_time: 1000, value: [[20.5]] }] }` — request URL must include `?home_id=abc&room_id=123&scale=1hour&type=temperature`, assert `mockProxyFn.toHaveBeenCalledWith(expect.any(URLSearchParams))`
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npm test -- --testPathPattern="app/api/v1/netatmo/(health|homesdata|homestatus|getthermstate|getroommeasure|gethomedata)" --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - types/netatmoProxy.ts contains `export interface NetatmoThermstateResponse`
    - types/netatmoProxy.ts contains `export interface RenameHomeRequest`
    - types/netatmoProxy.ts contains `export interface NetatmoHomedataResponse`
    - types/netatmoProxy.ts contains `export interface CalibrateValveResponse`
    - lib/netatmo/netatmoProxy.ts contains `export async function getProxyThermState`
    - lib/netatmo/netatmoProxy.ts contains `export async function proxyCalibrateValve`
    - lib/netatmo/netatmoProxy.ts contains `export async function proxyRenameHome`
    - lib/netatmo/netatmoProxy.ts contains `export async function getProxyHomeData`
    - app/api/v1/netatmo/health/route.ts contains `export const GET`
    - app/api/v1/netatmo/homesdata/route.ts contains `export const GET`
    - app/api/v1/netatmo/homestatus/route.ts contains `export const GET`
    - app/api/v1/netatmo/getthermstate/route.ts contains `new URL(request.url)`
    - app/api/v1/netatmo/getthermstate/route.ts contains `getProxyThermState(searchParams)`
    - app/api/v1/netatmo/getroommeasure/route.ts contains `getProxyRoomMeasure(searchParams)`
    - app/api/v1/netatmo/gethomedata/route.ts contains `getProxyHomeData()`
    - All 6 test files contain `expect(response.status).toBe(401)`
    - All 6 test files contain `expect(response.status).toBe(200)`
    - All 12 tests pass with 0 failures
  </acceptance_criteria>
  <done>4 new proxy functions and 4 new types added. 6 GET v1 routes created with co-located tests. getthermstate and getroommeasure forward query params. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create 7 POST/command route wrappers + valve GET + tests (NETA-02, NETA-03, NETA-08)</name>
  <files>
    app/api/v1/netatmo/setroomthermpoint/route.ts
    app/api/v1/netatmo/setroomthermpoint/__tests__/route.test.ts
    app/api/v1/netatmo/setthermmode/route.ts
    app/api/v1/netatmo/setthermmode/__tests__/route.test.ts
    app/api/v1/netatmo/switchhomeschedule/route.ts
    app/api/v1/netatmo/switchhomeschedule/__tests__/route.test.ts
    app/api/v1/netatmo/synchomeschedule/route.ts
    app/api/v1/netatmo/synchomeschedule/__tests__/route.test.ts
    app/api/v1/netatmo/createnewhomeschedule/route.ts
    app/api/v1/netatmo/createnewhomeschedule/__tests__/route.test.ts
    app/api/v1/netatmo/renamehome/route.ts
    app/api/v1/netatmo/renamehome/__tests__/route.test.ts
    app/api/v1/netatmo/valves/route.ts
    app/api/v1/netatmo/valves/__tests__/route.test.ts
    app/api/v1/netatmo/valves/calibrate/route.ts
    app/api/v1/netatmo/valves/calibrate/__tests__/route.test.ts
    app/api/v1/netatmo/valves/[moduleId]/calibrate/route.ts
    app/api/v1/netatmo/valves/[moduleId]/calibrate/__tests__/route.test.ts
  </files>
  <read_first>
    app/api/v1/sonos/zones/[groupId]/play/route.ts
    app/api/v1/sonos/zones/[groupId]/play/__tests__/route.test.ts
    app/api/v1/sonos/zones/[groupId]/seek/route.ts
    lib/netatmo/netatmoProxy.ts
  </read_first>
  <action>
**Step A: Create 1 GET route — valves** (per D-05)

**valves/route.ts** — tag `'Netatmo/Valves'`:
```typescript
import { withAuthAndErrorHandler, success } from '@/lib/core';
import { getProxyValves } from '@/lib/netatmo/netatmoProxy';
export const dynamic = 'force-dynamic';
export const GET = withAuthAndErrorHandler(async () => {
  const data = await getProxyValves();
  return success(data as unknown as Record<string, unknown>);
}, 'Netatmo/Valves');
```

**Step B: Create 5 POST command routes with body parsing** (per D-02, D-09 — 202 Accepted with `suggested_poll_delay_s: 1`)

All follow this exact pattern:
```typescript
import { withAuthAndErrorHandler, success, parseJson, HTTP_STATUS } from '@/lib/core';
import { PROXY_FN } from '@/lib/netatmo/netatmoProxy';
import type { REQUEST_TYPE } from '@/types/netatmoProxy';
export const dynamic = 'force-dynamic';
export const POST = withAuthAndErrorHandler(async (request) => {
  const body = await parseJson(request) as REQUEST_TYPE;
  const data = await PROXY_FN(body);
  return success(
    { ...data, suggested_poll_delay_s: 1 } as unknown as Record<string, unknown>,
    null,
    HTTP_STATUS.ACCEPTED
  );
}, 'TAG');
```

Route-specific values:

| Route | Proxy fn | Request type | Tag |
|-------|----------|--------------|-----|
| setroomthermpoint | `proxySetRoomThermpoint(body)` | `SetRoomThermpointRequest` | `'Netatmo/SetRoomThermpoint'` |
| setthermmode | `proxySetThermMode(body)` | `SetThermmodeRequest` | `'Netatmo/SetThermMode'` |
| switchhomeschedule | `proxySwitchHomeSchedule(body)` | `SwitchHomeScheduleRequest` | `'Netatmo/SwitchHomeSchedule'` |
| synchomeschedule | `proxySyncHomeSchedule(body)` | `Record<string, unknown>` (no import needed) | `'Netatmo/SyncHomeSchedule'` |
| createnewhomeschedule | `proxyCreateNewHomeSchedule(body)` | `Record<string, unknown>` (no import needed) | `'Netatmo/CreateNewHomeSchedule'` |

For synchomeschedule and createnewhomeschedule, cast body as `Record<string, unknown>` directly — their proxy functions already accept `Record<string, unknown>`.

**Step C: Create renamehome POST route** (NETA-08, per D-03)

**renamehome/route.ts** — tag `'Netatmo/RenameHome'`:
```typescript
import { withAuthAndErrorHandler, success, parseJson, HTTP_STATUS } from '@/lib/core';
import { proxyRenameHome } from '@/lib/netatmo/netatmoProxy';
import type { RenameHomeRequest } from '@/types/netatmoProxy';
export const dynamic = 'force-dynamic';
export const POST = withAuthAndErrorHandler(async (request) => {
  const body = await parseJson(request) as RenameHomeRequest;
  const data = await proxyRenameHome(body);
  return success(
    { ...data, suggested_poll_delay_s: 1 } as unknown as Record<string, unknown>,
    null,
    HTTP_STATUS.ACCEPTED
  );
}, 'Netatmo/RenameHome');
```

**Step D: Create 2 valve command routes** (NETA-02, NETA-03)

**valves/calibrate/route.ts** — tag `'Netatmo/Valves/CalibrateBatch'`, no body, calls `proxyCalibrateValves()`:
```typescript
import { withAuthAndErrorHandler, success, HTTP_STATUS } from '@/lib/core';
import { proxyCalibrateValves } from '@/lib/netatmo/netatmoProxy';
export const dynamic = 'force-dynamic';
export const POST = withAuthAndErrorHandler(async () => {
  const data = await proxyCalibrateValves();
  return success(
    { ...data, suggested_poll_delay_s: 1 } as unknown as Record<string, unknown>,
    null,
    HTTP_STATUS.ACCEPTED
  );
}, 'Netatmo/Valves/CalibrateBatch');
```

**valves/[moduleId]/calibrate/route.ts** — tag `'Netatmo/Valves/Calibrate'`, uses path param (NETA-03):
```typescript
import { withAuthAndErrorHandler, success, getPathParam, HTTP_STATUS } from '@/lib/core';
import { proxyCalibrateValve } from '@/lib/netatmo/netatmoProxy';
export const dynamic = 'force-dynamic';
export const POST = withAuthAndErrorHandler(async (_request, context) => {
  const moduleId = await getPathParam(context, 'moduleId');
  const data = await proxyCalibrateValve(moduleId);
  return success(
    { ...data, suggested_poll_delay_s: 1 } as unknown as Record<string, unknown>,
    null,
    HTTP_STATUS.ACCEPTED
  );
}, 'Netatmo/Valves/Calibrate');
```

**Step E: Create 9 test files** (per D-08)

All POST test files follow this pattern:
```typescript
jest.mock('@/lib/netatmo/netatmoProxy');
jest.mock('@/lib/auth0', () => ({
  auth0: { getSession: jest.fn() },
}));

import { POST } from '../route';
import * as netatmoProxy from '@/lib/netatmo/netatmoProxy';
import { auth0 } from '@/lib/auth0';

const mockGetSession = jest.mocked(auth0.getSession);
const mockProxyFn = jest.mocked(netatmoProxy.FUNCTION_NAME);
const mockSession = { user: { sub: 'auth0|123', email: 'test@test.com' } };

describe('POST /api/v1/netatmo/ENDPOINT', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    mockGetSession.mockResolvedValue(mockSession as any);
    jest.spyOn(console, 'error').mockImplementation(() => {});
    jest.spyOn(console, 'warn').mockImplementation(() => {});
  });

  it('should return 401 when not authenticated', async () => {
    mockGetSession.mockResolvedValue(null);
    const request = new Request('http://localhost:3000/api/v1/netatmo/ENDPOINT', { method: 'POST', body: JSON.stringify({}), headers: { 'Content-Type': 'application/json' } });
    const response = await POST(request as any, {} as any);
    const data = await response.json();
    expect(response.status).toBe(401);
    expect(data.code).toBe('UNAUTHORIZED');
  });

  it('should return 202 with suggested_poll_delay_s', async () => {
    mockProxyFn.mockResolvedValue({ status: 'ok' } as any);
    const request = new Request('http://localhost:3000/api/v1/netatmo/ENDPOINT', { method: 'POST', body: JSON.stringify(MOCK_BODY), headers: { 'Content-Type': 'application/json' } });
    const response = await POST(request as any, {} as any);
    const data = await response.json();
    expect(response.status).toBe(202);
    expect(data.success).toBe(true);
    expect(data.suggested_poll_delay_s).toBe(1);
    expect(mockProxyFn).toHaveBeenCalled();
  });
});
```

Mock bodies per route:
- **setroomthermpoint**: `{ home_id: 'abc', room_id: '1', temp: 20, mode: 'manual' }`
- **setthermmode**: `{ home_id: 'abc', mode: 'schedule' }`
- **switchhomeschedule**: `{ home_id: 'abc', schedule_id: 'sched1' }`
- **synchomeschedule**: `{ home_id: 'abc', zones: [] }`
- **createnewhomeschedule**: `{ home_id: 'abc', name: 'New Schedule' }`
- **renamehome**: `{ home_id: 'abc', name: 'My Home' }`
- **valves/calibrate**: no body (empty `{}`)
- **valves/[moduleId]/calibrate**: no body, but needs `mockContext = { params: Promise.resolve({ moduleId: '04:00:00:aa:bb:cc' }) }`. Assert `mockProxyFn.toHaveBeenCalledWith('04:00:00:aa:bb:cc')`

**valves GET test**: import `GET` (not POST), mock `getProxyValves`, test 401, test 200 with mock data `{ valves: [], freshness: 'LIVE' }`.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npm test -- --testPathPattern="app/api/v1/netatmo/(setroomthermpoint|setthermmode|switchhomeschedule|synchomeschedule|createnewhomeschedule|renamehome|valves)" --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - app/api/v1/netatmo/setroomthermpoint/route.ts contains `export const POST`
    - app/api/v1/netatmo/setthermmode/route.ts contains `export const POST`
    - app/api/v1/netatmo/switchhomeschedule/route.ts contains `export const POST`
    - app/api/v1/netatmo/synchomeschedule/route.ts contains `export const POST`
    - app/api/v1/netatmo/createnewhomeschedule/route.ts contains `export const POST`
    - app/api/v1/netatmo/renamehome/route.ts contains `RenameHomeRequest`
    - app/api/v1/netatmo/renamehome/route.ts contains `proxyRenameHome`
    - app/api/v1/netatmo/valves/route.ts contains `export const GET`
    - app/api/v1/netatmo/valves/calibrate/route.ts contains `proxyCalibrateValves()`
    - app/api/v1/netatmo/valves/[moduleId]/calibrate/route.ts contains `getPathParam(context, 'moduleId')`
    - app/api/v1/netatmo/valves/[moduleId]/calibrate/route.ts contains `proxyCalibrateValve(moduleId)`
    - All 7 POST route files contain `HTTP_STATUS.ACCEPTED`
    - All 7 POST route files contain `suggested_poll_delay_s: 1`
    - All 9 test files contain `expect(response.status).toBe(401)`
    - All 7 POST test files contain `expect(response.status).toBe(202)`
    - valves GET test contains `expect(response.status).toBe(200)`
    - All tests pass with 0 failures
  </acceptance_criteria>
  <done>7 POST command routes + 1 GET valves route created with 9 co-located test files. All POST routes return 202 with suggested_poll_delay_s. Valve single-calibrate uses moduleId path param. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API route | Unauthenticated requests must be rejected with 401 |
| client -> POST body | setroomthermpoint, setthermmode, switchhomeschedule, synchomeschedule, createnewhomeschedule, renamehome accept JSON body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-01 | S (Spoofing) | All 13 route handlers | mitigate | `withAuthAndErrorHandler` rejects null sessions with 401 — verified in every test file |
| T-161-02 | T (Tampering) | POST body fields | accept | TypeScript type casting on parsed body; HA proxy validates upstream; single-user app |
| T-161-03 | I (Information Disclosure) | Error messages | mitigate | `withAuthAndErrorHandler` catches errors and returns RFC 9457 generic messages, no stack traces |
| T-161-04 | T (Tampering) | moduleId path param | accept | Path param is MAC address format; HA proxy validates module existence upstream |
</threat_model>

<verification>
All 13 routes respond correctly:
- 6 GET routes: 200 with data (health, homesdata, homestatus, getthermstate, getroommeasure, gethomedata, valves)
- 7 POST command routes: 202 Accepted with suggested_poll_delay_s
- All reject with 401 when unauthenticated

```bash
npm test -- --testPathPattern="app/api/v1/netatmo/(health|homesdata|homestatus|getthermstate|getroommeasure|gethomedata|setroomthermpoint|setthermmode|switchhomeschedule|synchomeschedule|createnewhomeschedule|renamehome|valves)" --no-coverage
```
</verification>

<success_criteria>
- 4 new proxy functions exist in lib/netatmo/netatmoProxy.ts
- 4 new types exist in types/netatmoProxy.ts
- 13 new route.ts files exist under app/api/v1/netatmo/
- 13 new test files exist with 100% pass rate
- getthermstate and getroommeasure forward query params via URLSearchParams
- valves/[moduleId]/calibrate uses getPathParam for dynamic segment
- No changes to old-path routes or frontend hooks (per D-06 and D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/161-netatmo-gap-closure/161-01-SUMMARY.md`
</output>
