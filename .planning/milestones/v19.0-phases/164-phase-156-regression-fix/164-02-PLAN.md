---
phase: 164
plan: 02
type: execute
wave: 2
depends_on:
  - 164-01
files_modified:
  - __tests__/components/devices/stove/hooks/useStoveData.test.ts
  - __tests__/components/devices/stove/hooks/useStoveCommands.test.ts
  - lib/retry/__tests__/idempotencyManager.test.ts
  - lib/hooks/__tests__/useRetryableCommand.test.ts
autonomous: true
requirements:
  - PATH-02
must_haves:
  truths:
    - "useStoveData test asserts fetch against canonical status path"
    - "useStoveCommands tests assert canonical paths for ignite, shutdown, power, fan-level flows"
    - "idempotencyManager tests use canonical paths as cache-key discriminators"
    - "useRetryableCommand tests invoke execute() with canonical path inputs"
    - "Entire repository (excluding lib/version.ts and .planning/) contains zero /api/stove/ string literals"
  artifacts:
    - path: "__tests__/components/devices/stove/hooks/useStoveData.test.ts"
      provides: "Status fetch assertion"
      contains: "/api/v1/thermorossi/status"
    - path: "__tests__/components/devices/stove/hooks/useStoveCommands.test.ts"
      provides: "Command fetch assertions"
      contains: "/api/v1/thermorossi/"
    - path: "lib/retry/__tests__/idempotencyManager.test.ts"
      provides: "Cache-key fixtures"
      contains: "/api/v1/thermorossi/"
    - path: "lib/hooks/__tests__/useRetryableCommand.test.ts"
      provides: "Retry path inputs"
      contains: "/api/v1/thermorossi/"
  key_links:
    - from: "Jest test suites (stove|idempotency|useRetryableCommand)"
      to: "canonical /api/v1/thermorossi/* paths"
      via: "string literals in expect.stringContaining / execute() inputs / fixture endpoint fields"
      pattern: "/api/v1/thermorossi/"
    - from: "Repo-wide grep sweep"
      to: "zero /api/stove/ refs"
      via: "final verification command excluding lib/version.ts"
      pattern: "^0$"
---

<objective>
Retarget the four test files that still assert against legacy `/api/stove/*` paths to the canonical `/api/v1/thermorossi/*` tree, then run a final repo-wide grep sweep proving zero legacy references remain outside `lib/version.ts`.

Purpose: Close the test-assertion half of PATH-02. Plan 01 left `/api/stove/` refs only in test fixtures and `execute()` input strings — this plan eliminates them while preserving test coverage. The final grep sweep is the phase-level exit gate for PATH-01 + PATH-02.

Output: All four test files green against the canonical paths; repo-wide grep returns zero `/api/stove/` matches (excluding the historical `lib/version.ts` and the `.planning/` directory).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-phase-156-regression-fix/164-RESEARCH.md
@.planning/phases/164-phase-156-regression-fix/164-VALIDATION.md
@.planning/phases/164-phase-156-regression-fix/164-01-SUMMARY.md

<interfaces>
Legacy-to-canonical mapping (authoritative — same as Plan 01):

| Legacy fixture string | Canonical replacement | Notes |
|-----------------------|-----------------------|-------|
| /api/stove/status | /api/v1/thermorossi/status | GET |
| /api/stove/health | /api/v1/thermorossi/health | GET |
| /api/stove/getPower | /api/v1/thermorossi/power | GET |
| /api/stove/getFan | /api/v1/thermorossi/fan-level | GET |
| /api/stove/history | /api/v1/thermorossi/history | GET |
| /api/stove/ignite | /api/v1/thermorossi/commands/ignit | POST — note `ignit` no trailing `e` |
| /api/stove/shutdown | /api/v1/thermorossi/commands/shutdown | POST |
| /api/stove/setPower | /api/v1/thermorossi/settings/power | POST, body `{ value }` |
| /api/stove/setFan | /api/v1/thermorossi/settings/fan-level | POST, body `{ value }` |
| /api/stove/setWaterTemperature | /api/v1/thermorossi/settings/temperature/water | POST, body `{ value }` |

Idempotency manager tests use URLs as opaque cache-key discriminators — any unique string works. Retarget mechanically to the canonical equivalent per the table above; test semantics are unaffected.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retarget useStoveData test to canonical status path</name>
  <files>__tests__/components/devices/stove/hooks/useStoveData.test.ts</files>
  <read_first>
    - `__tests__/components/devices/stove/hooks/useStoveData.test.ts` — the single legacy assertion is around line 141: `expect.stringContaining('/api/stove/status')` or equivalent. Find every `/api/stove/` occurrence via Grep before editing.
    - `lib/routes.ts` STOVE_ROUTES block (now canonical after Plan 01) — confirm `STOVE_ROUTES.status` resolves to `/api/v1/thermorossi/status` so the test remains semantically correct.
  </read_first>
  <behavior>
    - The test that previously asserted the hook fetched `'/api/stove/status'` now asserts the hook fetches `'/api/v1/thermorossi/status'`.
    - No test is added, removed, renamed, or restructured. Only string literals change.
    - All other tests in the file continue to pass.
  </behavior>
  <action>
    Replace every `/api/stove/status` string literal in this file with `/api/v1/thermorossi/status`. Typical patterns to touch:
    - `expect(fetchSpy).toHaveBeenCalledWith(expect.stringContaining('/api/stove/status'), ...)` → replace inner string
    - Any mocked response keyed by URL path
    - Any fixture constant defined as `const STATUS_URL = '/api/stove/status'`

    Do not alter test names, describe blocks, beforeEach/afterEach setup, mock factories, or assertion helpers.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=useStoveData --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=useStoveData --no-coverage` exits 0
    - `grep -c "/api/stove/" __tests__/components/devices/stove/hooks/useStoveData.test.ts` returns 0
    - `grep -c "/api/v1/thermorossi/status" __tests__/components/devices/stove/hooks/useStoveData.test.ts` returns at least 1
  </acceptance_criteria>
  <done>useStoveData test asserts canonical status path and passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retarget useStoveCommands test to canonical command paths</name>
  <files>__tests__/components/devices/stove/hooks/useStoveCommands.test.ts</files>
  <read_first>
    - `__tests__/components/devices/stove/hooks/useStoveCommands.test.ts` — 9 legacy refs at approximately lines 71, 187, 246, 260, 284, 298, 331, 355, 369. These are a mix of `poll_endpoint` fixture values and `expect.stringContaining` assertions.
    - `lib/routes.ts` STOVE_ROUTES (canonical after Plan 01) — the hook under test consumes these constants, so tests assert against the canonical paths they now resolve to.
    - `app/components/devices/stove/hooks/useStoveCommands.ts` — skim to confirm which endpoints are exercised (ignite, shutdown, setPower, setFan).
  </read_first>
  <behavior>
    - Every legacy URL string is replaced by its canonical counterpart per the mapping table.
    - `ignite` flows assert against `/api/v1/thermorossi/commands/ignit` (note `ignit` — no trailing `e`).
    - `shutdown` flows assert against `/api/v1/thermorossi/commands/shutdown`.
    - `setPower` / `setFan` flows assert against `/api/v1/thermorossi/settings/power` and `/api/v1/thermorossi/settings/fan-level`.
    - `poll_endpoint` fixture values align with canonical GET paths (`/api/v1/thermorossi/status` or equivalent per hook behavior).
    - All tests continue to pass; no test is added, removed, renamed, or restructured.
  </behavior>
  <action>
    Work through each legacy occurrence and replace per the mapping table. Representative rewrites:
    - `expect.stringContaining('/api/stove/ignite')` → `expect.stringContaining('/api/v1/thermorossi/commands/ignit')`
    - `expect.stringContaining('/api/stove/shutdown')` → `expect.stringContaining('/api/v1/thermorossi/commands/shutdown')`
    - `expect.stringContaining('/api/stove/setPower')` → `expect.stringContaining('/api/v1/thermorossi/settings/power')`
    - `expect.stringContaining('/api/stove/setFan')` → `expect.stringContaining('/api/v1/thermorossi/settings/fan-level')`
    - `poll_endpoint: '/api/stove/status'` → `poll_endpoint: '/api/v1/thermorossi/status'`

    Do not change mock bodies, response shapes, command parameters, or request body keys unless the test explicitly asserts against the body `{ level }` key — in which case update it to `{ value }` to match canonical validation (Plan 01 changed the hook to send `{ value }`; if any assertion checks the outgoing body, align it here).

    If you discover a legacy reference not listed in the mapping table, stop and flag it in the summary. Do not invent a canonical equivalent.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=useStoveCommands --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=useStoveCommands --no-coverage` exits 0
    - `grep -c "/api/stove/" __tests__/components/devices/stove/hooks/useStoveCommands.test.ts` returns 0
    - `grep -c "/api/v1/thermorossi/" __tests__/components/devices/stove/hooks/useStoveCommands.test.ts` returns at least 4 (ignit, shutdown, settings/power, settings/fan-level)
  </acceptance_criteria>
  <done>useStoveCommands test suite green with canonical path assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retarget idempotencyManager test cache-key fixtures</name>
  <files>lib/retry/__tests__/idempotencyManager.test.ts</files>
  <read_first>
    - `lib/retry/__tests__/idempotencyManager.test.ts` — 20+ legacy refs. These strings function as opaque cache-key discriminators inside the idempotency tests; swapping them for canonical paths preserves test semantics exactly.
    - `lib/retry/idempotencyManager.ts` — JSDoc-edited by Plan 01; skim to confirm the public API the tests exercise.
  </read_first>
  <behavior>
    - Every `/api/stove/<segment>` string in fixtures, test inputs, and assertions is replaced by its canonical equivalent per the mapping table.
    - Test semantics are unchanged (strings are opaque discriminators).
    - All existing tests pass.
  </behavior>
  <action>
    Use a targeted find-and-replace per segment (NOT a blind `sed`, because segments map heterogeneously):
    - `/api/stove/status` → `/api/v1/thermorossi/status`
    - `/api/stove/ignite` → `/api/v1/thermorossi/commands/ignit`
    - `/api/stove/shutdown` → `/api/v1/thermorossi/commands/shutdown`
    - `/api/stove/getPower` → `/api/v1/thermorossi/power`
    - `/api/stove/getFan` → `/api/v1/thermorossi/fan-level`
    - `/api/stove/setPower` → `/api/v1/thermorossi/settings/power`
    - `/api/stove/setFan` → `/api/v1/thermorossi/settings/fan-level`
    - `/api/stove/setWaterTemperature` → `/api/v1/thermorossi/settings/temperature/water`
    - `/api/stove/history` → `/api/v1/thermorossi/history`
    - `/api/stove/health` → `/api/v1/thermorossi/health`

    Do not refactor test structure, rename tests, or change mock factories.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=idempotencyManager --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=idempotencyManager --no-coverage` exits 0
    - `grep -c "/api/stove/" lib/retry/__tests__/idempotencyManager.test.ts` returns 0
    - `grep -c "/api/v1/thermorossi/" lib/retry/__tests__/idempotencyManager.test.ts` returns at least 20
  </acceptance_criteria>
  <done>idempotencyManager tests pass with canonical cache-key fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Retarget useRetryableCommand test execute() inputs</name>
  <files>lib/hooks/__tests__/useRetryableCommand.test.ts</files>
  <read_first>
    - `lib/hooks/__tests__/useRetryableCommand.test.ts` — 12 legacy refs at approximately lines 94, 101, 106, 126, 143, 175, 205, 223, 241, 274, 306, 329. All are `execute('/api/stove/<segment>')` test inputs.
    - `lib/hooks/useRetryableCommand.ts` — JSDoc edited by Plan 01; confirm the hook contract is unchanged.
  </read_first>
  <behavior>
    - Every `execute('/api/stove/<segment>')` test input uses the canonical path.
    - Test semantics are unchanged; all tests continue to pass.
  </behavior>
  <action>
    Replace every legacy path per the mapping table. Typical lines:
    - `execute('/api/stove/ignite', { ... })` → `execute('/api/v1/thermorossi/commands/ignit', { ... })`
    - `execute('/api/stove/shutdown', { ... })` → `execute('/api/v1/thermorossi/commands/shutdown', { ... })`
    - `execute('/api/stove/setPower', { ... })` → `execute('/api/v1/thermorossi/settings/power', { ... })`

    If any test asserts against an outgoing request body using `{ level }`, change the assertion to `{ value }` to match canonical validation (consistent with Plan 01's deviceCommands refactor).

    Do not add, remove, or rename tests.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=useRetryableCommand --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=useRetryableCommand --no-coverage` exits 0
    - `grep -c "/api/stove/" lib/hooks/__tests__/useRetryableCommand.test.ts` returns 0
    - `grep -c "/api/v1/thermorossi/" lib/hooks/__tests__/useRetryableCommand.test.ts` returns at least 12
  </acceptance_criteria>
  <done>useRetryableCommand tests pass with canonical execute() inputs.</done>
</task>

<task type="auto">
  <name>Task 5: Final repo-wide grep sweep + targeted Jest run</name>
  <files>(verification only — no edits)</files>
  <read_first>
    - No file reads required. This is a verification-only task that runs grep and Jest against the now-complete codebase.
  </read_first>
  <action>
    Execute the two phase-exit verifications in order:

    1. Repo-wide grep sweep (PATH-01 + PATH-02 joint exit gate):
       ```bash
       grep -rn '/api/stove/' app/ lib/ components/ __tests__/ \
         --include='*.ts' --include='*.tsx' \
         | grep -v 'lib/version.ts' \
         | wc -l
       ```
       Expected output: `0`.

       If non-zero, list every offending line and stop. Do NOT attempt to fix outside the planned file set. Flag any unexpected file in the summary and report back to the orchestrator.

    2. Targeted Jest run (covers all four retargeted suites in a single pass):
       ```bash
       npm test -- --testPathPattern="stove|idempotency|useRetryableCommand" --no-coverage
       ```
       Expected: exit code 0, all tests pass.

    Do NOT run `npm run build`. Do NOT run `npm install`. Do NOT run the full Jest suite (that belongs to `/gsd-verify-work`).
  </action>
  <verify>
    <automated>bash -c "count=$(grep -rn '/api/stove/' app/ lib/ components/ __tests__/ --include='*.ts' --include='*.tsx' | grep -v 'lib/version.ts' | wc -l); test \"$count\" -eq 0 && npm test -- --testPathPattern='stove|idempotency|useRetryableCommand' --no-coverage"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '/api/stove/' app/ lib/ components/ __tests__/ --include='*.ts' --include='*.tsx' | grep -v 'lib/version.ts' | wc -l` returns exactly 0
    - `npm test -- --testPathPattern="stove|idempotency|useRetryableCommand" --no-coverage` exits 0
    - `lib/version.ts` is untouched (historical changelog preserved per D-06)
  </acceptance_criteria>
  <done>Zero `/api/stove/` refs in production + test code (excluding historical `lib/version.ts`); all four retargeted Jest suites green.</done>
</task>

</tasks>

<verification>
Phase-exit verifications (Wave 2 gate + phase completion):

```bash
# Phase exit: zero legacy refs repo-wide (excluding lib/version.ts and .planning/)
count=$(grep -rn '/api/stove/' app/ lib/ components/ __tests__/ --include='*.ts' --include='*.tsx' | grep -v 'lib/version.ts' | wc -l)
echo "Legacy /api/stove/ refs: $count"   # must print 0

# Historical record preserved
grep -c '/api/stove/' lib/version.ts     # must be > 0 (intentional, D-06)

# All four retargeted suites green
npm test -- --testPathPattern="stove|idempotency|useRetryableCommand" --no-coverage

# PATH-01 filesystem check (still true from Plan 01)
test ! -d app/api/stove && echo PATH-01_OK
```
</verification>

<success_criteria>
- PATH-02 (tests half): all four test files assert against canonical paths and pass
- Repo-wide grep (excluding lib/version.ts and .planning/) returns 0 `/api/stove/` refs
- `lib/version.ts` historical changelog preserved unchanged
- Combined Jest targeted run (`stove|idempotency|useRetryableCommand`) exits 0
- No modifications to production code (that was Plan 01's scope)
</success_criteria>

<output>
After completion, create `.planning/phases/164-phase-156-regression-fix/164-02-SUMMARY.md` describing:
- Four test files updated with exact count of string replacements per file
- Result of final repo-wide grep sweep (must be 0 — quote the command output)
- Result of targeted Jest run (must be pass — quote the pass/fail summary line)
- Confirmation that `lib/version.ts` remains untouched (D-06)
- Any unexpected legacy reference discovered outside the planned file set (should be none — but flag if found)
- Notes on any `{ level }` → `{ value }` body-key assertions updated (if encountered during test retargeting)
</output>
