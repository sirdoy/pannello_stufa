---
phase: 166-hue-frontend-cutover
plan: 02
type: execute
wave: 2
depends_on: [166-01]
files_modified:
  - app/components/devices/lights/hooks/useLightsData.ts
  - app/components/devices/lights/hooks/useLightsCommands.ts
  - app/lights/page.tsx
  - app/lights/scenes/page.tsx
  - app/components/lights/CreateSceneModal.tsx
  - app/components/lights/EditSceneModal.tsx
  - app/debug/components/tabs/HueTab.tsx
  - app/debug/api/components/tabs/HueTab.tsx
  - app/debug/components/tabs/__tests__/HueTab.test.tsx
  - app/debug/api/components/tabs/__tests__/HueTab.test.tsx
  - app/registry/devices/page.tsx
autonomous: true
requirements: [HUE-01, HUE-02, HUE-03, HUE-04, HUE-05, HUE-06, HUE-07]

must_haves:
  truths:
    - "useLightsData fetches from /api/v1/hue/health, /api/v1/hue/lights, /api/v1/hue/groups, /api/v1/hue/scenes"
    - "useLightsCommands sends to /api/v1/hue/groups/{id}/action and /api/v1/hue/groups/{gid}/scenes/{sid}"
    - "lights/page.tsx PUT calls target /api/v1/hue/lights/{id}/state (path split)"
    - "lights/scenes/page.tsx fetches from /api/v1/hue/scenes and /api/v1/hue/groups"
    - "Both HueTab debug panels use /api/v1/hue/* URLs for all GET and PUT calls"
    - "registry/devices/page.tsx fetches from /api/v1/hue/lights"
    - "No /api/hue/ references remain in any non-legacy file"
  artifacts:
    - path: "app/components/devices/lights/hooks/useLightsData.ts"
      provides: "Hue data hook with v1 URLs"
      contains: "/api/v1/hue/health"
    - path: "app/components/devices/lights/hooks/useLightsCommands.ts"
      provides: "Hue command hook with v1 URLs"
      contains: "/api/v1/hue/groups/"
    - path: "app/lights/page.tsx"
      provides: "Lights page with path-split PUT"
      contains: "/api/v1/hue/lights/${lightId}/state"
    - path: "app/debug/components/tabs/HueTab.tsx"
      provides: "Debug panel with v1 URLs"
      contains: "/api/v1/hue/health"
  key_links:
    - from: "app/components/devices/lights/hooks/useLightsData.ts"
      to: "/api/v1/hue/lights"
      via: "fetch call in fetchData"
      pattern: "fetch\\('/api/v1/hue/lights'\\)"
    - from: "app/lights/page.tsx"
      to: "/api/v1/hue/lights/{id}/state"
      via: "fetch PUT call"
      pattern: "/api/v1/hue/lights/.*?/state"
---

<objective>
Rewrite all frontend Hue URL references from legacy `/api/hue/*` to canonical `/api/v1/hue/*` paths across hooks, pages, modals, debug panels, registry page, and test assertions.

Purpose: Complete the frontend cutover so all Hue consumers use the v1 route tree (which includes Firebase adminDbPush logging on write routes per D-05). After this plan, the legacy `app/api/hue/` tree has zero consumers.
Output: 11 files updated, zero `/api/hue/` references in non-legacy files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-hue-frontend-cutover/166-CONTEXT.md
@.planning/phases/166-hue-frontend-cutover/166-PATTERNS.md
@.planning/phases/166-hue-frontend-cutover/166-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite hooks + pages + modals + registry (per D-03, D-04, D-06, D-07, D-08, D-12)</name>
  <files>
    app/components/devices/lights/hooks/useLightsData.ts,
    app/components/devices/lights/hooks/useLightsCommands.ts,
    app/lights/page.tsx,
    app/lights/scenes/page.tsx,
    app/components/lights/CreateSceneModal.tsx,
    app/components/lights/EditSceneModal.tsx,
    app/registry/devices/page.tsx
  </files>
  <read_first>
    - app/components/devices/lights/hooks/useLightsData.ts (6 URL replacements)
    - app/components/devices/lights/hooks/useLightsCommands.ts (4 URL replacements)
    - app/lights/page.tsx (3 PUT calls needing path split)
    - app/lights/scenes/page.tsx (3 URL replacements)
    - app/components/lights/CreateSceneModal.tsx (1 URL replacement)
    - app/components/lights/EditSceneModal.tsx (1 URL replacement)
    - app/registry/devices/page.tsx (1 URL replacement at line ~149)
  </read_first>
  <action>
Apply the exact URL mapping per D-04 across all 7 files. This is mechanical string replacement with one structural change (path split for lights PUT).

**useLightsData.ts** — 6 replacements per D-06:
- JSDoc line: `/api/hue/status` -> `/api/v1/hue/health`
- `fetch('/api/hue/status')` -> `fetch('/api/v1/hue/health')`
- `fetch('/api/hue/scenes')` (fetchScenes) -> `fetch('/api/v1/hue/scenes')`
- `fetch('/api/hue/rooms')` -> `fetch('/api/v1/hue/groups')`
- `fetch('/api/hue/lights')` -> `fetch('/api/v1/hue/lights')`
- `fetch('/api/hue/scenes')` (fetchData) -> `fetch('/api/v1/hue/scenes')`

**useLightsCommands.ts** — 4 replacements per D-06:
- `` `/api/hue/rooms/${groupId}` `` (line ~85) -> `` `/api/v1/hue/groups/${groupId}/action` ``
- `` `/api/hue/rooms/${groupId}` `` (line ~121) -> `` `/api/v1/hue/groups/${groupId}/action` ``
- `` `/api/hue/groups/${groupId}/scenes/${sceneId}` `` (line ~155) -> `` `/api/v1/hue/groups/${groupId}/scenes/${sceneId}` ``
- `` `/api/hue/rooms/${group.group_id}` `` (line ~188) -> `` `/api/v1/hue/groups/${group.group_id}/action` ``

**lights/page.tsx** — 3 replacements per D-07 + D-12 (PATH SPLIT):
- `` `/api/hue/lights/${lightId}` `` (line ~26, handleLightToggle) -> `` `/api/v1/hue/lights/${lightId}/state` ``
- `` `/api/hue/lights/${lightId}` `` (line ~42, handleLightBrightnessChange) -> `` `/api/v1/hue/lights/${lightId}/state` ``
- `` `/api/hue/lights/${lightId}` `` (line ~58, handleLightColorChange) -> `` `/api/v1/hue/lights/${lightId}/state` ``

**lights/scenes/page.tsx** — 3 replacements per D-07:
- `fetch('/api/hue/scenes')` -> `fetch('/api/v1/hue/scenes')`
- `fetch('/api/hue/rooms')` -> `fetch('/api/v1/hue/groups')`
- `fetch('/api/hue/status')` -> `fetch('/api/v1/hue/health')`

**CreateSceneModal.tsx** — 1 replacement per D-08:
- `fetch('/api/hue/lights')` -> `fetch('/api/v1/hue/lights')`

**EditSceneModal.tsx** — 1 replacement per D-08:
- `fetch('/api/hue/lights')` -> `fetch('/api/v1/hue/lights')`

**registry/devices/page.tsx** — 1 replacement (research-discovered, not in D-09 but required):
- `fetch('/api/hue/lights')` (line ~149) -> `fetch('/api/v1/hue/lights')`

Method, headers, body, and response handling remain unchanged in all files. No structural changes except the `/state` suffix on lights PUT calls.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -rn "/api/hue/" app/components/devices/lights/hooks/ app/lights/ app/components/lights/ app/registry/devices/page.tsx --include="*.ts" --include="*.tsx" | grep -v "__tests__" | grep -v "node_modules" ; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `/api/hue/` in the 7 modified files returns zero matches (all replaced with `/api/v1/hue/`)
    - `useLightsData.ts` contains `fetch('/api/v1/hue/health')` (status->health)
    - `useLightsData.ts` contains `fetch('/api/v1/hue/groups')` (rooms->groups)
    - `useLightsData.ts` contains `fetch('/api/v1/hue/lights')`
    - `useLightsData.ts` contains `fetch('/api/v1/hue/scenes')`
    - `useLightsCommands.ts` contains `/api/v1/hue/groups/${groupId}/action` (rooms->groups/action)
    - `useLightsCommands.ts` contains `/api/v1/hue/groups/${group.group_id}/action`
    - `lights/page.tsx` contains `/api/v1/hue/lights/${lightId}/state` (path split, 3 occurrences)
    - `lights/scenes/page.tsx` contains `fetch('/api/v1/hue/health')`
    - `CreateSceneModal.tsx` contains `fetch('/api/v1/hue/lights')`
    - `EditSceneModal.tsx` contains `fetch('/api/v1/hue/lights')`
    - `registry/devices/page.tsx` contains `fetch('/api/v1/hue/lights')`
  </acceptance_criteria>
  <done>All 7 frontend files use /api/v1/hue/* URLs exclusively, with correct path split for lights PUT</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite debug panels + test assertions (per D-09, D-10)</name>
  <files>
    app/debug/components/tabs/HueTab.tsx,
    app/debug/api/components/tabs/HueTab.tsx,
    app/debug/components/tabs/__tests__/HueTab.test.tsx,
    app/debug/api/components/tabs/__tests__/HueTab.test.tsx
  </files>
  <read_first>
    - app/debug/components/tabs/HueTab.tsx (~15 URL references)
    - app/debug/api/components/tabs/HueTab.tsx (~15 URL references, identical structure)
    - app/debug/components/tabs/__tests__/HueTab.test.tsx (3 assertion updates)
    - app/debug/api/components/tabs/__tests__/HueTab.test.tsx (3 assertion updates)
  </read_first>
  <action>
Apply URL mapping per D-09 and D-10 across both HueTab files and their tests. Both HueTab files are structurally identical — apply the same changes to each.

**Both HueTab.tsx files** — ~15 replacements each per D-09:

fetchAllGetEndpoints function (4 replacements):
- `'/api/hue/status'` -> `'/api/v1/hue/health'`
- `'/api/hue/lights'` -> `'/api/v1/hue/lights'`
- `'/api/hue/rooms'` -> `'/api/v1/hue/groups'`
- `'/api/hue/scenes'` -> `'/api/v1/hue/scenes'`

EndpointCard url props (4 replacements):
- `url="/api/hue/status"` -> `url="/api/v1/hue/health"`
- `url="/api/hue/lights"` -> `url="/api/v1/hue/lights"`
- `url="/api/hue/rooms"` -> `url="/api/v1/hue/groups"`
- `url="/api/hue/scenes"` -> `url="/api/v1/hue/scenes"`

EndpointCard onRefresh callbacks (4 replacements):
- `'/api/hue/status'` in onRefresh -> `'/api/v1/hue/health'`
- `'/api/hue/lights'` in onRefresh -> `'/api/v1/hue/lights'`
- `'/api/hue/rooms'` in onRefresh -> `'/api/v1/hue/groups'`
- `'/api/hue/scenes'` in onRefresh -> `'/api/v1/hue/scenes'`

PostEndpointCard "Control Light" section:
- url prop: `url="/api/hue/lights/[id]"` -> `url="/api/v1/hue/lights/[id]/state"`
- callPutEndpoint target: `` `/api/hue/lights/${values.lightId}` `` -> `` `/api/v1/hue/lights/${values.lightId}/state` ``

PostEndpointCard "Control Room" section:
- url prop: `url="/api/hue/rooms/[id]"` -> `url="/api/v1/hue/groups/[id]/action"`
- callPutEndpoint target: `` `/api/hue/rooms/${values.roomId}` `` -> `` `/api/v1/hue/groups/${values.roomId}/action` ``

PostEndpointCard "Activate Scene" section:
- url prop: `url="/api/hue/groups/[groupId]/scenes/[sceneId]"` -> `url="/api/v1/hue/groups/[groupId]/scenes/[sceneId]"`
- callPostEndpoint target: `` `/api/hue/groups/${values.groupId}/scenes/${values.sceneId}` `` -> `` `/api/v1/hue/groups/${values.groupId}/scenes/${values.sceneId}` ``

**Both HueTab.test.tsx files** — 3 assertion updates each per D-10:
- `stringContaining('/api/hue/lights/')` -> `stringContaining('/api/v1/hue/lights/')`
- `stringContaining('/api/hue/rooms/')` -> `stringContaining('/api/v1/hue/groups/')`
- `stringContaining('/api/hue/groups/test-id/scenes/test-id')` -> `stringContaining('/api/v1/hue/groups/test-id/scenes/test-id')`
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -rn "/api/hue/" app/debug/ --include="*.ts" --include="*.tsx" ; echo "REMAINING: $?" && npm test -- --testPathPattern="HueTab" --passWithNoTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep for `/api/hue/` in `app/debug/` returns zero matches across all 4 files
    - `app/debug/components/tabs/HueTab.tsx` contains `'/api/v1/hue/health'`
    - `app/debug/components/tabs/HueTab.tsx` contains `'/api/v1/hue/groups'`
    - `app/debug/components/tabs/HueTab.tsx` contains `url="/api/v1/hue/lights/[id]/state"`
    - `app/debug/components/tabs/HueTab.tsx` contains `url="/api/v1/hue/groups/[id]/action"`
    - `app/debug/api/components/tabs/HueTab.tsx` contains `'/api/v1/hue/health'`
    - `app/debug/api/components/tabs/HueTab.tsx` contains `url="/api/v1/hue/groups/[id]/action"`
    - `app/debug/components/tabs/__tests__/HueTab.test.tsx` contains `stringContaining('/api/v1/hue/lights/')`
    - `app/debug/components/tabs/__tests__/HueTab.test.tsx` contains `stringContaining('/api/v1/hue/groups/')`
    - `app/debug/api/components/tabs/__tests__/HueTab.test.tsx` contains `stringContaining('/api/v1/hue/groups/')`
    - HueTab tests pass (exit 0)
  </acceptance_criteria>
  <done>Both debug HueTab panels and their tests use /api/v1/hue/* URLs exclusively, all HueTab tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API route | URL paths change but auth mechanism (withAuthAndErrorHandler) unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-04 | Spoofing | Frontend URL strings | accept | URLs are first-party API paths, not external; no spoofing risk |
| T-166-05 | Information Disclosure | Debug panel URL labels | accept | Debug panels are authenticated-only pages; showing v1 paths is informational |
</threat_model>

<verification>
```bash
# Zero legacy references in non-legacy files
grep -r "/api/hue/" app/ --include="*.ts" --include="*.tsx" | grep -v "app/api/hue/" | wc -l
# Expected: 0

# All HueTab tests pass
npm test -- --testPathPattern="HueTab" --passWithNoTests

# All hue-related tests pass
npm test -- --testPathPattern="hue" --passWithNoTests
```
</verification>

<success_criteria>
- Zero `/api/hue/` references in any file outside `app/api/hue/` directory
- useLightsData, useLightsCommands, both pages, both modals, registry page, both debug panels all use /api/v1/hue/* exclusively
- lights/page.tsx PUT calls target /api/v1/hue/lights/{id}/state (path split complete)
- All HueTab test assertions updated and passing
</success_criteria>

<output>
After completion, create `.planning/phases/166-hue-frontend-cutover/166-02-SUMMARY.md`
</output>
