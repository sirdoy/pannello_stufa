---
phase: 172-fritzbox-v1-path-migration
plan: 03
type: execute
wave: 3
depends_on: [172-01, 172-02]
files_modified:
  - app/network/page.tsx
  - app/registry/devices/page.tsx
  - app/debug/components/tabs/NetworkTab.tsx
autonomous: true
requirements: [FRITZ-01, FRITZ-02, FRITZ-03, FRITZ-04, FRITZ-05, FRITZ-06, FRITZ-07]
tags: [fritzbox, path-migration, v1, pages, debug, verification]

must_haves:
  truths:
    - "app/network/page.tsx references /api/v1/fritzbox/category-override (not /api/fritzbox/)"
    - "app/registry/devices/page.tsx line ~164 references /api/v1/fritzbox/devices (not /api/fritzbox/)"
    - "app/debug/components/tabs/NetworkTab.tsx has zero /api/fritzbox/ references (all 37 occurrences retargeted)"
    - "Repo-wide grep for /api/fritzbox/ in *.ts and *.tsx under app/ and lib/ returns zero matches"
    - "Playwright smoke tests pass for /telefonia, /network, /debug, /registry/devices"
    - "Scoped Jest subsets green: test:api and test:components"
  artifacts:
    - path: "app/network/page.tsx"
      provides: "category-override POST routed to v1 path"
      contains: "/api/v1/fritzbox/category-override"
    - path: "app/registry/devices/page.tsx"
      provides: "registry devices list sourced from v1 fritzbox devices endpoint"
      contains: "/api/v1/fritzbox/devices"
    - path: "app/debug/components/tabs/NetworkTab.tsx"
      provides: "debug NetworkTab admin surface fully aligned to v1 routes"
      contains: "/api/v1/fritzbox/"
  key_links:
    - from: "app/network/page.tsx, app/registry/devices/page.tsx, app/debug/components/tabs/NetworkTab.tsx"
      to: "app/api/v1/fritzbox/** (Plan 01 routes)"
      via: "fetch() literal URL strings"
      pattern: "/api/v1/fritzbox/"
    - from: "Repo-wide grep guardrail"
      to: "Zero `/api/fritzbox/` matches in production code"
      via: "grep -rn \"/api/fritzbox/\" app/ lib/ --include=\"*.ts\" --include=\"*.tsx\" --exclude-dir=\".next\""
      pattern: "(empty output)"
---

<objective>
Close the path migration by sweeping the 3 remaining consumer surfaces (one page, one registry page, one debug panel tab with 37 URL occurrences), then run the repo-wide verification guardrail that proves zero legacy references remain in production code. Final scoped Jest subsets plus Playwright smoke confirm nothing regressed on the 4 affected user-visible routes (`/telefonia`, `/network`, `/debug`, `/registry/devices`).

Purpose: Satisfy ROADMAP §Phase 172 Success Criterion 4: "Repo-wide grep for `/api/fritzbox/` outside `.planning/` archive returns zero matches; Jest + Playwright smoke green." This is the phase gate that unblocks `/gsd-verify-work`.

Output: 3 modified consumer files, one commit, guardrail `grep` with zero matches, scoped Jest + Playwright smoke all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-fritzbox-v1-path-migration/172-RESEARCH.md
@.planning/phases/172-fritzbox-v1-path-migration/172-VALIDATION.md
@CLAUDE.md

<prior_art>
Phase 164 Plan 02 and Phase 168 Plan 02 ran the same final-sweep pattern: debug panel URL swap + repo-wide grep guardrail + Playwright smoke. Pitfall 1 (forgetting NetworkTab.tsx has 37 URL occurrences — not just 1) is called out in RESEARCH.md. Pitfall 6 (docs/setup/fritzbox-setup.md has legacy refs in structural filesystem-example context, not production URL strings) is resolved by scoping the final grep to `*.ts` and `*.tsx` only.
</prior_art>

<interfaces>
Same URL-string substitution as Plan 02:

Before: `/api/fritzbox/`
After:  `/api/v1/fritzbox/`

Repo-wide verification guardrail (per RESEARCH.md §Code Examples Pattern 3):
```bash
grep -rn "/api/fritzbox/" app/ lib/ \
  --include="*.ts" --include="*.tsx" \
  --exclude-dir=".next" \
  --exclude-dir="node_modules"
```
Expected: empty output, exit code 1.

Playwright smoke invocation (per VALIDATION.md):
```bash
npx playwright test tests/smoke/page-loads.spec.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget pages and debug NetworkTab</name>
  <files>app/network/page.tsx, app/registry/devices/page.tsx, app/debug/components/tabs/NetworkTab.tsx</files>
  <read_first>
    - app/network/page.tsx (line ~147 — single POST URL)
    - app/registry/devices/page.tsx (line ~164 — single GET URL)
    - app/debug/components/tabs/NetworkTab.tsx (37 URL occurrences across health, devices, bandwidth, wan, bandwidth-history, history, vendor-lookup, category-override — per RESEARCH.md Pitfall 1)
    - .planning/phases/172-fritzbox-v1-path-migration/172-RESEARCH.md §Consumer Files (Pages and Debug rows)
    - .planning/phases/172-fritzbox-v1-path-migration/172-RESEARCH.md Pitfall 1 (NetworkTab blind spot)
  </read_first>
  <action>
    Sweep the 3 remaining production consumer files with the same `sed` literal-string substitution used in Plan 02. These files were deliberately held for Plan 03 because they are the final batch after hooks and also the site where Pitfall 1 (NetworkTab.tsx with 37 occurrences) risk lives.

    Exact command:
    ```bash
    sed -i '' 's|/api/fritzbox/|/api/v1/fritzbox/|g' \
      app/network/page.tsx \
      app/registry/devices/page.tsx \
      app/debug/components/tabs/NetworkTab.tsx
    ```

    DO NOT modify anything else:
    - `app/network/page.tsx` line ~147 is a `fetch('/api/fritzbox/category-override', ...)` POST call — preserve the headers, body shape (`{ mac, category }`), and error handling.
    - `app/registry/devices/page.tsx` line ~164 is a `fetch('/api/fritzbox/devices')` GET call — preserve the surrounding pagination/loading logic.
    - `app/debug/components/tabs/NetworkTab.tsx` has 37 occurrences. The `sed -g` flag rewrites all of them in-place. Do not manually list or re-order; do not refactor the tab structure.

    Commit:
    ```bash
    git add app/network/page.tsx app/registry/devices/page.tsx app/debug/components/tabs/NetworkTab.tsx
    git commit -m "$(cat <<'EOF'
refactor(172-03): retarget pages + debug NetworkTab to /api/v1/fritzbox/*

Final batch of consumer URL swaps: /network/page.tsx category-override POST
(1 site), /registry/devices/page.tsx devices GET (1 site), debug NetworkTab
tab (37 sites spanning health, devices, bandwidth, wan, bandwidth-history,
history, vendor-lookup, category-override endpoints).

Requirements: FRITZ-01..07
EOF
)"
    ```
  </action>
  <verify>
    <automated>! grep -n "/api/fritzbox/" app/network/page.tsx && ! grep -n "/api/fritzbox/" app/registry/devices/page.tsx && ! grep -n "/api/fritzbox/" app/debug/components/tabs/NetworkTab.tsx && grep -q "/api/v1/fritzbox/category-override" app/network/page.tsx && grep -q "/api/v1/fritzbox/devices" app/registry/devices/page.tsx && [ "$(grep -c '/api/v1/fritzbox/' app/debug/components/tabs/NetworkTab.tsx)" -ge 30 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "/api/fritzbox/" app/network/page.tsx` returns zero matches
    - `grep -n "/api/fritzbox/" app/registry/devices/page.tsx` returns zero matches
    - `grep -n "/api/fritzbox/" app/debug/components/tabs/NetworkTab.tsx` returns zero matches
    - `grep -c "/api/v1/fritzbox/" app/debug/components/tabs/NetworkTab.tsx` returns ≥30 (RESEARCH.md says 37; allow small margin for consolidated multi-URL strings)
    - `app/network/page.tsx` contains exactly one `/api/v1/fritzbox/category-override` reference (preserving surrounding POST logic)
    - `app/registry/devices/page.tsx` contains exactly one `/api/v1/fritzbox/devices` reference on line ~164
    - HEAD commit message starts with `refactor(172-03):`
    - `git diff HEAD~1 HEAD --stat` lists only the 3 expected files
  </acceptance_criteria>
  <done>
    All 3 remaining consumer surfaces retargeted. Pitfall 1 closed (NetworkTab 37 occurrences swept).
  </done>
</task>

<task type="auto">
  <name>Task 2: Repo-wide grep guardrail (Success Criterion 4)</name>
  <files>(read-only verification — no writes)</files>
  <read_first>
    - .planning/phases/172-fritzbox-v1-path-migration/172-RESEARCH.md §Code Examples Pattern 3 (exact grep invocation)
    - .planning/phases/172-fritzbox-v1-path-migration/172-RESEARCH.md Pitfall 6 (docs/setup/fritzbox-setup.md scoping decision)
    - .planning/phases/172-fritzbox-v1-path-migration/172-RESEARCH.md Pitfall 7 (.next/ exclusion)
    - .planning/phases/172-fritzbox-v1-path-migration/172-VALIDATION.md row 172-03-02
    - ROADMAP.md §Phase 172 Success Criterion 4
  </read_first>
  <action>
    Execute the repo-wide verification guardrail that proves zero legacy `/api/fritzbox/` references remain in production code. This is ROADMAP Success Criterion 4 — the phase gate.

    Exact command:
    ```bash
    ! grep -rn "/api/fritzbox/" app/ lib/ \
        --include="*.ts" --include="*.tsx" \
        --exclude-dir=".next" \
        --exclude-dir="node_modules"
    ```

    The leading `!` inverts the exit status: the command succeeds (exit 0) only when grep found NO matches (grep exits 1 on zero matches). If the command fails (exit non-zero), grep found legacy refs — report the file+line, add them to Plan 02 or Task 1 above, and re-run.

    Scoping rationale (Pitfall 6): this phase's Success Criterion 4 targets production code, not `.md` documentation. `docs/setup/fritzbox-setup.md` contains legacy filesystem-example paths in a structural context (pre-TypeScript migration) and is explicitly out of scope for this phase. Narrowing `--include` to `*.ts` and `*.tsx` captures the intent. `.next/` generated types auto-regenerate on dev start (Pitfall 7) — excluded.

    No file writes. No commit. This is a verification gate only.
  </action>
  <verify>
    <automated>! grep -rn "/api/fritzbox/" app/ lib/ --include="*.ts" --include="*.tsx" --exclude-dir=".next" --exclude-dir="node_modules"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "/api/fritzbox/" app/ lib/ --include="*.ts" --include="*.tsx" --exclude-dir=".next" --exclude-dir="node_modules"` produces empty output
    - The inverted command (`! grep ...`) exits 0
    - Zero matches in `app/api/` (Plan 01 moved the tree)
    - Zero matches in `app/telefonia/`, `app/network/`, `app/debug/`, `app/components/`, `app/registry/` (Plans 02+03 swept the consumers)
    - Zero matches in `lib/` (confirmed pre-phase; re-verified here as regression check)
  </acceptance_criteria>
  <done>
    Repo-wide guardrail green. ROADMAP Success Criterion 4 met.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final scoped Jest subsets + Playwright smoke (phase-gate verification)</name>
  <files>(read-only verification — no writes)</files>
  <read_first>
    - .planning/phases/172-fritzbox-v1-path-migration/172-VALIDATION.md §Sampling Rate + rows 172-03-01, 172-03-03
    - CLAUDE.md Rule 8 (scoped subsets only — never bare `npm test`)
    - tests/smoke/page-loads.spec.ts (confirm /telefonia, /network, /debug, /registry/devices specs exist)
  </read_first>
  <action>
    Run the phase-gate verification per VALIDATION.md §Sampling Rate "Before `/gsd-verify-work`: All scoped Jest subsets + Playwright smoke green".

    Step 1 — Re-run the moved co-located route tests (regression check after Plan 02+03 left them untouched, but consumer-side mocks may have changed behavior):
    ```bash
    npm test -- app/api/v1/fritzbox/
    ```

    Step 2 — Run the full scoped subsets per CLAUDE.md Rule 8. These cover all consumer test suites:
    ```bash
    npm run test:api
    npm run test:components
    ```

    Step 3 — Run Playwright smoke covering the 4 Fritz-consuming routes (`/telefonia`, `/network`, `/debug`, `/registry/devices`):
    ```bash
    npx playwright test tests/smoke/page-loads.spec.ts
    ```

    Playwright smoke requires a running dev server per Phase 171 verification pattern — if the executor does not have a dev server running, mark this step as "discoverability-verified via `--list`" (matching Phase 171's accepted approach) and document in the SUMMARY.

    No file writes. No commit. This is the phase gate.

    If any subset fails, report the failing suite+test and do NOT proceed to `/gsd-verify-work`. Diagnose root cause (likely a consumer file missed in Plan 02 or 03), fix via an additional sweep, re-run.
  </action>
  <verify>
    <automated>npm test -- app/api/v1/fritzbox/ --passWithNoTests=false --silent 2>&1 | tail -5 && npm run test:api --silent 2>&1 | tail -5 && npm run test:components --silent 2>&1 | tail -5 && npx playwright test tests/smoke/page-loads.spec.ts --list 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- app/api/v1/fritzbox/` exits 0 (20 co-located route suites green from new location)
    - `npm run test:api` exits 0
    - `npm run test:components` exits 0
    - `npx playwright test tests/smoke/page-loads.spec.ts --list` lists specs for `/telefonia`, `/network`, `/debug`, `/registry/devices` (or full run passes if dev server available)
    - Zero new test failures introduced by Plans 172-01, 172-02, or 172-03
    - Full Jest and Playwright gate green; phase ready for `/gsd-verify-work`
  </acceptance_criteria>
  <done>
    All 3 verification layers green (co-located routes, full scoped Jest, Playwright smoke). Phase 172 ready for verification.
  </done>
</task>

</tasks>

<verification>
Plan 03 complete when:
1. `app/network/page.tsx`, `app/registry/devices/page.tsx`, `app/debug/components/tabs/NetworkTab.tsx` contain zero `/api/fritzbox/` references and at least one `/api/v1/fritzbox/` reference each
2. Repo-wide grep guardrail returns empty output (ROADMAP SC-4)
3. `npm run test:api && npm run test:components` exits 0
4. `npm test -- app/api/v1/fritzbox/` exits 0 (route tree regression check)
5. Playwright smoke for `/telefonia`, `/network`, `/debug`, `/registry/devices` discoverable (or fully green if dev server running)
6. Single Plan 03 commit with `refactor(172-03):` prefix covering only the 3 expected files
</verification>

<success_criteria>
- Three consumer surfaces retargeted (page + registry + debug tab)
- Pitfall 1 closed (NetworkTab 37 occurrences swept via single sed invocation)
- Pitfall 6 resolved (grep scoped to `*.ts`/`*.tsx`)
- ROADMAP §Phase 172 Success Criteria 1, 2, 3, 4 all verified in this plan (SC-1 Plan 01; SC-2 Plan 02; SC-3 Task 1 + Plan 02; SC-4 Task 2 + Task 3)
- Phase gate (full scoped Jest + Playwright smoke) green
- Single commit for Task 1 file swaps
</success_criteria>

<output>
After completion, create `.planning/phases/172-fritzbox-v1-path-migration/172-03-SUMMARY.md`. Record:
- Task 1 commit SHA from `git log -1 --format=%H`
- `git diff --stat HEAD~1 HEAD` (3 files, modest line counts)
- NetworkTab.tsx `/api/v1/fritzbox/` count (should be ≥30, expected ~37)
- Repo-wide grep guardrail output (must be empty)
- Test result summary: `npm test -- app/api/v1/fritzbox/` suite/test counts, `npm run test:api` + `npm run test:components` suite/test counts, Playwright smoke list or run output
- Phase 172 complete: confirm all 7 FRITZ-XX requirements satisfied end-to-end (route path + hook wiring + page wiring + debug panel + registry)
- Recommendation to run `/gsd-verify-work 172-fritzbox-v1-path-migration` next
</output>
