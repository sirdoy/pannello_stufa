---
phase: 74-suspense-streaming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/loading.tsx
  - app/page.tsx
  - app/components/DashboardCards.tsx
autonomous: true
requirements:
  - SUSP-01
  - SUSP-02

must_haves:
  truths:
    - "User sees six skeleton card placeholders within ~300ms of navigating to the dashboard (before any server data resolves)"
    - "User sees the SandboxPanel rendered immediately outside the Suspense boundary (not blocked by data fetching)"
    - "User who is not authenticated is redirected to /auth/login after the DashboardCards async component resolves session check"
    - "User with all 6 devices enabled sees the same masonry layout (even->left, odd->right) as before the refactor"
    - "User with fewer than 6 devices sees the correct subset of cards with proper column assignment"
  artifacts:
    - path: "app/loading.tsx"
      provides: "Page-level instant skeleton fallback for all 6 dashboard cards"
      contains: "Skeleton.StovePanel"
    - path: "app/components/DashboardCards.tsx"
      provides: "Async Server Component that fetches session + deviceConfig and renders card list"
      contains: "getUnifiedDeviceConfigAdmin"
    - path: "app/page.tsx"
      provides: "Synchronous page shell with Suspense boundary wrapping DashboardCards"
      contains: "Suspense"
  key_links:
    - from: "app/page.tsx"
      to: "app/components/DashboardCards.tsx"
      via: "Suspense boundary with DashboardSkeleton fallback"
      pattern: "Suspense.*fallback.*DashboardCards"
    - from: "app/loading.tsx"
      to: "app/components/ui/Skeleton.tsx"
      via: "Import of Skeleton sub-components"
      pattern: "Skeleton\\.(StovePanel|ThermostatCard)"
    - from: "app/components/DashboardCards.tsx"
      to: "lib/services/unifiedDeviceConfigService.ts"
      via: "Server-side deviceConfig fetch"
      pattern: "getUnifiedDeviceConfigAdmin"
---

<objective>
Create `loading.tsx` for instant skeleton fallbacks and restructure `page.tsx` into a synchronous shell wrapping an async `DashboardCards` server component via Suspense.

Purpose: Users currently see a blank page while `auth0.getSession()` + `getUnifiedDeviceConfigAdmin()` resolve server-side. This plan makes the dashboard shell (with skeleton placeholders for all 6 cards) appear instantly on navigation, then streams in the actual cards when data is ready. This directly satisfies SUSP-01 (skeleton fallbacks visible within ~300ms) and partially satisfies SUSP-02 (progressive appearance via existing client-side stagger from Phase 73).

Output: `app/loading.tsx` (new), `app/components/DashboardCards.tsx` (new), `app/page.tsx` (refactored to synchronous shell)
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-suspense-streaming/74-RESEARCH.md

@app/page.tsx
@app/components/ui/Skeleton.tsx
@app/template.tsx
@lib/utils/dashboardColumns.ts
@lib/services/unifiedDeviceConfigService.ts
@app/components/ErrorBoundary.tsx
@app/components/sandbox/SandboxPanel.tsx
@app/components/ui/EmptyState.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx with full 6-card skeleton masonry layout</name>
  <files>app/loading.tsx</files>
  <action>
Create `app/loading.tsx` as a static Server Component (NO `'use client'` directive) that renders the full dashboard skeleton layout matching the current `page.tsx` structure. This file is auto-detected by Next.js and wraps the page in a Suspense boundary.

The layout MUST match the current `page.tsx` masonry structure exactly:

**Mobile (sm:hidden):** Single column with all 6 skeletons in flat order:
1. `Skeleton.StovePanel`
2. `Skeleton.ThermostatCard`
3. `Skeleton.WeatherCard`
4. `Skeleton.LightsCard`
5. `Skeleton.CameraCard`
6. `Skeleton.NetworkCard`

Gap: `gap-6` (same as current page.tsx mobile layout).

**Desktop (hidden sm:flex):** Two-column masonry with even->left, odd->right:
- Left column: StovePanel (flatIndex 0), WeatherCard (flatIndex 2), NetworkCard (flatIndex 4)
- Right column: ThermostatCard (flatIndex 1), LightsCard (flatIndex 3), CameraCard (flatIndex 5)

Gap: `gap-8 lg:gap-10` between columns, same within columns.

Wrap the whole thing in `<section className="py-8 sm:py-12 lg:py-16">` with `<h1 className="sr-only">Dashboard</h1>` to match the page shell structure.

Import Skeleton from `@/app/components/ui/Skeleton` (the default export with sub-components).

Do NOT add animation classes (no `animate-spring-in`) -- this is a static skeleton shown before hydration. The skeletons' own shimmer animation is sufficient.

Do NOT add `export const dynamic` or any runtime config -- loading.tsx is a static fallback.
  </action>
  <verify>
Verify the file exists and has the correct structure:
- `app/loading.tsx` exists
- Contains `Skeleton.StovePanel`, `Skeleton.ThermostatCard`, `Skeleton.WeatherCard`, `Skeleton.LightsCard`, `Skeleton.CameraCard`, `Skeleton.NetworkCard`
- Has mobile single-column layout (`sm:hidden`)
- Has desktop two-column masonry layout (`hidden sm:flex`)
- Does NOT contain `'use client'`
- Does NOT contain `export const dynamic`
  </verify>
  <done>loading.tsx renders all 6 skeleton cards in matching mobile/desktop masonry layout without any client-side JavaScript required</done>
</task>

<task type="auto">
  <name>Task 2: Extract DashboardCards async Server Component and restructure page.tsx</name>
  <files>app/components/DashboardCards.tsx, app/page.tsx</files>
  <action>
**Step 1: Create `app/components/DashboardCards.tsx`**

Create a new async Server Component (NO `'use client'` directive) that contains ALL the current async logic from `page.tsx`:

1. Import everything currently imported in `page.tsx` EXCEPT `SandboxPanel` (stays in page.tsx)
2. Export `default async function DashboardCards()`
3. Inside the function:
   - `const session = await auth0.getSession();`
   - If no session/user, `redirect('/auth/login')` (import `redirect` from `next/navigation` at top level, NOT dynamic import -- this is a Server Component, no need for dynamic imports)
   - `const deviceConfig = await getUnifiedDeviceConfigAdmin(userId);`
   - `const visibleCards = getVisibleDashboardCards(deviceConfig);`
   - `const { left: leftColumn, right: rightColumn } = splitIntoColumns(visibleCards);`
   - Render the card layout (mobile + desktop masonry) using the same `renderCard` helper and identical JSX as current `page.tsx` lines 63-115
   - Include the `EmptyState` component for `visibleCards.length === 0`

4. Keep the `CARD_COMPONENTS` and `DEVICE_META` registries in this file (move from page.tsx)
5. Keep the `renderCard` helper in this file
6. Import all 6 card components, `DeviceCardErrorBoundary`, `EmptyState`, `splitIntoColumns`, `auth0`, `getUnifiedDeviceConfigAdmin`, `getVisibleDashboardCards`

**Step 2: Refactor `app/page.tsx` to synchronous shell**

Replace the current async `page.tsx` with a synchronous shell:

```tsx
import { Suspense } from 'react';
import DashboardCards from './components/DashboardCards';
import SandboxPanel from './components/sandbox/SandboxPanel';
import DashboardSkeleton from './loading';

export const dynamic = 'force-dynamic';

export default function Home() {
  return (
    <section className="py-8 sm:py-12 lg:py-16">
      <h1 className="sr-only">Dashboard</h1>
      {/* Sandbox Panel - Solo in localhost quando abilitato */}
      <SandboxPanel />

      <Suspense fallback={<DashboardSkeleton />}>
        <DashboardCards />
      </Suspense>
    </section>
  );
}
```

Key details:
- `Home` is now a regular function (NOT async) -- this makes it render synchronously
- `SandboxPanel` renders OUTSIDE the Suspense boundary (visible immediately)
- `DashboardCards` is wrapped in `<Suspense>` with `DashboardSkeleton` (imported from `./loading`) as fallback
- The `<section>` wrapper with padding and sr-only h1 stays in page.tsx (the outer shell)
- Remove the `<section>` wrapper from `DashboardCards.tsx` -- it only renders the card content (mobile layout div + desktop layout div + empty state)
- Keep `export const dynamic = 'force-dynamic'` in page.tsx

**IMPORTANT:** The `loading.tsx` handles the initial page-level loading (before ANY component mounts). The Suspense boundary in `page.tsx` handles client-side navigation (soft nav) where `loading.tsx` may not fire. Both use the same skeleton layout for visual consistency.

**IMPORTANT:** Do NOT change any of the 6 card components. Their internal loading/skeleton states remain unchanged.

**IMPORTANT:** The `redirect('/auth/login')` in DashboardCards is fine -- unauthenticated users will see the loading.tsx skeleton briefly before the redirect fires. There is no root middleware.ts auth guard in this project.
  </action>
  <verify>
1. `app/page.tsx` is NOT async (no `async` keyword on `Home` function)
2. `app/page.tsx` imports `Suspense` from `react`
3. `app/page.tsx` imports `DashboardCards` from `./components/DashboardCards`
4. `app/page.tsx` keeps `SandboxPanel` outside the Suspense boundary
5. `app/page.tsx` keeps `export const dynamic = 'force-dynamic'`
6. `app/components/DashboardCards.tsx` is `async function DashboardCards()`
7. `app/components/DashboardCards.tsx` contains `await auth0.getSession()`
8. `app/components/DashboardCards.tsx` contains `await getUnifiedDeviceConfigAdmin`
9. `app/components/DashboardCards.tsx` contains all 6 card component imports
10. `app/components/DashboardCards.tsx` has `CARD_COMPONENTS` and `DEVICE_META` registries
11. `app/components/DashboardCards.tsx` uses `splitIntoColumns` for masonry
12. `app/components/DashboardCards.tsx` renders `EmptyState` for zero visible cards
13. `npm test -- --testPathPattern="dashboardColumns|ErrorBoundary" --passWithNoTests` passes (no regressions in shared utilities)
  </verify>
  <done>page.tsx is synchronous and renders immediately; DashboardCards fetches data asynchronously inside a Suspense boundary; the dashboard shell with skeleton placeholders appears before any server data resolves</done>
</task>

</tasks>

<verification>
1. `app/loading.tsx` exists as a static Server Component with 6 skeleton cards in masonry layout
2. `app/page.tsx` is synchronous, wraps DashboardCards in Suspense
3. `app/components/DashboardCards.tsx` is async, fetches session + deviceConfig, renders cards
4. SandboxPanel renders outside Suspense boundary
5. EmptyState renders inside DashboardCards for zero-card case
6. No changes to any of the 6 card components
7. Existing tests pass: `npm test -- --passWithNoTests`
</verification>

<success_criteria>
- loading.tsx renders 6 skeleton cards in masonry layout (mobile single-column + desktop two-column)
- page.tsx is synchronous and renders the page shell immediately
- DashboardCards is an async Server Component that fetches session + deviceConfig
- The Suspense boundary shows DashboardSkeleton while DashboardCards resolves
- All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/74-suspense-streaming/74-01-SUMMARY.md`
</output>
