---
phase: 105-fix-debug-panel-urls-stale-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/debug/components/tabs/StoveTab.tsx
  - app/debug/api/components/tabs/StoveTab.tsx
  - lib/routes.ts
autonomous: true
requirements:
  - DEBUG-01
  - CLEAN-04
gap_closure: true

must_haves:
  truths:
    - "StoveTab POST buttons hit actual Next.js routes (ignite, shutdown, setPower, setFan, setWaterTemperature) instead of 404ing"
    - "lib/routes.ts contains no entries for deleted API routes (getRoomTemperature, getSettings, setSettings)"
  artifacts:
    - path: "app/debug/components/tabs/StoveTab.tsx"
      provides: "Debug panel StoveTab with correct POST URLs"
      contains: "/api/stove/ignite"
    - path: "app/debug/api/components/tabs/StoveTab.tsx"
      provides: "API debug panel StoveTab with correct POST URLs"
      contains: "/api/stove/ignite"
    - path: "lib/routes.ts"
      provides: "Centralized route definitions without stale entries"
  key_links:
    - from: "app/debug/components/tabs/StoveTab.tsx"
      to: "app/api/stove/ignite/route.ts"
      via: "fetch POST to /api/stove/ignite"
      pattern: "/api/stove/ignite"
    - from: "app/debug/components/tabs/StoveTab.tsx"
      to: "app/api/stove/setPower/route.ts"
      via: "fetch POST to /api/stove/setPower"
      pattern: "/api/stove/setPower"
---

<objective>
Fix StoveTab POST URLs that point to non-existent HA proxy internal paths (causing 404s) and remove stale route entries from lib/routes.ts for API routes deleted in Phase 103.

Purpose: Gap closure for BROKEN-01 (debug panel POST 404s) and BROKEN-03 (dead route definitions) from v13.0 audit.
Output: Working debug panel POST operations and clean route definitions.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-fix-debug-panel-urls-stale-routes/105-CONTEXT.md

<interfaces>
<!-- Actual Next.js route files that StoveTab POST URLs must point to -->
<!-- These are the ONLY valid POST endpoints: -->
app/api/stove/ignite/route.ts          → POST /api/stove/ignite
app/api/stove/shutdown/route.ts        → POST /api/stove/shutdown
app/api/stove/setPower/route.ts        → POST /api/stove/setPower
app/api/stove/setFan/route.ts          → POST /api/stove/setFan
app/api/stove/setWaterTemperature/route.ts → POST /api/stove/setWaterTemperature

<!-- STOVE_ROUTES in lib/routes.ts is exported and used by useStoveCommands/useStoveData -->
<!-- Only getRoomTemperature, getSettings, setSettings are stale — all others are live -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix StoveTab POST URLs in both debug panel files</name>
  <files>app/debug/components/tabs/StoveTab.tsx, app/debug/api/components/tabs/StoveTab.tsx</files>
  <read_first>
    - app/debug/components/tabs/StoveTab.tsx (current state with wrong POST URLs)
    - app/debug/api/components/tabs/StoveTab.tsx (identical copy with wrong POST URLs)
  </read_first>
  <action>
In BOTH StoveTab.tsx files, replace all occurrences of HA proxy internal paths with actual Next.js API route paths. Each endpoint has 3 occurrences (url prop, onExecute callback, onCopyUrl/isCopied):

1. Ignite: replace all `/api/stove/commands/ignit` with `/api/stove/ignite`
   - Lines ~191,195,196,197 in both files

2. Shutdown: replace all `/api/stove/commands/shutdown` with `/api/stove/shutdown`
   - Lines ~202,206,207,208 in both files

3. Set Power: replace all `/api/stove/settings/power` with `/api/stove/setPower`
   - Lines ~213,220,221,222 in both files

4. Set Fan: replace all `/api/stove/settings/fan-level` with `/api/stove/setFan`
   - Lines ~226,234,235,236 in both files

5. Set Water Temperature: replace all `/api/stove/settings/temperature/water` with `/api/stove/setWaterTemperature`
   - Lines ~240,248,249,250 in both files

Total: 15 string replacements per file, 30 total across both files. Both files must end up identical except for the import path of EndpointCard/PostEndpointCard.
  </action>
  <verify>
    <automated>grep -c "commands/ignit\|commands/shutdown\|settings/power\|settings/fan-level\|settings/temperature/water" app/debug/components/tabs/StoveTab.tsx app/debug/api/components/tabs/StoveTab.tsx | grep -v ":0$" && echo "FAIL: stale URLs remain" || echo "PASS: all URLs updated"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `/api/stove/commands/ignit` in both files returns 0 matches
    - grep for `/api/stove/commands/shutdown` in both files returns 0 matches
    - grep for `/api/stove/settings/power` in both files returns 0 matches
    - grep for `/api/stove/settings/fan-level` in both files returns 0 matches
    - grep for `/api/stove/settings/temperature/water` in both files returns 0 matches
    - grep for `/api/stove/ignite` in app/debug/components/tabs/StoveTab.tsx returns exactly 3 matches
    - grep for `/api/stove/shutdown` in app/debug/components/tabs/StoveTab.tsx returns exactly 3 matches
    - grep for `/api/stove/setPower` in app/debug/components/tabs/StoveTab.tsx returns exactly 3 matches
    - grep for `/api/stove/setFan` in app/debug/components/tabs/StoveTab.tsx returns exactly 3 matches
    - grep for `/api/stove/setWaterTemperature` in app/debug/components/tabs/StoveTab.tsx returns exactly 3 matches
    - Same 5 counts apply to app/debug/api/components/tabs/StoveTab.tsx
  </acceptance_criteria>
  <done>All 5 POST endpoints in both StoveTab files point to actual Next.js API routes that exist on disk</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale route entries from lib/routes.ts</name>
  <files>lib/routes.ts</files>
  <read_first>
    - lib/routes.ts (current state with 3 stale entries in STOVE_ROUTES)
  </read_first>
  <action>
Remove these 3 lines from the STOVE_ROUTES object in lib/routes.ts:

1. Delete: `getRoomTemperature: \`${API_BASE}/stove/getRoomTemperature\`,` (line 52)
2. Delete: `getSettings: \`${API_BASE}/stove/settings\`,` (line 55)
3. Delete: `setSettings: \`${API_BASE}/stove/setSettings\`,` (line 56)

These route files were deleted in Phase 103. No consumers reference these keys (verified: grep for `STOVE_ROUTES.getRoomTemperature`, `STOVE_ROUTES.getSettings`, `STOVE_ROUTES.setSettings` returns 0 matches).

Do NOT touch any other entries in STOVE_ROUTES — status, ignite, shutdown, getFan, getPower, setFan, setPower are all live.

Also note: lib/version.ts line 1498 contains a historical changelog string mentioning getRoomTemperature. Do NOT modify version.ts — changelog entries are historical records.
  </action>
  <verify>
    <automated>grep -c "getRoomTemperature\|getSettings\|setSettings" lib/routes.ts | grep -v ":0$" && echo "FAIL: stale entries remain" || echo "PASS: stale entries removed"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `getRoomTemperature` in lib/routes.ts returns 0 matches
    - grep for `getSettings` in lib/routes.ts returns 0 matches
    - grep for `setSettings` in lib/routes.ts returns 0 matches
    - STOVE_ROUTES object still contains: status, ignite, shutdown, getFan, getPower, setFan, setPower (7 entries)
    - File has no TypeScript syntax errors (trailing commas, missing braces, etc.)
  </acceptance_criteria>
  <done>STOVE_ROUTES contains only live route entries — no references to deleted API routes remain</done>
</task>

</tasks>

<verification>
1. Both StoveTab files contain only valid Next.js API route paths for POST endpoints
2. lib/routes.ts STOVE_ROUTES has exactly 7 entries (no stale ones)
3. No file references deleted routes getRoomTemperature, getSettings, setSettings in a functional (non-historical) context
</verification>

<success_criteria>
- All 5 POST operations in StoveTab would return non-404 responses (URLs match existing route files)
- lib/routes.ts is clean of stale entries
- Both debug panel StoveTab files are consistent with each other
</success_criteria>

<output>
After completion, create `.planning/phases/105-fix-debug-panel-urls-stale-routes/105-01-SUMMARY.md`
</output>
