---
phase: 117-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/index.ts
  - lib/core/apiResponse.ts
  - lib/errorMonitor.ts
  - lib/tokenRefresh.ts
  - lib/tokenStorage.ts
  - lib/health/healthDeadManSwitch.ts
  - lib/health/healthMonitoring.ts
  - lib/health/healthLogger.ts
  - lib/notifications/notificationPreferencesService.ts
  - lib/notifications/notificationTriggersServer.ts
  - lib/schemas/notificationPreferences.ts
  - lib/scheduler/schedulesApiClient.ts
  - lib/pwa/installPromptService.ts
  - lib/services/unifiedDeviceConfigService.ts
  - lib/services/locationService.ts
  - lib/services/pidAutomationService.ts
  - lib/rateLimiter.ts
  - lib/devices/deviceRegistry.ts
  - app/hooks/useDebounce.ts
  - app/hooks/useHaptic.ts
  - app/network/components/DeviceListTable.tsx
  - app/network/components/DeviceCategoryBadge.tsx
  - app/network/components/DeviceStatusBadge.tsx
  - app/thermostat/components/ThermostatTabs.tsx
  - app/components/weather/WeatherCard.tsx
  - app/components/weather/CurrentConditions.tsx
  - app/components/weather/ForecastRow.tsx
  - app/components/weather/ForecastDaySheet.tsx
  - app/components/weather/ForecastDayCard.tsx
  - app/components/weather/HourlyForecast.tsx
  - app/debug/design-system/data/component-docs.ts
  - app/components/ErrorBoundary/index.ts
  - tests/helpers/test-context.ts
autonomous: true
requirements:
  - CLEAN-01

must_haves:
  truths:
    - "knip reports zero unused exports outside the design system barrel"
    - "All lib/ utility exports flagged by knip are removed or de-exported"
    - "No import statements in the codebase are broken by the removals"
  artifacts:
    - path: "lib/core/index.ts"
      provides: "Barrel with unused exports removed"
      contains: "export"
    - path: "lib/tokenStorage.ts"
      provides: "Token storage with unused exports removed"
  key_links:
    - from: "lib/core/index.ts"
      to: "lib/core/apiResponse.ts"
      via: "re-export"
      pattern: "from.*apiResponse"
---

<objective>
Remove all in-scope unused exports identified by knip across lib/ utilities, app/ hooks, app/ components, and test helpers.

Purpose: Reduce the codebase surface area by eliminating dead exports — the final cleanup step of v14.1.
Output: All flagged files have unused exports removed (export keyword stripped or entire symbol deleted).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-dead-code-cleanup/117-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused exports from lib/ files (per D-01, D-02, D-03, D-04)</name>
  <read_first>
    - lib/core/index.ts
    - lib/core/apiResponse.ts
    - lib/errorMonitor.ts
    - lib/tokenRefresh.ts
    - lib/tokenStorage.ts
    - lib/health/healthDeadManSwitch.ts
    - lib/health/healthMonitoring.ts
    - lib/health/healthLogger.ts
    - lib/notifications/notificationPreferencesService.ts
    - lib/notifications/notificationTriggersServer.ts
    - lib/schemas/notificationPreferences.ts
    - lib/scheduler/schedulesApiClient.ts
    - lib/pwa/installPromptService.ts
    - lib/services/unifiedDeviceConfigService.ts
    - lib/services/locationService.ts
    - lib/services/pidAutomationService.ts
    - lib/rateLimiter.ts
    - lib/devices/deviceRegistry.ts
  </read_first>
  <files>
    lib/core/index.ts,
    lib/core/apiResponse.ts,
    lib/errorMonitor.ts,
    lib/tokenRefresh.ts,
    lib/tokenStorage.ts,
    lib/health/healthDeadManSwitch.ts,
    lib/health/healthMonitoring.ts,
    lib/health/healthLogger.ts,
    lib/notifications/notificationPreferencesService.ts,
    lib/notifications/notificationTriggersServer.ts,
    lib/schemas/notificationPreferences.ts,
    lib/scheduler/schedulesApiClient.ts,
    lib/pwa/installPromptService.ts,
    lib/services/unifiedDeviceConfigService.ts,
    lib/services/locationService.ts,
    lib/services/pidAutomationService.ts,
    lib/rateLimiter.ts,
    lib/devices/deviceRegistry.ts
  </files>
  <action>
For each file listed in the research "In-Scope Exports Grouped by File" under lib/:

1. **Run `npx knip --reporter json 2>/dev/null` first** to get the live current list (per D-01).

2. **Per export, apply this decision tree (per D-02):**
   - Search the codebase (excluding the file itself and test files) for the symbol name using grep.
   - If found in non-test source files: remove only the `export` keyword, keep the function/constant.
   - If found only in test files OR not found anywhere: delete the entire symbol (function, const, type).
   - Exception: `detectStateMismatch` and `determineConnectionStatus` in `healthMonitoring.ts` — keep exported for test access even though knip flags them (per research Open Question 2).

3. **Specific removals by file:**

   **lib/core/index.ts** (18 exports): Remove re-exports for: `ERROR_MESSAGES`, `mapLegacyError`, `created`, `handleError`, `unauthorized`, `timeout`, `serverError`, `stoveOffline`, `maintenanceRequired`, `netatmoReconnect`, `redirect`, `parseQueryObject`, `validateRange`, `validateEmail`, `validateString`, `validateArray`, `validateBoolean`, `getOptionalPathParam`. These are barrel re-exports — remove the export lines from index.ts. Check if the source files (apiResponse.ts, etc.) still need the `export` keyword on the original definitions by searching for direct imports.

   **lib/core/apiResponse.ts** (2 exports): `serverError`, `redirect` — if no direct imports remain after cleaning index.ts, remove `export` keyword from these functions.

   **lib/errorMonitor.ts**: `sendErrorPushNotification` — check usage; delete symbol if unused.

   **lib/tokenRefresh.ts**: `forceTokenRefresh` — check usage; delete or de-export.

   **lib/tokenStorage.ts** (4 exports): `requestPersistentStorage`, `checkPersistence`, `clearToken`, `getStorageStatus` — check each; delete or de-export.

   **lib/health/healthDeadManSwitch.ts**: `alertDeadManSwitch` — keep exported (used in tests).

   **lib/health/healthMonitoring.ts**: `determineConnectionStatus`, `detectStateMismatch` — keep exported (used in tests).

   **lib/health/healthLogger.ts** (2 exports): `getRecentHealthLogs`, `getHealthCheckDetails` — keep exported (used in tests).

   **NOTE:** `lib/notifications/notificationService.ts` re-export removal is handled in Plan 02 (which also edits that file for CLEAN-02). Do NOT touch notificationService.ts in this plan.

   **lib/notifications/notificationPreferencesService.ts**: `shouldSendErrorNotification` — check usage; delete or de-export.

   **lib/notifications/notificationTriggersServer.ts**: `triggerNetatmoAlertServer` — check usage; delete or de-export.

   **lib/schemas/notificationPreferences.ts** (2 exports): `dndWindowSchema`, `rateLimitSchema` — check usage; delete or de-export.

   **lib/scheduler/schedulesApiClient.ts** (2 exports): `getScheduleById`, `getActiveScheduleId` — check usage; delete or de-export.

   **lib/pwa/installPromptService.ts** (2 exports): `canUseLocalStorage`, `getVisitCount` — check usage; delete or de-export.

   **lib/services/unifiedDeviceConfigService.ts** (4 exports): `isDisplayOnly`, `hasHomepageCard`, `getDefaultDeviceConfig`, `getUnifiedDeviceConfig` — check usage; delete or de-export.

   **lib/services/locationService.ts** (2 exports): `getLocation`, `setLocation` — check usage; delete or de-export.

   **lib/services/pidAutomationService.ts**: `DEFAULT_PID_CONFIG` — check usage; delete or de-export.

   **lib/rateLimiter.ts** (3 exports): `clearRateLimitForUser`, `getRateLimitStatus`, `_internals` — check usage; delete or de-export.

   **lib/devices/deviceRegistry.ts** (2 exports): `DEVICE_CONFIG`, `GLOBAL_SECTIONS` — check usage; delete or de-export. These are re-exported from deviceTypes.ts.

4. **Per D-03:** If removing exports leaves a file with no remaining exports AND no internal usage, delete the file and clean any barrel/index imports referencing it.

5. **Per D-04:** Do NOT touch any file in `app/components/ui/` — neither the barrel `index.ts` nor individual component files.
  </action>
  <verify>
    <automated>npx knip --reporter json 2>/dev/null | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); const unused=(d.exports||[]).filter(e=>!e.file?.includes('app/components/ui/')&&e.file?.startsWith('lib/')&&!e.file?.includes('notificationService')); console.log('lib unused exports:', unused.length); if(unused.length>0){unused.forEach(e=>console.log(' -',e.file,e.name));process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "ERROR_MESSAGES" lib/core/index.ts returns no matches (export removed)
    - grep -r "mapLegacyError" lib/core/index.ts returns no matches (export removed)
    - grep -c "export.*serverError" lib/core/apiResponse.ts returns 0 OR the function is still used by a direct import
    - No `import ... from` statements in app/ or lib/ are broken (npm test passes)
    - All `app/components/ui/*.tsx` files are untouched
    - lib/notifications/notificationService.ts is NOT modified by this plan
  </acceptance_criteria>
  <done>All lib/ unused exports identified by knip are removed or de-exported (except notificationService.ts re-exports, handled in Plan 02). No import breakage.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused exports from app/ files (non-UI) and test helpers</name>
  <read_first>
    - app/hooks/useDebounce.ts
    - app/hooks/useHaptic.ts
    - app/network/components/DeviceListTable.tsx
    - app/network/components/DeviceCategoryBadge.tsx
    - app/network/components/DeviceStatusBadge.tsx
    - app/thermostat/components/ThermostatTabs.tsx
    - app/components/weather/WeatherCard.tsx
    - app/components/weather/CurrentConditions.tsx
    - app/components/weather/ForecastRow.tsx
    - app/components/weather/ForecastDaySheet.tsx
    - app/components/weather/ForecastDayCard.tsx
    - app/components/weather/HourlyForecast.tsx
    - app/debug/design-system/data/component-docs.ts
    - app/components/ErrorBoundary/index.ts
    - tests/helpers/test-context.ts
  </read_first>
  <files>
    app/hooks/useDebounce.ts,
    app/hooks/useHaptic.ts,
    app/network/components/DeviceListTable.tsx,
    app/network/components/DeviceCategoryBadge.tsx,
    app/network/components/DeviceStatusBadge.tsx,
    app/thermostat/components/ThermostatTabs.tsx,
    app/components/weather/WeatherCard.tsx,
    app/components/weather/CurrentConditions.tsx,
    app/components/weather/ForecastRow.tsx,
    app/components/weather/ForecastDaySheet.tsx,
    app/components/weather/ForecastDayCard.tsx,
    app/components/weather/HourlyForecast.tsx,
    app/debug/design-system/data/component-docs.ts,
    app/components/ErrorBoundary/index.ts,
    tests/helpers/test-context.ts
  </files>
  <action>
For each file, check if the flagged export is used anywhere in the codebase. Apply the same D-02 decision tree:

1. **app/hooks/useDebounce.ts** — `default` export. Check if the hook is imported anywhere. If not, check if the named export (if any) is used. If the file has both named + default, remove only the default. If the file is entirely unused, delete it.

2. **app/hooks/useHaptic.ts** — `default` export. Same approach.

3. **app/network/components/DeviceListTable.tsx** — `DeviceListTable` export. Check if imported by any page or parent component. If not, the component is dead code — delete the file.

4. **app/network/components/DeviceCategoryBadge.tsx** — `DeviceCategoryBadge` export. Same check.

5. **app/network/components/DeviceStatusBadge.tsx** — `DeviceStatusBadge` export. Same check.

6. **app/thermostat/components/ThermostatTabs.tsx** — `ThermostatTabs` export. Same check.

7. **app/components/weather/*.tsx** (6 files) — default exports from WeatherCard, CurrentConditions, ForecastRow, ForecastDaySheet, ForecastDayCard, HourlyForecast. These are likely imported by a parent weather component. If imported: the `default` keyword may just be what knip flags; check if the parent uses `import WeatherCard from '...'` (default) or `import { WeatherCard } from '...'` (named). If default is flagged but the component IS used via named import, remove the `export default` and keep the named export.

8. **app/debug/design-system/data/component-docs.ts** (4 exports) — `getComponentsByCategory`, `getCategories`, `getComponentDoc`, `default`. Check usage within the design system debug page. If unused, delete exports or entire file.

9. **app/components/ErrorBoundary/index.ts** — `ErrorFallback` export. Check if imported anywhere. If only the main ErrorBoundary is used, remove the ErrorFallback re-export from the barrel.

10. **tests/helpers/test-context.ts** — `AUTH_FILE`, `BASE_URL`. Check if used in any test file. If not, delete the exports or the entire file if empty.

**Per D-03:** If any file becomes entirely empty after removals, delete the file.

**Per D-04:** Do NOT touch `app/components/ui/` files.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 (no broken imports)
    - No files in app/components/ui/ are modified (git diff --name-only shows no ui/ files)
    - grep -rn "AUTH_FILE\|BASE_URL" tests/helpers/test-context.ts returns 0 matches OR the symbols are still used in test files
    - knip reports fewer unused exports than before (lib/ + app/ combined)
  </acceptance_criteria>
  <done>All in-scope app/ and test helper unused exports are removed. Full test suite passes. No design system files touched.</done>
</task>

</tasks>

<verification>
1. `npx knip 2>&1 | grep -v "app/components/ui"` shows zero lib/ unused exports (excluding notificationService.ts re-exports handled by Plan 02)
2. `npm test` passes with no regressions
3. `git diff --name-only` confirms no app/components/ui/*.tsx files were modified
</verification>

<success_criteria>
- Zero unused exports in lib/ files (per knip, excluding design system barrel and notificationService.ts re-exports deferred to Plan 02)
- All in-scope app/ unused exports handled
- Full test suite green
- No import breakage anywhere in the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/117-dead-code-cleanup/117-01-SUMMARY.md`
</output>
