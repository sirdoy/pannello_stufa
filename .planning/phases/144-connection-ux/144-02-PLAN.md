---
phase: 144-connection-ux
plan: 02
type: execute
wave: 2
depends_on: [144-01]
files_modified:
  - app/components/devices/stove/hooks/useStoveData.ts
  - app/components/devices/network/hooks/useNetworkData.ts
  - app/components/devices/lights/hooks/useLightsData.ts
  - app/components/devices/sonos/hooks/useSonosData.ts
  - app/components/devices/dirigera/hooks/useDirigeraData.ts
  - app/components/devices/thermostat/hooks/useThermostatData.ts
  - app/components/devices/stove/StoveCard.tsx
  - app/components/devices/network/NetworkCard.tsx
  - app/components/devices/lights/LightsCard.tsx
  - app/components/devices/sonos/SonosCard.tsx
  - app/components/devices/dirigera/DirigeraCard.tsx
  - app/components/devices/thermostat/ThermostatCard.tsx
autonomous: true
requirements: [UX-02, UX-03]
must_haves:
  truths:
    - "Every WS-migrated dashboard card displays a 'last updated' timestamp in its footer area"
    - "Timestamp shows Italian relative time that updates every 10 seconds without new data arriving"
    - "When WS is active, lastUpdatedAt is set to Date.now() on each WS message receipt"
    - "When polling is active, lastUpdatedAt is set to Date.now() on each HTTP response"
    - "Switching from WS to polling does not cause any card to flash, blank, or show stale data"
    - "The lastUpdatedAt value transitions seamlessly between WS and polling sources"
  artifacts:
    - path: "app/components/devices/stove/hooks/useStoveData.ts"
      provides: "lastUpdatedAt: number | null in return type"
      contains: "lastUpdatedAt"
    - path: "app/components/devices/network/hooks/useNetworkData.ts"
      provides: "lastUpdatedAt: number | null in return type (already has lastUpdated, may alias)"
      contains: "lastUpdatedAt"
    - path: "app/components/devices/lights/hooks/useLightsData.ts"
      provides: "lastUpdatedAt: number | null in return type"
      contains: "lastUpdatedAt"
    - path: "app/components/devices/sonos/hooks/useSonosData.ts"
      provides: "lastUpdatedAt: number | null in return type"
      contains: "lastUpdatedAt"
    - path: "app/components/devices/dirigera/hooks/useDirigeraData.ts"
      provides: "lastUpdatedAt: number | null in return type"
      contains: "lastUpdatedAt"
    - path: "app/components/devices/thermostat/hooks/useThermostatData.ts"
      provides: "lastUpdatedAt: number | null in return type"
      contains: "lastUpdatedAt"
  key_links:
    - from: "app/components/devices/stove/StoveCard.tsx"
      to: "app/components/ui/LastUpdated.tsx"
      via: "imports LastUpdated, passes lastUpdatedAt from useStoveData"
      pattern: "LastUpdated.*tsMs="
    - from: "app/components/devices/dirigera/DirigeraCard.tsx"
      to: "app/components/ui/LastUpdated.tsx"
      via: "imports LastUpdated, passes lastUpdatedAt from useDirigeraData"
      pattern: "LastUpdated.*tsMs="
---

<objective>
Extend all 6 WS-migrated device hooks to expose `lastUpdatedAt: number | null` and wire the `LastUpdated` component into all 6 dashboard card footers.

Purpose: UX-03 (per-card timestamps showing when data was last received) and UX-02 (verify flicker-free transitions by ensuring data persists across source switches).
Output: 6 modified hooks, 6 modified cards, all showing live Italian timestamps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-connection-ux/144-CONTEXT.md
@.planning/phases/144-connection-ux/144-RESEARCH.md
@.planning/phases/144-connection-ux/144-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From app/components/ui/LastUpdated.tsx:
```typescript
interface LastUpdatedProps {
  tsMs: number | null;
  className?: string;
}
export function LastUpdated({ tsMs, className }: LastUpdatedProps): JSX.Element | null;
```

From lib/hooks/useRelativeTime.ts:
```typescript
export function formatRelativeTime(tsMs: number): string;
export function useRelativeTime(tsMs: number | null): string | null;
```

<!-- Existing hook patterns -->

From app/components/devices/stove/hooks/useStoveData.ts:
```typescript
// Already has: const [lastPollAt, setLastPollAt] = useState<Date | null>(null);
// WS handler calls: setLastPollAt(new Date());
// HTTP handler calls: setLastPollAt(last_poll_at ? new Date(last_poll_at) : null);
// Return type: UseStoveDataReturn (does NOT include lastUpdatedAt yet)
```

From app/components/devices/network/hooks/useNetworkData.ts:
```typescript
// Already has: const [lastUpdated, setLastUpdated] = useState<number | null>(null);
// WS handler calls: setLastUpdated(Date.now()); (line 221)
// HTTP handler calls: setLastUpdated(Date.now()); (line 354)
// Already in return: lastUpdated (number | null)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend all 6 device hooks to expose lastUpdatedAt</name>
  <files>
    app/components/devices/stove/hooks/useStoveData.ts,
    app/components/devices/network/hooks/useNetworkData.ts,
    app/components/devices/lights/hooks/useLightsData.ts,
    app/components/devices/sonos/hooks/useSonosData.ts,
    app/components/devices/dirigera/hooks/useDirigeraData.ts,
    app/components/devices/thermostat/hooks/useThermostatData.ts
  </files>
  <read_first>
    app/components/devices/stove/hooks/useStoveData.ts,
    app/components/devices/network/hooks/useNetworkData.ts,
    app/components/devices/lights/hooks/useLightsData.ts,
    app/components/devices/sonos/hooks/useSonosData.ts,
    app/components/devices/dirigera/hooks/useDirigeraData.ts,
    app/components/devices/thermostat/hooks/useThermostatData.ts
  </read_first>
  <action>
    For each of the 6 device hooks, add `lastUpdatedAt: number | null` to the return type and set it on both WS and HTTP data receipt. The exact changes per hook:

    **1. useStoveData.ts** — Already has `lastPollAt: Date | null`. Add a derived value:
    - Add to `UseStoveDataReturn` interface: `lastUpdatedAt: number | null;`
    - In the return object, add: `lastUpdatedAt: lastPollAt ? lastPollAt.getTime() : null,`
    - Do NOT add new state — derive from existing `lastPollAt` which is already set in both WS (`setLastPollAt(new Date())`) and HTTP handlers.

    **2. useNetworkData.ts** — Already has `lastUpdated: number | null` set in both WS (line 221) and HTTP (line 354).
    - Add `lastUpdatedAt` as an alias in the return object: `lastUpdatedAt: lastUpdated,`
    - Add `lastUpdatedAt: number | null;` to the return type interface (if one exists) or inline type.
    - Keep the existing `lastUpdated` field too for backward compatibility.

    **3. useLightsData.ts** — Has NO timestamp tracking.
    - Add state: `const [lastUpdatedAt, setLastUpdatedAt] = useState<number | null>(null);`
    - In the WS `handleMessage` callback (inside the `useEffect` that calls `subscribe('hue', ...)`), add: `setLastUpdatedAt(Date.now());`
    - In the HTTP fetch success callback (wherever `setLights(...)` or equivalent is called after fetch), add: `setLastUpdatedAt(Date.now());`
    - Add `lastUpdatedAt` to the return object.

    **4. useSonosData.ts** — Has NO timestamp tracking.
    - Add state: `const [lastUpdatedAt, setLastUpdatedAt] = useState<number | null>(null);`
    - In the WS `handleMessage` callback (subscribe to `'sonos'` topic), add: `setLastUpdatedAt(Date.now());`
    - In the HTTP fetch success callback, add: `setLastUpdatedAt(Date.now());`
    - Add `lastUpdatedAt` to the return object.

    **5. useDirigeraData.ts** — Has NO timestamp tracking.
    - Add state: `const [lastUpdatedAt, setLastUpdatedAt] = useState<number | null>(null);`
    - In the WS `handleMessage` callback (subscribe to `'dirigera'` topic), add: `setLastUpdatedAt(Date.now());`
    - In the HTTP fetch success callback, add: `setLastUpdatedAt(Date.now());`
    - Add `lastUpdatedAt` to the return object.

    **6. useThermostatData.ts** — Has NO timestamp tracking.
    - Add state: `const [lastUpdatedAt, setLastUpdatedAt] = useState<number | null>(null);`
    - In the WS `handleMessage` callback (subscribe to `'netatmo'` topic), add: `setLastUpdatedAt(Date.now());`
    - In the HTTP fetch success callback, add: `setLastUpdatedAt(Date.now());`
    - Add `lastUpdatedAt` to the return object.

    **IMPORTANT per D-04/D-05 (UX-02):** Do NOT clear `lastUpdatedAt` to null during WS/polling transitions. The state persists across source switches. Only update it when new data arrives from either source.

    **IMPORTANT per D-09:** Use `Date.now()` (not `data.ts * 1000`) in WS handlers because the WS manager dispatches only `msg.data` to callbacks — the `ts` envelope field is not available. `Date.now()` is functionally equivalent since the message just arrived.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npm test -- --testPathPattern="useStoveData|useNetworkData|useLightsData|useSonosData|useDirigeraData|useThermostatData" --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - app/components/devices/stove/hooks/useStoveData.ts contains `lastUpdatedAt`
    - app/components/devices/stove/hooks/useStoveData.ts return object contains `lastUpdatedAt: lastPollAt`
    - app/components/devices/network/hooks/useNetworkData.ts contains `lastUpdatedAt`
    - app/components/devices/lights/hooks/useLightsData.ts contains `const [lastUpdatedAt, setLastUpdatedAt] = useState`
    - app/components/devices/lights/hooks/useLightsData.ts contains `setLastUpdatedAt(Date.now())`
    - app/components/devices/sonos/hooks/useSonosData.ts contains `const [lastUpdatedAt, setLastUpdatedAt] = useState`
    - app/components/devices/sonos/hooks/useSonosData.ts contains `setLastUpdatedAt(Date.now())`
    - app/components/devices/dirigera/hooks/useDirigeraData.ts contains `const [lastUpdatedAt, setLastUpdatedAt] = useState`
    - app/components/devices/dirigera/hooks/useDirigeraData.ts contains `setLastUpdatedAt(Date.now())`
    - app/components/devices/thermostat/hooks/useThermostatData.ts contains `const [lastUpdatedAt, setLastUpdatedAt] = useState`
    - app/components/devices/thermostat/hooks/useThermostatData.ts contains `setLastUpdatedAt(Date.now())`
    - Existing tests pass: npm test exits 0 for affected hook test files
  </acceptance_criteria>
  <done>All 6 device hooks expose lastUpdatedAt: number | null. Stove derives from existing lastPollAt. Network aliases existing lastUpdated. Lights/Sonos/DIRIGERA/Thermostat have new state set on both WS and HTTP receipt. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire LastUpdated into all 6 dashboard cards</name>
  <files>
    app/components/devices/stove/StoveCard.tsx,
    app/components/devices/network/NetworkCard.tsx,
    app/components/devices/lights/LightsCard.tsx,
    app/components/devices/sonos/SonosCard.tsx,
    app/components/devices/dirigera/DirigeraCard.tsx,
    app/components/devices/thermostat/ThermostatCard.tsx
  </files>
  <read_first>
    app/components/devices/stove/StoveCard.tsx,
    app/components/devices/network/NetworkCard.tsx,
    app/components/devices/lights/LightsCard.tsx,
    app/components/devices/sonos/SonosCard.tsx,
    app/components/devices/dirigera/DirigeraCard.tsx,
    app/components/devices/thermostat/ThermostatCard.tsx,
    app/components/ui/LastUpdated.tsx
  </read_first>
  <action>
    For each of the 6 dashboard cards, import `LastUpdated` and render it as the last child inside the card's main `SmartHomeCard` component, with a top border separator.

    **Import to add to each card file:**
    ```typescript
    import { LastUpdated } from '@/app/components/ui/LastUpdated';
    ```

    **Pattern for each card:** Destructure `lastUpdatedAt` from the hook, then add `<LastUpdated>` as the last child inside the main `<SmartHomeCard>` (inside the success/data render path, NOT in loading/error states):

    ```tsx
    <SmartHomeCard ...>
      {/* ... existing content ... */}
      <LastUpdated tsMs={lastUpdatedAt} className="mt-3 pt-2 border-t border-slate-800/30 dark:border-slate-700/30" />
    </SmartHomeCard>
    ```

    **Per-card specifics:**

    **1. StoveCard.tsx** — Uses orchestrator pattern with sub-components. The main card renders via a presentation component. Read the file to find where the final `<SmartHomeCard>` closes and insert `<LastUpdated>` before the closing tag. Destructure `lastUpdatedAt` from `useStoveData(...)`.

    **2. NetworkCard.tsx** — Read the file to find the main SmartHomeCard render. Destructure `lastUpdatedAt` from `useNetworkData()`. Add `<LastUpdated>` as last child.

    **3. LightsCard.tsx** — Destructure `lastUpdatedAt` from `useLightsData()`. Add `<LastUpdated>` as last child of SmartHomeCard.

    **4. SonosCard.tsx** — Destructure `lastUpdatedAt` from `useSonosData()`. Add `<LastUpdated>` as last child.

    **5. DirigeraCard.tsx** — Destructure `lastUpdatedAt` from `useDirigeraData()`. The card wraps SmartHomeCard in a clickable div. Add `<LastUpdated>` inside the SmartHomeCard, after the `{data && ...}` block and before the `</SmartHomeCard>` closing tag.

    **6. ThermostatCard.tsx** — Destructure `lastUpdatedAt` from `useThermostatData()`. Add `<LastUpdated>` as last child.

    **Styling note (per D-07):** The `className="mt-3 pt-2 border-t border-slate-800/30 dark:border-slate-700/30"` creates a subtle separator line above the timestamp. Adjust dark mode border if needed based on the card's existing color theme.

    **DO NOT** add `<LastUpdated>` in loading skeleton states or error-only states. Only render it in the normal data display path.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npm test -- --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/components/devices/stove/StoveCard.tsx contains `import { LastUpdated }` and `<LastUpdated`
    - app/components/devices/network/NetworkCard.tsx contains `import { LastUpdated }` and `<LastUpdated`
    - app/components/devices/lights/LightsCard.tsx contains `import { LastUpdated }` and `<LastUpdated`
    - app/components/devices/sonos/SonosCard.tsx contains `import { LastUpdated }` and `<LastUpdated`
    - app/components/devices/dirigera/DirigeraCard.tsx contains `import { LastUpdated }` and `<LastUpdated`
    - app/components/devices/thermostat/ThermostatCard.tsx contains `import { LastUpdated }` and `<LastUpdated`
    - All 6 cards pass `lastUpdatedAt` as tsMs prop: grep for `tsMs={lastUpdatedAt}` in each card
    - npm test exits 0 (full suite, no regressions)
  </acceptance_criteria>
  <done>All 6 WS-migrated dashboard cards display a "last updated" timestamp in their footer area. Timestamps show Italian relative time from the device hook's lastUpdatedAt value. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `npm test` — full suite passes with no regressions
- All 6 hooks expose `lastUpdatedAt: number | null` in their return
- All 6 cards import and render `<LastUpdated tsMs={lastUpdatedAt} />`
- No state clearing during WS/polling transitions (UX-02 / D-04 verified)
- Timestamps update reactively every 10s (UX-03 / D-10 via useRelativeTime from Plan 01)
</verification>

<success_criteria>
1. All 6 device hooks expose lastUpdatedAt: number | null set on both WS and HTTP data receipt
2. All 6 dashboard cards render LastUpdated in the footer with a border-t separator
3. Timestamps show Italian relative time ("5s fa", "2m fa") that updates every 10s
4. No card flashes, blanks, or clears during WS/polling transitions (D-04, D-05)
5. Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/144-connection-ux/144-02-SUMMARY.md`
</output>
