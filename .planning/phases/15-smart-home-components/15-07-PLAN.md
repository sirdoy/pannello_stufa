---
phase: 15-smart-home-components
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.js
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Home page renders device cards in 2-column grid on desktop"
    - "Grid uses valid CVA variant props (numeric cols, short gap names)"
  artifacts:
    - path: "app/page.js"
      provides: "Home page with device grid"
      contains: "cols={2}"
  key_links:
    - from: "app/page.js"
      to: "app/components/ui/Grid.js"
      via: "Grid component import"
      pattern: "cols=\\{2\\}"
---

<objective>
Fix invalid Grid props on home page that cause rendering issues.

Purpose: Grid component uses CVA variants with numeric cols (1-6) and short gap names (none/sm/md/lg), but app/page.js passes object for cols and "large" for gap which are invalid.

Output: Home page renders correctly with valid Grid props.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Grid props in home page</name>
  <files>app/page.js</files>
  <action>
  Update line 38 from:
  ```jsx
  <Grid cols={{ mobile: 1, desktop: 2, wide: 2 }} gap="large">
  ```

  To:
  ```jsx
  <Grid cols={2} gap="lg">
  ```

  Rationale:
  - Grid CVA variants only accept numeric cols (1-6), not objects
  - Grid gap variants are: none, sm, md, lg (not "large")
  - cols={2} provides responsive behavior automatically via CVA: "grid-cols-1 sm:grid-cols-2"
  </action>
  <verify>
  Run `npm run dev` and visit http://localhost:3000
  Visually confirm device cards display in 2-column grid on desktop
  </verify>
  <done>
  Grid displays correctly with 2 columns on desktop, 1 column on mobile
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. Home page loads at http://localhost:3000
3. Device cards display in responsive 2-column grid
4. No console warnings about invalid props
</verification>

<success_criteria>
- Grid cols prop is numeric (2)
- Grid gap prop is valid variant ("lg")
- Home page renders device cards correctly
</success_criteria>

<rollback>
If fix causes issues, revert to original props and investigate Grid component variants.
Original line 38: `<Grid cols={{ mobile: 1, desktop: 2, wide: 2 }} gap="large">`
</rollback>

<output>
After completion, create `.planning/phases/15-smart-home-components/15-07-SUMMARY.md`
</output>
