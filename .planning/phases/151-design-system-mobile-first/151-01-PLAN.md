---
phase: 151-design-system-mobile-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/ui/Button.tsx
  - app/components/ui/__tests__/Button.test.tsx
  - app/components/Navbar.tsx
autonomous: true
requirements:
  - MOBILE-01
  - MOBILE-02
  - MOBILE-06

must_haves:
  truths:
    - "ButtonGroup with 4+ buttons wraps to a second row at 375px instead of overflowing"
    - "Bottom nav bar displays all 4 columns without clipping at 375px"
    - "All layout-responsible DS components render without horizontal overflow at 375px"
  artifacts:
    - path: "app/components/ui/Button.tsx"
      provides: "ButtonGroup with flex-wrap"
      contains: "flex flex-wrap items-center gap-2"
    - path: "app/components/ui/__tests__/Button.test.tsx"
      provides: "Unit test for ButtonGroup flex-wrap"
      contains: "flex-wrap"
  key_links:
    - from: "app/components/ui/__tests__/Button.test.tsx"
      to: "app/components/ui/Button.tsx"
      via: "import ButtonGroup"
      pattern: "import.*ButtonGroup.*from.*Button"
---

<objective>
Fix ButtonGroup wrapping, verify bottom nav bar at 375px, and audit all layout-responsible DS components for mobile safety.

Purpose: Close the two concrete layout gaps (ButtonGroup overflow, bottom nav risk) and confirm the remaining layout components are already mobile-safe.
Output: ButtonGroup wraps at narrow viewports, bottom nav verified safe, layout audit complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-design-system-mobile-first/151-CONTEXT.md
@.planning/phases/151-design-system-mobile-first/151-RESEARCH.md

<interfaces>
<!-- ButtonGroup current implementation — executor modifies this -->
From app/components/ui/Button.tsx (lines 350-360):
```typescript
function ButtonGroup({ children, className, ...props }: ButtonGroupProps) {
  return (
    <div
      className={cn('flex items-center gap-2', className)}
      role="group"
      {...props}
    >
      {children}
    </div>
  );
}
```

From app/components/ui/__tests__/Button.test.tsx (lines 570-572):
```typescript
test('ButtonGroup is exported', () => {
  expect(ButtonGroup).toBeDefined();
});
```

From app/components/Navbar.tsx (lines 678-681):
```typescript
const gridCols = quickActions.length === 3 ? 'grid-cols-3' : 'grid-cols-4';
return (
  <div className={`grid ${gridCols} gap-2 p-2`}>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add flex-wrap to ButtonGroup and write unit test</name>
  <files>app/components/ui/Button.tsx, app/components/ui/__tests__/Button.test.tsx</files>
  <read_first>
    - app/components/ui/Button.tsx (lines 340-376 — ButtonGroup definition)
    - app/components/ui/__tests__/Button.test.tsx (lines 567-574 — existing ButtonGroup tests)
  </read_first>
  <behavior>
    - Test: ButtonGroup container element has class "flex-wrap" when rendered with children
    - Test: ButtonGroup container element still has classes "flex", "items-center", "gap-2"
  </behavior>
  <action>
Per D-01 and D-02: Add `flex-wrap` to ButtonGroup's className string.

1. In `app/components/ui/__tests__/Button.test.tsx`, add a new test inside the existing `describe('Exports')` block (after line 572):
```typescript
test('ButtonGroup renders with flex-wrap class', () => {
  const { container } = render(
    <ButtonGroup>
      <Button>A</Button>
      <Button>B</Button>
    </ButtonGroup>
  );
  const group = container.firstChild as HTMLElement;
  expect(group.className).toContain('flex-wrap');
  expect(group.className).toContain('items-center');
  expect(group.className).toContain('gap-2');
});
```

2. Run the test — it MUST fail (RED) because ButtonGroup currently lacks `flex-wrap`.

3. In `app/components/ui/Button.tsx` line 353, change:
   FROM: `className={cn('flex items-center gap-2', className)}`
   TO:   `className={cn('flex flex-wrap items-center gap-2', className)}`

4. Run the test again — it MUST pass (GREEN).

Do NOT add `overflow-hidden` or any other layout change. Do NOT add equal-sizing or vertical stacking (per D-02).
  </action>
  <verify>
    <automated>npx jest --testPathPattern="Button" --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/components/ui/Button.tsx line 353 contains the string `flex flex-wrap items-center gap-2`
    - app/components/ui/__tests__/Button.test.tsx contains `flex-wrap` in a test assertion
    - `npx jest --testPathPattern="Button" --no-coverage` exits 0
  </acceptance_criteria>
  <done>ButtonGroup renders with flex-wrap class, test passes, no other ButtonGroup changes</done>
</task>

<task type="auto">
  <name>Task 2: Verify bottom nav and audit layout DS components at 375px</name>
  <files>app/components/Navbar.tsx</files>
  <read_first>
    - app/components/Navbar.tsx (lines 670-710 — bottom nav grid)
    - app/components/ui/DataTable.tsx (search for "overflow-x-auto")
    - app/components/ui/Grid.tsx (search for "grid-cols-1")
    - app/components/ui/PageLayout.tsx (search for "px-4")
    - app/components/ui/FormModal.tsx (search for "w-full" or "max-w")
    - app/components/ui/BottomSheet.tsx (search for "w-full" or "max-w")
    - app/components/ui/Card.tsx (search for "w-" or fixed width)
    - app/components/ui/SmartHomeCard.tsx (search for "w-" or fixed width)
    - app/components/ui/CommandPalette.tsx (search for "w-" or "max-w")
    - app/components/ui/Container.tsx (search for "max-w" or "px-")
  </read_first>
  <action>
Per D-03, D-04, D-05, D-11, D-12: Audit the 10-12 layout-responsible DS components and the bottom nav.

**Bottom Nav Verification (MOBILE-06, D-11):**
Research confirms the 4-column layout at 375px yields ~84px per column. Labels are "Home" (4 chars), "Orari" (5), "Errori" (6), "Log" (3) at `text-[10px]` — all fit. The 3-column "Programmazione" has ~117px — also fits. No code changes expected. If the labels DO clip (unlikely), apply D-12: reduce inner padding from `px-2` to `px-1` or switch to icon-only at base breakpoint.

**Layout Component Audit (MOBILE-02):**
For each component below, read the file, check for hardcoded widths or missing responsive patterns at base breakpoint. Record findings. Only modify files that have actual 375px issues.

Components to audit (per D-03 priority list):
1. **DataTable** (`ui/DataTable.tsx`): Verify `overflow-x-auto` wrapper exists on table container. Expected: already present (confirmed by research). No change needed.
2. **Grid** (`ui/Grid.tsx`): Verify base responsive is `grid-cols-1`. Expected: already correct. No change needed.
3. **PageLayout** (`ui/PageLayout.tsx`): Verify base padding is `px-4` (16px). Expected: already correct. No change needed.
4. **FormModal** (`ui/FormModal.tsx`): Verify modal is full-width on mobile (`w-full` or `max-w-lg` with responsive). If modal has a fixed `min-w` > 375px, remove it or wrap with `max-w-[calc(100vw-2rem)]`.
5. **BottomSheet** (`ui/BottomSheet.tsx`): Verify uses `w-full` or percentage width. Expected: mobile-native, likely fine.
6. **Card** (`ui/Card.tsx`): Verify no fixed `w-` classes. Expected: responsive by default.
7. **SmartHomeCard** (`ui/SmartHomeCard.tsx`): Verify no fixed widths.
8. **CommandPalette** (`ui/CommandPalette.tsx`): Verify overlay width is responsive (not fixed `w-[600px]` without mobile override). If fixed width found, add `w-full sm:w-[600px]` or `max-w-[calc(100vw-2rem)]`.
9. **Container** (`ui/Container.tsx`): Verify max-width doesn't prevent 375px rendering.

**Inline Components Quick Check (per D-04):**
Scan Badge, Button, Checkbox, Input, Select, Toggle for hardcoded `w-` values > 200px. Expected: none found, no changes.

For any component that DOES need a fix, make the minimal change (add responsive class or `max-w` constraint). Document all findings in the SUMMARY.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/components/Navbar.tsx bottom nav grid at line ~681 uses `grid ${gridCols} gap-2 p-2` (no change needed, just verified)
    - app/components/ui/DataTable.tsx contains `overflow-x-auto`
    - app/components/ui/Grid.tsx contains `grid-cols-1`
    - app/components/ui/PageLayout.tsx contains `px-4`
    - `npm test` exits 0 (no regressions from any fixes applied)
  </acceptance_criteria>
  <done>All layout DS components verified at 375px, bottom nav confirmed safe, any overflow issues fixed, full test suite green</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern="Button" --no-coverage` passes with new flex-wrap test
- `npm test` full suite passes with no regressions
- `grep -n "flex flex-wrap" app/components/ui/Button.tsx` returns the ButtonGroup line
</verification>

<success_criteria>
- ButtonGroup has `flex-wrap` class (MOBILE-01)
- All 10-12 layout components audited and confirmed mobile-safe or fixed (MOBILE-02)
- Bottom nav verified safe at 375px (MOBILE-06)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/151-design-system-mobile-first/151-01-SUMMARY.md`
</output>
