---
phase: 152-pages-audit-core-device-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/stove/errors/page.tsx
  - app/thermostat/schedule/page.tsx
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03

must_haves:
  truths:
    - "Dashboard (/) renders all cards in single-column at 375px with no horizontal scroll"
    - "All stove sub-pages display without overflow or clipped buttons at 375px"
    - "Both thermostat pages show full controls without horizontal scroll at 375px"
  artifacts:
    - path: "app/stove/errors/page.tsx"
      provides: "Responsive header row that wraps at 375px"
      contains: "flex-wrap"
    - path: "app/thermostat/schedule/page.tsx"
      provides: "Responsive header row that wraps at 375px"
      contains: "flex-wrap"
  key_links:
    - from: "app/stove/errors/page.tsx"
      to: "375px viewport"
      via: "flex-wrap on header flex container"
      pattern: "flex.*flex-wrap.*items-center.*justify-between"
    - from: "app/thermostat/schedule/page.tsx"
      to: "375px viewport"
      via: "flex-wrap on header flex container"
      pattern: "flex.*flex-wrap.*items-start.*justify-between"
---

<objective>
Audit and fix the dashboard, stove, and thermostat pages for 375px mobile viewport.

Purpose: Ensure the core pages (dashboard home, 4 stove sub-pages, 2 thermostat pages) render without horizontal overflow, clipped controls, or layout breakage at 375px (iPhone SE).

Output: Two files with minimal responsive class fixes, all 7 pages verified at 375px.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-pages-audit-core-device-pages/152-CONTEXT.md
@.planning/phases/152-pages-audit-core-device-pages/152-RESEARCH.md

<interfaces>
<!-- Key layout patterns already in place (confirmed mobile-safe, no changes needed): -->

From app/components/DashboardCards.tsx:
- Mobile block: `<div className="flex flex-col gap-6 sm:hidden">` — single column at 375px
- Desktop block: `<div className="hidden sm:flex sm:flex-row gap-8 lg:gap-10">` — hidden at 375px

From app/stove/page.tsx:
- Loading skeleton: `grid grid-cols-1 md:grid-cols-3` — 1 col at mobile
- StovePageNavigation: `grid grid-cols-1 sm:grid-cols-3 gap-4` — 1 col at mobile

From app/stove/maintenance/page.tsx:
- Status grid: `grid grid-cols-1 md:grid-cols-3 gap-4` — 1 col at mobile
- Quick presets: `flex gap-2 flex-wrap` — already wrapping

From app/stove/scheduler/page.tsx:
- Outer grid: `grid grid-cols-1 lg:grid-cols-2 gap-6` — 1 col at mobile
- Stove WeeklyTimeline: percentage-based bars, no min-width — already mobile-safe

From app/thermostat/page.tsx:
- Mode control: `flex flex-col lg:flex-row lg:items-center lg:justify-between gap-4` — stacks vertically
- Grid cols={3}: resolves to `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` — 1 col at mobile

From app/thermostat/schedule/components/WeeklyTimeline.tsx:
- Line 105: `<div className="overflow-x-auto pb-4 -mx-4 px-4 scrollbar-thin scrollbar-thumb-slate-600">`
- Line 106: `<div className="min-w-[600px]">` — already wrapped in overflow-x-auto, ALREADY HANDLED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stove errors and thermostat schedule header rows for 375px</name>
  <files>app/stove/errors/page.tsx, app/thermostat/schedule/page.tsx</files>
  <read_first>
    - app/stove/errors/page.tsx (line 102: flex header with heading + button that may clip at 375px)
    - app/thermostat/schedule/page.tsx (line 69: flex header with heading + button that may clip at 375px)
  </read_first>
  <action>
    **File 1: app/stove/errors/page.tsx**
    At line 102, the header uses `flex items-center justify-between mb-6`. This row contains a heading block ("Storico Allarmi" with emoji) and a "Torna alla Home" button. At 375px with p-6 card padding, the usable width is ~327px, which may not fit both elements on one line.

    Change line 102 from:
    ```
    <div className="flex items-center justify-between mb-6">
    ```
    to:
    ```
    <div className="flex flex-wrap items-center justify-between gap-3 mb-6">
    ```

    This adds `flex-wrap` so the button wraps below the heading at narrow widths, and `gap-3` provides vertical spacing when wrapped. Per D-01: minimal targeted fix. Per D-02: mobile-first convention.

    **File 2: app/thermostat/schedule/page.tsx**
    At line 69, the header uses `flex items-start justify-between`. This row contains the "Programmazione" heading block and an "Aggiorna" button. Same overflow risk at 375px.

    Change line 69 from:
    ```
    <div className="flex items-start justify-between">
    ```
    to:
    ```
    <div className="flex flex-wrap items-start justify-between gap-3">
    ```

    Same rationale: adds `flex-wrap` and `gap-3` for graceful wrapping at narrow viewports.

    **No other files need changes** for AUDIT-01, AUDIT-02, AUDIT-03. The research confirms:
    - Dashboard: already has dedicated sm:hidden mobile block (no changes)
    - Stove main page: grid-cols-1 md:grid-cols-3 (mobile-safe)
    - Stove maintenance: grid-cols-1 md:grid-cols-3 + flex-wrap (mobile-safe)
    - Stove scheduler: grid-cols-1 lg:grid-cols-2 (mobile-safe)
    - Thermostat main: Grid cols={3} resolves to grid-cols-1 at mobile (mobile-safe)
    - Thermostat schedule WeeklyTimeline: already has overflow-x-auto wrapper (mobile-safe)
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -n "flex-wrap" app/stove/errors/page.tsx app/thermostat/schedule/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/stove/errors/page.tsx contains `flex flex-wrap items-center justify-between gap-3 mb-6` on the header div (around line 102)
    - app/thermostat/schedule/page.tsx contains `flex flex-wrap items-start justify-between gap-3` on the header div (around line 69)
    - No other lines in either file were changed
    - `npm test -- --testPathPattern="stove/errors|thermostat/schedule" --passWithNoTests` passes
  </acceptance_criteria>
  <done>Stove errors header and thermostat schedule header wrap gracefully at 375px. All 7 pages in scope (dashboard, 4 stove, 2 thermostat) are mobile-safe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of dashboard, stove, and thermostat pages at 375px</name>
  <what-built>
    Minimal flex-wrap fixes to stove errors header and thermostat schedule header. Dashboard and remaining stove/thermostat pages were confirmed mobile-safe via code inspection and require visual verification only.
  </what-built>
  <how-to-verify>
    Open each page in browser at 375px width (DevTools responsive mode, iPhone SE preset) and confirm:

    1. **Dashboard (/)** — All cards render in single column, no horizontal scrollbar
    2. **Stove (/stove)** — Hero metrics (2-col grid), navigation cards, and adjustment controls all visible without overflow
    3. **Stove errors (/stove/errors)** — Header wraps gracefully (heading on first line, button below), filter tabs wrap, error cards in single column
    4. **Stove maintenance (/stove/maintenance)** — Status grid in single column, quick presets wrap
    5. **Stove scheduler (/stove/scheduler)** — Schedule grid in single column, timeline bars visible
    6. **Thermostat (/thermostat)** — Mode controls stack vertically, room grid in single column
    7. **Thermostat schedule (/thermostat/schedule)** — Header wraps gracefully, WeeklyTimeline scrolls horizontally within its container

    For each page: no horizontal page scroll, no clipped text or buttons, all controls tappable.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 pages look correct, or describe which page(s) have issues</resume-signal>
</task>

</tasks>

<verification>
All 7 pages (/, /stove, /stove/errors, /stove/maintenance, /stove/scheduler, /thermostat, /thermostat/schedule) render without horizontal overflow at 375px viewport width.
</verification>

<success_criteria>
- AUDIT-01: Dashboard renders single-column cards at 375px, no horizontal scrollbar
- AUDIT-02: All 4 stove pages display without overflow or clipped buttons at 375px
- AUDIT-03: Both thermostat pages show full controls without horizontal scroll at 375px
</success_criteria>

<output>
After completion, create `.planning/phases/152-pages-audit-core-device-pages/152-01-SUMMARY.md`
</output>
