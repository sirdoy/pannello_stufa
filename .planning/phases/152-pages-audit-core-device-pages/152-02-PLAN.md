---
phase: 152-pages-audit-core-device-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lights/page.tsx
  - app/network/page.tsx
  - app/network/components/SystemInfoCard.tsx
autonomous: true
requirements:
  - AUDIT-04
  - AUDIT-05

must_haves:
  truths:
    - "All lights pages (/lights, /lights/scenes, /lights/automation) are fully operable on touch at 375px"
    - "The network page (/network) displays charts and tables without overflow at 375px"
  artifacts:
    - path: "app/lights/page.tsx"
      provides: "Responsive grid for stats and color presets at 375px"
      contains: "sm:grid-cols-3"
    - path: "app/network/page.tsx"
      provides: "Tab navigation that wraps or scrolls at 375px"
      contains: "flex-wrap"
    - path: "app/network/components/SystemInfoCard.tsx"
      provides: "Responsive grid for system info at 375px"
      contains: "sm:grid-cols-3"
  key_links:
    - from: "app/lights/page.tsx"
      to: "375px viewport"
      via: "responsive grid columns"
      pattern: "grid-cols.*sm:grid-cols"
    - from: "app/network/components/SystemInfoCard.tsx"
      to: "375px viewport"
      via: "responsive grid columns"
      pattern: "grid-cols-1 sm:grid-cols-3"
---

<objective>
Audit and fix the lights and network pages for 375px mobile viewport.

Purpose: Ensure all lights pages (/lights, /lights/scenes, /lights/automation) and the network page (/network) render without horizontal overflow, clipped controls, or layout breakage at 375px (iPhone SE).

Output: Three files with minimal responsive class fixes. Lights scenes/automation are already mobile-safe (no changes needed). All 4 pages verified at 375px.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-pages-audit-core-device-pages/152-CONTEXT.md
@.planning/phases/152-pages-audit-core-device-pages/152-RESEARCH.md

<interfaces>
<!-- Known issues from research that need fixing -->

From app/lights/page.tsx line 111 (stats summary grid):
```typescript
<div className="grid grid-cols-3 gap-6">
```
Issue: Non-responsive 3-column grid, tight at 375px with gap-6.

From app/lights/page.tsx line 233 (color presets grid):
```typescript
<div className="grid grid-cols-5 gap-1.5">
```
Issue: 5 columns at 375px = ~54px per swatch, too tight.

From app/network/page.tsx line 215 (tab navigation):
```typescript
<div className="flex gap-1 border-b border-white/[0.06] pb-0">
```
Issue: 4 tabs with px-4 py-2 text-sm, may overflow at 375px.

From app/network/components/SystemInfoCard.tsx line 67:
```typescript
<div className="grid grid-cols-3 gap-3">
```
Issue: No mobile breakpoint, 3 columns at 375px = ~106px each.

<!-- Already mobile-safe (no changes needed): -->
From app/lights/scenes/page.tsx:
- Grid: `grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5` — already responsive

From app/lights/automation/page.tsx:
- Grid: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` — already responsive

From app/network tables:
- DeviceListTable, WifiClientsTable use DataTable which has overflow-x-auto — already safe
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix lights page grids for 375px</name>
  <files>app/lights/page.tsx</files>
  <read_first>
    - app/lights/page.tsx (lines 105-120 for stats grid, lines 225-240 for color presets grid)
  </read_first>
  <action>
    **Fix 1 — Stats summary grid (line 111, D-05):**
    Change from:
    ```
    <div className="grid grid-cols-3 gap-6">
    ```
    to:
    ```
    <div className="grid grid-cols-3 gap-3 sm:gap-6">
    ```
    Rationale: Keep 3 columns (stats are just label + number, they fit at 375px) but reduce gap from 24px to 12px at mobile. At 375px with px-4 padding: (343-24)/3 ≈ 106px per column — adequate for short stat labels. Per D-01: minimal fix.

    **Fix 2 — Color presets grid (line 233, D-06):**
    Change from:
    ```
    <div className="grid grid-cols-5 gap-1.5">
    ```
    to:
    ```
    <div className="grid grid-cols-3 sm:grid-cols-5 gap-1.5">
    ```
    Rationale: 5 color swatches are too tight at 375px (~54px each). 3 columns at mobile gives ~95px each — comfortable for color circles. Per D-06 from CONTEXT.md.

    **No changes needed** for /lights/scenes and /lights/automation — already responsive per research.
  </action>
  <verify>
    <automated>grep -n "grid-cols-3 gap-3 sm:gap-6\|grid-cols-3 sm:grid-cols-5" app/lights/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/lights/page.tsx line ~111 contains `grid grid-cols-3 gap-3 sm:gap-6`
    - app/lights/page.tsx line ~233 contains `grid grid-cols-3 sm:grid-cols-5 gap-1.5`
    - No other lines in the file were changed
    - `npm test -- --testPathPattern="lights" --passWithNoTests --no-coverage` passes
  </acceptance_criteria>
  <done>Lights stats grid uses tighter gap at mobile, color presets show 3 columns at mobile / 5 at desktop.</done>
</task>

<task type="auto">
  <name>Task 2: Fix network page tab nav and SystemInfoCard grid for 375px</name>
  <files>app/network/page.tsx, app/network/components/SystemInfoCard.tsx</files>
  <read_first>
    - app/network/page.tsx (lines 210-225 for tab navigation bar)
    - app/network/components/SystemInfoCard.tsx (lines 60-75 for grid layout)
  </read_first>
  <action>
    **Fix 1 — Tab navigation bar (network/page.tsx line 215):**
    Change from:
    ```
    <div className="flex gap-1 border-b border-white/[0.06] pb-0">
    ```
    to:
    ```
    <div className="flex flex-wrap gap-1 border-b border-white/[0.06] pb-0">
    ```
    Adds `flex-wrap` so tabs wrap to a second row at 375px rather than overflowing. The 4 tab labels ("Dispositivi", "WiFi Clients", "Servizi di Rete", "Reti WiFi") total ~44+ chars with padding — likely to overflow at 375px. Per D-01: minimal targeted fix.

    **Fix 2 — SystemInfoCard grid (SystemInfoCard.tsx line 67):**
    Change from:
    ```
    <div className="grid grid-cols-3 gap-3">
    ```
    to:
    ```
    <div className="grid grid-cols-1 sm:grid-cols-3 gap-3">
    ```
    Stacks the 3 InfoBox items (Modello, Firmware, Uptime) vertically at 375px, 3-column at desktop. Per D-01: minimal fix following mobile-first convention.

    **No changes needed** for:
    - WanStatusCard (`grid-cols-2`) — 2 columns fit at 375px (~163px each)
    - BudgetStatsCard (`grid-cols-2`) — same
    - DeviceListTable, WifiClientsTable — DataTable has overflow-x-auto
    - Charts — Recharts components use responsive containers
  </action>
  <verify>
    <automated>grep -n "flex-wrap" app/network/page.tsx && grep -n "grid-cols-1 sm:grid-cols-3" app/network/components/SystemInfoCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/network/page.tsx tab nav div contains `flex flex-wrap gap-1 border-b`
    - app/network/components/SystemInfoCard.tsx contains `grid grid-cols-1 sm:grid-cols-3 gap-3`
    - No other lines in either file were changed
    - `npm test -- --testPathPattern="network" --passWithNoTests --no-coverage` passes
  </acceptance_criteria>
  <done>Network tab nav wraps at narrow viewports, SystemInfoCard stacks on mobile. All network content accessible at 375px.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of lights and network pages at 375px</name>
  <what-built>
    Responsive grid fixes on /lights (stats gap, color presets columns) and /network (tab nav wrap, SystemInfoCard stack). Lights scenes, automation, and network tables were confirmed mobile-safe.
  </what-built>
  <how-to-verify>
    Open each page in browser at 375px width and confirm:

    1. **Lights (/lights)** — Stats summary shows 3 columns with tighter gap, color presets show 3 columns, all controls tappable
    2. **Lights scenes (/lights/scenes)** — Scene grid shows 2 columns at mobile, no overflow
    3. **Lights automation (/lights/automation)** — Automation cards stack in single column, no overflow
    4. **Network (/network)** — Tab bar wraps to second row if needed, SystemInfoCard stacks vertically, tables scroll horizontally, charts fit within viewport

    For each page: no horizontal page scroll, no clipped text or buttons, all controls tappable.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 pages look correct, or describe which page(s) have issues</resume-signal>
</task>

</tasks>

<verification>
All 4 pages (/lights, /lights/scenes, /lights/automation, /network) render without horizontal overflow at 375px viewport width.
</verification>

<success_criteria>
- AUDIT-04: All lights pages fully operable on touch at 375px
- AUDIT-05: Network page displays charts and tables without overflow at 375px
</success_criteria>

<output>
After completion, create `.planning/phases/152-pages-audit-core-device-pages/152-02-SUMMARY.md`
</output>
