---
phase: 153-pages-audit-extended-device-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/devices/sonos/components/SonosSleepTimer.tsx
  - app/sonos/page.tsx
autonomous: true
requirements:
  - AUDIT-06
  - AUDIT-07
  - AUDIT-08
  - AUDIT-09

must_haves:
  truths:
    - "The /sonos page renders without horizontal overflow at 375px viewport"
    - "The /dirigera page renders without horizontal overflow at 375px viewport"
    - "The /raspi page renders without horizontal overflow at 375px viewport"
    - "The /tuya page renders without horizontal overflow at 375px viewport"
  artifacts:
    - path: "app/components/devices/sonos/components/SonosSleepTimer.tsx"
      provides: "Preset buttons row with flex-wrap for mobile safety"
      contains: "flex-wrap"
  key_links:
    - from: "app/sonos/page.tsx"
      to: "SonosSleepTimer"
      via: "component import through SonosZoneSection"
      pattern: "flex-wrap"
---

<objective>
Audit and fix the Sonos, DIRIGERA, Raspi, and Tuya pages at 375px viewport width.

Purpose: Ensure all extended device pages are fully usable on mobile without horizontal overflow or clipped controls.
Output: Responsive CSS fixes applied to any components that overflow at 375px. Most pages are expected to be already mobile-safe based on code inspection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-pages-audit-extended-device-pages/153-CONTEXT.md
@.planning/phases/153-pages-audit-extended-device-pages/153-RESEARCH.md

<interfaces>
<!-- Key responsive patterns from Phase 152 that this plan follows -->

Fix toolkit (from Phase 152):
- flex-wrap on multi-item flex rows: `flex flex-wrap items-center gap-2`
- overflow-x-auto on tables: `<div className="overflow-x-auto"><table>...</table></div>`
- Responsive grid: `grid-cols-1 sm:grid-cols-N`
- Gap reduction: `gap-3 sm:gap-6`

Width math at 375px:
- Usable width: 343px (375 - 32px px-4 padding)
- Inside card p-5: 303px
- grid-cols-2 gap-3: 165px per cell
- grid-cols-3 gap-3: 106px per cell
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix Sonos sub-components at 375px</name>
  <files>app/components/devices/sonos/components/SonosSleepTimer.tsx</files>
  <read_first>
    - app/components/devices/sonos/components/SonosSleepTimer.tsx
    - app/components/devices/sonos/components/SonosEqControls.tsx
    - app/components/devices/sonos/components/SonosHomeTheater.tsx
    - app/components/devices/sonos/components/SonosSpeakerVolume.tsx
    - app/components/devices/sonos/components/SonosHistoryChart.tsx
    - app/components/devices/sonos/components/SonosTransportControls.tsx
    - app/components/devices/sonos/components/SonosQueueViewer.tsx
    - app/components/devices/sonos/components/SonosNowPlaying.tsx
    - app/components/devices/sonos/components/SonosZoneSection.tsx
    - app/components/devices/sonos/components/SonosGroupControls.tsx
    - app/components/devices/sonos/components/SonosPlayModeControls.tsx
    - app/components/devices/sonos/components/SonosSourceSwitch.tsx
    - app/components/devices/sonos/components/SonosVolumeChart.tsx
    - app/components/devices/sonos/components/SonosSeekControl.tsx
    - app/sonos/page.tsx
    - app/dirigera/page.tsx
    - app/components/devices/dirigera/components/DirigeraHealthSection.tsx
    - app/components/devices/dirigera/components/DirigeraSensorList.tsx
    - app/components/devices/dirigera/components/DirigeraStats.tsx
    - app/raspi/page.tsx
    - app/raspi/components/RaspiSystemInfo.tsx
    - app/raspi/components/RaspiMemoryDisk.tsx
    - app/raspi/components/RaspiCpuTemp.tsx
    - app/raspi/components/RaspiNetworkIO.tsx
    - app/tuya/page.tsx
  </read_first>
  <action>
    Audit all 14 Sonos sub-components, all DIRIGERA components, all Raspi components, and Tuya page at 375px viewport width. Per D-01, apply minimal targeted fixes only. Per D-04, drill into every sub-component.

    **Sonos — Expected fix (1 file):**
    In `SonosSleepTimer.tsx`, find the preset buttons row at approximately line 47:
    ```tsx
    <div className="flex items-center gap-1">
    ```
    Add `flex-wrap` preemptively per research recommendation (5 buttons may overflow on some font sizes):
    ```tsx
    <div className="flex flex-wrap items-center gap-1">
    ```

    **Sonos — Components expected to be already safe (verify by reading code, NO changes):**
    - SonosHistoryChart: already has `overflow-x-auto` wrapper at line 154
    - SonosHomeTheater: already has `flex flex-wrap gap-2` on toggle buttons at line 98
    - SonosZoneSection: already has `flex flex-col sm:flex-row` at line 96
    - SonosTransportControls: 4 icon buttons at ~136px total — fits in 303px
    - SonosSpeakerVolume: min-w-[100px] + controls = ~169px fixed + flex-1 slider — fits in 303px
    - SonosEqControls: w-14 label + flex-1 slider + min-w-[28px] value — bounded, fits
    - SonosNowPlaying: uses `truncate` on title/artist — safe
    - SonosQueueViewer: max-w-[120px] truncate on artist — intentional
    - SonosSourceSwitch: 2 buttons inline — safe
    - SonosGroupControls: select dropdown — safe
    - SonosPlayModeControls: small icon buttons — safe
    - SonosVolumeChart: ResponsiveContainer width="100%" — safe
    - SonosSeekControl: single slider — safe

    **DIRIGERA — Expected: 0 changes.**
    - DirigeraHealthSection: already has `flex flex-wrap gap-6`
    - DirigeraSensorList/DirigeraSensorRow: `flex items-center justify-between gap-4` with `min-w-0` — mobile-safe by construction
    - DirigeraStats: `grid-cols-2 gap-3` = 165px per cell — safe
    - dirigera/page.tsx filter buttons: 3 `flex-1` buttons at ~114px each — safe

    **Raspi — Expected: 0 changes. Per D-05 and D-06, keep grid-cols-2 and grid-cols-3 as-is.**
    - RaspiCpuTemp: grid-cols-2 gap-3 = 145px per InfoBox — safe
    - RaspiMemoryDisk: grid-cols-2 gap-3 = 145px per InfoBox — safe
    - RaspiSystemInfo: grid-cols-2 + grid-cols-3 gap-3 = 145px / 96px per cell — tight but numeric "0.00" fits
    - RaspiNetworkIO: grid-cols-2 gap-3 = 145px per InfoBox — safe

    **Tuya — Expected: 0 changes. Per D-11, already responsive.**
    - tuya/page.tsx: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` — already mobile-first

    After reading all files and applying the SonosSleepTimer fix, run Playwright MCP at 375x812 to verify all 4 pages:
    - /sonos: `document.body.scrollWidth <= window.innerWidth` must be true
    - /dirigera: same check
    - /raspi: same check
    - /tuya: same check
    Take screenshots for visual confirmation per D-15 and D-17.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npx playwright test tests/e2e/ --grep "sonos|dirigera|raspi|tuya" 2>/dev/null || echo "Use Playwright MCP: navigate to /sonos, /dirigera, /raspi, /tuya at 375x812 and evaluate document.body.scrollWidth <= window.innerWidth on each"</automated>
  </verify>
  <acceptance_criteria>
    - SonosSleepTimer.tsx contains `flex flex-wrap items-center gap-1` on the preset buttons row
    - No other Sonos sub-component files are modified (all confirmed safe by code inspection)
    - No DIRIGERA files are modified
    - No Raspi files are modified
    - No Tuya files are modified
    - Playwright MCP scrollWidth check passes on /sonos at 375px
    - Playwright MCP scrollWidth check passes on /dirigera at 375px
    - Playwright MCP scrollWidth check passes on /raspi at 375px
    - Playwright MCP scrollWidth check passes on /tuya at 375px
  </acceptance_criteria>
  <done>All 4 extended device pages (/sonos, /dirigera, /raspi, /tuya) pass the overflow check at 375px viewport with visual screenshot confirmation</done>
</task>

</tasks>

<verification>
- Playwright MCP at 375x812 on /sonos: `document.body.scrollWidth <= window.innerWidth` returns true
- Playwright MCP at 375x812 on /dirigera: same check passes
- Playwright MCP at 375x812 on /raspi: same check passes
- Playwright MCP at 375x812 on /tuya: same check passes
- Visual screenshots show no clipped controls or unreadable text on any page
</verification>

<success_criteria>
All four extended device pages render fully usable on a 375px mobile viewport with no horizontal overflow, clipped controls, or layout breakage.
</success_criteria>

<output>
After completion, create `.planning/phases/153-pages-audit-extended-device-pages/153-01-SUMMARY.md`
</output>
