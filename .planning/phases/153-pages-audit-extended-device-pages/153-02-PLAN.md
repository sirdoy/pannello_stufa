---
phase: 153-pages-audit-extended-device-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/rooms/page.tsx
autonomous: true
requirements:
  - AUDIT-10

must_haves:
  truths:
    - "The /rooms page renders without horizontal overflow at 375px viewport"
    - "The /rooms/status page renders without horizontal overflow at 375px viewport"
    - "The /rooms/[id] page renders without horizontal overflow at 375px viewport"
  artifacts:
    - path: "app/rooms/page.tsx"
      provides: "Health stats row with flex-wrap for mobile safety"
      contains: "flex-wrap"
  key_links:
    - from: "app/rooms/page.tsx"
      to: "DataTable"
      via: "component composition"
      pattern: "overflow-x-auto"
---

<objective>
Audit and fix all Rooms pages (/rooms, /rooms/status, /rooms/[id]) at 375px viewport width.

Purpose: Ensure the rooms management pages are fully usable on mobile without horizontal overflow.
Output: Responsive CSS fix on the /rooms health stats row. Other rooms pages are expected to be already mobile-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-pages-audit-extended-device-pages/153-CONTEXT.md
@.planning/phases/153-pages-audit-extended-device-pages/153-RESEARCH.md

<interfaces>
<!-- Key responsive patterns from Phase 152 -->

Fix toolkit:
- flex-wrap on multi-item flex rows: `flex flex-wrap items-center gap-4 sm:gap-6`
- Gap reduction: `gap-4 sm:gap-6` (keep readability on mobile, restore spacing on desktop)

Width math at 375px:
- Usable width: 343px (375 - 32px px-4 padding)
- flex items-center gap-6 with 3+ spans at text-sm: easily exceeds 343px
- flex-wrap allows items to flow to next line when width is exceeded

From app/rooms/page.tsx line 220:
```tsx
<div className="flex items-center gap-6 text-sm text-slate-400 mb-4">
```

From app/rooms/status/page.tsx line 149 (already safe):
```tsx
<div className="flex flex-wrap items-center gap-4 sm:gap-6 ...">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rooms/page.tsx health stats row and verify all Rooms pages at 375px</name>
  <files>app/rooms/page.tsx</files>
  <read_first>
    - app/rooms/page.tsx
    - app/rooms/status/page.tsx
    - app/rooms/[room_id]/page.tsx
  </read_first>
  <action>
    Audit all three Rooms pages at 375px viewport width. Per D-01, apply minimal targeted fixes only.

    **rooms/page.tsx — 1 fix required:**
    At line 220, the health stats row uses `flex items-center gap-6` with 3 span elements (e.g., "Stanze: 3", "Dispositivi: 12", "Orfani: 2"). At 375px with text-sm and gap-6 (24px gaps), this row overflows.

    Change line 220 from:
    ```tsx
    <div className="flex items-center gap-6 text-sm text-slate-400 mb-4">
    ```
    To:
    ```tsx
    <div className="flex flex-wrap items-center gap-4 sm:gap-6 text-sm text-slate-400 mb-4">
    ```

    This adds `flex-wrap` for mobile line-wrapping and reduces base gap from 6 to 4 (restoring gap-6 at sm: breakpoint). This matches the pattern already used in `/rooms/status/page.tsx` line 149.

    **rooms/page.tsx — Other elements confirmed safe:**
    - Line 235: `flex items-center justify-end gap-2` toolbar with 2 buttons — safe (small total width)
    - Line 267: DataTable already has `overflow-x-auto` wrapper — safe

    **rooms/status/page.tsx — Expected: 0 changes.**
    - Line 149: health row already has `flex flex-wrap items-center gap-4 sm:gap-6` — already mobile-safe
    - Line 182: toolbar with 1 button — safe
    - Line 211: `grid-cols-1 sm:grid-cols-2` — already responsive
    - Line 215: room card header already has `flex flex-wrap items-center gap-2` — safe

    **rooms/[room_id]/page.tsx — Expected: 0 changes.**
    - Line 223: toolbar with 1 button — safe
    - Line 253: DataTable already has `overflow-x-auto` — safe
    - No grid patterns or fixed-width elements found (per D-14)

    After applying the fix, run Playwright MCP at 375x812 to verify all 3 rooms pages:
    - /rooms: `document.body.scrollWidth <= window.innerWidth` must be true
    - /rooms/status: same check
    - /rooms/[id] (use any valid room_id or first room): same check
    Take screenshots for visual confirmation per D-15 and D-17.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npx playwright test tests/e2e/ --grep "rooms" 2>/dev/null || echo "Use Playwright MCP: navigate to /rooms, /rooms/status at 375x812 and evaluate document.body.scrollWidth <= window.innerWidth on each"</automated>
  </verify>
  <acceptance_criteria>
    - app/rooms/page.tsx line 220 contains `flex flex-wrap items-center gap-4 sm:gap-6 text-sm text-slate-400 mb-4`
    - app/rooms/status/page.tsx is NOT modified (already mobile-safe)
    - app/rooms/[room_id]/page.tsx is NOT modified (already mobile-safe)
    - Playwright MCP scrollWidth check passes on /rooms at 375px
    - Playwright MCP scrollWidth check passes on /rooms/status at 375px
  </acceptance_criteria>
  <done>All 3 Rooms pages (/rooms, /rooms/status, /rooms/[id]) pass the overflow check at 375px viewport with visual screenshot confirmation</done>
</task>

</tasks>

<verification>
- Playwright MCP at 375x812 on /rooms: `document.body.scrollWidth <= window.innerWidth` returns true
- Playwright MCP at 375x812 on /rooms/status: same check passes
- Playwright MCP at 375x812 on /rooms/[id]: same check passes (if accessible without specific data)
- Visual screenshots show health stats wrapping gracefully and DataTable scrolling horizontally within its container
</verification>

<success_criteria>
All three Rooms pages render fully usable on a 375px mobile viewport with no horizontal overflow. The health stats row on /rooms wraps to a second line when needed instead of overflowing.
</success_criteria>

<output>
After completion, create `.planning/phases/153-pages-audit-extended-device-pages/153-02-SUMMARY.md`
</output>
