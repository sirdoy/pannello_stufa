---
phase: 154-pages-audit-admin-support-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/registry/devices/page.tsx
  - app/settings/notifications/devices/page.tsx
autonomous: true
requirements:
  - AUDIT-11
  - AUDIT-12

must_haves:
  truths:
    - "Registry devices page renders without horizontal viewport overflow at 375px"
    - "Registry types page renders without horizontal viewport overflow at 375px"
    - "All 8 settings pages render without horizontal overflow at 375px"
  artifacts:
    - path: "app/registry/devices/page.tsx"
      provides: "Registry devices page with mobile-safe toolbar"
      contains: "flex-wrap"
    - path: "app/registry/types/page.tsx"
      provides: "Registry types page (likely unchanged)"
  key_links:
    - from: "app/registry/devices/page.tsx"
      to: "DataTable"
      via: "overflow-x-auto already present in DataTable component"
      pattern: "overflow-x-auto"
---

<objective>
Audit and fix all registry pages (/registry/devices, /registry/types) and settings pages (/settings and all 7 sub-pages) at 375px mobile viewport.

Purpose: Ensure AUDIT-11 and AUDIT-12 requirements are met — admin pages usable on mobile.
Output: Pages verified at 375px with minimal targeted CSS fixes applied where needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-pages-audit-admin-support-pages/154-CONTEXT.md
@.planning/phases/154-pages-audit-admin-support-pages/154-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix registry pages at 375px</name>
  <files>app/registry/devices/page.tsx, app/registry/types/page.tsx</files>
  <read_first>
    - app/registry/devices/page.tsx (full file — check toolbar at line ~532, pagination at line ~567)
    - app/registry/types/page.tsx (full file — check card header at line ~193)
  </read_first>
  <action>
**Registry Devices page** (app/registry/devices/page.tsx):
1. Use Playwright MCP to screenshot `/registry/devices` at 375x812 viewport
2. Check `document.body.scrollWidth <= window.innerWidth` for horizontal overflow
3. The toolbar at line ~532 has `flex items-center justify-between mb-4` containing a Select + Button. Per D-01 and the Phase 152-01 pattern: if screenshot shows overflow or crowding, change to `flex flex-wrap items-center gap-3 mb-4` (removing `justify-between`). If it fits fine at 375px, leave unchanged.
4. DataTable already has `overflow-x-auto` (confirmed at DataTable.tsx:496) — no changes needed for table content
5. Check pagination row at line ~567 — `flex items-center justify-between` with short text + 2 buttons should be fine

**Registry Types page** (app/registry/types/page.tsx):
1. Use Playwright MCP to screenshot `/registry/types` at 375x812 viewport
2. Check `document.body.scrollWidth <= window.innerWidth` for horizontal overflow
3. Card header at line ~193 has `flex items-center justify-between mb-4` with Heading + "Crea tipo" button — likely fine (short labels), but fix with flex-wrap if overflow detected

Per D-01: Only add responsive Tailwind classes. Do NOT restructure layouts.
Per D-02: base = 375px+, sm: = 640px+.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -n "flex-wrap\|flex items-center" app/registry/devices/page.tsx app/registry/types/page.tsx | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Playwright screenshot of /registry/devices at 375x812 shows no horizontal overflow
    - Playwright screenshot of /registry/types at 375x812 shows no horizontal overflow
    - document.body.scrollWidth <= window.innerWidth returns true for both pages
    - If toolbar was fixed: `app/registry/devices/page.tsx` contains `flex flex-wrap items-center gap-3` replacing the old `flex items-center justify-between` on the toolbar line
    - No layout restructuring — only Tailwind class additions per D-01
  </acceptance_criteria>
  <done>Both registry pages render correctly at 375px with no horizontal viewport overflow. DataTable scrolls horizontally within its container (not the viewport).</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix settings pages at 375px</name>
  <files>app/settings/page.tsx, app/settings/dashboard/page.tsx, app/settings/devices/page.tsx, app/settings/location/page.tsx, app/settings/notifications/page.tsx, app/settings/notifications/devices/page.tsx, app/settings/notifications/history/page.tsx, app/settings/thermostat/page.tsx</files>
  <read_first>
    - app/settings/page.tsx (check line ~345 device rows, line ~409 action buttons)
    - app/settings/notifications/devices/page.tsx (unknown layout — must read before touching)
    - app/settings/notifications/history/page.tsx (check max-w-[200px] truncate per D-13)
  </read_first>
  <action>
Audit all 8 settings pages at 375px using Playwright MCP screenshots. Per RESEARCH findings, most settings pages are already mobile-safe:

**Pages expected to be safe (verify with screenshot only):**
1. `/settings` — Tabs with 2 items, device rows use `flex items-center justify-between gap-2 sm:gap-4`, action buttons use `flex flex-col sm:flex-row` — already responsive
2. `/settings/notifications` — grids are `grid-cols-1 sm:grid-cols-2` — already responsive
3. `/settings/notifications/history` — DataTable with `overflow-x-auto`, `max-w-[200px] truncate` on title column — fine per D-13
4. `/settings/dashboard` — form-based layout, naturally responsive via block flow
5. `/settings/devices` — form-based layout, naturally responsive
6. `/settings/location` — form-based layout, naturally responsive
7. `/settings/thermostat` — form-based layout, naturally responsive

**Page requiring closer inspection:**
8. `/settings/notifications/devices` — not yet scanned. Read full file, screenshot at 375x812, apply fixes if overflow detected.

For each page:
1. Navigate to the page at 375x812 viewport using Playwright MCP
2. Take screenshot
3. Run `document.body.scrollWidth <= window.innerWidth` check
4. If overflow detected, apply minimal fix following established patterns:
   - Grid overflow: change to `grid-cols-1 sm:grid-cols-N`
   - Flex row overflow: add `flex-wrap` and replace `justify-between` with `gap-3`
   - Table overflow: add `overflow-x-auto` wrapper (DataTable already has this)

Per D-01: minimal targeted fixes only.
Per D-02: base = mobile (375px+), sm: = desktop (640px+).
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -rn "grid-cols-1\|flex-wrap\|overflow-x-auto" app/settings/ | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Playwright screenshots taken for all 8 settings pages at 375x812
    - document.body.scrollWidth <= window.innerWidth returns true for all 8 pages
    - No horizontal viewport overflow on any settings page
    - If any fix was applied: only Tailwind class additions, no layout restructuring per D-01
    - app/settings/notifications/page.tsx still contains `grid-cols-1 sm:grid-cols-2` (unchanged, already responsive)
  </acceptance_criteria>
  <done>All 8 settings pages render correctly at 375px mobile viewport with no horizontal overflow. Forms, tables, and grids are fully usable.</done>
</task>

</tasks>

<verification>
- All registry and settings pages pass Playwright 375x812 viewport check
- No page has `document.body.scrollWidth > window.innerWidth`
- Only minimal Tailwind class changes applied (no layout restructuring)
</verification>

<success_criteria>
- AUDIT-11: /registry/devices and /registry/types verified at 375px with no horizontal overflow
- AUDIT-12: All 8 settings sub-pages verified at 375px with no horizontal overflow
- Screenshots taken as evidence for each page
</success_criteria>

<output>
After completion, create `.planning/phases/154-pages-audit-admin-support-pages/154-01-SUMMARY.md`
</output>
