---
phase: 154-pages-audit-admin-support-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/offline/page.tsx
  - app/debug/logs/page.tsx
autonomous: true
requirements:
  - AUDIT-13
  - AUDIT-14
  - AUDIT-15

must_haves:
  truths:
    - "Debug pages render without horizontal viewport overflow at 375px"
    - "Offline page temperature grid stacks on mobile (grid-cols-1) and shows 3 columns on desktop"
    - "Changelog and log pages render without horizontal overflow at 375px"
    - "Camera pages marked N/A — directory does not exist"
  artifacts:
    - path: "app/offline/page.tsx"
      provides: "Offline page with responsive temperature grid"
      contains: "grid-cols-1 sm:grid-cols-3"
    - path: "app/debug/logs/page.tsx"
      provides: "Debug logs page (fix header if needed)"
  key_links:
    - from: "app/offline/page.tsx"
      to: "stove temperature grid"
      via: "grid-cols-1 sm:grid-cols-3 replacing grid-cols-3"
      pattern: "grid-cols-1 sm:grid-cols-3"
---

<objective>
Audit and fix all debug pages (/debug and sub-pages), camera pages (N/A), and remaining pages (changelog, offline, log) at 375px mobile viewport.

Purpose: Ensure AUDIT-13, AUDIT-14, and AUDIT-15 requirements are met — debug and support pages usable on mobile.
Output: Pages verified at 375px with minimal targeted CSS fixes applied where needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-pages-audit-admin-support-pages/154-CONTEXT.md
@.planning/phases/154-pages-audit-admin-support-pages/154-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix debug pages at 375px</name>
  <files>app/debug/page.tsx, app/debug/api/page.tsx, app/debug/logs/page.tsx, app/debug/notifications/page.tsx, app/debug/notifications/test/page.tsx, app/debug/transitions/page.tsx, app/debug/weather-test/page.tsx</files>
  <read_first>
    - app/debug/logs/page.tsx (check header at line ~76 for flex justify-between, category selector at line ~104, back-links grid at line ~187)
    - app/debug/notifications/test/page.tsx (check delivery trace grid at line ~379 for grid-cols-2)
    - app/debug/page.tsx (verify Tabs.List overflow="scroll" at line ~381)
  </read_first>
  <action>
Audit 7 debug pages at 375px. Per D-15, EXCLUDE `/debug/design-system` (developer showcase, already audited Phase 151). Per D-16, include `/debug/transitions` and `/debug/weather-test` with lower priority.

**Pages expected to be safe (verify with screenshot only):**
1. `/debug` — Tabs.List has `overflow="scroll"` (line ~381), NotificheContent grid is `grid-cols-1 md:grid-cols-3`, header flex already has `flex-wrap gap-4` — all confirmed safe per RESEARCH
2. `/debug/api` — header is `flex flex-col md:flex-row md:items-center md:justify-between gap-4` — already responsive
3. `/debug/notifications` — grids are `grid-cols-1 md:grid-cols-3` and `grid-cols-1 md:grid-cols-2` — already mobile-safe
4. `/debug/notifications/test` — delivery trace grid at line ~379 is `grid-cols-2 gap-3` for 4 small stat cells (Sent at, Target devices, Delivered, Failed). At 375px = ~165px per cell — acceptable per Phase 153 stat grid precedent. Verify with screenshot.
5. `/debug/transitions` — status grid `grid-cols-2 gap-4` for 2 cells is fine. Transition cards default to 1 column at base — safe.
6. `/debug/weather-test` — single column layout `max-w-2xl mx-auto p-4 space-y-4` — safe.

**Page requiring closer inspection:**
7. `/debug/logs` — three risk areas:
   a. Header (line ~76): `flex items-center justify-between mb-6` with heading div + buttons row. If the 2 buttons ("Refresh" + "Auto (5s)") cause overflow at 375px, change to `flex flex-wrap items-center gap-3 mb-6` (removing `justify-between`)
   b. Category selector (line ~104): `flex gap-2 mb-6` with 3 category buttons. If overflow, add `flex-wrap`
   c. Back-links grid (line ~187): `grid-cols-2 md:grid-cols-4 gap-3` — 4 navigation buttons in 2 cols, ~165px each — likely fine

For each page:
1. Navigate at 375x812 viewport using Playwright MCP
2. Take screenshot
3. Run `document.body.scrollWidth <= window.innerWidth` check
4. Fix only confirmed overflows using established patterns

Per D-01: minimal targeted fixes only.
Per D-02: base = mobile (375px+), sm: = desktop (640px+).
AUDIT-14 (camera): Per D-04, `app/camera/` directory does NOT exist. Mark AUDIT-14 as N/A in the SUMMARY — no screenshots or fixes needed.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -n "flex-wrap\|overflow.*scroll\|grid-cols-1" app/debug/page.tsx app/debug/logs/page.tsx app/debug/api/page.tsx | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Playwright screenshots taken for all 7 debug pages at 375x812 (NOT /debug/design-system per D-15)
    - document.body.scrollWidth <= window.innerWidth returns true for all 7 pages
    - app/debug/page.tsx line ~381 still has Tabs.List with overflow="scroll" (unchanged)
    - If debug/logs header was fixed: contains `flex flex-wrap items-center gap-3` replacing `flex items-center justify-between`
    - AUDIT-14 marked N/A — no app/camera/ directory exists (verify with `ls app/camera/ 2>/dev/null` returning empty)
  </acceptance_criteria>
  <done>All 7 debug pages render correctly at 375px. Camera pages confirmed N/A (AUDIT-14). No horizontal viewport overflow on any debug page.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix remaining pages (offline, changelog, log) at 375px</name>
  <files>app/offline/page.tsx, app/changelog/page.tsx, app/log/page.tsx</files>
  <read_first>
    - app/offline/page.tsx (CONFIRMED FIX at line 213: grid-cols-3; also check line 295: grid-cols-2)
    - app/changelog/page.tsx (check pagination min-w-[40px] at line ~274, footer legend at line ~315)
    - app/log/page.tsx (check flex-wrap patterns)
  </read_first>
  <action>
**Offline page** (app/offline/page.tsx) — CONFIRMED FIX per D-05:
1. Line 213: Change `grid grid-cols-3 gap-4` to `grid grid-cols-1 sm:grid-cols-3 gap-4`. This is the stove temperature grid with 3 cells (Ambiente, Setpoint, Scarico) — at 375px with px-4 padding = 343px usable, 3 columns of ~107px each will overflow with padding.
2. Line 295: `grid grid-cols-2 gap-4` for thermostat temperatures (Current + Target). Per D-06 and Phase 153 stat grid precedent, 2 columns of ~165px each in 343px usable space is acceptable. Verify with screenshot — if content overflows, change to `grid-cols-1 sm:grid-cols-2`, but likely leave unchanged.
3. Take Playwright screenshot at 375x812 after fix
4. Verify `document.body.scrollWidth <= window.innerWidth`

**Changelog page** (app/changelog/page.tsx):
1. Screenshot at 375x812
2. Pagination `min-w-[40px]` buttons are fine per D-14
3. Footer legend `flex flex-col sm:flex-row` is already responsive per RESEARCH
4. Expect no fixes needed — verify only

**Log page** (app/log/page.tsx):
1. Screenshot at 375x812
2. Device filter `flex flex-wrap gap-2` is already responsive per RESEARCH
3. LogEntry items are single-column card list — safe
4. Expect no fixes needed — verify only

Per D-01: minimal targeted fixes only.
Per D-02: base = mobile (375px+), sm: = desktop (640px+).
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -n "grid-cols-1 sm:grid-cols-3\|grid-cols-2\|grid-cols-3" app/offline/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/offline/page.tsx line 213 contains `grid grid-cols-1 sm:grid-cols-3 gap-4` (was `grid grid-cols-3 gap-4`)
    - app/offline/page.tsx line 295 still contains `grid-cols-2` (unchanged — acceptable for 2 stat cells)
    - Playwright screenshots taken for /offline, /changelog, /log at 375x812
    - document.body.scrollWidth <= window.innerWidth returns true for all 3 pages
    - No layout restructuring — only Tailwind class addition on offline line 213
  </acceptance_criteria>
  <done>Offline page temperature grid is responsive (1-col mobile, 3-col desktop). Changelog and log pages confirmed mobile-safe. All 3 remaining pages pass 375px viewport check.</done>
</task>

</tasks>

<verification>
- All debug and remaining pages pass Playwright 375x812 viewport check
- No page has `document.body.scrollWidth > window.innerWidth`
- Offline page grid-cols-3 fixed to grid-cols-1 sm:grid-cols-3
- Camera pages (AUDIT-14) documented as N/A
- Only minimal Tailwind class changes applied (no layout restructuring)
</verification>

<success_criteria>
- AUDIT-13: All 7 debug pages verified at 375px with no horizontal overflow
- AUDIT-14: Camera pages confirmed N/A (directory does not exist) per D-04
- AUDIT-15: Offline page fixed (grid-cols-1 sm:grid-cols-3), changelog and log pages verified at 375px
- Screenshots taken as evidence for each page
</success_criteria>

<output>
After completion, create `.planning/phases/154-pages-audit-admin-support-pages/154-02-SUMMARY.md`
</output>
