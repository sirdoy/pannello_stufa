---
phase: 154-pages-audit-admin-support-pages
plan: 03
type: gap-closure
wave: 2
depends_on: [02]
files_modified:
  - app/(pages)/camera/CameraDashboard.tsx
  - app/(pages)/camera/events/CameraEventsPage.tsx
autonomous: true
requirements:
  - AUDIT-14

must_haves:
  truths:
    - "Camera dashboard (/camera) renders without horizontal viewport overflow at 375px"
    - "Camera events page (/camera/events) renders without horizontal viewport overflow at 375px"
    - "AUDIT-14 is satisfied — both camera pages audited and verified at 375px mobile viewport"
  artifacts:
    - path: "app/(pages)/camera/CameraDashboard.tsx"
      provides: "Camera dashboard page verified at 375px"
      contains: "grid grid-cols-2 gap-3 (stat cells) — confirmed acceptable or fixed"
    - path: "app/(pages)/camera/events/CameraEventsPage.tsx"
      provides: "Camera events page verified at 375px"
      contains: "flex items-center gap-4 with flex-shrink-0 thumbnail — confirmed acceptable or fixed"
  key_links:
    - from: "app/(pages)/camera/CameraDashboard.tsx"
      to: "/camera route"
      via: "Next.js route group (pages)"
      pattern: "app/(pages)/camera/page.tsx re-exports CameraDashboard"
    - from: "app/(pages)/camera/events/CameraEventsPage.tsx"
      to: "/camera/events route"
      via: "Next.js route group (pages)"
      pattern: "app/(pages)/camera/events/page.tsx re-exports CameraEventsPage"

gap_source: "154-VERIFICATION.md"
gap_reason: "Plan 02 checked app/camera/ (flat path) but camera pages live at app/(pages)/camera/ (Next.js route group). AUDIT-14 was incorrectly declared N/A. Neither /camera nor /camera/events was audited at 375px."
---

<objective>
Audit camera pages (/camera, /camera/events) at 375px mobile viewport — closing the AUDIT-14 gap identified in verification.

Purpose: Camera pages exist at app/(pages)/camera/ (Next.js route group) but were missed because Plan 02 checked the non-existent flat path app/camera/. This gap closure audits both pages and applies minimal responsive fixes if needed.
Output: Both camera pages verified at 375px with Playwright screenshots and scrollWidth checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-pages-audit-admin-support-pages/154-CONTEXT.md
@.planning/phases/154-pages-audit-admin-support-pages/154-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit /camera page at 375px</name>
  <files>app/(pages)/camera/CameraDashboard.tsx, app/(pages)/camera/page.tsx</files>
  <read_first>app/(pages)/camera/CameraDashboard.tsx, app/(pages)/camera/page.tsx</read_first>
  <steps>
    1. Read CameraDashboard.tsx fully — identify all layout patterns (grids, flex rows, fixed widths)
    2. Navigate to /camera at 375x812 viewport using Playwright MCP
    3. Run scrollWidth check: `document.body.scrollWidth <= window.innerWidth`
    4. Take screenshot for visual verification
    5. If scrollWidth > 375: identify the overflow source and apply minimal responsive fix following Phase 151-153 patterns (grid-cols-1 at base, overflow-x-auto, flex-wrap, etc.)
    6. If scrollWidth === 375: confirm page passes — no changes needed
  </steps>
  <done_when>
    - /camera at 375x812 has scrollWidth === 375 (no horizontal overflow)
    - Screenshot taken showing page renders correctly on mobile
    - Any fixes applied follow established responsive patterns (minimal changes only)
  </done_when>
</task>

<task type="auto">
  <name>Task 2: Audit /camera/events page at 375px</name>
  <files>app/(pages)/camera/events/CameraEventsPage.tsx, app/(pages)/camera/events/page.tsx</files>
  <read_first>app/(pages)/camera/events/CameraEventsPage.tsx, app/(pages)/camera/events/page.tsx</read_first>
  <steps>
    1. Read CameraEventsPage.tsx fully — identify all layout patterns (flex rows, fixed-width thumbnails, grids)
    2. Navigate to /camera/events at 375x812 viewport using Playwright MCP
    3. Run scrollWidth check: `document.body.scrollWidth <= window.innerWidth`
    4. Take screenshot for visual verification
    5. If scrollWidth > 375: identify the overflow source and apply minimal responsive fix
    6. If scrollWidth === 375: confirm page passes — no changes needed
    7. Key areas to check:
       - `w-32 h-20` thumbnail (128px) in flex row with gap-4 — verify remaining space for text
       - Camera filter row uses `overflow-x-auto pb-2` — should be safe
  </steps>
  <done_when>
    - /camera/events at 375x812 has scrollWidth === 375 (no horizontal overflow)
    - Screenshot taken showing page renders correctly on mobile
    - Any fixes applied follow established responsive patterns
  </done_when>
</task>

<task type="auto">
  <name>Task 3: Update AUDIT-14 status</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <steps>
    1. AUDIT-14 is already marked [x] Complete in REQUIREMENTS.md — verify the checkbox remains checked
    2. This task confirms AUDIT-14 is genuinely complete (not just incorrectly marked)
    3. No file changes needed if audit passes — the requirement was marked complete prematurely but is now actually satisfied
  </steps>
  <done_when>
    - AUDIT-14 status accurately reflects reality (both camera pages audited and verified)
  </done_when>
</task>

</tasks>

<constraints>
- Apply ONLY minimal responsive CSS fixes — do NOT restructure pages or rewrite components
- Follow Phase 151-153 conventions: base = mobile (375px+), sm: = desktop (640px+)
- For data-heavy content, prefer overflow-x-auto over column hiding
- Do NOT modify functionality — CSS/layout changes only
- Use Playwright at 375x812 for verification (same as all prior audit phases)
</constraints>
