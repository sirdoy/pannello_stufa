---
phase: 156-path-migration-common-endpoints
plan: 02
type: execute
wave: 2
depends_on: ["156-01"]
files_modified:
  - lib/commands/deviceCommands.tsx
  - app/debug/api/components/tabs/StoveTab.tsx
  - app/debug/components/tabs/StoveTab.tsx
  - app/sw.ts
  - lib/hooks/useRetryableCommand.ts
  - types/api/responses.ts
  - __tests__/components/devices/stove/hooks/useStoveData.test.ts
  - __tests__/components/devices/stove/hooks/useStoveCommands.test.ts
  - lib/retry/__tests__/idempotencyManager.test.ts
  - lib/hooks/__tests__/useRetryableCommand.test.ts
autonomous: true
requirements:
  - PATH-02

must_haves:
  truths:
    - "No frontend file references /api/stove/ (excluding lib/version.ts changelog)"
    - "Service worker caches and fetches from /api/v1/thermorossi/status"
    - "Debug panels show canonical /api/v1/thermorossi/* URLs"
    - "All stove-related tests pass with new URL paths"
  artifacts:
    - path: "lib/commands/deviceCommands.tsx"
      provides: "Command palette stove actions with correct API paths"
      contains: "/api/v1/thermorossi/"
    - path: "app/sw.ts"
      provides: "Service worker with updated stove status path"
      contains: "/api/v1/thermorossi/status"
  key_links:
    - from: "lib/commands/deviceCommands.tsx"
      to: "/api/v1/thermorossi/*"
      via: "fetch() calls"
      pattern: "/api/v1/thermorossi/"
    - from: "app/sw.ts"
      to: "/api/v1/thermorossi/status"
      via: "pathname match and fetch"
      pattern: "/api/v1/thermorossi/status"
---

<objective>
Update all frontend references from `/api/stove/*` to canonical `/api/v1/thermorossi/*` paths: hooks, commands, debug panels, service worker, and tests.

Purpose: Complete the path migration so no runtime code references the deleted `/api/stove/` routes (PATH-02).
Output: All frontend and test files updated, zero references to old paths, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-path-migration-common-endpoints/156-CONTEXT.md
@.planning/phases/156-path-migration-common-endpoints/156-RESEARCH.md
@.planning/phases/156-path-migration-common-endpoints/156-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all frontend source files from /api/stove/* to /api/v1/thermorossi/*</name>
  <files>
    lib/commands/deviceCommands.tsx
    app/debug/api/components/tabs/StoveTab.tsx
    app/debug/components/tabs/StoveTab.tsx
    app/sw.ts
    lib/hooks/useRetryableCommand.ts
    types/api/responses.ts
  </files>
  <read_first>
    lib/commands/deviceCommands.tsx
    app/debug/api/components/tabs/StoveTab.tsx
    app/debug/components/tabs/StoveTab.tsx
    app/sw.ts
    lib/hooks/useRetryableCommand.ts
    types/api/responses.ts
  </read_first>
  <action>
    Update all `/api/stove/` references to canonical `/api/v1/thermorossi/` paths in 6 files. Per D-03, the mapping is NOT a simple string replacement — old path names differ from new canonical names.

    **1. `lib/commands/deviceCommands.tsx`** — This file is a bug-fix opportunity (Pitfall 3 from RESEARCH). The `executeStoveAction` helper always POSTs, but `power` and `fan-level` reads are GETs. Apply these changes:

    a. Replace the `executeStoveAction` function: change the fetch URL template from `` `/api/stove/${endpoint}` `` to use the full path passed as argument (since new paths have varying depths like `/api/v1/thermorossi/commands/ignit`).

    Rewrite `executeStoveAction` to accept a full path instead of a segment:
    ```typescript
    async function executeStoveAction(path: string, method: string = 'POST', body: Record<string, unknown> = {}): Promise<unknown> {
      try {
        const response = await fetch(path, {
          method,
          headers: { 'Content-Type': 'application/json' },
          ...(method !== 'GET' && { body: JSON.stringify({ ...body, source: 'command_palette' }) }),
        });
        const data = await response.json();
        if (data.error) {
          console.error(`[CommandPalette] Stove ${path} error:`, data.error);
        }
        return data;
      } catch (err) {
        console.error(`[CommandPalette] Stove ${path} failed:`, err);
      }
    }
    ```

    b. Update `getStoveCommands()` calls:
    - `executeStoveAction('ignite')` → `executeStoveAction('/api/v1/thermorossi/commands/ignit')`
    - `executeStoveAction('shutdown')` → `executeStoveAction('/api/v1/thermorossi/commands/shutdown')`
    - `fetch('/api/stove/get-power')` → `fetch('/api/v1/thermorossi/power')`
    - `executeStoveAction('set-power', { level: ... })` → `executeStoveAction('/api/v1/thermorossi/settings/power', 'POST', { value: ... })` (NOTE: the body key is `value` not `level`, matching the route handler's `body['value']`)
    - `fetch('/api/stove/get-fan')` → `fetch('/api/v1/thermorossi/fan-level')`
    - `executeStoveAction('set-fan', { level: ... })` → `executeStoveAction('/api/v1/thermorossi/settings/fan-level', 'POST', { value: ... })` (same: `value` not `level`)

    **2. `app/debug/api/components/tabs/StoveTab.tsx`** — Replace all URL strings:
    - `/api/stove/health` → `/api/v1/thermorossi/health`
    - `/api/stove/status` → `/api/v1/thermorossi/status`
    - `/api/stove/getPower` → `/api/v1/thermorossi/power`
    - `/api/stove/getFan` → `/api/v1/thermorossi/fan-level`
    - `/api/stove/history` → `/api/v1/thermorossi/history`
    - `/api/stove/ignite` → `/api/v1/thermorossi/commands/ignit`
    - `/api/stove/shutdown` → `/api/v1/thermorossi/commands/shutdown`
    - `/api/stove/setPower` → `/api/v1/thermorossi/settings/power`
    - `/api/stove/setFan` → `/api/v1/thermorossi/settings/fan-level`
    - `/api/stove/setWaterTemperature` → `/api/v1/thermorossi/settings/temperature/water`

    **3. `app/debug/components/tabs/StoveTab.tsx`** — Same replacements as #2 above.

    **4. `app/sw.ts`** — Per D-02, update BOTH occurrences (Pitfall 4):
    - Line ~597: `url.pathname === '/api/stove/status'` → `url.pathname === '/api/v1/thermorossi/status'`
    - Line ~718: `fetch('/api/stove/status')` → `fetch('/api/v1/thermorossi/status')`

    **5. `lib/hooks/useRetryableCommand.ts`** — JSDoc comment only:
    - `/api/stove/ignite` → `/api/v1/thermorossi/commands/ignit`

    **6. `types/api/responses.ts`** — JSDoc comment only:
    - Any `/api/stove/` reference → corresponding `/api/v1/thermorossi/` path

    Do NOT modify `lib/version.ts` — it contains historical changelog strings.
  </action>
  <verify>
    <automated>grep -r '/api/stove/' lib/commands/deviceCommands.tsx app/debug/ app/sw.ts lib/hooks/useRetryableCommand.ts types/api/responses.ts 2>/dev/null | wc -l | xargs test 0 -eq && echo "ZERO OLD REFS IN SOURCE"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '/api/v1/thermorossi/' lib/commands/deviceCommands.tsx` returns at least 6
    - `grep -c '/api/stove/' lib/commands/deviceCommands.tsx` returns 0
    - `grep -c '/api/v1/thermorossi/status' app/sw.ts` returns 2
    - `grep -c '/api/stove/' app/sw.ts` returns 0
    - `grep -c '/api/v1/thermorossi/' app/debug/api/components/tabs/StoveTab.tsx` returns at least 10
    - `grep -c '/api/stove/' app/debug/api/components/tabs/StoveTab.tsx` returns 0
    - `grep -c '/api/v1/thermorossi/' app/debug/components/tabs/StoveTab.tsx` returns at least 10
    - `grep -c '/api/stove/' app/debug/components/tabs/StoveTab.tsx` returns 0
    - `lib/commands/deviceCommands.tsx` contains `/api/v1/thermorossi/commands/ignit` (no trailing `e`)
    - `lib/commands/deviceCommands.tsx` uses `{ value:` not `{ level:` for power/fan settings body
  </acceptance_criteria>
  <done>All 6 frontend source files reference canonical /api/v1/thermorossi/* paths with zero references to old /api/stove/ paths</done>
</task>

<task type="auto">
  <name>Task 2: Update test files and run full verification grep</name>
  <files>
    __tests__/components/devices/stove/hooks/useStoveData.test.ts
    __tests__/components/devices/stove/hooks/useStoveCommands.test.ts
    lib/retry/__tests__/idempotencyManager.test.ts
    lib/hooks/__tests__/useRetryableCommand.test.ts
  </files>
  <read_first>
    __tests__/components/devices/stove/hooks/useStoveData.test.ts
    __tests__/components/devices/stove/hooks/useStoveCommands.test.ts
    lib/retry/__tests__/idempotencyManager.test.ts
    lib/hooks/__tests__/useRetryableCommand.test.ts
  </read_first>
  <action>
    Update all test files with canonical paths, then run verification.

    **1. `__tests__/components/devices/stove/hooks/useStoveData.test.ts`:**
    - Line ~141: `expect.stringContaining('/api/stove/status')` → `expect.stringContaining('/api/v1/thermorossi/status')`
    - Search for any other `/api/stove/` strings in this file and update them

    **2. `__tests__/components/devices/stove/hooks/useStoveCommands.test.ts`:**
    - Update ALL `/api/stove/` strings to canonical paths using D-03 mapping:
      - `/api/stove/ignite` → `/api/v1/thermorossi/commands/ignit` (no trailing `e`)
      - `/api/stove/shutdown` → `/api/v1/thermorossi/commands/shutdown`
      - `/api/stove/setPower` → `/api/v1/thermorossi/settings/power`
      - `/api/stove/setFan` → `/api/v1/thermorossi/settings/fan-level`
      - `/api/stove/setWaterTemperature` → `/api/v1/thermorossi/settings/temperature/water`
    - Search the whole file for ALL occurrences (expect.stringContaining, mockFetch assertions, etc.)

    **3. `lib/retry/__tests__/idempotencyManager.test.ts`:**
    - Lines ~25-71: Replace test key strings:
      - `/api/stove/ignite` → `/api/v1/thermorossi/commands/ignit`
      - `/api/stove/shutdown` → `/api/v1/thermorossi/commands/shutdown`
    - These are just key strings for idempotency tests — semantically any string works, but update for consistency

    **4. `lib/hooks/__tests__/useRetryableCommand.test.ts`:**
    - Line ~74: JSDoc example `/api/stove/ignite` → `/api/v1/thermorossi/commands/ignit`
    - Check for any other `/api/stove/` references

    **5. Final verification sweep:**
    Run `grep -r '/api/stove/' --include='*.ts' --include='*.tsx' . | grep -v 'node_modules' | grep -v '.next' | grep -v 'lib/version.ts'` — this MUST return ZERO results.

    **6. Run tests:**
    Run `npm test -- --testPathPattern="useStoveData|useStoveCommands|idempotencyManager|useRetryableCommand" --no-coverage` to confirm all updated tests pass.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="useStoveData|useStoveCommands|idempotencyManager|useRetryableCommand" --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '/api/stove/' __tests__/components/devices/stove/hooks/useStoveData.test.ts` returns 0
    - `grep -c '/api/stove/' __tests__/components/devices/stove/hooks/useStoveCommands.test.ts` returns 0
    - `grep -c '/api/stove/' lib/retry/__tests__/idempotencyManager.test.ts` returns 0
    - `grep -c '/api/stove/' lib/hooks/__tests__/useRetryableCommand.test.ts` returns 0
    - `grep -c '/api/v1/thermorossi/commands/ignit' __tests__/components/devices/stove/hooks/useStoveCommands.test.ts` returns at least 1
    - `grep -r '/api/stove/' --include='*.ts' --include='*.tsx' . | grep -v node_modules | grep -v .next | grep -v lib/version.ts | wc -l` returns 0
    - `npm test -- --testPathPattern="useStoveData|useStoveCommands|idempotencyManager|useRetryableCommand" --no-coverage` exits 0
  </acceptance_criteria>
  <done>All test files use canonical paths, zero /api/stove/ references remain in codebase (excluding historical changelog), all stove-related tests pass</done>
</task>

</tasks>

<verification>
1. `grep -r '/api/stove/' --include='*.ts' --include='*.tsx' . | grep -v node_modules | grep -v .next | grep -v lib/version.ts | wc -l` returns 0
2. `npm test -- --testPathPattern="stove|idempotency|useRetryableCommand" --no-coverage` all pass
3. `grep -c '/api/v1/thermorossi/' lib/commands/deviceCommands.tsx` returns at least 6
4. `grep -c '/api/v1/thermorossi/status' app/sw.ts` returns 2
</verification>

<success_criteria>
- Zero references to /api/stove/ in any .ts/.tsx file (excluding lib/version.ts changelog)
- All stove-related tests pass with new URL paths
- Service worker caches /api/v1/thermorossi/status
- Debug panels show canonical URLs
- Command palette uses correct new paths with proper HTTP methods and body keys
</success_criteria>

<output>
After completion, create `.planning/phases/156-path-migration-common-endpoints/156-02-SUMMARY.md`
</output>
