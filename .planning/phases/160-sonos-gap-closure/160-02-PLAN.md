---
phase: 160-sonos-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/v1/sonos/zones/[groupId]/volume/route.ts
  - app/api/v1/sonos/zones/[groupId]/volume/__tests__/route.test.ts
  - app/api/v1/sonos/zones/[groupId]/seek/route.ts
  - app/api/v1/sonos/zones/[groupId]/seek/__tests__/route.test.ts
  - app/api/v1/sonos/zones/[groupId]/queue/route.ts
  - app/api/v1/sonos/zones/[groupId]/queue/__tests__/route.test.ts
  - app/api/v1/sonos/zones/[groupId]/play-mode/route.ts
  - app/api/v1/sonos/zones/[groupId]/play-mode/__tests__/route.test.ts
  - app/api/v1/sonos/zones/[groupId]/sleep-timer/route.ts
  - app/api/v1/sonos/zones/[groupId]/sleep-timer/__tests__/route.test.ts
autonomous: true
requirements:
  - SONOS-07
  - SONOS-08
  - SONOS-09
  - SONOS-10
  - SONOS-11
  - SONOS-12
  - SONOS-13

must_haves:
  truths:
    - "PUT /api/v1/sonos/zones/{groupId}/volume returns 202 Accepted"
    - "PUT /api/v1/sonos/zones/{groupId}/seek returns 202 Accepted"
    - "GET /api/v1/sonos/zones/{groupId}/queue returns 200 with queue data and supports limit/offset query params"
    - "GET /api/v1/sonos/zones/{groupId}/play-mode returns 200 with play mode"
    - "PUT /api/v1/sonos/zones/{groupId}/play-mode returns 202 Accepted"
    - "GET /api/v1/sonos/zones/{groupId}/sleep-timer returns 200 with timer state"
    - "PUT /api/v1/sonos/zones/{groupId}/sleep-timer returns 202 Accepted"
    - "All 5 routes return 401 when unauthenticated"
  artifacts:
    - path: "app/api/v1/sonos/zones/[groupId]/volume/route.ts"
      provides: "PUT volume control"
      exports: ["PUT"]
    - path: "app/api/v1/sonos/zones/[groupId]/seek/route.ts"
      provides: "PUT seek position"
      exports: ["PUT"]
    - path: "app/api/v1/sonos/zones/[groupId]/queue/route.ts"
      provides: "GET queue listing"
      exports: ["GET"]
    - path: "app/api/v1/sonos/zones/[groupId]/play-mode/route.ts"
      provides: "GET + PUT play mode"
      exports: ["GET", "PUT"]
    - path: "app/api/v1/sonos/zones/[groupId]/sleep-timer/route.ts"
      provides: "GET + PUT sleep timer"
      exports: ["GET", "PUT"]
  key_links:
    - from: "app/api/v1/sonos/zones/[groupId]/volume/route.ts"
      to: "@/lib/sonos/sonosProxy"
      via: "import { setZoneVolume }"
      pattern: "import.*setZoneVolume.*from.*sonosProxy"
    - from: "app/api/v1/sonos/zones/[groupId]/queue/route.ts"
      to: "@/lib/sonos/sonosProxy"
      via: "import { getQueue }"
      pattern: "import.*getQueue.*from.*sonosProxy"
---

<objective>
Create v1 Sonos zone routes for volume (PUT), seek (PUT), queue (GET), play-mode (GET+PUT), and sleep-timer (GET+PUT). Each route is a thin wrapper around existing proxy functions per D-01 and D-02. Volume and seek extract specific body fields; queue passes query params; play-mode and sleep-timer are dual-export routes.

Purpose: API alignment — canonical v1 paths for Sonos zone-level settings and state endpoints per D-03.
Output: 5 route files + 5 test files under `app/api/v1/sonos/zones/[groupId]/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-sonos-gap-closure/160-CONTEXT.md
@.planning/phases/160-sonos-gap-closure/160-RESEARCH.md

<interfaces>
<!-- Existing proxy functions and types — executor must NOT create new proxy code or types -->

From lib/sonos/sonosProxy.ts:
- setZoneVolume(groupId: string, volume: number): Promise<SonosCommandResponse>
- seek(groupId: string, position: string): Promise<SonosCommandResponse>
- getQueue(groupId: string, limit?: string, offset?: string): Promise<SonosQueueResponse>
- getPlayMode(groupId: string): Promise<SonosPlayModeResponse>
- setPlayMode(groupId: string, body: SetPlayModeRequest): Promise<SonosCommandResponse>
- getSleepTimer(groupId: string): Promise<SonosSleepTimerResponse>
- setSleepTimer(groupId: string, body: SetSleepTimerRequest): Promise<SonosCommandResponse>

From types/sonosProxy.ts:
- SetVolumeRequest: { volume: number }
- SetSeekRequest: { position: string }
- SetPlayModeRequest: { mode: SonosPlayMode }
- SetSleepTimerRequest: { duration: number }

From lib/core:
- withAuthAndErrorHandler, success, getPathParam, parseJson, HTTP_STATUS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create volume, seek, and queue routes + tests (SONOS-07, SONOS-08, SONOS-11)</name>
  <files>
    app/api/v1/sonos/zones/[groupId]/volume/route.ts
    app/api/v1/sonos/zones/[groupId]/volume/__tests__/route.test.ts
    app/api/v1/sonos/zones/[groupId]/seek/route.ts
    app/api/v1/sonos/zones/[groupId]/seek/__tests__/route.test.ts
    app/api/v1/sonos/zones/[groupId]/queue/route.ts
    app/api/v1/sonos/zones/[groupId]/queue/__tests__/route.test.ts
  </files>
  <read_first>
    app/api/sonos/zones/[groupId]/volume/route.ts
    app/api/sonos/zones/[groupId]/seek/route.ts
    app/api/sonos/zones/[groupId]/queue/route.ts
    app/api/v1/hue/lights/[lightId]/__tests__/route.test.ts
  </read_first>
  <action>
Create 3 route files under `app/api/v1/sonos/zones/[groupId]/`. Each is an exact copy of its old-path counterpart with updated JSDoc path.

**volume/route.ts** (per D-03, SONOS-07):
1. `import { withAuthAndErrorHandler, success, getPathParam, parseJson, HTTP_STATUS } from '@/lib/core'`
2. `import { setZoneVolume } from '@/lib/sonos/sonosProxy'`
3. `import type { SetVolumeRequest } from '@/types/sonosProxy'`
4. `export const dynamic = 'force-dynamic'`
5. `export const PUT = withAuthAndErrorHandler(async (request, context) => { ... }, 'Sonos/Zones/SetVolume')`
6. Inside: `const groupId = await getPathParam(context, 'groupId')`, `const body = await parseJson(request) as SetVolumeRequest`, `const data = await setZoneVolume(groupId, body.volume)`, return 202 with `suggested_poll_delay_s: 1`

**seek/route.ts** (per D-03, SONOS-08):
1. Same imports as volume but `import { seek } from '@/lib/sonos/sonosProxy'` and `import type { SetSeekRequest } from '@/types/sonosProxy'`
2. `export const PUT` with tag `'Sonos/Zones/Seek'`
3. Inside: extract `body.position` (string, not the full body object), call `seek(groupId, body.position)`, return 202

**queue/route.ts** (per D-03, SONOS-11):
1. `import { withAuthAndErrorHandler, success, getPathParam } from '@/lib/core'`
2. `import { getQueue } from '@/lib/sonos/sonosProxy'`
3. `export const GET` with tag `'Sonos/Zones/Queue'`
4. Inside: `const { searchParams } = request.nextUrl`, `const limit = searchParams.get('limit')`, `const offset = searchParams.get('offset')`, `const data = await getQueue(groupId, limit ?? undefined, offset ?? undefined)` — use `?? undefined` to convert null to undefined (pitfall 2 from RESEARCH.md), return 200

**Tests** — for each, create `__tests__/route.test.ts`:

All tests use: `jest.mock('@/lib/sonos/sonosProxy')` and `jest.mock('@/lib/auth0', ...)` BEFORE imports, `mockContext = { params: Promise.resolve({ groupId: 'RINCON_123' }) }`.

**volume test**: import `PUT`, mock `setZoneVolume`, test 401 unauthenticated, test 202 with body `{ volume: 50 }` — assert `mockSetZoneVolume.toHaveBeenCalledWith('RINCON_123', 50)`, `data.suggested_poll_delay_s` is 1. For the PUT test, create request with: `new Request('http://localhost:3000/...', { method: 'PUT', body: JSON.stringify({ volume: 50 }), headers: { 'Content-Type': 'application/json' } })`.

**seek test**: import `PUT`, mock `seek`, test 401, test 202 with body `{ position: '0:02:30' }` — assert `mockSeek.toHaveBeenCalledWith('RINCON_123', '0:02:30')`.

**queue test**: import `GET`, mock `getQueue`, test 401, test 200 with queue data `{ items: [], total: 0 }`, test that limit/offset query params are passed — create request with URL `?limit=10&offset=5` and assert `mockGetQueue.toHaveBeenCalledWith('RINCON_123', '10', '5')`. Also test without query params: assert `mockGetQueue.toHaveBeenCalledWith('RINCON_123', undefined, undefined)`.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npm test -- --testPathPattern="app/api/v1/sonos/zones/\\[groupId\\]/(volume|seek|queue)/" --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - app/api/v1/sonos/zones/[groupId]/volume/route.ts contains `export const PUT`
    - app/api/v1/sonos/zones/[groupId]/volume/route.ts contains `body.volume`
    - app/api/v1/sonos/zones/[groupId]/volume/route.ts contains `SetVolumeRequest`
    - app/api/v1/sonos/zones/[groupId]/seek/route.ts contains `export const PUT`
    - app/api/v1/sonos/zones/[groupId]/seek/route.ts contains `body.position`
    - app/api/v1/sonos/zones/[groupId]/seek/route.ts contains `SetSeekRequest`
    - app/api/v1/sonos/zones/[groupId]/queue/route.ts contains `export const GET`
    - app/api/v1/sonos/zones/[groupId]/queue/route.ts contains `searchParams.get('limit')`
    - app/api/v1/sonos/zones/[groupId]/queue/route.ts contains `?? undefined`
    - All 3 test files contain `expect(response.status).toBe(401)`
    - volume and seek test files contain `expect(response.status).toBe(202)`
    - queue test file contains `expect(response.status).toBe(200)`
    - All tests pass with 0 failures
  </acceptance_criteria>
  <done>Volume PUT, seek PUT, and queue GET routes exist at v1 paths. Volume extracts body.volume, seek extracts body.position, queue passes limit/offset query params with null-to-undefined coercion. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create play-mode and sleep-timer dual-export routes + tests (SONOS-09, SONOS-10, SONOS-12, SONOS-13)</name>
  <files>
    app/api/v1/sonos/zones/[groupId]/play-mode/route.ts
    app/api/v1/sonos/zones/[groupId]/play-mode/__tests__/route.test.ts
    app/api/v1/sonos/zones/[groupId]/sleep-timer/route.ts
    app/api/v1/sonos/zones/[groupId]/sleep-timer/__tests__/route.test.ts
  </files>
  <read_first>
    app/api/sonos/zones/[groupId]/play-mode/route.ts
    app/api/sonos/zones/[groupId]/sleep-timer/route.ts
    app/api/v1/hue/lights/[lightId]/__tests__/route.test.ts
  </read_first>
  <action>
Create 2 dual-export route files (GET + PUT in same file) under `app/api/v1/sonos/zones/[groupId]/`. Each is an exact copy of its old-path counterpart with updated JSDoc path. Per D-06, response shapes are identical.

**play-mode/route.ts** (per D-03, SONOS-09/SONOS-10):
1. `import { withAuthAndErrorHandler, success, getPathParam, parseJson, HTTP_STATUS } from '@/lib/core'`
2. `import { getPlayMode, setPlayMode } from '@/lib/sonos/sonosProxy'`
3. `import type { SetPlayModeRequest } from '@/types/sonosProxy'`
4. `export const dynamic = 'force-dynamic'`
5. `export const GET = withAuthAndErrorHandler(async (_request, context) => { ... }, 'Sonos/Zones/PlayMode/Get')` — calls `getPlayMode(groupId)`, returns 200
6. `export const PUT = withAuthAndErrorHandler(async (request, context) => { ... }, 'Sonos/Zones/PlayMode/Set')` — parses body as `SetPlayModeRequest`, calls `setPlayMode(groupId, body)` (passes full body object, NOT a field), returns 202 with `suggested_poll_delay_s: 1`

**sleep-timer/route.ts** (per D-03, SONOS-12/SONOS-13):
1. Same pattern as play-mode but imports `getSleepTimer, setSleepTimer` and `SetSleepTimerRequest`
2. GET tag: `'Sonos/Zones/SleepTimer/Get'`, PUT tag: `'Sonos/Zones/SleepTimer/Set'`
3. GET calls `getSleepTimer(groupId)`, returns 200
4. PUT parses body as `SetSleepTimerRequest`, calls `setSleepTimer(groupId, body)` (full body), returns 202

**Tests** — for each, create `__tests__/route.test.ts` (pitfall 5: test BOTH GET and PUT):

All tests use standard mock setup. Import BOTH `GET` and `PUT` from `../route`.

**play-mode test**:
- `describe('GET /api/v1/sonos/zones/[groupId]/play-mode')`: test 401, test 200 with mock data `{ mode: 'NORMAL' }`, assert `mockGetPlayMode.toHaveBeenCalledWith('RINCON_123')`
- `describe('PUT /api/v1/sonos/zones/[groupId]/play-mode')`: test 401, test 202 with body `{ mode: 'SHUFFLE' }`, assert `mockSetPlayMode.toHaveBeenCalledWith('RINCON_123', { mode: 'SHUFFLE' })`, assert `data.suggested_poll_delay_s` is 1. Create request with PUT method and JSON body.

**sleep-timer test**:
- `describe('GET /api/v1/sonos/zones/[groupId]/sleep-timer')`: test 401, test 200 with mock data `{ remaining_seconds: 300 }`, assert `mockGetSleepTimer.toHaveBeenCalledWith('RINCON_123')`
- `describe('PUT /api/v1/sonos/zones/[groupId]/sleep-timer')`: test 401, test 202 with body `{ duration: 600 }`, assert `mockSetSleepTimer.toHaveBeenCalledWith('RINCON_123', { duration: 600 })`, assert `data.suggested_poll_delay_s` is 1.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npm test -- --testPathPattern="app/api/v1/sonos/zones/\\[groupId\\]/(play-mode|sleep-timer)/" --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - app/api/v1/sonos/zones/[groupId]/play-mode/route.ts contains `export const GET`
    - app/api/v1/sonos/zones/[groupId]/play-mode/route.ts contains `export const PUT`
    - app/api/v1/sonos/zones/[groupId]/play-mode/route.ts contains `setPlayMode(groupId, body)`
    - app/api/v1/sonos/zones/[groupId]/sleep-timer/route.ts contains `export const GET`
    - app/api/v1/sonos/zones/[groupId]/sleep-timer/route.ts contains `export const PUT`
    - app/api/v1/sonos/zones/[groupId]/sleep-timer/route.ts contains `setSleepTimer(groupId, body)`
    - play-mode test file contains `describe('GET`
    - play-mode test file contains `describe('PUT`
    - sleep-timer test file contains `describe('GET`
    - sleep-timer test file contains `describe('PUT`
    - All tests pass with 0 failures
  </acceptance_criteria>
  <done>Play-mode and sleep-timer dual-export routes exist at v1 paths. Each exports both GET (200) and PUT (202). Both pass full body object to proxy. All tests cover both methods and pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API route | Unauthenticated requests must be rejected with 401 |
| client -> PUT body | Volume, seek, play-mode, sleep-timer accept JSON body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-04 | S (Spoofing) | All 5 route handlers | mitigate | `withAuthAndErrorHandler` rejects null sessions with 401 |
| T-160-05 | T (Tampering) | PUT body fields | accept | TypeScript type casting on parsed body; proxy validates upstream; single-user app |
| T-160-06 | I (Information Disclosure) | Error messages | mitigate | `withAuthAndErrorHandler` returns generic errors, no stack traces |
| T-160-07 | D (Denial of Service) | Queue endpoint limit/offset | accept | Proxy enforces limits upstream; no local pagination logic |
</threat_model>

<verification>
All 5 routes respond correctly:
- Volume PUT: 202 with body.volume extraction
- Seek PUT: 202 with body.position extraction
- Queue GET: 200 with limit/offset query param passthrough
- Play-mode GET: 200, PUT: 202 with full body passthrough
- Sleep-timer GET: 200, PUT: 202 with full body passthrough
- All reject with 401 when unauthenticated

```bash
npm test -- --testPathPattern="app/api/v1/sonos/zones" --no-coverage
```
</verification>

<success_criteria>
- 5 new route.ts files exist under app/api/v1/sonos/zones/[groupId]/
- 5 new test files exist with 100% pass rate
- Dual-export routes (play-mode, sleep-timer) export both GET and PUT
- Queue route uses `?? undefined` for null-to-undefined coercion on query params
- Volume extracts body.volume, seek extracts body.position, play-mode and sleep-timer pass full body
- No changes to old-path routes or frontend hooks (per D-01 and D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/160-sonos-gap-closure/160-02-SUMMARY.md`
</output>
