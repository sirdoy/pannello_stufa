---
phase: 166-hue-frontend-cutover
plan: 03
type: execute
wave: 3
depends_on: [166-02]
files_modified:
  - app/api/hue/ (entire directory deleted)
autonomous: true
requirements: [HUE-01, HUE-02, HUE-03, HUE-04, HUE-05, HUE-06, HUE-07]

must_haves:
  truths:
    - "app/api/hue/ directory no longer exists"
    - "Zero /api/hue/ string references remain anywhere in the codebase (excluding planning docs)"
    - "All hue-related tests still pass after legacy deletion"
  artifacts:
    - path: "app/api/hue/"
      provides: "DELETED — legacy route tree removed"
  key_links:
    - from: "app/components/devices/lights/hooks/useLightsData.ts"
      to: "app/api/v1/hue/lights/route.ts"
      via: "fetch('/api/v1/hue/lights')"
      pattern: "fetch\\('/api/v1/hue/lights'\\)"
---

<objective>
Delete the entire legacy `app/api/hue/` directory tree per D-11. This is the final cleanup step after all frontend consumers have been migrated to v1 paths.

Purpose: Remove dead code. After Plan 02, the legacy routes have zero consumers. Keeping them creates confusion and maintenance burden.
Output: `app/api/hue/` deleted, repo-wide grep confirms zero legacy references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-hue-frontend-cutover/166-CONTEXT.md
@.planning/phases/166-hue-frontend-cutover/166-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify zero legacy consumers then delete app/api/hue/ tree (per D-11)</name>
  <files>app/api/hue/ (entire directory tree deleted)</files>
  <read_first>
    - (none — this task only deletes files, but must run grep verification first)
  </read_first>
  <action>
**Step 1: Pre-deletion verification** — Run grep to confirm zero `/api/hue/` references outside the legacy tree:

```bash
grep -r "/api/hue" app/ --include="*.ts" --include="*.tsx" | grep -v "app/api/hue"
```

Expected: zero matches. If ANY matches are found, STOP and fix them before proceeding.

**Step 2: Delete the legacy route tree** per D-11:

```bash
rm -rf app/api/hue/
```

This removes the entire directory including:
- `app/api/hue/status/route.ts`
- `app/api/hue/lights/route.ts`, `app/api/hue/lights/[id]/route.ts`
- `app/api/hue/rooms/route.ts`, `app/api/hue/rooms/[id]/route.ts`
- `app/api/hue/scenes/route.ts`
- `app/api/hue/groups/[groupId]/scenes/[sceneId]/route.ts`
- `app/api/hue/history/route.ts`
- All associated `__tests__/` directories

**Step 3: Post-deletion verification** — Run grep again across the ENTIRE codebase:

```bash
grep -rn "/api/hue" app/ lib/ types/ --include="*.ts" --include="*.tsx"
```

Expected: zero matches. Any remaining reference is a bug.

**Step 4: Run full hue test suite** to confirm nothing depends on deleted files:

```bash
npm test -- --testPathPattern="hue" --passWithNoTests
```

All tests must pass. Deleted test files will naturally be excluded.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && test ! -d app/api/hue && echo "DELETED" || echo "STILL EXISTS" ; grep -rn "/api/hue" app/ lib/ types/ --include="*.ts" --include="*.tsx" | head -5 ; echo "GREP_EXIT: $?" && npm test -- --testPathPattern="hue" --passWithNoTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/hue/` directory does not exist (`test ! -d app/api/hue` returns 0)
    - `grep -rn "/api/hue" app/ lib/ types/ --include="*.ts" --include="*.tsx"` returns zero matches
    - `npm test -- --testPathPattern="hue" --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>Legacy app/api/hue/ tree deleted, zero /api/hue/ references remain in codebase, all hue tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Deletion removes code, does not add new boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-06 | Denial of Service | Legacy route deletion | mitigate | Pre-deletion grep confirms zero consumers; post-deletion grep + test suite confirms no breakage |
</threat_model>

<verification>
```bash
# Directory gone
test ! -d app/api/hue && echo "OK: deleted"

# Zero legacy references
grep -rn "/api/hue" app/ lib/ types/ --include="*.ts" --include="*.tsx" | wc -l
# Expected: 0

# Full hue test suite green
npm test -- --testPathPattern="hue" --passWithNoTests
```
</verification>

<success_criteria>
- `app/api/hue/` directory does not exist
- Zero `/api/hue/` string references in `app/`, `lib/`, `types/`
- All hue-related tests pass
- Phase 166 complete: production Hue UI consumes /api/v1/hue/* exclusively
</success_criteria>

<output>
After completion, create `.planning/phases/166-hue-frontend-cutover/166-03-SUMMARY.md`
</output>
