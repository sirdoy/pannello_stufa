---
phase: 168-netatmo-frontend-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/debug/api/components/tabs/NetatmoTab.tsx
  - app/debug/components/tabs/NetatmoTab.tsx
autonomous: true
requirements: [NETA-01, NETA-02, NETA-03, NETA-04, NETA-05, NETA-06, NETA-07, NETA-08, NETA-09]

must_haves:
  truths:
    - "Both debug NetatmoTab files (app/debug/api/components/tabs/NetatmoTab.tsx and app/debug/components/tabs/NetatmoTab.tsx) reference only /api/v1/netatmo/* URLs"
    - "Zero /api/netatmo/ string occurrences remain in either debug NetatmoTab file"
    - "The `schedules` GET tile (url=/api/netatmo/schedules) is entirely removed from both files, and the sixth fetchGetEndpoint('schedules', ...) call in fetchAllGetEndpoints is removed"
    - "The three param-required camera tiles (camera/snapshot, camera/stream, camera/monitoring) are NOT added — they were never present in the current files"
    - "The `calibrate` POST tile URL is rewritten from /api/netatmo/calibrate to /api/v1/netatmo/valves/calibrate (semantic-equivalent mapping per D-04)"
    - "Every remaining tile has all 4 URL references (url=, onRefresh callback arg, onCopyUrl callback arg, isCopied comparison) rewritten in lockstep"
    - "Both files continue to compile (tsc noEmit equivalent via jest runs clean)"
  artifacts:
    - path: "app/debug/api/components/tabs/NetatmoTab.tsx"
      provides: "Debug panel tab pointing to v1 Netatmo URLs (schedules tile removed, calibrate relabelled to valves/calibrate)"
      contains: "/api/v1/netatmo/health"
    - path: "app/debug/components/tabs/NetatmoTab.tsx"
      provides: "Near-duplicate twin of the /debug/api/ tab — identical v1 URLs"
      contains: "/api/v1/netatmo/health"
  key_links:
    - from: "app/debug/api/components/tabs/NetatmoTab.tsx"
      to: "/api/v1/netatmo/valves/calibrate"
      via: "calibrate tile url= + callPostEndpoint URL (D-04 semantic mapping)"
      pattern: "/api/v1/netatmo/valves/calibrate"
    - from: "app/debug/components/tabs/NetatmoTab.tsx"
      to: "/api/v1/netatmo/valves/calibrate"
      via: "calibrate tile url= + callPostEndpoint URL (D-04 semantic mapping)"
      pattern: "/api/v1/netatmo/valves/calibrate"
---

<objective>
Rewrite both debug NetatmoTab files (near-duplicate twins at `app/debug/api/components/tabs/NetatmoTab.tsx` and `app/debug/components/tabs/NetatmoTab.tsx`) to point exclusively at `/api/v1/netatmo/*` URLs. Remove the `schedules` tile entirely (no v1 equivalent per D-04 — schedules are embedded in `homesdata` response). Rewrite the `calibrate` POST tile URL from `/api/netatmo/calibrate` to `/api/v1/netatmo/valves/calibrate` (semantic-equivalent mapping per D-04). Every tile's 4 URL refs (`url=`, `onRefresh` arg, `onCopyUrl` arg, `isCopied` comparison) must be updated in lockstep to prevent copy-button / refresh-button drift.

Purpose: After Wave 3 deletes the legacy `app/api/netatmo/` tree, debug tabs still pointing at legacy URLs would 404. Migrating debug surfaces in lockstep with production is the established discipline from Phase 112 (Hue) and Phase 166/167 cutovers (CONTEXT D-09).
Output: Two files updated, 42 URL occurrences per file eliminated, 37 v1 URL occurrences per file introduced (net -4 per tile drop + +1 from loader-call drop), zero `/api/netatmo/` refs remain in either file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-netatmo-frontend-cutover/168-CONTEXT.md
@.planning/phases/168-netatmo-frontend-cutover/168-RESEARCH.md
@.planning/phases/168-netatmo-frontend-cutover/168-PATTERNS.md
@.planning/phases/168-netatmo-frontend-cutover/168-VALIDATION.md
@.planning/phases/167-sonos-frontend-cutover/167-01-PLAN.md
@app/debug/api/components/tabs/NetatmoTab.tsx
@app/debug/components/tabs/NetatmoTab.tsx

<interfaces>
<!-- Analog (Phase 166 HueTab 4-ref lockstep pattern) — see PATTERNS.md section "app/debug/{api/,/}components/tabs/NetatmoTab.tsx" -->

<!-- GET-endpoint loader pattern (PATTERNS.md lines 50-57, both NetatmoTab files lines 53-60 BEFORE): -->
```typescript
const fetchAllGetEndpoints = () => {
  fetchGetEndpoint('health', '/api/netatmo/health');
  fetchGetEndpoint('homesdata', '/api/netatmo/homesdata');
  fetchGetEndpoint('homestatus', '/api/netatmo/homestatus');
  fetchGetEndpoint('valves', '/api/netatmo/valves');
  fetchGetEndpoint('cameraStatus', '/api/netatmo/camera/status');
  fetchGetEndpoint('schedules', '/api/netatmo/schedules');   // ← DELETE this entire line
};
```
AFTER — 5 calls remain, `schedules` loader call deleted:
```typescript
const fetchAllGetEndpoints = () => {
  fetchGetEndpoint('health', '/api/v1/netatmo/health');
  fetchGetEndpoint('homesdata', '/api/v1/netatmo/homesdata');
  fetchGetEndpoint('homestatus', '/api/v1/netatmo/homestatus');
  fetchGetEndpoint('valves', '/api/v1/netatmo/valves');
  fetchGetEndpoint('cameraStatus', '/api/v1/netatmo/camera/status');
};
```

<!-- EndpointCard 4-ref lockstep (PATTERNS.md lines 60-73) — each GET tile has 4 URL occurrences: -->
```typescript
<EndpointCard
  name="Health"
  url="/api/netatmo/health"                                        // ← ref 1
  response={getResponses.health}
  loading={loadingGet.health ?? false}
  onRefresh={() => fetchGetEndpoint('health', '/api/netatmo/health')}  // ← ref 2
  onCopyUrl={() => copyUrlToClipboard('/api/netatmo/health')}           // ← ref 3
  isCopied={copiedUrl === '/api/netatmo/health'}                         // ← ref 4
/>
```

<!-- Tile-drop pattern (PATTERNS.md lines 93-94): remove entire <EndpointCard ... /> block plus any wrapping JSX + the corresponding loader call. -->

<!-- Semantic-mapping pattern (CONTEXT D-04 + PATTERNS.md line 91): calibrate tile URL goes from /api/netatmo/calibrate → /api/v1/netatmo/valves/calibrate (NOT a prefix swap — the `valves/` segment is added). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite app/debug/api/components/tabs/NetatmoTab.tsx — 42 URL swaps (tile drop + semantic mapping + 11 tile × 4-ref lockstep)</name>
  <files>app/debug/api/components/tabs/NetatmoTab.tsx</files>
  <read_first>
    - app/debug/api/components/tabs/NetatmoTab.tsx (full file — 42 URL occurrences on lines 54-243 per RESEARCH Grep Inventory A5)
    - app/debug/components/tabs/HueTab.tsx (Phase 166 analog — 4-ref lockstep pattern landed, see PATTERNS.md lines 60-73)
    - .planning/phases/168-netatmo-frontend-cutover/168-RESEARCH.md (Grep Inventory A5 table — exact line-number map of the 42 refs)
  </read_first>
  <action>
Apply three distinct edits to `app/debug/api/components/tabs/NetatmoTab.tsx`:

**Edit A — Loader call cleanup (lines 54-60):**

Delete line 59 entirely (`fetchGetEndpoint('schedules', '/api/netatmo/schedules');`). Rewrite the 5 remaining calls to v1 URLs:

| Line | Before | After |
|------|--------|-------|
| 54 | `fetchGetEndpoint('health', '/api/netatmo/health');` | `fetchGetEndpoint('health', '/api/v1/netatmo/health');` |
| 55 | `fetchGetEndpoint('homesdata', '/api/netatmo/homesdata');` | `fetchGetEndpoint('homesdata', '/api/v1/netatmo/homesdata');` |
| 56 | `fetchGetEndpoint('homestatus', '/api/netatmo/homestatus');` | `fetchGetEndpoint('homestatus', '/api/v1/netatmo/homestatus');` |
| 57 | `fetchGetEndpoint('valves', '/api/netatmo/valves');` | `fetchGetEndpoint('valves', '/api/v1/netatmo/valves');` |
| 58 | `fetchGetEndpoint('cameraStatus', '/api/netatmo/camera/status');` | `fetchGetEndpoint('cameraStatus', '/api/v1/netatmo/camera/status');` |
| 59 | `fetchGetEndpoint('schedules', '/api/netatmo/schedules');` | **DELETE THIS LINE** (no v1 equivalent per D-04) |

**Edit B — GET tile lockstep rewrites (lines 129-179) — 5 tiles × 4 refs each = 20 URL swaps:**

For each tile (health lines 129/133/134/135; homesdata 140/144/145/146; homestatus 151/155/156/157; valves 162/166/167/168; cameraStatus 173/177/178/179), rewrite all 4 URL refs to the v1 prefix in one lockstep Edit per tile. Exact mapping:

| Tile | Before `url=` / `onRefresh` / `onCopyUrl` / `isCopied` | After |
|------|-------------------------------------------------------|-------|
| health | `'/api/netatmo/health'` (4×) | `'/api/v1/netatmo/health'` (4×) |
| homesdata | `'/api/netatmo/homesdata'` (4×) | `'/api/v1/netatmo/homesdata'` (4×) |
| homestatus | `'/api/netatmo/homestatus'` (4×) | `'/api/v1/netatmo/homestatus'` (4×) |
| valves | `'/api/netatmo/valves'` (4×) | `'/api/v1/netatmo/valves'` (4×) |
| cameraStatus (camera/status) | `'/api/netatmo/camera/status'` (4×) | `'/api/v1/netatmo/camera/status'` (4×) |

**Edit C — Drop the `schedules` tile entirely (lines 183-191 approximately — the `<EndpointCard>` block with `url="/api/netatmo/schedules"`):**

Delete the entire `<EndpointCard>` JSX block for the schedules tile. This removes lines 184, 188, 189, 190 (all 4 URL refs) plus any surrounding JSX belonging only to that tile (the opening `<EndpointCard`, the `name=` prop, the `response=` / `loading=` / `timing=` props, and the closing `/>`). Use the Read tool first to identify the exact line range for the `<EndpointCard ... schedules ... />` block, then delete it.

**Edit D — POST tile lockstep rewrites (lines 203-243) — 3 tiles × 4 refs:**

| Tile | Lines | Before | After |
|------|-------|--------|-------|
| setthermmode | 203, 216, 217, 218 | url/onExecute/copy/copied = `'/api/netatmo/setthermmode'` (4×) | `'/api/v1/netatmo/setthermmode'` (4×) |
| setroomthermpoint | 223, 230, 231, 232 | `'/api/netatmo/setroomthermpoint'` (4×) | `'/api/v1/netatmo/setroomthermpoint'` (4×) |
| calibrate **(SEMANTIC MAPPING — NOT a prefix swap)** | 237, 241, 242, 243 | `'/api/netatmo/calibrate'` (4×) | `'/api/v1/netatmo/valves/calibrate'` (4×) — per D-04, the v1 bulk-calibrate endpoint lives under `/valves/` |

**Implementation approach for Edits A, B, D:** Use the Edit tool with `replace_all: true` and `old_string: '/api/netatmo/'` → `new_string: '/api/v1/netatmo/'` — this catches 40 of the 41 non-dropped refs in one operation (every non-calibrate URL is a pure prefix swap). Then apply a second Edit for the calibrate semantic mapping: `old_string: '/api/v1/netatmo/calibrate'` → `new_string: '/api/v1/netatmo/valves/calibrate'` (`replace_all: true` — catches all 4 calibrate refs at once).

**Implementation approach for Edit C (tile drop):** Read the file to confirm the exact line range of the schedules `<EndpointCard>` block, then use Edit to delete the entire block with a single `old_string` → `new_string: ''` replacement.

After all edits, run the verify grep — must return zero occurrences of `/api/netatmo/` in this file.

**Pitfall — identical filename twin:** The file `app/debug/components/tabs/NetatmoTab.tsx` (Task 2) is a NEAR-DUPLICATE with the same 42 refs on identical line numbers. Do NOT modify it here. Task 2 handles it in lockstep.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -n "/api/netatmo/" app/debug/api/components/tabs/NetatmoTab.tsx ; echo "LEGACY_GREP_EXIT: $?" ; grep -c "/api/v1/netatmo/" app/debug/api/components/tabs/NetatmoTab.tsx ; grep -c "/api/v1/netatmo/valves/calibrate" app/debug/api/components/tabs/NetatmoTab.tsx ; grep -c "schedules" app/debug/api/components/tabs/NetatmoTab.tsx ; npm test -- --testPathPattern="debug.*NetatmoTab|app/debug" --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "/api/netatmo/" app/debug/api/components/tabs/NetatmoTab.tsx` returns zero matches (no legacy refs)
    - `grep -c "/api/v1/netatmo/" app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 37 (36 remaining tile refs + 1 calibrate extra from `valves/calibrate` being a longer string)
    - `grep -c "/api/v1/netatmo/valves/calibrate" app/debug/api/components/tabs/NetatmoTab.tsx` returns exactly 4 (calibrate tile's 4 refs — url=, onExecute, onCopyUrl, isCopied)
    - `grep -c "/api/v1/netatmo/health" app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 5 (4 tile refs + 1 loader call)
    - `grep -c "/api/v1/netatmo/homesdata" app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 5 (4 tile refs + 1 loader call)
    - `grep -c "/api/v1/netatmo/homestatus" app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 5
    - `grep -c "/api/v1/netatmo/valves\"" app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 4 (valves list tile's 4 refs — exact-match trailing quote guards against `valves/calibrate` matches)
    - `grep -c "/api/v1/netatmo/camera/status" app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 5
    - `grep -c "/api/v1/netatmo/setthermmode" app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 4
    - `grep -c "/api/v1/netatmo/setroomthermpoint" app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 4
    - `grep -n "fetchGetEndpoint('schedules'" app/debug/api/components/tabs/NetatmoTab.tsx` returns zero matches (loader call deleted per Edit A)
    - `grep -n "url=\"/api/v1/netatmo/schedules\"" app/debug/api/components/tabs/NetatmoTab.tsx` returns zero matches (tile deleted per Edit C)
    - `grep -n "name=\"Schedules\"" app/debug/api/components/tabs/NetatmoTab.tsx` returns zero matches (the schedules `<EndpointCard>` block removed entirely)
    - `grep -c 'name="Proxy Health"' app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 1 (tile survives; confirmed tile-name token present in file before commit)
    - `grep -c 'name="Homes Data"' app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -c 'name="Home Status"' app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -c 'name="Valves"' app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -c 'name="Camera Status"' app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -c 'name="Set Therm Mode"' app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -c 'name="Set Room Therm Point"' app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -c 'name="Calibrate Valves"' app/debug/api/components/tabs/NetatmoTab.tsx` returns >= 1
    - Jest still green after the edit (no test-file depends on this debug tab's tile count; fast check proves compilation)
  </acceptance_criteria>
  <done>app/debug/api/components/tabs/NetatmoTab.tsx rewritten: 40 prefix swaps + 4 calibrate semantic mappings + 1 loader-call deletion + 1 tile-block deletion = all 42 legacy refs eliminated. Zero /api/netatmo/ remains. calibrate URL now /api/v1/netatmo/valves/calibrate (4 refs). schedules tile and loader call fully removed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite app/debug/components/tabs/NetatmoTab.tsx in lockstep with Task 1 — same 42 URL swaps + same tile drop + same semantic mapping</name>
  <files>app/debug/components/tabs/NetatmoTab.tsx</files>
  <read_first>
    - app/debug/components/tabs/NetatmoTab.tsx (near-duplicate twin of the file edited in Task 1 — identical 42 refs per RESEARCH A6)
    - app/debug/api/components/tabs/NetatmoTab.tsx (Task 1 OUTPUT — use this as the reference for what the edited file should look like; RESEARCH A6 confirms both files have identical line numbers for the 42 refs, so the same edit sequence applies)
  </read_first>
  <action>
Apply the **exact same** Edit A / Edit B / Edit C / Edit D sequence from Task 1, targeting `app/debug/components/tabs/NetatmoTab.tsx` instead of `app/debug/api/components/tabs/NetatmoTab.tsx`.

**Why a separate task, not a shared edit:** The two files are near-duplicates (same 42 refs on same line numbers) but separate files. Apply the edits one file at a time so grep verifications confirm each file is fully clean before moving on (per PATTERNS.md "Debug-panel dual-file lockstep" — drift between them would leave one debug panel broken post-cutover).

**Edits to apply:**

1. **Edit A:** Delete line 59 (`fetchGetEndpoint('schedules', '/api/netatmo/schedules');`); rewrite lines 54-58 prefix `/api/netatmo/` → `/api/v1/netatmo/`.
2. **Edit B:** Rewrite 5 GET tile blocks (health, homesdata, homestatus, valves, cameraStatus) — 4 URL refs per tile to v1 prefix. Same line numbers as Task 1 (129-179).
3. **Edit C:** Delete the `<EndpointCard>` JSX block for the schedules tile entirely (url=`/api/netatmo/schedules`). Same approximate line range (183-191) as Task 1.
4. **Edit D:** Rewrite 3 POST tile blocks (setthermmode, setroomthermpoint, calibrate) — 4 URL refs each. For the calibrate tile, the semantic mapping `/api/netatmo/calibrate` → `/api/v1/netatmo/valves/calibrate` applies (same as Task 1 Edit D).

**Implementation approach:**
1. First pass (prefix swap): Edit tool with `replace_all: true`, `old_string: '/api/netatmo/'` → `new_string: '/api/v1/netatmo/'` — catches 40 of 41 non-dropped refs.
2. Second pass (calibrate semantic mapping): Edit tool with `replace_all: true`, `old_string: '/api/v1/netatmo/calibrate'` → `new_string: '/api/v1/netatmo/valves/calibrate'` — catches 4 calibrate refs.
3. Third pass (loader-call delete): Edit the line 59 `fetchGetEndpoint('schedules', '/api/v1/netatmo/schedules');` — replace with empty string (or find the exact line content after first pass has already swapped its prefix, then delete).
4. Fourth pass (tile-block delete): Edit the `<EndpointCard>` block for the schedules tile — replace with empty string.

**Final grep verification:** `grep -n "/api/netatmo/" app/debug/components/tabs/NetatmoTab.tsx` must return zero.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -n "/api/netatmo/" app/debug/components/tabs/NetatmoTab.tsx ; echo "LEGACY_GREP_EXIT: $?" ; grep -c "/api/v1/netatmo/" app/debug/components/tabs/NetatmoTab.tsx ; grep -c "/api/v1/netatmo/valves/calibrate" app/debug/components/tabs/NetatmoTab.tsx ; diff <(grep -o "/api/v1/netatmo/[^\"' )]*" app/debug/api/components/tabs/NetatmoTab.tsx | sort -u) <(grep -o "/api/v1/netatmo/[^\"' )]*" app/debug/components/tabs/NetatmoTab.tsx | sort -u) ; echo "DIFF_EXIT: $?" ; npm test -- --testPathPattern="debug" --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "/api/netatmo/" app/debug/components/tabs/NetatmoTab.tsx` returns zero matches
    - `grep -c "/api/v1/netatmo/" app/debug/components/tabs/NetatmoTab.tsx` returns >= 37
    - `grep -c "/api/v1/netatmo/valves/calibrate" app/debug/components/tabs/NetatmoTab.tsx` returns exactly 4
    - `diff` of unique v1 URLs between Task 1's output and Task 2's output returns zero lines (both debug files expose the EXACT SAME v1 URL set — no drift; this is the PATTERNS.md "Debug-panel dual-file lockstep" invariant)
    - `grep -n "fetchGetEndpoint('schedules'" app/debug/components/tabs/NetatmoTab.tsx` returns zero matches
    - `grep -n "url=\"/api/v1/netatmo/schedules\"" app/debug/components/tabs/NetatmoTab.tsx` returns zero matches
    - `grep -c 'name="Proxy Health"' app/debug/components/tabs/NetatmoTab.tsx` returns >= 1 (tile survives)
    - `grep -c 'name="Homes Data"' app/debug/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -c 'name="Home Status"' app/debug/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -c 'name="Valves"' app/debug/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -c 'name="Camera Status"' app/debug/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -c 'name="Set Therm Mode"' app/debug/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -c 'name="Set Room Therm Point"' app/debug/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -c 'name="Calibrate Valves"' app/debug/components/tabs/NetatmoTab.tsx` returns >= 1
    - `grep -n "name=\"Schedules\"" app/debug/components/tabs/NetatmoTab.tsx` returns zero matches (tile deletion confirmed)
    - Jest still green (no regressions from the edits)
  </acceptance_criteria>
  <done>app/debug/components/tabs/NetatmoTab.tsx rewritten in exact lockstep with Task 1's output. Both debug NetatmoTab files expose identical v1 URL sets (diff returns zero). Zero /api/netatmo/ refs remain in either file. Plan 168-01 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Debug panel → v1 API | Authenticated fetch via existing `/api/v1/netatmo/*` routes (all Phase 161 routes inherit `withAuthAndErrorHandler`); same trust boundary as legacy |
| Debug panel → clipboard | `copyUrlToClipboard` writes URL strings to the user's clipboard — no server round-trip, no new privilege |
| Dual-file lockstep | Two near-duplicate files must emit identical URL sets — drift would leave one debug surface broken |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168-01-a | Tampering | Partial rewrite leaves some tile tiles pointing at legacy URLs that will 404 after Wave 3 deletes legacy tree | mitigate | Verify acceptance criterion `grep -n "/api/netatmo/" file` returns zero for BOTH files before Wave 3 begins. Tasks 1 and 2 each enforce this gate. |
| T-168-01-b | Tampering | 4-ref lockstep drift: `url=` string updated but one of the 3 callback args (onRefresh/onCopyUrl/isCopied) missed — copy button copies legacy URL, confusing user | mitigate | Use `replace_all: true` on the Edit tool's prefix swap — catches all 4 refs per tile simultaneously. Acceptance criteria enforce per-tile URL counts (e.g., health ≥5) which would fail if any of the 4 refs was missed. |
| T-168-01-c | Tampering | Wrong-file edit: Task 1 accidentally edits `app/debug/components/tabs/NetatmoTab.tsx` (Task 2's target) or vice versa | mitigate | Each task's `<files>` list a single file path; the diff verification in Task 2 confirms both files emit identical v1 URL sets after both tasks complete (a wrong-file edit would fail the diff). |
| T-168-01-d | Denial of Service | Schedules tile left in place would 404 immediately after Wave 3 deletes `/api/netatmo/schedules` (no v1 equivalent per D-04) | mitigate | Edit A (loader-call deletion) + Edit C (tile JSX deletion) — acceptance criterion `grep -n "url=\"/api/v1/netatmo/schedules\"" file` returns zero enforces complete removal. |
| T-168-01-e | Information Disclosure | Semantic mapping mistake: calibrate URL rewritten as `/api/v1/netatmo/calibrate` instead of `/api/v1/netatmo/valves/calibrate` (v1 route lives under `/valves/`) | mitigate | Second-pass Edit with `replace_all: true` on `/api/v1/netatmo/calibrate` → `/api/v1/netatmo/valves/calibrate` runs after the prefix swap. Acceptance criterion `grep -c "/api/v1/netatmo/valves/calibrate" file` returns exactly 4 confirms all 4 calibrate refs mapped. |
</threat_model>

<verification>
```bash
# Both debug files reference only v1 URLs
grep -n "/api/netatmo/" app/debug/api/components/tabs/NetatmoTab.tsx    # expect: zero matches
grep -n "/api/netatmo/" app/debug/components/tabs/NetatmoTab.tsx        # expect: zero matches

# calibrate tile uses semantic-mapped v1 URL (not a prefix swap)
grep -c "/api/v1/netatmo/valves/calibrate" app/debug/api/components/tabs/NetatmoTab.tsx   # expect: 4
grep -c "/api/v1/netatmo/valves/calibrate" app/debug/components/tabs/NetatmoTab.tsx       # expect: 4

# schedules tile and loader call fully removed
grep -n "schedules" app/debug/api/components/tabs/NetatmoTab.tsx | grep -v "cameraStatus"  # expect: minimal/zero functional refs
grep -n "url=\"/api/v1/netatmo/schedules\"" app/debug/api/components/tabs/NetatmoTab.tsx   # expect: zero
grep -n "url=\"/api/v1/netatmo/schedules\"" app/debug/components/tabs/NetatmoTab.tsx       # expect: zero

# Dual-file lockstep invariant: both files expose identical v1 URL set
diff <(grep -o "/api/v1/netatmo/[^\"' )]*" app/debug/api/components/tabs/NetatmoTab.tsx | sort -u) \
     <(grep -o "/api/v1/netatmo/[^\"' )]*" app/debug/components/tabs/NetatmoTab.tsx | sort -u)
# expect: zero lines of diff output

# Jest still green
npm test -- --testPathPattern="debug" --passWithNoTests
```
</verification>

<success_criteria>
- Zero `/api/netatmo/` string occurrences in either `app/debug/api/components/tabs/NetatmoTab.tsx` or `app/debug/components/tabs/NetatmoTab.tsx`
- Both files contain exactly 4 occurrences of `/api/v1/netatmo/valves/calibrate` (calibrate tile's 4 URL refs — url=, onExecute, onCopyUrl, isCopied)
- `schedules` loader call deleted from `fetchAllGetEndpoints` in both files
- `schedules` `<EndpointCard>` JSX block deleted from both files
- Both files emit identical v1 URL sets (`diff` check returns zero)
- `npm test -- --testPathPattern="debug"` still exits 0
- Addresses NETA-01..NETA-09 by ensuring debug surfaces exercise the v1 routes that satisfy each requirement (health, homesdata, homestatus, valves, camera/status, setthermmode, setroomthermpoint, valves/calibrate), with param-required camera endpoints intentionally not exposed in the debug UI (existing state; CONTEXT D-10 confirms no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/168-netatmo-frontend-cutover/168-01-SUMMARY.md`
</output>
</content>
</invoke>