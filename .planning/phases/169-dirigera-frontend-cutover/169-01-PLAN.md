---
phase: 169-dirigera-frontend-cutover
plan: 01
type: execute
wave: 1
depends_on: []
requirements: []
files_modified:
  - app/components/devices/dirigera/hooks/useDirigeraData.ts
  - app/components/devices/dirigera/hooks/useDirigeraFullData.ts
  - app/components/devices/dirigera/hooks/__tests__/useDirigeraData.test.ts
  - tests/smoke/page-loads.spec.ts
files_created:
  - app/api/v1/dirigera/health/route.ts
  - app/api/v1/dirigera/health/__tests__/route.test.ts
  - app/api/v1/dirigera/sensors/route.ts
  - app/api/v1/dirigera/sensors/__tests__/route.test.ts
  - app/api/v1/dirigera/sensors/summary/route.ts
  - app/api/v1/dirigera/sensors/summary/__tests__/route.test.ts
  - app/api/v1/dirigera/sensors/contact/route.ts
  - app/api/v1/dirigera/sensors/contact/__tests__/route.test.ts
  - app/api/v1/dirigera/sensors/motion/route.ts
  - app/api/v1/dirigera/sensors/motion/__tests__/route.test.ts
files_deleted: []
autonomous: true
must_haves:
  truths:
    - "Five new v1 wrapper routes exist at /api/v1/dirigera/{health,sensors,sensors/summary,sensors/contact,sensors/motion} and return 200 with the same response envelopes as the legacy routes"
    - "Each new v1 route has a co-located Jest test that asserts 401 on missing session and 200 with expected envelope fields on authenticated request"
    - "useDirigeraData.ts fetches only /api/v1/dirigera/* URLs (health + sensors/summary); no /api/dirigera/ strings remain in the file"
    - "useDirigeraFullData.ts FILTER_ENDPOINTS map and health fetch all point at /api/v1/dirigera/* URLs"
    - "useDirigeraData.test.ts assertions on lines around 121/122/309 all reference /api/v1/dirigera/* URLs"
    - "tests/smoke/page-loads.spec.ts contains a /dirigera test case that navigates, waits for networkidle, asserts main attached, and reports zero console errors"
    - "WebSocket subscribe('dirigera', ...) / unsubscribe('dirigera', ...) calls in useDirigeraData.ts are unchanged"
    - "Full Jest suite green; Playwright page-loads.spec.ts green"
  artifacts:
    - path: "app/api/v1/dirigera/health/route.ts"
      provides: "v1 health wrapper delegating to getHealth() with full passthrough envelope"
      contains: "withAuthAndErrorHandler"
    - path: "app/api/v1/dirigera/sensors/route.ts"
      provides: "v1 sensors wrapper with explicit { sensors, count, is_stale } spread"
      contains: "success({ sensors: data.sensors, count: data.count, is_stale: data.is_stale })"
    - path: "app/api/v1/dirigera/sensors/summary/route.ts"
      provides: "v1 sensor-summary wrapper with full passthrough"
      contains: "getSensorSummary"
    - path: "app/api/v1/dirigera/sensors/contact/route.ts"
      provides: "v1 contact-sensors wrapper with explicit spread"
      contains: "getContactSensors"
    - path: "app/api/v1/dirigera/sensors/motion/route.ts"
      provides: "v1 motion-sensors wrapper with explicit spread"
      contains: "getMotionSensors"
  key_links:
    - from: "app/components/devices/dirigera/hooks/useDirigeraData.ts"
      to: "/api/v1/dirigera/health and /api/v1/dirigera/sensors/summary"
      via: "fetch() calls in fetchData and fetchHealth"
      pattern: "fetch\\('/api/v1/dirigera/"
    - from: "app/components/devices/dirigera/hooks/useDirigeraFullData.ts"
      to: "/api/v1/dirigera/sensors, /sensors/contact, /sensors/motion, /health"
      via: "FILTER_ENDPOINTS map and healthRes fetch"
      pattern: "/api/v1/dirigera/"
    - from: "tests/smoke/page-loads.spec.ts"
      to: "/dirigera route"
      via: "new test case in Device Pages describe block"
      pattern: "page\\.goto\\('/dirigera'\\)"
---

<objective>
Create 5 thin v1 wrapper routes at `/api/v1/dirigera/{health,sensors,sensors/summary,sensors/contact,sensors/motion}` that mirror the exact response envelopes of the existing legacy `/api/dirigera/*` routes (per D-01, D-02, D-03), then atomically flip the two production consumer hooks (`useDirigeraData`, `useDirigeraFullData`) and the `useDirigeraData.test.ts` assertions to the new v1 URLs (per D-04, D-05, D-06). Finally, add a `/dirigera` test case to `tests/smoke/page-loads.spec.ts` to close the Wave 0 gap flagged in RESEARCH §Open Questions Q2.

Purpose: Establish the complete v1 route surface so ROADMAP SC-2 ("zero /api/dirigera/ references outside debug/archived paths") becomes achievable in Wave 3, and enable Wave 2 hooks/panels to build against `/api/v1/dirigera/*` exclusively.

Output: 10 new files (5 routes + 5 tests), 4 modified files (2 hooks + 1 hook test + 1 smoke spec). Jest full suite green, Playwright page-loads.spec.ts green.

**This wave satisfies zero DIR-XX requirements by itself** — DIR-01/02/03 require the new history/stats/telemetry HOOKS built in Wave 2. Wave 1 only establishes the route surface and swaps the two existing hooks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/169-dirigera-frontend-cutover/169-CONTEXT.md
@.planning/phases/169-dirigera-frontend-cutover/169-RESEARCH.md
@.planning/phases/169-dirigera-frontend-cutover/169-PATTERNS.md
@app/api/v1/dirigera/history/route.ts
@app/api/v1/dirigera/stats/route.ts
@app/api/v1/dirigera/stats/__tests__/route.test.ts
@app/api/v1/dirigera/history/__tests__/route.test.ts
@app/api/dirigera/health/route.ts
@app/api/dirigera/sensors/route.ts
@app/api/dirigera/sensors/summary/route.ts
@app/api/dirigera/sensors/contact/route.ts
@app/api/dirigera/sensors/motion/route.ts
@app/components/devices/dirigera/hooks/useDirigeraData.ts
@app/components/devices/dirigera/hooks/useDirigeraFullData.ts
@app/components/devices/dirigera/hooks/__tests__/useDirigeraData.test.ts
@tests/smoke/page-loads.spec.ts

<interfaces>
<!-- Extracted from lib/dirigera/dirigeraProxy.ts and types/dirigeraProxy.ts — do not re-explore. -->
<!-- All 8 proxy functions are already exported; wrappers are pure delegates. -->

From `lib/dirigera/dirigeraProxy.ts`:
```typescript
export function getHealth(): Promise<DirigeraHealthResponse>;
export function getSensors(): Promise<DirigeraSensorsResponse>;
export function getSensorSummary(): Promise<SensorSummaryResponse>;
export function getContactSensors(): Promise<ContactSensorsResponse>;
export function getMotionSensors(): Promise<MotionSensorsResponse>;
```

From `types/dirigeraProxy.ts`:
```typescript
export interface DirigeraHealthResponse { firmware_version: string; connected_sensors: number; is_reachable: boolean; }
export interface DirigeraSensorsResponse { sensors: DirigeraSensor[]; count: number; is_stale: boolean; }
export interface SensorSummaryResponse { total_sensors: number; open_count: number; offline_count: number; low_battery_count: number; is_stale: boolean; }
export interface ContactSensorsResponse { sensors: ContactSensor[]; count: number; is_stale: boolean; }
export interface MotionSensorsResponse { sensors: MotionSensor[]; count: number; is_stale: boolean; }
```

From `lib/core`:
```typescript
export function withAuthAndErrorHandler(handler, contextLabel: string): RouteHandler;
export function success(data: Record<string, unknown>): NextResponse;
```

Response envelope pattern split (CRITICAL — D-02):
- Full passthrough (`success(data as unknown as Record<string, unknown>)`): `health`, `sensors/summary`
- Explicit field spread (`success({ sensors: data.sensors, count: data.count, is_stale: data.is_stale })`): `sensors`, `sensors/contact`, `sensors/motion`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5 v1 wrapper routes mirroring legacy envelopes exactly (D-01, D-02)</name>
  <files>
    app/api/v1/dirigera/health/route.ts,
    app/api/v1/dirigera/sensors/route.ts,
    app/api/v1/dirigera/sensors/summary/route.ts,
    app/api/v1/dirigera/sensors/contact/route.ts,
    app/api/v1/dirigera/sensors/motion/route.ts
  </files>
  <read_first>
    - app/api/v1/dirigera/stats/route.ts (closest structural analog — no-params v1 route)
    - app/api/v1/dirigera/history/route.ts (v1 structural template)
    - app/api/dirigera/health/route.ts (body to mirror for health — full passthrough)
    - app/api/dirigera/sensors/route.ts (body to mirror for sensors — explicit spread)
    - app/api/dirigera/sensors/summary/route.ts (body to mirror for summary — full passthrough)
    - app/api/dirigera/sensors/contact/route.ts (body to mirror for contact — explicit spread)
    - app/api/dirigera/sensors/motion/route.ts (body to mirror for motion — explicit spread)
    - lib/dirigera/dirigeraProxy.ts (confirm getHealth/getSensors/getSensorSummary/getContactSensors/getMotionSensors signatures)
    - .planning/phases/169-dirigera-frontend-cutover/169-PATTERNS.md (sections "Pattern 1", "Pattern 2", and "Response Shape Reference" table)
  </read_first>
  <action>
    Create the 5 new v1 wrapper files listed under `<files>`. For each file, set `export const dynamic = 'force-dynamic';`, import `{ withAuthAndErrorHandler, success } from '@/lib/core'`, import the matching proxy function from `@/lib/dirigera/dirigeraProxy`, and export a `GET` handler wrapped in `withAuthAndErrorHandler(..., 'Dirigera/<Label>')`. Use TWO response patterns (do NOT unify them):

    1. **Full passthrough** (`health`, `sensors/summary`):
       ```typescript
       export const GET = withAuthAndErrorHandler(async () => {
         const data = await getHealth();   // or getSensorSummary()
         return success(data as unknown as Record<string, unknown>);
       }, 'Dirigera/Health');               // or 'Dirigera/SensorsSummary'
       ```

    2. **Explicit field spread** (`sensors`, `sensors/contact`, `sensors/motion`):
       ```typescript
       export const GET = withAuthAndErrorHandler(async () => {
         const data = await getSensors();   // or getContactSensors() / getMotionSensors()
         return success({ sensors: data.sensors, count: data.count, is_stale: data.is_stale });
       }, 'Dirigera/Sensors');              // or 'Dirigera/SensorsContact' / 'Dirigera/SensorsMotion'
       ```

    Copy JSDoc comment style from the legacy files, updating each comment's path line to `/api/v1/dirigera/...`. Keep `'Dirigera/<Label>'` context labels consistent with existing v1 routes (`'Dirigera/History'`, `'Dirigera/Stats'`, `'Dirigera/Telemetry'`).

    Context-label map (exact strings to use):
    - `health/route.ts` → `'Dirigera/Health'`
    - `sensors/route.ts` → `'Dirigera/Sensors'`
    - `sensors/summary/route.ts` → `'Dirigera/SensorsSummary'`
    - `sensors/contact/route.ts` → `'Dirigera/SensorsContact'`
    - `sensors/motion/route.ts` → `'Dirigera/SensorsMotion'`

    Do NOT add query-param parsing — none of these 5 routes take query params. Do NOT change the response shape from what the legacy routes emit (verified in 169-PATTERNS.md "Response Shape Reference" table).
  </action>
  <verify>
    <automated>
      test -f app/api/v1/dirigera/health/route.ts && \
      test -f app/api/v1/dirigera/sensors/route.ts && \
      test -f app/api/v1/dirigera/sensors/summary/route.ts && \
      test -f app/api/v1/dirigera/sensors/contact/route.ts && \
      test -f app/api/v1/dirigera/sensors/motion/route.ts && \
      grep -q "getHealth" app/api/v1/dirigera/health/route.ts && \
      grep -q "getSensors\b" app/api/v1/dirigera/sensors/route.ts && \
      grep -q "getSensorSummary" app/api/v1/dirigera/sensors/summary/route.ts && \
      grep -q "getContactSensors" app/api/v1/dirigera/sensors/contact/route.ts && \
      grep -q "getMotionSensors" app/api/v1/dirigera/sensors/motion/route.ts && \
      grep -q "withAuthAndErrorHandler" app/api/v1/dirigera/health/route.ts && \
      grep -q "withAuthAndErrorHandler" app/api/v1/dirigera/sensors/route.ts && \
      grep -q "withAuthAndErrorHandler" app/api/v1/dirigera/sensors/summary/route.ts && \
      grep -q "withAuthAndErrorHandler" app/api/v1/dirigera/sensors/contact/route.ts && \
      grep -q "withAuthAndErrorHandler" app/api/v1/dirigera/sensors/motion/route.ts && \
      grep -q "force-dynamic" app/api/v1/dirigera/health/route.ts && \
      grep -q "data.sensors, count: data.count, is_stale: data.is_stale" app/api/v1/dirigera/sensors/route.ts && \
      grep -q "data.sensors, count: data.count, is_stale: data.is_stale" app/api/v1/dirigera/sensors/contact/route.ts && \
      grep -q "data.sensors, count: data.count, is_stale: data.is_stale" app/api/v1/dirigera/sensors/motion/route.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/api/v1/dirigera/health/route.ts` succeeds.
    - `test -f app/api/v1/dirigera/sensors/route.ts` succeeds.
    - `test -f app/api/v1/dirigera/sensors/summary/route.ts` succeeds.
    - `test -f app/api/v1/dirigera/sensors/contact/route.ts` succeeds.
    - `test -f app/api/v1/dirigera/sensors/motion/route.ts` succeeds.
    - Each of the 5 files contains `withAuthAndErrorHandler` and `export const dynamic = 'force-dynamic'`.
    - `health/route.ts` and `sensors/summary/route.ts` use `success(data as unknown as Record<string, unknown>)` (full passthrough).
    - `sensors/route.ts`, `sensors/contact/route.ts`, `sensors/motion/route.ts` each call `success({ sensors: data.sensors, count: data.count, is_stale: data.is_stale })` (verified by the grep assertions above).
    - `[ $(find app/api/v1/dirigera -name route.ts | wc -l) -eq 8 ]` — exactly 8 v1 route files now exist (3 from Phase 163 + 5 new).
  </acceptance_criteria>
  <done>All 5 v1 wrapper route files exist with the correct envelope pattern per route (full passthrough for health/summary, explicit spread for sensors/contact/motion), `withAuthAndErrorHandler` applied, `force-dynamic` set, and `find app/api/v1/dirigera -name route.ts | wc -l` returns 8.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 5 co-located Jest route tests (D-03)</name>
  <files>
    app/api/v1/dirigera/health/__tests__/route.test.ts,
    app/api/v1/dirigera/sensors/__tests__/route.test.ts,
    app/api/v1/dirigera/sensors/summary/__tests__/route.test.ts,
    app/api/v1/dirigera/sensors/contact/__tests__/route.test.ts,
    app/api/v1/dirigera/sensors/motion/__tests__/route.test.ts
  </files>
  <behavior>
    - Each test file mocks `@/lib/dirigera/dirigeraProxy` and `@/lib/auth0`; imports `GET` from `../route`.
    - Test 1 per file: `returns 401 when not authenticated` — `mockGetSession.mockResolvedValue(null)` → response.status === 401, `data.code === 'UNAUTHORIZED'`.
    - Test 2 per file: `returns 200 with <resource> data when authenticated` — mock proxy function, call GET, assert `response.status === 200`, `data.success === true`, and at least one representative field from the mock payload is present (see per-file assertions in `<action>`).
    - Test 3 for `sensors`, `sensors/contact`, `sensors/motion` ONLY: assert the response body contains exactly `sensors`, `count`, `is_stale` fields matching the mock (verifies explicit field spread, not passthrough). Confirm proxy called with no args: `expect(mockGetX).toHaveBeenCalledWith()`.
  </behavior>
  <read_first>
    - app/api/v1/dirigera/stats/__tests__/route.test.ts (closest analog — no-params, full-passthrough route test; pattern for health + sensors/summary)
    - app/api/v1/dirigera/history/__tests__/route.test.ts (analog for explicit-response-field assertions; pattern for sensors + sensors/contact + sensors/motion)
    - jest.setup.ts (if present — confirm Next Response mock shape)
    - .planning/phases/169-dirigera-frontend-cutover/169-PATTERNS.md (sections "Route Test: health", "Route Tests: sensors, ...", "Console Suppression in Route Tests")
  </read_first>
  <action>
    Create 5 co-located test files. Use the 169-PATTERNS.md "Route test template" and the `stats/__tests__/route.test.ts` structure. Required boilerplate (at the top of every file):

    ```typescript
    jest.mock('@/lib/dirigera/dirigeraProxy');
    jest.mock('@/lib/auth0', () => ({
      auth0: { getSession: jest.fn() },
    }));

    import { GET } from '../route';
    import * as dirigeraProxy from '@/lib/dirigera/dirigeraProxy';
    import { auth0 } from '@/lib/auth0';

    const mockGetSession = jest.mocked(auth0.getSession);
    const mockSession = { user: { sub: 'auth0|123', email: 'test@test.com' } };

    beforeEach(() => {
      jest.clearAllMocks();
      mockGetSession.mockResolvedValue(mockSession as any);
      jest.spyOn(console, 'error').mockImplementation(() => {});
      jest.spyOn(console, 'warn').mockImplementation(() => {});
    });
    ```

    Per-file specifics (mock + describe + assertions):

    1. **health/__tests__/route.test.ts** — `describe('GET /api/v1/dirigera/health')`. Mock `mockGetHealth = jest.mocked(dirigeraProxy.getHealth)`. Mock data: `{ firmware_version: '2.465.0', connected_sensors: 6, is_reachable: true }`. Test 2 asserts `data.firmware_version === '2.465.0'`, `data.connected_sensors === 6`, and `expect(mockGetHealth).toHaveBeenCalledWith()`.

    2. **sensors/__tests__/route.test.ts** — `describe('GET /api/v1/dirigera/sensors')`. Mock `mockGetSensors = jest.mocked(dirigeraProxy.getSensors)`. Mock data: `{ sensors: [{ id: 'abc', type: 'openCloseSensor', custom_name: 'Door', room: 'Hall', firmware_version: '1.0', battery_percentage: 85, is_reachable: true, last_seen: '2026-04-01T10:00:00Z', is_open: false }], count: 1, is_stale: false }`. Test 2 asserts `data.sensors.length === 1`, `data.count === 1`, `data.is_stale === false`. Test 3 asserts `expect(mockGetSensors).toHaveBeenCalledWith()` (no args).

    3. **sensors/summary/__tests__/route.test.ts** — `describe('GET /api/v1/dirigera/sensors/summary')`. Mock `mockGetSensorSummary = jest.mocked(dirigeraProxy.getSensorSummary)`. Mock data: `{ total_sensors: 3, open_count: 1, offline_count: 0, low_battery_count: 0, is_stale: false }`. Test 2 asserts `data.total_sensors === 3`, `data.open_count === 1`, and `expect(mockGetSensorSummary).toHaveBeenCalledWith()`.

    4. **sensors/contact/__tests__/route.test.ts** — `describe('GET /api/v1/dirigera/sensors/contact')`. Mock `mockGetContactSensors = jest.mocked(dirigeraProxy.getContactSensors)`. Mock data: `{ sensors: [{ id: 'c1', type: 'openCloseSensor', custom_name: 'Front', room: 'Hall', firmware_version: '1.0', battery_percentage: 85, is_reachable: true, last_seen: '2026-04-01T10:00:00Z', is_open: true, data_freshness: 'LIVE' }], count: 1, is_stale: false }`. Test 2 asserts `data.sensors[0].id === 'c1'`, `data.count === 1`, `data.is_stale === false`. Test 3 asserts `expect(mockGetContactSensors).toHaveBeenCalledWith()`.

    5. **sensors/motion/__tests__/route.test.ts** — `describe('GET /api/v1/dirigera/sensors/motion')`. Mock `mockGetMotionSensors = jest.mocked(dirigeraProxy.getMotionSensors)`. Mock data: `{ sensors: [{ id: 'm1', type: 'occupancySensor', custom_name: 'Hallway', room: 'Hall', firmware_version: '1.0', battery_percentage: null, is_reachable: true, last_seen: '2026-04-01T10:00:00Z', is_open: null, light_level: 120, data_freshness: 'LIVE' }], count: 1, is_stale: false }`. Test 2 asserts `data.sensors[0].id === 'm1'`, `data.count === 1`. Test 3 asserts `expect(mockGetMotionSensors).toHaveBeenCalledWith()`.

    Request URL pattern for all tests: `new Request('http://localhost:3000/api/v1/dirigera/<path>')`. Call pattern: `const response = await GET(request as any, {} as any)`. Use `as any` on mock proxy return values where TypeScript strict mode complains.
  </action>
  <verify>
    <automated>
      npx jest app/api/v1/dirigera/health/__tests__/route.test.ts app/api/v1/dirigera/sensors/__tests__/route.test.ts app/api/v1/dirigera/sensors/summary/__tests__/route.test.ts app/api/v1/dirigera/sensors/contact/__tests__/route.test.ts app/api/v1/dirigera/sensors/motion/__tests__/route.test.ts --silent
    </automated>
  </verify>
  <acceptance_criteria>
    - All 5 test files exist.
    - Each file contains `jest.mock('@/lib/dirigera/dirigeraProxy')` AND `jest.mock('@/lib/auth0'`.
    - Each file contains `returns 401 when not authenticated` and `returns 200` test cases.
    - `npx jest app/api/v1/dirigera/health/__tests__/route.test.ts app/api/v1/dirigera/sensors/__tests__/route.test.ts app/api/v1/dirigera/sensors/summary/__tests__/route.test.ts app/api/v1/dirigera/sensors/contact/__tests__/route.test.ts app/api/v1/dirigera/sensors/motion/__tests__/route.test.ts --silent` exits 0.
    - `grep -l "returns 401 when not authenticated" app/api/v1/dirigera/{health,sensors,sensors/summary,sensors/contact,sensors/motion}/__tests__/route.test.ts | wc -l` returns 5.
  </acceptance_criteria>
  <done>All 5 co-located route tests exist and pass, each asserting 401/200 behaviour plus (where applicable) the explicit-spread envelope shape.</done>
</task>

<task type="auto">
  <name>Task 3: Swap legacy URLs to /api/v1/dirigera/* in the two consumer hooks and the hook test (D-04, D-05, D-06)</name>
  <files>
    app/components/devices/dirigera/hooks/useDirigeraData.ts,
    app/components/devices/dirigera/hooks/useDirigeraFullData.ts,
    app/components/devices/dirigera/hooks/__tests__/useDirigeraData.test.ts
  </files>
  <read_first>
    - app/components/devices/dirigera/hooks/useDirigeraData.ts (current file — identify the 3 fetch sites at lines 51, 52, 81; confirm WS subscribe/unsubscribe block is orthogonal)
    - app/components/devices/dirigera/hooks/useDirigeraFullData.ts (current file — FILTER_ENDPOINTS map at lines 16-20, health fetch at line 55)
    - app/components/devices/dirigera/hooks/__tests__/useDirigeraData.test.ts (current file — assertions at lines 121, 122, 309)
    - .planning/phases/169-dirigera-frontend-cutover/169-PATTERNS.md (section "Pattern 3: Hook URL Swap" and sections for self-modifying files)
    - .planning/phases/169-dirigera-frontend-cutover/169-RESEARCH.md (Pitfall 2 — Touching WebSocket subscription)
  </read_first>
  <action>
    Apply pure prefix swaps. NO logic changes, NO signature changes, NO type changes.

    **`useDirigeraData.ts`** — replace exactly 3 string literals:
    - Line ~51: `fetch('/api/dirigera/health')` → `fetch('/api/v1/dirigera/health')`
    - Line ~52: `fetch('/api/dirigera/sensors/summary')` → `fetch('/api/v1/dirigera/sensors/summary')`
    - Line ~81: `fetch('/api/dirigera/health')` → `fetch('/api/v1/dirigera/health')` (inside `fetchHealth`)
    Do NOT modify the `subscribe('dirigera', handleMessage)` call at line ~128 or the matching `unsubscribe('dirigera', handleMessage)` at line ~129 — these are WebSocket topic keys, not URLs (per Pitfall 2).

    **`useDirigeraFullData.ts`** — replace exactly 4 string literals:
    - FILTER_ENDPOINTS `all` → `/api/v1/dirigera/sensors`
    - FILTER_ENDPOINTS `contact` → `/api/v1/dirigera/sensors/contact`
    - FILTER_ENDPOINTS `motion` → `/api/v1/dirigera/sensors/motion`
    - `fetch('/api/dirigera/health')` at line ~55 → `fetch('/api/v1/dirigera/health')`

    **`useDirigeraData.test.ts`** — replace exactly 3 string literals:
    - Line ~121: `expect(fetchMock).toHaveBeenCalledWith('/api/dirigera/health')` → `expect(fetchMock).toHaveBeenCalledWith('/api/v1/dirigera/health')`
    - Line ~122: `expect(fetchMock).toHaveBeenCalledWith('/api/dirigera/sensors/summary')` → `expect(fetchMock).toHaveBeenCalledWith('/api/v1/dirigera/sensors/summary')`
    - Line ~309: `expect(global.fetch).toHaveBeenCalledWith('/api/dirigera/health')` → `expect(global.fetch).toHaveBeenCalledWith('/api/v1/dirigera/health')`

    After all edits, the three modified files must contain zero occurrences of the substring `/api/dirigera/` (only `/api/v1/dirigera/` remains).
  </action>
  <verify>
    <automated>
      ! grep -n "'/api/dirigera/" app/components/devices/dirigera/hooks/useDirigeraData.ts app/components/devices/dirigera/hooks/useDirigeraFullData.ts app/components/devices/dirigera/hooks/__tests__/useDirigeraData.test.ts && \
      grep -c "'/api/v1/dirigera/health'" app/components/devices/dirigera/hooks/useDirigeraData.ts | grep -q "^2$" && \
      grep -q "'/api/v1/dirigera/sensors/summary'" app/components/devices/dirigera/hooks/useDirigeraData.ts && \
      grep -q "all: '/api/v1/dirigera/sensors'" app/components/devices/dirigera/hooks/useDirigeraFullData.ts && \
      grep -q "contact: '/api/v1/dirigera/sensors/contact'" app/components/devices/dirigera/hooks/useDirigeraFullData.ts && \
      grep -q "motion: '/api/v1/dirigera/sensors/motion'" app/components/devices/dirigera/hooks/useDirigeraFullData.ts && \
      grep -q "subscribe('dirigera'" app/components/devices/dirigera/hooks/useDirigeraData.ts && \
      grep -q "unsubscribe('dirigera'" app/components/devices/dirigera/hooks/useDirigeraData.ts && \
      npx jest app/components/devices/dirigera/hooks/__tests__/useDirigeraData.test.ts --silent
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'/api/dirigera/" app/components/devices/dirigera/hooks/useDirigeraData.ts` returns zero matches.
    - `grep -n "'/api/dirigera/" app/components/devices/dirigera/hooks/useDirigeraFullData.ts` returns zero matches.
    - `grep -n "'/api/dirigera/" app/components/devices/dirigera/hooks/__tests__/useDirigeraData.test.ts` returns zero matches.
    - `grep -c "'/api/v1/dirigera/health'" app/components/devices/dirigera/hooks/useDirigeraData.ts` is `2` (two health fetch sites).
    - `grep -q "'/api/v1/dirigera/sensors/summary'" app/components/devices/dirigera/hooks/useDirigeraData.ts` succeeds.
    - FILTER_ENDPOINTS map in `useDirigeraFullData.ts` contains all three `/api/v1/dirigera/sensors*` paths (`all`, `contact`, `motion`) and the health fetch URL is `/api/v1/dirigera/health`.
    - `subscribe('dirigera'` and `unsubscribe('dirigera'` calls still present in `useDirigeraData.ts` (WS topic untouched).
    - `npx jest app/components/devices/dirigera/hooks/__tests__/useDirigeraData.test.ts --silent` exits 0.
  </acceptance_criteria>
  <done>Both hooks fetch only `/api/v1/dirigera/*` URLs; the `useDirigeraData` test asserts the new URLs and passes; the WS subscription path is byte-for-byte identical.</done>
</task>

<task type="auto">
  <name>Task 4: Add /dirigera Playwright smoke test case (closes RESEARCH Q2 gap)</name>
  <files>tests/smoke/page-loads.spec.ts</files>
  <read_first>
    - tests/smoke/page-loads.spec.ts (current file — find the `Device Pages` describe block; the `/raspi` test on lines ~79-88 is the closest analog because `/dirigera` also uses a skeleton guard)
    - app/dirigera/page.tsx (confirm the page renders a `main` element and a skeleton during loading — no data-driven heading guaranteed in test env)
    - .planning/phases/169-dirigera-frontend-cutover/169-PATTERNS.md (section "`tests/smoke/page-loads.spec.ts`")
    - .planning/phases/169-dirigera-frontend-cutover/169-RESEARCH.md (Open Question Q2 — Playwright smoke assertion for new panels)
  </read_first>
  <action>
    Inside the `test.describe('Device Pages', () => {` block in `tests/smoke/page-loads.spec.ts`, append a new test case after the existing `/raspi` test (which ends around line 88). Use `collectConsoleErrors` and the same pattern as `/raspi`:

    ```typescript
    test('/dirigera loads and shows data', async ({ page }) => {
      // E2E-10: DIRIGERA hub page loads
      const { errors, cleanup } = collectConsoleErrors(page);
      await page.goto('/dirigera');
      await page.waitForLoadState('networkidle');
      await expect(page.locator('main')).toBeAttached({ timeout: 15000 });
      cleanup();
      expect(errors, `Console errors on /dirigera: ${errors.join(', ')}`).toHaveLength(0);
    });
    ```

    Do NOT add heading-visibility assertions — the page shows a skeleton while `loading && !data`, so asserting the `DIRIGERA` heading is flaky in test env (matches `/raspi` rationale). Wave 2 will extend this test with panel-heading assertions for the three new panels.
  </action>
  <verify>
    <automated>
      grep -q "'/dirigera loads and shows data'" tests/smoke/page-loads.spec.ts && \
      grep -q "page\\.goto\\('/dirigera'\\)" tests/smoke/page-loads.spec.ts && \
      grep -q "Console errors on /dirigera" tests/smoke/page-loads.spec.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "page.goto('/dirigera')" tests/smoke/page-loads.spec.ts` returns `1`.
    - `grep -q "'/dirigera loads and shows data'" tests/smoke/page-loads.spec.ts` succeeds.
    - The new test is located inside the `Device Pages` describe (verified by its position between `/raspi` test and the `Support Pages` describe when reading the file).
  </acceptance_criteria>
  <done>`tests/smoke/page-loads.spec.ts` includes a `/dirigera` test case that matches the `/raspi` pattern (skeleton-tolerant `main` attached, console-error collector, 15s timeout).</done>
</task>

<task type="auto">
  <name>Task 5: Full Jest suite + Playwright smoke green (wave exit gate)</name>
  <files>(verification only — no code changes)</files>
  <read_first>
    - package.json (confirm `npm test` script and Playwright availability)
    - playwright.config.ts (confirm project configuration and base URL)
    - .planning/phases/169-dirigera-frontend-cutover/169-RESEARCH.md (§Validation Architecture — sampling rate rules)
  </read_first>
  <action>
    Run the full Jest suite and the Playwright page-loads smoke spec. If any suite fails, triage:
    - If the failure is inside a DIRIGERA file modified in Tasks 1-4, fix immediately within this wave.
    - If the failure is in an unrelated suite that was already red at phase start, document under "Pre-existing failures (not caused by this plan)" in the SUMMARY (per D-13) but do NOT attempt to fix in Wave 1.

    Required commands:
    - `npm test -- --testPathPattern="dirigera" --silent` — quick dirigera-focused gate
    - `npm test -- --silent` — full suite gate
    - `npx playwright test tests/smoke/page-loads.spec.ts` — Playwright smoke (or equivalent project-configured command if Playwright requires a dev server; the smoke suite already has its own webServer config if applicable)
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="dirigera" --silent && npm test -- --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern="dirigera" --silent` exits 0.
    - `npm test -- --silent` exits 0 OR any failures are documented as pre-existing (not touching files this plan modified).
    - `npx playwright test tests/smoke/page-loads.spec.ts` exits 0 and the `/dirigera` test case appears in the report.
    - `[ $(find app/api/v1/dirigera -name route.ts | wc -l) -eq 8 ]` — v1 route count intact.
    - `! grep -rn "'/api/dirigera/" app/components/devices/dirigera/hooks app/components/devices/dirigera/hooks/__tests__ --include='*.ts' --include='*.tsx'` — no legacy URLs remain in the migrated hooks (legacy route tree still intact; Wave 3 handles deletion).
  </acceptance_criteria>
  <done>All Jest tests pass (or pre-existing failures documented); Playwright `page-loads.spec.ts` passes including the new `/dirigera` test; no legacy `/api/dirigera/` URLs remain in any hook file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js API (`/api/v1/dirigera/*`) | Same-origin GET requests carry Auth0 session cookie; no untrusted user input beyond absent query params |
| Next.js API → HA proxy (`haGet`) | Server-side authenticated call; inherits Auth0/API-Key controls from Phase 163 baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-01 | Information Disclosure | 5 new v1 routes (`health`, `sensors`, `sensors/summary`, `sensors/contact`, `sensors/motion`) | mitigate | `withAuthAndErrorHandler` wraps every handler — returns 401 on missing Auth0 session (verified by Task 2 tests) |
| T-169-02 | Tampering | Hook fetch URL strings | accept | Pure internal same-origin prefix swap; no user-controlled URL construction; WS subscribe topic string unchanged (Pitfall 2 explicitly called out in Task 3) |
| T-169-03 | Denial of Service | 5 new routes | accept | No new query-param surface (all 5 are no-params); HA proxy owns upstream rate limiting; no amplification over legacy equivalent |

**Summary:** No new threat surface — inherits existing DIRIGERA/HA proxy controls. All 5 new routes reuse `withAuthAndErrorHandler`; URL swap is same-origin and deterministic.
</threat_model>

<verification>
Wave-exit gates (must all be green before Wave 2 starts):
1. `find app/api/v1/dirigera -name route.ts | wc -l` returns `8` (3 existing + 5 new).
2. No `'/api/dirigera/'` string literal in `app/components/devices/dirigera/hooks/useDirigeraData.ts`, `useDirigeraFullData.ts`, or `__tests__/useDirigeraData.test.ts`.
3. `subscribe('dirigera', ...)` / `unsubscribe('dirigera', ...)` WS topic calls in `useDirigeraData.ts` unchanged.
4. `npm test -- --silent` passes (or pre-existing failures documented).
5. `npx playwright test tests/smoke/page-loads.spec.ts` passes including the new `/dirigera` case.
</verification>

<success_criteria>
- 5 new v1 wrapper routes exist at `/api/v1/dirigera/{health,sensors,sensors/summary,sensors/contact,sensors/motion}` with exact-envelope parity to legacy.
- 5 co-located Jest tests exist, each asserting 401 + 200, and all pass.
- `useDirigeraData.ts` and `useDirigeraFullData.ts` fetch only `/api/v1/dirigera/*` URLs.
- `useDirigeraData.test.ts` assertions reference `/api/v1/dirigera/*` URLs and the suite passes.
- `tests/smoke/page-loads.spec.ts` contains a `/dirigera` test case that passes.
- WebSocket subscription path in `useDirigeraData.ts` byte-for-byte unchanged.
- Full Jest + Playwright smoke green (pre-existing unrelated failures documented if any).
</success_criteria>

<output>
After completion, create `.planning/phases/169-dirigera-frontend-cutover/169-01-SUMMARY.md` documenting:
- 5 new v1 route files + 5 new Jest tests created.
- 3 hook/test files with URL-prefix swaps applied (exact line-number diffs noted).
- New `/dirigera` smoke test case added to `tests/smoke/page-loads.spec.ts`.
- Full Jest + Playwright run results.
- v1-intact count verification (`wc -l == 8`) as Wave 3 will re-check this.
</output>
