---
phase: 169-dirigera-frontend-cutover
plan: 03
type: execute
wave: 3
depends_on: [169-01, 169-02]
requirements: []
files_modified: []
files_created: []
files_deleted:
  - app/api/dirigera/health/route.ts
  - app/api/dirigera/sensors/route.ts
  - app/api/dirigera/sensors/summary/route.ts
  - app/api/dirigera/sensors/contact/route.ts
  - app/api/dirigera/sensors/motion/route.ts
autonomous: true
must_haves:
  truths:
    - "The entire app/api/dirigera/ directory tree is deleted (all 5 route.ts files plus any other files inside)"
    - "A repo-wide grep shows zero /api/dirigera/ string matches under app/, lib/, types/ (excluding /api/v1/dirigera/, lib/version.ts, and .planning/)"
    - "find app/api/v1/dirigera -name route.ts | wc -l returns 8 both before and after the deletion (pre- and post- safety gates)"
    - "Full Jest suite green after deletion"
    - "Playwright /dirigera smoke green after deletion"
  artifacts:
    - path: "app/api/dirigera"
      provides: "DELETED — the entire legacy tree no longer exists"
      contains: "N/A (directory should not exist post-wave)"
  key_links:
    - from: "app/api/v1/dirigera tree"
      to: "8 route.ts files total (health, sensors, sensors/summary, sensors/contact, sensors/motion, history, stats, telemetry)"
      via: "canonical v1 surface matches docs/api/dirigera.md spec"
      pattern: "find app/api/v1/dirigera -name route.ts"
---

<objective>
Destructively close the v1 cutover: delete the legacy `app/api/dirigera/` tree (5 `route.ts` files), prove via repo-wide grep that no production code references legacy paths, and re-verify the v1 surface still has exactly 8 `route.ts` files (per D-11, D-12). This wave has two `[BLOCKING]` safety gates — one pre-deletion, one post-deletion — that prevent catastrophic mis-deletion (e.g., accidentally removing the v1 tree instead of the legacy tree).

Purpose: Close ROADMAP Phase 169 Success Criterion #2 ("Zero `/api/dirigera/` references outside debug/archived paths") and prevent future drift where new code accidentally adds a legacy path.

Output: The `app/api/dirigera/` directory no longer exists; no production TypeScript file references `/api/dirigera/` (legacy) except `lib/version.ts` changelog and `.planning/` archival docs; full Jest + Playwright smoke green.

**This wave satisfies zero DIR-XX requirements** — it is a cleanup wave. DIR-01/02/03 are closed by Wave 2 (Plan 169-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/169-dirigera-frontend-cutover/169-CONTEXT.md
@.planning/phases/169-dirigera-frontend-cutover/169-RESEARCH.md
@.planning/phases/169-dirigera-frontend-cutover/169-PATTERNS.md
@.planning/phases/169-dirigera-frontend-cutover/169-01-PLAN.md
@.planning/phases/169-dirigera-frontend-cutover/169-02-PLAN.md
@.planning/phases/168-netatmo-frontend-cutover/168-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Pre-deletion safety gate — verify v1 tree has 8 routes and legacy tree has exactly 5 files</name>
  <files>(verification only — no code changes)</files>
  <read_first>
    - .planning/phases/169-dirigera-frontend-cutover/169-CONTEXT.md (D-12 — safety gate spec)
    - .planning/phases/169-dirigera-frontend-cutover/169-RESEARCH.md (Pattern 6 — Legacy Deletion and Grep Sweep; Pitfall 4 — delete before consumers ready)
    - .planning/phases/169-dirigera-frontend-cutover/169-01-PLAN.md (must confirm Wave 1 landed the 5 new v1 routes)
  </read_first>
  <action>
    [BLOCKING] Do NOT proceed to Task 2 unless ALL checks below return the exact expected value. If any check fails, STOP and report the failure; do not attempt any `rm` operation.

    Required pre-deletion assertions:

    1. **V1 tree has exactly 8 route.ts files** (3 Phase-163 + 5 Wave-1):
       ```bash
       [ $(find app/api/v1/dirigera -name route.ts -type f | wc -l) -eq 8 ]
       ```
       If this returns non-zero exit, Wave 1 did not land all 5 wrappers — abort and revisit Plan 169-01.

    2. **Each expected v1 route file exists individually** (defense in depth — `wc -l == 8` could in theory be satisfied by duplicates):
       ```bash
       test -f app/api/v1/dirigera/health/route.ts && \
       test -f app/api/v1/dirigera/sensors/route.ts && \
       test -f app/api/v1/dirigera/sensors/summary/route.ts && \
       test -f app/api/v1/dirigera/sensors/contact/route.ts && \
       test -f app/api/v1/dirigera/sensors/motion/route.ts && \
       test -f app/api/v1/dirigera/history/route.ts && \
       test -f app/api/v1/dirigera/stats/route.ts && \
       test -f app/api/v1/dirigera/telemetry/route.ts
       ```

    3. **Legacy tree contains exactly 5 route.ts files** (what we are about to delete):
       ```bash
       [ $(find app/api/dirigera -name route.ts -type f | wc -l) -eq 5 ]
       ```

    4. **No production consumer still points at legacy paths** (Wave 1 + Wave 2 already swapped hooks; this is the final check before deletion):
       ```bash
       ! grep -rn "'/api/dirigera/" app/components app/dirigera --include='*.ts' --include='*.tsx'
       ```
       Expected: zero matches.

    5. **All Jest DIRIGERA suites green** (gate against deleting while tests are red):
       ```bash
       npm test -- --testPathPattern="dirigera" --silent
       ```
       Expected: exit 0.
  </action>
  <verify>
    <automated>
      [ $(find app/api/v1/dirigera -name route.ts -type f | wc -l) -eq 8 ] && \
      test -f app/api/v1/dirigera/health/route.ts && \
      test -f app/api/v1/dirigera/sensors/route.ts && \
      test -f app/api/v1/dirigera/sensors/summary/route.ts && \
      test -f app/api/v1/dirigera/sensors/contact/route.ts && \
      test -f app/api/v1/dirigera/sensors/motion/route.ts && \
      test -f app/api/v1/dirigera/history/route.ts && \
      test -f app/api/v1/dirigera/stats/route.ts && \
      test -f app/api/v1/dirigera/telemetry/route.ts && \
      [ $(find app/api/dirigera -name route.ts -type f | wc -l) -eq 5 ] && \
      ! grep -rn "'/api/dirigera/" app/components app/dirigera --include='*.ts' --include='*.tsx' && \
      npm test -- --testPathPattern="dirigera" --silent
    </automated>
  </verify>
  <acceptance_criteria>
    - `find app/api/v1/dirigera -name route.ts -type f | wc -l` outputs exactly `8`.
    - All 8 expected v1 `route.ts` files exist (verified by `test -f` for each).
    - `find app/api/dirigera -name route.ts -type f | wc -l` outputs exactly `5` (the 5 legacy routes to be deleted).
    - Zero `'/api/dirigera/` string matches under `app/components` and `app/dirigera`.
    - DIRIGERA-focused Jest subset exits 0.
    - If any assertion fails, the task reports the failure and STOPS — no deletion proceeds.
  </acceptance_criteria>
  <done>All 5 pre-deletion safety checks pass. Proceeding to Task 2 is safe.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the legacy app/api/dirigera/ tree (D-11)</name>
  <files>
    app/api/dirigera/health/route.ts,
    app/api/dirigera/sensors/route.ts,
    app/api/dirigera/sensors/summary/route.ts,
    app/api/dirigera/sensors/contact/route.ts,
    app/api/dirigera/sensors/motion/route.ts
  </files>
  <read_first>
    - .planning/phases/169-dirigera-frontend-cutover/169-CONTEXT.md (D-11 — deletion scope)
    - .planning/phases/169-dirigera-frontend-cutover/169-RESEARCH.md (Pattern 6 — Legacy Deletion command)
    - app/api/dirigera (confirm exact tree contents with `ls -la app/api/dirigera` before deletion)
  </read_first>
  <action>
    Atomically remove the entire `app/api/dirigera/` subtree. Use `git rm -r` so the deletion is staged (the executor agent's commit step will pick it up cleanly and the git history shows these files as deleted, not simply missing):

    ```bash
    git rm -r app/api/dirigera
    ```

    If `git rm` fails because some files are untracked (unlikely — all 5 route files are tracked), fall back to `rm -rf app/api/dirigera` for any remainder.

    Do NOT delete:
    - `app/api/v1/dirigera/` — the canonical surface stays intact.
    - `lib/dirigera/` — proxy layer still used by v1 routes.
    - `types/dirigeraProxy.ts` — types still imported by hooks and v1 routes.
    - Any `.planning/` file — archival docs retain legacy references by design.
    - `lib/version.ts` — historical changelog retains legacy path strings by design.

    After the removal, the directory `app/api/dirigera` must not exist on disk.
  </action>
  <verify>
    <automated>
      ! test -d app/api/dirigera && \
      ! test -f app/api/dirigera/health/route.ts && \
      ! test -f app/api/dirigera/sensors/route.ts && \
      ! test -f app/api/dirigera/sensors/summary/route.ts && \
      ! test -f app/api/dirigera/sensors/contact/route.ts && \
      ! test -f app/api/dirigera/sensors/motion/route.ts && \
      test -d app/api/v1/dirigera && \
      test -d lib/dirigera && \
      test -f types/dirigeraProxy.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -d app/api/dirigera` fails (directory does not exist).
    - None of the 5 listed legacy route.ts files exists.
    - `app/api/v1/dirigera` still exists as a directory.
    - `lib/dirigera` still exists (proxy layer intact).
    - `types/dirigeraProxy.ts` still exists (types intact).
    - `git status` shows the 5 legacy files as `deleted` (either staged via `git rm` or in the worktree).
  </acceptance_criteria>
  <done>The legacy tree is gone, all canonical dependencies (v1 routes, proxy layer, types) are intact.</done>
</task>

<task type="auto">
  <name>Task 3: Repo-wide grep sweep — prove zero residual /api/dirigera/ references in production source</name>
  <files>(verification only)</files>
  <read_first>
    - .planning/phases/169-dirigera-frontend-cutover/169-CONTEXT.md (D-11 — grep sweep scope; excludes `lib/version.ts` and `.planning/`)
    - .planning/phases/169-dirigera-frontend-cutover/169-RESEARCH.md (Pattern 6 — Post-deletion grep sweep; Note about `.planning/` exclusion)
  </read_first>
  <action>
    Run the canonical grep sweep to prove production source is clean. Exclusions (by design):
    - `/api/v1/dirigera/` — canonical path, not a legacy reference.
    - `lib/version.ts` — historical changelog lists legacy paths intentionally.
    - `.planning/` — archival docs retain legacy references by design.
    - `tests/` — legacy strings may appear in archived specs; check separately (should also be zero now).
    - Top-level `docs/` — docs/api/dirigera.md already documents only v1 paths but `docs/api/README.md` and changelog files may retain legacy mentions for historical context. Warn but do NOT fail on docs matches.

    Canonical sweep (hard fail — production source tree):
    ```bash
    grep -rn "/api/dirigera/" app/ lib/ types/ --include='*.ts' --include='*.tsx' \
      | grep -v "/api/v1/dirigera/" \
      | grep -v "lib/version.ts"
    ```
    Expected: zero matching lines (command exit code still 0 since grep finds no matches; the inner `grep -v` filters leave an empty pipeline).

    Handle the grep-exit-code quirk explicitly: `grep` returns 1 when no matches, which causes `set -e` scripts to fail. Use this idiom to convert "zero matches" into a successful assertion:
    ```bash
    MATCHES=$(grep -rn "/api/dirigera/" app/ lib/ types/ --include='*.ts' --include='*.tsx' 2>/dev/null \
      | grep -v "/api/v1/dirigera/" \
      | grep -v "lib/version.ts" || true)
    if [ -n "$MATCHES" ]; then
      echo "FAIL: residual legacy references found:"
      echo "$MATCHES"
      exit 1
    fi
    ```

    Additional sweep — test files:
    ```bash
    MATCHES=$(grep -rn "/api/dirigera/" tests/ --include='*.ts' --include='*.tsx' 2>/dev/null \
      | grep -v "/api/v1/dirigera/" || true)
    if [ -n "$MATCHES" ]; then
      echo "FAIL: residual legacy references in tests/:"
      echo "$MATCHES"
      exit 1
    fi
    ```
    Expected: zero matching lines.

    Log any matches found to the SUMMARY. If everything is clean, record the command runs and zero-match confirmation.
  </action>
  <verify>
    <automated>
      MATCHES=$(grep -rn "/api/dirigera/" app/ lib/ types/ --include='*.ts' --include='*.tsx' 2>/dev/null | grep -v "/api/v1/dirigera/" | grep -v "lib/version.ts" || true); if [ -n "$MATCHES" ]; then echo "FAIL: $MATCHES"; exit 1; fi && \
      MATCHES_TESTS=$(grep -rn "/api/dirigera/" tests/ --include='*.ts' --include='*.tsx' 2>/dev/null | grep -v "/api/v1/dirigera/" || true); if [ -n "$MATCHES_TESTS" ]; then echo "FAIL tests: $MATCHES_TESTS"; exit 1; fi
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "/api/dirigera/" app/ lib/ types/ --include='*.ts' --include='*.tsx' | grep -v "/api/v1/dirigera/" | grep -v "lib/version.ts"` produces zero output lines.
    - `grep -rn "/api/dirigera/" tests/ --include='*.ts' --include='*.tsx' | grep -v "/api/v1/dirigera/"` produces zero output lines.
    - Any `lib/version.ts` legacy references are acceptable (historical changelog, excluded by design).
    - Any `.planning/**` legacy references are acceptable (archival).
  </acceptance_criteria>
  <done>Repo-wide grep sweep returns zero residual legacy-path references in production source tree (app/, lib/, types/) and tests/.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Post-deletion safety gate — re-verify v1 tree still has 8 routes</name>
  <files>(verification only)</files>
  <read_first>
    - .planning/phases/169-dirigera-frontend-cutover/169-CONTEXT.md (D-12 — post-deletion gate)
  </read_first>
  <action>
    [BLOCKING] Repeat the v1-intact count check AFTER the deletion. This is the catch-all gate that would fire if Task 2 accidentally deleted from the wrong path (e.g., `rm -rf app/api/v1/dirigera` instead of `rm -rf app/api/dirigera`). Same command as Task 1's first check:

    ```bash
    [ $(find app/api/v1/dirigera -name route.ts -type f | wc -l) -eq 8 ]
    ```

    If this returns non-zero, STOP immediately, restore the v1 tree via `git checkout` (files should still be tracked in HEAD), and investigate. Do NOT commit until the count is 8.

    Additionally assert each individual v1 file still exists (same as Task 1 check #2) to defend against duplicated/misnamed files somehow satisfying the count of 8:
    ```bash
    test -f app/api/v1/dirigera/health/route.ts && \
    test -f app/api/v1/dirigera/sensors/route.ts && \
    test -f app/api/v1/dirigera/sensors/summary/route.ts && \
    test -f app/api/v1/dirigera/sensors/contact/route.ts && \
    test -f app/api/v1/dirigera/sensors/motion/route.ts && \
    test -f app/api/v1/dirigera/history/route.ts && \
    test -f app/api/v1/dirigera/stats/route.ts && \
    test -f app/api/v1/dirigera/telemetry/route.ts
    ```
  </action>
  <verify>
    <automated>
      [ $(find app/api/v1/dirigera -name route.ts -type f | wc -l) -eq 8 ] && \
      test -f app/api/v1/dirigera/health/route.ts && \
      test -f app/api/v1/dirigera/sensors/route.ts && \
      test -f app/api/v1/dirigera/sensors/summary/route.ts && \
      test -f app/api/v1/dirigera/sensors/contact/route.ts && \
      test -f app/api/v1/dirigera/sensors/motion/route.ts && \
      test -f app/api/v1/dirigera/history/route.ts && \
      test -f app/api/v1/dirigera/stats/route.ts && \
      test -f app/api/v1/dirigera/telemetry/route.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `find app/api/v1/dirigera -name route.ts -type f | wc -l` outputs `8` (unchanged from pre-deletion gate).
    - All 8 expected v1 route.ts files still exist individually.
    - If any assertion fails, deletion is rolled back and the wave STOPS.
  </acceptance_criteria>
  <done>V1 surface count confirmed intact post-deletion. The wave is safe to commit.</done>
</task>

<task type="auto">
  <name>Task 5: Full Jest + Playwright smoke green (phase exit gate)</name>
  <files>(verification only)</files>
  <read_first>
    - package.json
    - playwright.config.ts
    - .planning/phases/169-dirigera-frontend-cutover/169-RESEARCH.md (§Validation Architecture — full phase gate)
  </read_first>
  <action>
    Run the full Jest suite and Playwright page-loads smoke to confirm that deletion did not break any consumer. Expected outcome: everything stays green (because Wave 1 + Wave 2 already moved all consumers to v1).

    Commands:
    - `npm test -- --silent` — full Jest suite.
    - `npx playwright test tests/smoke/page-loads.spec.ts` — Playwright smoke (confirms `/dirigera` still loads with all 3 new panels after legacy deletion).

    Triage:
    - Any failure referencing a missing `/api/dirigera/` path → regression introduced by deletion. Investigate whether a consumer was missed in Wave 1 (should not happen given the pre-deletion grep was clean).
    - Unrelated pre-existing failures → document in SUMMARY, do not fix here.
  </action>
  <verify>
    <automated>npm test -- --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --silent` exits 0 (or any failures are pre-existing and unrelated to DIRIGERA).
    - `npx playwright test tests/smoke/page-loads.spec.ts` exits 0.
    - `/dirigera` Playwright case still asserts `Statistiche`, `Eventi recenti`, `Telemetria` headings visible (no regression).
    - No DIRIGERA-related Jest suite reports a failure. Specifically: `useDirigeraData.test.ts`, `useDirigeraHistory.test.ts`, `useDirigeraStats.test.ts`, `useDirigeraTelemetry.test.ts`, and all 5 new v1 route tests from Wave 1 all pass.
  </acceptance_criteria>
  <done>Phase 169 exit gate green: legacy tree deleted, full Jest + Playwright smoke pass, no residual legacy references in production source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This wave only removes files; no new request surface, no new auth paths, no new user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-08 | Availability / self-inflicted DoS | Wrong-path `rm -rf` (e.g., deleting `app/api/v1/dirigera` instead of `app/api/dirigera`) | mitigate | Pre-deletion and post-deletion `[BLOCKING]` safety gates (Tasks 1 and 4) assert `wc -l == 8` v1 route count and each v1 file exists individually. Destruction only proceeds after pre-gate green; post-gate catches any mis-deletion before commit |
| T-169-09 | Stale attack surface | Legacy `/api/dirigera/*` routes remaining in production after v1 migration | mitigate | Deletion in Task 2 removes the entire tree; Task 3 grep sweep proves no residual references can re-animate the surface |

**Summary:** No new threat surface — this wave destroys code only. The two `[BLOCKING]` safety gates (pre- and post-deletion) are the sole mitigation for accidental self-inflicted damage, and Phase 168 Plan-03 proved the protocol works cheaply and reliably.
</threat_model>

<verification>
Phase exit gates (must all be green to close Phase 169):
1. `test -d app/api/dirigera` fails (directory removed).
2. `find app/api/v1/dirigera -name route.ts -type f | wc -l` returns `8`.
3. Zero `/api/dirigera/` string matches in `app/`, `lib/`, `types/`, `tests/` (excluding `/api/v1/dirigera/`, `lib/version.ts`, and `.planning/`).
4. `npm test -- --silent` passes (or documented pre-existing failures only).
5. `npx playwright test tests/smoke/page-loads.spec.ts` passes with `/dirigera` case asserting all 3 new panel headings.
6. No DIRIGERA-related Jest suite fails.
</verification>

<success_criteria>
- Legacy `app/api/dirigera/` tree deleted (5 route.ts files gone).
- V1 tree unchanged (8 route.ts files still present).
- Zero residual `/api/dirigera/` production references.
- Full Jest + Playwright smoke green.
- Phase 169 closed: DIRIGERA is now the last v19.0 provider on the canonical v1 surface with zero legacy paths.
</success_criteria>

<output>
After completion, create `.planning/phases/169-dirigera-frontend-cutover/169-03-SUMMARY.md` documenting:
- The 5 files deleted (with pre-deletion `ls` snapshot).
- Pre- and post-deletion `[BLOCKING]` safety gate outputs (both `wc -l == 8`).
- Repo-wide grep sweep output (expected: empty).
- Full Jest + Playwright smoke results.
- Git status / commit for the deletion.
- Phase 169 closeout note: all DIR-XX requirements closed by Wave 2 (169-02); Wave 3 is the cleanup closure.
</output>
