---
phase: 172-fritzbox-v1-path-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/fritzbox/**
  - app/api/v1/fritzbox/**
autonomous: true
requirements: [FRITZ-01, FRITZ-02, FRITZ-03, FRITZ-04, FRITZ-05, FRITZ-06, FRITZ-07]
tags: [fritzbox, path-migration, v1, refactor]

must_haves:
  truths:
    - "Directory app/api/fritzbox/ no longer exists"
    - "Directory app/api/v1/fritzbox/ exists with every original subtree"
    - "All 20 co-located route Jest tests execute green from their new location"
    - "Git history preserved for all 48 moved files (`git log --follow` works)"
  artifacts:
    - path: "app/api/v1/fritzbox/telephony/dect/route.ts"
      provides: "FRITZ-01 DECT handsets route at canonical path"
    - path: "app/api/v1/fritzbox/telephony/calls/route.ts"
      provides: "FRITZ-02 calls history route at canonical path"
    - path: "app/api/v1/fritzbox/telephony/tam/route.ts"
      provides: "FRITZ-03 TAM route at canonical path"
    - path: "app/api/v1/fritzbox/history/bandwidth/route.ts"
      provides: "FRITZ-04 bandwidth history route at canonical path"
    - path: "app/api/v1/fritzbox/history/devices/route.ts"
      provides: "FRITZ-05 device presence history route at canonical path"
    - path: "app/api/v1/fritzbox/history/device-events/route.ts"
      provides: "FRITZ-06 device events route at canonical path"
    - path: "app/api/v1/fritzbox/service-discovery/route.ts"
      provides: "FRITZ-07 service discovery route at canonical path"
  key_links:
    - from: "git mv app/api/fritzbox"
      to: "app/api/v1/fritzbox"
      via: "atomic directory rename"
      pattern: "renamed: app/api/fritzbox/.* -> app/api/v1/fritzbox/.*"
---

<objective>
Atomically move every file under `app/api/fritzbox/**` to `app/api/v1/fritzbox/**` using `git mv`, preserving git history for all 28 production route.ts files + 20 co-located test files (48 total). This creates the canonical v1 Fritz!Box surface that REQUIREMENTS.md FRITZ-01..07 specify. After this plan, the app will be temporarily broken (consumers still reference `/api/fritzbox/*`) — Plan 02 restores it by sweeping consumer URL strings.

Purpose: Close the v19.0 audit FRITZ namespace mismatch — REQUIREMENTS.md specifies `/api/v1/fritzbox/*` but the as-built routes live at `/api/fritzbox/*`. This is the route-side half of the migration.

Output: 28 production routes + 20 co-located tests relocated under `/api/v1/fritzbox/**` with a single `git mv` commit preserving history.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-fritzbox-v1-path-migration/172-RESEARCH.md
@.planning/phases/172-fritzbox-v1-path-migration/172-VALIDATION.md
@CLAUDE.md

<prior_art>
Phase 164 (thermorossi) and Phase 169 (DIRIGERA) removed legacy trees AFTER v1 routes existed. Phase 172 is different: there is no existing `app/api/v1/fritzbox/` — `git mv` creates the v1 surface in one atomic operation. All 28 route.ts files use ONLY `@/lib/...` absolute imports (zero relative imports), so the move produces zero broken imports. Co-located `__tests__/route.test.ts` use `from '../route'` which remains valid because the relative relationship travels with the subtree.
</prior_art>

<interfaces>
All 28 route files declare:
```typescript
export const dynamic = 'force-dynamic';
```
All 28 route files import ONLY from `@/lib/...` absolute paths (zero relative imports in production routes — verified in RESEARCH.md §Exhaustive File Inventory).

Co-located test pattern (20 files):
```typescript
// app/api/fritzbox/<sub>/__tests__/route.test.ts
import { GET } from '../route';  // or POST, PUT, DELETE as applicable
// This relative path stays valid after `git mv` of the entire tree.
```

Aggregate test files (2):
- `app/api/fritzbox/__tests__/devices-events.test.ts` → imports `../devices/route`
- `app/api/fritzbox/__tests__/history.test.ts` → imports `../history/route`
Both remain valid after atomic move.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic git mv of entire Fritz!Box route tree</name>
  <files>app/api/fritzbox/ (source, will disappear), app/api/v1/fritzbox/ (destination, will be created)</files>
  <read_first>
    - .planning/phases/172-fritzbox-v1-path-migration/172-RESEARCH.md §Exhaustive File Inventory (file list)
    - .planning/phases/172-fritzbox-v1-path-migration/172-RESEARCH.md §Migration Strategy (git mv rationale)
    - .planning/phases/172-fritzbox-v1-path-migration/172-RESEARCH.md §Code Examples Pattern 1 (exact commands)
    - CLAUDE.md Rule 8 (scoped test subsets only)
  </read_first>
  <action>
    Execute the single atomic rename that creates the v1 surface from the legacy tree. Exact commands (run sequentially in the repo root):

    ```bash
    # Verify source exists and v1 target does NOT already exist
    test -d app/api/fritzbox || { echo "FATAL: app/api/fritzbox missing"; exit 1; }
    test ! -d app/api/v1/fritzbox || { echo "FATAL: app/api/v1/fritzbox already exists"; exit 1; }

    # Ensure v1 parent directory exists (it does — confirmed: app/api/v1/ already contains automations, devices, dirigera, hue, netatmo, sonos, thermorossi)
    test -d app/api/v1 || { echo "FATAL: app/api/v1 parent missing"; exit 1; }

    # Atomic move — Next.js App Router discovers routes by filesystem structure;
    # all 28 route.ts + 20 __tests__/ files travel together as one rename.
    git mv app/api/fritzbox app/api/v1/fritzbox

    # Verify staged as rename (not delete+add — git should detect 48 renames)
    git status --short | head -60
    ```

    DO NOT modify any file contents. DO NOT run `sed` or edit imports — all production routes use `@/lib/...` absolute imports (verified in RESEARCH.md), and co-located tests use `from '../route'` relative imports that remain valid because the relative relationship is preserved atomically.

    DO NOT touch consumer files (hooks, pages, debug panels, hook tests) — those are Plan 02's scope. After this commit, `/api/fritzbox/*` endpoints will return 404; `/api/v1/fritzbox/*` endpoints will serve the same handlers from the new path. This is expected and Plan 02 restores consumer alignment immediately.

    Commit with message:
    ```
    refactor(172-01): move app/api/fritzbox to app/api/v1/fritzbox (28 routes + 20 tests)

    Atomic git mv preserves history for all 48 files. Next.js App Router
    discovers the new route surface automatically. Consumers still reference
    /api/fritzbox/* and will be retargeted in Plan 172-02.

    Closes v19.0 audit FRITZ namespace mismatch (route-side).
    Requirements: FRITZ-01..07
    ```

    Do NOT push. The user runs `git push` manually per CLAUDE.md Rule 7.
  </action>
  <verify>
    <automated>test ! -d app/api/fritzbox && test -d app/api/v1/fritzbox/telephony/dect && test -f app/api/v1/fritzbox/telephony/dect/route.ts && test -f app/api/v1/fritzbox/telephony/dect/__tests__/route.test.ts && test -f app/api/v1/fritzbox/service-discovery/route.ts && test -f app/api/v1/fritzbox/history/bandwidth/route.ts && test -f app/api/v1/fritzbox/history/devices/route.ts && test -f app/api/v1/fritzbox/history/device-events/route.ts && test -f app/api/v1/fritzbox/__tests__/history.test.ts && test -f app/api/v1/fritzbox/__tests__/devices-events.test.ts && git log --follow --oneline app/api/v1/fritzbox/telephony/dect/route.ts | wc -l | awk '{exit !($1>=2)}'</automated>
  </verify>
  <acceptance_criteria>
    - `ls app/api/fritzbox 2>/dev/null` exits non-zero (directory gone)
    - `ls app/api/v1/fritzbox` lists: bandwidth, bandwidth-history, budget-stats, category-override, debug, devices, health, history, network, service-discovery, system, telephony, vendor-lookup, wan, wifi (14 entries + __tests__)
    - All 28 route.ts files exist under the new tree (spot check: `find app/api/v1/fritzbox -name 'route.ts' | wc -l` returns 28)
    - All 20 co-located test files exist under the new tree (`find app/api/v1/fritzbox -path '*/__tests__/*.test.ts' | wc -l` returns 20)
    - `git log --follow app/api/v1/fritzbox/telephony/dect/route.ts` shows pre-rename history (history preserved)
    - `git status --short` shows 48 `R` (renamed) entries, zero `D` (deleted) or `A` (added) for fritzbox files
    - HEAD commit message starts with `refactor(172-01): move app/api/fritzbox to app/api/v1/fritzbox`
    - No file contents modified (`git log -1 --stat HEAD` shows 0 lines changed, only renames)
  </acceptance_criteria>
  <done>
    Atomic rename committed. v1 Fritz!Box surface exists with preserved history. Consumers not yet retargeted (Plan 02 scope).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify co-located route tests execute from new location</name>
  <files>(read-only verification — no writes)</files>
  <read_first>
    - .planning/phases/172-fritzbox-v1-path-migration/172-RESEARCH.md §Validation Architecture (test map)
    - .planning/phases/172-fritzbox-v1-path-migration/172-VALIDATION.md §Per-Task Verification Map row 172-01-*
  </read_first>
  <action>
    Run the scoped Jest subset against the moved route tree to prove that all co-located tests still import their sibling `route.ts` correctly after the rename. This catches any case where a test file used an unusual import path that broke during the move.

    Command (project rule §8 — scoped subset only, never bare `npm test`):
    ```bash
    npm test -- app/api/v1/fritzbox/
    ```

    If any test fails with an import-resolution error, the rename missed an import rewrite (should not happen per RESEARCH.md inventory, but this is the safety net). Do not fix by editing — re-read the failing test's import paths and report the exact file + line to determine whether an exception exists that was not captured in RESEARCH.md.

    If the suite passes, no file changes are required. No commit is produced by this task — it is a verification gate for Plan 01 before handing off to Plan 02.
  </action>
  <verify>
    <automated>npm test -- app/api/v1/fritzbox/ --passWithNoTests=false --silent 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- app/api/v1/fritzbox/` exits 0
    - Output shows at least 20 test suites detected (all co-located route tests under the new tree)
    - Zero `Cannot find module` or `SyntaxError` failures
    - Zero test failures related to broken imports
    - If any suite fails, the failure cause is documented in the SUMMARY.md (not silently accepted)
  </acceptance_criteria>
  <done>
    Scoped Jest subset green from the new route location. Plan 02 is unblocked.
  </done>
</task>

</tasks>

<verification>
Plan 01 is complete when:
1. `app/api/fritzbox/` does not exist on disk
2. `app/api/v1/fritzbox/` contains all 28 routes + 20 tests
3. Git history preserved (`git log --follow` works on any moved file)
4. `npm test -- app/api/v1/fritzbox/` exits 0
5. Consumer files untouched (Plan 02 scope)
6. Single commit with message matching `refactor(172-01):` prefix

Expected temporary app state after Plan 01: broken (consumers reference non-existent `/api/fritzbox/*` URLs). This is intentional and corrected by Plan 02 in the next wave.
</verification>

<success_criteria>
- Directory rename atomic via `git mv`
- All 48 files show as `R` (rename) in git status, not `D`+`A`
- Co-located test tree green from new location via scoped Jest
- Zero production source file contents modified
- Single commit produced with conventional message
</success_criteria>

<output>
After completion, create `.planning/phases/172-fritzbox-v1-path-migration/172-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Record:
- Commit SHA from `git log -1 --format=%H`
- Output of `git status --short | wc -l` before and after (should be 0 after commit)
- Output of `find app/api/v1/fritzbox -name 'route.ts' | wc -l` (should be 28)
- Output of `find app/api/v1/fritzbox -path '*/__tests__/*.test.ts' | wc -l` (should be 20)
- Jest suite count and pass/fail for `npm test -- app/api/v1/fritzbox/`
</output>
