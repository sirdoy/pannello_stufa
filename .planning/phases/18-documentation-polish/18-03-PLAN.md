---
phase: 18-documentation-polish
plan: 03
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - app/debug/design-system/page.js
autonomous: true

must_haves:
  truths:
    - "Developer can view syntax-highlighted code for each component"
    - "Developer can copy any code example with one click"
    - "Developer can see prop tables for at least 5 key components"
    - "Developer can see accessibility info for interactive components"
  artifacts:
    - path: "app/debug/design-system/page.js"
      provides: "Enhanced design system showcase with code examples, prop tables, accessibility sections"
      exports: ["default"]
  key_links:
    - from: "app/debug/design-system/page.js"
      to: "app/debug/design-system/components/CodeBlock.js"
      via: "import"
      pattern: "import CodeBlock from"
    - from: "app/debug/design-system/page.js"
      to: "app/debug/design-system/data/component-docs.js"
      via: "import"
      pattern: "import.*componentDocs"
---

<objective>
Integrate documentation components into the design system showcase page.

Purpose: Transform the existing showcase into an interactive documentation hub with copyable code examples, prop tables, and accessibility information.
Output: Enhanced design-system page with documentation components integrated
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-documentation-polish/18-RESEARCH.md
@.planning/phases/18-documentation-polish/18-01-SUMMARY.md
@.planning/phases/18-documentation-polish/18-02-SUMMARY.md

# Existing page to enhance
@app/debug/design-system/page.js

# Documentation components from Plan 01
@app/debug/design-system/components/CodeBlock.js
@app/debug/design-system/components/PropTable.js
@app/debug/design-system/components/AccessibilitySection.js
@app/debug/design-system/components/ComponentDemo.js

# Component metadata from Plan 02
@app/debug/design-system/data/component-docs.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add documentation components to Button section</name>
  <files>
    app/debug/design-system/page.js
  </files>
  <action>
1. Add imports at top of file:
   ```javascript
   import CodeBlock from './components/CodeBlock';
   import PropTable from './components/PropTable';
   import AccessibilitySection from './components/AccessibilitySection';
   import ComponentDemo from './components/ComponentDemo';
   import { componentDocs } from './data/component-docs';
   ```

2. Enhance the Buttons section (SectionShowcase title="Buttons"):
   - After the existing button examples, add a new subsection for documentation
   - Add CardDivider after "Button Group" section
   - Add new section with:
     a. "API Reference" heading (Text variant="label" size="xs")
     b. PropTable component with componentDocs.Button.props
     c. "Code Example" heading
     d. CodeBlock with componentDocs.Button.codeExample
     e. AccessibilitySection with componentDocs.Button.keyboard, aria, screenReader

3. Structure for the new documentation subsection:
   ```jsx
   <CardDivider />

   {/* API Reference */}
   <div>
     <Text variant="label" size="xs" className="mb-3">API Reference</Text>
     <PropTable props={componentDocs.Button.props} />
   </div>

   <CardDivider />

   {/* Code Example */}
   <div>
     <Text variant="label" size="xs" className="mb-3">Code Example</Text>
     <CodeBlock code={componentDocs.Button.codeExample} />
   </div>

   <CardDivider />

   {/* Accessibility */}
   <AccessibilitySection
     keyboard={componentDocs.Button.keyboard}
     aria={componentDocs.Button.aria}
     screenReader={componentDocs.Button.screenReader}
   />
   ```
  </action>
  <verify>
    - Imports added for CodeBlock, PropTable, AccessibilitySection, componentDocs
    - Buttons section includes PropTable with Button props
    - Buttons section includes CodeBlock with code example
    - Buttons section includes AccessibilitySection with keyboard/aria/screenReader
  </verify>
  <done>
    - Button section fully documented with props table, code example, and accessibility info
    - All documentation components render correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Add documentation to Form Inputs section</name>
  <files>
    app/debug/design-system/page.js
  </files>
  <action>
Enhance the Form Inputs section with documentation for each component:

1. After each component demo (Input, Select, Toggle, Checkbox), add:
   - PropTable with that component's props from componentDocs
   - CodeBlock with codeExample
   - AccessibilitySection with keyboard/aria/screenReader

2. Structure (repeat for each component in the section):
   ```jsx
   {/* After the Input demos */}
   <div className="mt-4">
     <Text variant="tertiary" size="xs" className="mb-2">Props Reference</Text>
     <PropTable props={componentDocs.Input.props} />
   </div>
   <div className="mt-4">
     <Text variant="tertiary" size="xs" className="mb-2">Usage</Text>
     <CodeBlock code={componentDocs.Input.codeExample} />
   </div>
   <AccessibilitySection
     keyboard={componentDocs.Input.keyboard}
     aria={componentDocs.Input.aria}
     screenReader={componentDocs.Input.screenReader}
   />
   ```

3. Components to document in Form Inputs section:
   - Input (already has demos, add docs after)
   - Select (already has demos, add docs after)
   - Toggle/Switch (already has demos, add docs after componentDocs.Switch)
   - Checkbox (already has demos, add docs after)

4. Keep existing demos intact - add documentation AFTER each demo section, before CardDivider
  </action>
  <verify>
    - Form Inputs section has PropTable for Input, Select, Switch, Checkbox
    - Each has CodeBlock with code example
    - Each has AccessibilitySection with keyboard/aria/screenReader
  </verify>
  <done>
    - Input component fully documented
    - Select component fully documented
    - Switch/Toggle component fully documented
    - Checkbox component fully documented
  </done>
</task>

<task type="auto">
  <name>Task 3: Add documentation to Smart Home and Feedback sections</name>
  <files>
    app/debug/design-system/page.js
  </files>
  <action>
1. Enhance Smart Home Components section:
   - Add documentation for ConnectionStatus, HealthIndicator, SmartHomeCard, StatusCard, ControlButton
   - For each component's Card subsection, add:
     - PropTable after the description Text
     - CodeBlock with codeExample
     - AccessibilitySection (for ConnectionStatus, HealthIndicator, ControlButton)

2. Enhance Modal & Overlays section:
   - Add PropTable and AccessibilitySection for Modal
   - Modal has important keyboard (Escape closes) and aria (role="dialog")

3. Enhance Toast Notifications section:
   - Add PropTable and AccessibilitySection for Toast
   - Toast has aria (role="status", aria-live="polite")

4. For Badge section (Badge CVA):
   - Add PropTable for Badge props
   - Badge is presentational, so AccessibilitySection can be minimal or omitted

5. Keep all existing demos and examples - add documentation alongside them

6. For sections where documentation would be too verbose, use a collapsible pattern or just add PropTable without full AccessibilitySection (except for interactive components that need a11y docs)
  </action>
  <verify>
    - Smart Home section has PropTable for key components
    - Modal section has PropTable and AccessibilitySection
    - Toast section has PropTable and AccessibilitySection
    - Badge section has PropTable
    - Interactive components have AccessibilitySection
  </verify>
  <done>
    - Smart Home components documented (ConnectionStatus, HealthIndicator, SmartHomeCard, StatusCard, ControlButton)
    - Modal documented with keyboard/aria
    - Toast documented with aria-live
    - Badge documented with props
    - Page renders without errors
  </done>
</task>

</tasks>

<verification>
1. Page imports all documentation components and componentDocs
2. Button section has PropTable, CodeBlock, AccessibilitySection
3. Form Inputs section documents Input, Select, Switch, Checkbox
4. Smart Home section documents ConnectionStatus, HealthIndicator, ControlButton
5. Modal and Toast sections have AccessibilitySection
6. Page renders without errors: `npm run dev` and visit /debug/design-system
</verification>

<success_criteria>
- [ ] Documentation components imported in page.js
- [ ] componentDocs imported and used
- [ ] Button section fully documented
- [ ] Form Inputs section (4 components) documented
- [ ] Smart Home section (key components) documented
- [ ] Modal and Toast have accessibility documentation
- [ ] Page renders correctly without errors
- [ ] Copy button on code examples works
</success_criteria>

<output>
After completion, create `.planning/phases/18-documentation-polish/18-03-SUMMARY.md`
</output>
