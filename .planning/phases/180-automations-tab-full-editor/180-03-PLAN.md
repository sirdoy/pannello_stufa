---
phase: 180-automations-tab-full-editor
plan: 03
type: execute
wave: 2
depends_on: [180-01]
files_modified:
  - app/components/EmberGlass/automations/primitives/FieldLabel.tsx
  - app/components/EmberGlass/automations/primitives/TextInput.tsx
  - app/components/EmberGlass/automations/primitives/NumInput.tsx
  - app/components/EmberGlass/automations/primitives/SegmentedControl.tsx
  - app/components/EmberGlass/automations/primitives/TwoCol.tsx
  - app/components/EmberGlass/automations/primitives/TypeTile.tsx
  - app/components/EmberGlass/automations/primitives/AddChip.tsx
  - app/components/EmberGlass/automations/primitives/Pill.tsx
  - app/components/EmberGlass/automations/primitives/CronHint.tsx
  - app/components/EmberGlass/automations/primitives/IconBtn.tsx
  - app/components/EmberGlass/automations/__tests__/primitives/FieldLabel.test.tsx
  - app/components/EmberGlass/automations/__tests__/primitives/TextInput.test.tsx
  - app/components/EmberGlass/automations/__tests__/primitives/NumInput.test.tsx
  - app/components/EmberGlass/automations/__tests__/primitives/SegmentedControl.test.tsx
  - app/components/EmberGlass/automations/__tests__/primitives/TypeTile.test.tsx
  - app/components/EmberGlass/automations/__tests__/primitives/Pill.test.tsx
  - app/components/EmberGlass/automations/__tests__/primitives/CronHint.test.tsx
autonomous: true
requirements: [AUTO-02, AUTO-04, AUTO-05]
tags: [phase-180, primitives, inline-style]

must_haves:
  truths:
    - "10 primitives exist under primitives/ matching UI-SPEC visual contract verbatim (38px TextInput, 9px radius, 0.5px borders, etc.)"
    - "TypeTile supports `disabled` prop for D-12 trigger read-only edit mode"
    - "NumInput supports `unit` and `allowNull` props"
    - "TextInput supports `mono` (monospace family) and `readOnly` props"
    - "Pill supports `tone?` (tone-colored) and `muted?` modes; 3rd default mode is neutral white"
    - "CronHint parses cron string into 5 segments labeled 'min'/'ora'/'giorno'/'mese'/'giorno sett.' (Italian)"
    - "Every primitive uses inline-style + var(--token); ZERO Tailwind classes for visual values inside primitives/ folder"
  artifacts:
    - path: app/components/EmberGlass/automations/primitives/TextInput.tsx
      provides: "TextInput component (38px h, 9px r, 0.5px border)"
      exports: ["TextInput"]
    - path: app/components/EmberGlass/automations/primitives/TypeTile.tsx
      provides: "TypeTile with selected/unselected/disabled states"
      exports: ["TypeTile"]
    - path: app/components/EmberGlass/automations/primitives/CronHint.tsx
      provides: "5-segment cron breakdown display"
      exports: ["CronHint"]
  key_links:
    - from: app/components/EmberGlass/automations/primitives/CronHint.tsx
      to: (none — no codebase analog, bundle line 907-924 verbatim)
      via: "parses cron string with .split(/\\s+/)"
      pattern: "split\\(/\\\\s\\+/\\)"
---

<objective>
Land all 10 automations-local primitives under `app/components/EmberGlass/automations/primitives/` per CONTEXT D-04 (NOT shared with Phase 178 sheets/primitives — the visual scale is different) and UI-SPEC § Spacing Component-Level Reference. Every primitive is bundle-verbatim inline-style.

Purpose: every form, section, and the editor itself imports these primitives. They are pure presentational components with no business logic — fast to build, fast to test, no upstream dependencies beyond the catalog types from Plan 02.

Output: 10 primitive files + 7 jest specs (FieldLabel, TextInput, NumInput, SegmentedControl, TypeTile, Pill, CronHint — TwoCol/AddChip/IconBtn are too trivial for dedicated specs but ship verified by visual + grep gates).

This plan can run in PARALLEL with Plan 02 — no shared files, no import dependency on `lib/automations-config.ts` (primitives are catalog-agnostic).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/180-automations-tab-full-editor/180-CONTEXT.md
@.planning/phases/180-automations-tab-full-editor/180-RESEARCH.md
@.planning/phases/180-automations-tab-full-editor/180-PATTERNS.md
@.planning/phases/180-automations-tab-full-editor/180-UI-SPEC.md
@./CLAUDE.md
@./.planning/inbox/ember-glass-design/project/components/automations.jsx
@./app/components/EmberGlass/rooms/primitives/StatChip.tsx

<spacing_contract>
<!-- UI-SPEC § Spacing — Component-Level Reference. Every value is bundle-verbatim. NO deviation. -->

**TextInput** (lines 824-834 of bundle):
- height: 38, borderRadius: 9, padding: '0 11px', fontSize: 13
- background: rgba(255,255,255,0.05); border: 0.5px solid rgba(255,255,255,0.08)
- color: #fff, outline: none, width: 100%, boxSizing: border-box
- mono variant: fontFamily: 'ui-monospace, SF Mono, monospace'

**NumInput** (lines 836-853):
- Same chrome as TextInput; padding: '0 36px 0 11px' (right padding for unit label)
- fontVariantNumeric: 'tabular-nums'
- Unit label positioned absolute right: 11px, fontSize: 11, color: var(--text-2)
- allowNull: when true, empty input emits null; when false, empty input emits 0

**SegmentedControl** (lines 855-871):
- Container: padding: 3, borderRadius: 9, background: rgba(255,255,255,0.05), border: 0.5px solid rgba(255,255,255,0.08), display: flex
- Segment: flex: 1, padding: '7px 4px', borderRadius: 7, border: none, fontSize: 12, fontWeight: 600
- Active: background: rgba(255,255,255,0.12), color: #fff
- Inactive: background: transparent, color: var(--text-2)

**TypeTile** (lines 877-896):
- padding: 10, borderRadius: 11, border: none
- Icon area: width: 26, height: 26, borderRadius: 7, marginBottom: 6, fontSize-of-svg: 13
- Icon area background: color-mix(in oklab, {tone} 20%, rgba(255,255,255,0.05)); color: tone
- Label: fontSize: 12, fontWeight: 600, lineHeight: 1.2
- Desc: fontSize: 10, color: var(--text-2), lineHeight: 1.3, marginTop: 2
- Selected: background: color-mix(in oklab, {tone} 18%, transparent); border: 0.5px solid color-mix(in oklab, {tone} 40%, transparent); boxShadow: 0 0 14px color-mix(in oklab, {tone} 25%, transparent)
- Unselected: background: rgba(255,255,255,0.04); border: 0.5px solid rgba(255,255,255,0.06)
- Disabled (D-12): opacity: 0.45, cursor: 'not-allowed', pointerEvents: 'none'

**FieldLabel** (lines 818-822):
- fontSize: 11 (normal) or 10 (small), fontWeight: 600, textTransform: 'uppercase', letterSpacing: 0.8
- color: var(--text-2), marginBottom: 6, display: 'block'

**TwoCol** (lines 873-875):
- display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10, marginBottom: 10

**AddChip** (lines 898-905):
- padding: '7px 12px', borderRadius: 999, border: 0.5px dashed rgba(255,255,255,0.15)
- background: rgba(255,255,255,0.05), color: '#fff', fontSize: 11, fontWeight: 600
- cursor: pointer

**Pill** (lines 214-225):
- padding: '4px 9px', borderRadius: 999, fontSize: 10, fontWeight: 600, letterSpacing: 0.2
- tone-colored: background: color-mix(in oklab, {tone} 16%, transparent); color: {tone}; border: 0.5px solid color-mix(in oklab, {tone} 25%, transparent)
- neutral (default): background: rgba(255,255,255,0.06); color: '#fff'; border: 0.5px solid rgba(255,255,255,0.08)
- muted: background: 'transparent'; color: var(--text-2); border: 0.5px solid rgba(255,255,255,0.08)

**CronHint** (lines 907-924):
- Container: display: flex, gap: 4, marginTop: 8
- Each segment: flex: 1, padding: '4px 6px', borderRadius: 6, background: rgba(255,255,255,0.04), textAlign: center
- Label: fontSize: 9, color: var(--text-2), textTransform: 'uppercase', letterSpacing: 0.5
- Value: fontSize: 12, color: '#fff', fontFamily: 'ui-monospace, monospace', marginTop: 1
- LABELS: ['min', 'ora', 'giorno', 'mese', 'giorno sett.'] (verbatim Italian, lowercase per UI-SPEC)
- Parsing: expr.trim().split(/\s+/); LABELS.map((_, i) => parts[i] ?? '—')

**IconBtn** (lines 676-684):
- width: 24, height: 24, borderRadius: 6, border: none, cursor: pointer
- background: rgba(255,255,255,0.05)
- Active color: var(--text-2); Disabled color: rgba(255,255,255,0.2)
- display: flex, alignItems: center, justifyContent: center
</spacing_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land 7 simple primitives (FieldLabel, TextInput, NumInput, SegmentedControl, TwoCol, AddChip, Pill) + tests for 5 of them</name>
  <files>app/components/EmberGlass/automations/primitives/FieldLabel.tsx, app/components/EmberGlass/automations/primitives/TextInput.tsx, app/components/EmberGlass/automations/primitives/NumInput.tsx, app/components/EmberGlass/automations/primitives/SegmentedControl.tsx, app/components/EmberGlass/automations/primitives/TwoCol.tsx, app/components/EmberGlass/automations/primitives/AddChip.tsx, app/components/EmberGlass/automations/primitives/Pill.tsx, app/components/EmberGlass/automations/__tests__/primitives/FieldLabel.test.tsx, app/components/EmberGlass/automations/__tests__/primitives/TextInput.test.tsx, app/components/EmberGlass/automations/__tests__/primitives/NumInput.test.tsx, app/components/EmberGlass/automations/__tests__/primitives/SegmentedControl.test.tsx, app/components/EmberGlass/automations/__tests__/primitives/Pill.test.tsx</files>
  <read_first>
    - `.planning/inbox/ember-glass-design/project/components/automations.jsx` lines 818-871 + 898-905 + 214-225 (verbatim source)
    - `.planning/phases/180-automations-tab-full-editor/180-UI-SPEC.md` § Spacing — Component-Level Reference (full table)
    - `.planning/phases/180-automations-tab-full-editor/180-PATTERNS.md` "TextInput" + "SegmentedControl" sections (code blocks)
    - `<spacing_contract>` block in this plan (locked values)
    - `./app/components/EmberGlass/rooms/primitives/StatChip.tsx` (analog — inline-style + 'use client' header pattern)
  </read_first>
  <behavior>
    - TextInput: renders as `<input>` with the EXACT inline-style values from `<spacing_contract>`; supports `value`, `onChange(value: string)`, `placeholder?`, `mono?`, `readOnly?`, `type?`, `aria-label?` props
    - NumInput: renders as `<input type="number">` with right-padding for unit; `unit?` (string suffix), `allowNull?` (when true, empty input fires onChange(null) — when false, empty input fires onChange(0)), `min?`, `max?`, `step?`, `value: number | null`, `onChange(v: number | null)`
    - SegmentedControl: `options: { value, label }[]`, `value`, `onChange(value)`; renders all options as `<button>`; active option has highlighted bg
    - FieldLabel: renders as `<label>` with optional `htmlFor` prop; `small?` shrinks font from 11 to 10
    - TwoCol: wraps children in 2-col grid; passthrough children
    - AddChip: `<button>` with dashed border + Plus icon space; accepts children as label + optional `onClick`
    - Pill: discriminated props — `tone?: string` (tone-colored), `muted?: boolean` (muted), default is neutral; never both `tone` and `muted` simultaneously
  </behavior>
  <action>
1. **Create `FieldLabel.tsx`**:
```tsx
import type { ReactNode } from 'react';

export interface FieldLabelProps {
  children: ReactNode;
  htmlFor?: string;
  small?: boolean;
}

export function FieldLabel({ children, htmlFor, small = false }: FieldLabelProps) {
  return (
    <label
      htmlFor={htmlFor}
      style={{
        display: 'block',
        fontSize: small ? 10 : 11,
        fontWeight: 600,
        textTransform: 'uppercase',
        letterSpacing: 0.8,
        color: 'var(--text-2)',
        marginBottom: 6,
      }}
    >
      {children}
    </label>
  );
}
```

2. **Create `TextInput.tsx`**:
```tsx
import type { ChangeEvent, InputHTMLAttributes } from 'react';

export interface TextInputProps {
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  mono?: boolean;
  readOnly?: boolean;
  type?: 'text' | 'url' | 'time';
  'aria-label'?: string;
  id?: string;
}

export function TextInput({
  value, onChange, placeholder, mono = false, readOnly = false, type = 'text', id,
  ...rest
}: TextInputProps & Pick<InputHTMLAttributes<HTMLInputElement>, 'aria-label'>) {
  return (
    <input
      id={id}
      type={type}
      value={value}
      onChange={(e: ChangeEvent<HTMLInputElement>) => onChange(e.target.value)}
      placeholder={placeholder}
      readOnly={readOnly}
      aria-label={rest['aria-label']}
      style={{
        height: 38,
        borderRadius: 9,
        background: 'rgba(255,255,255,0.05)',
        border: '0.5px solid rgba(255,255,255,0.08)',
        color: '#fff',
        padding: '0 11px',
        fontSize: 13,
        fontFamily: mono ? 'ui-monospace, SF Mono, monospace' : 'inherit',
        outline: 'none',
        width: '100%',
        boxSizing: 'border-box',
      }}
    />
  );
}
```

3. **Create `NumInput.tsx`** following `<spacing_contract>` for all visual values; key behavior:
   - `value: number | null`; when `value === null`, render input as empty string
   - `onChange(v: number | null)`: parse `e.target.value`; if empty + `allowNull` => null, else parseFloat; if NaN, ignore
   - When `unit` is set, render absolute-positioned `<span>` after the input with `right: 11px` (wrap in relative container)
   - Use `font-variant-numeric: tabular-nums`
   - Accept `min`, `max`, `step`, `id`, `aria-label`, `placeholder` props

4. **Create `SegmentedControl.tsx`**:
```tsx
export interface SegmentedControlProps<T extends string> {
  options: ReadonlyArray<{ value: T; label: string }>;
  value: T;
  onChange: (value: T) => void;
  'aria-label'?: string;
}

export function SegmentedControl<T extends string>({
  options, value, onChange, ...rest
}: SegmentedControlProps<T>) {
  return (
    <div
      role="radiogroup"
      aria-label={rest['aria-label']}
      style={{
        display: 'flex',
        padding: 3,
        borderRadius: 9,
        background: 'rgba(255,255,255,0.05)',
        border: '0.5px solid rgba(255,255,255,0.08)',
      }}
    >
      {options.map((opt) => {
        const active = value === opt.value;
        return (
          <button
            key={opt.value}
            type="button"
            role="radio"
            aria-checked={active}
            onClick={() => onChange(opt.value)}
            style={{
              flex: 1,
              padding: '7px 4px',
              borderRadius: 7,
              border: 'none',
              background: active ? 'rgba(255,255,255,0.12)' : 'transparent',
              color: active ? '#fff' : 'var(--text-2)',
              fontSize: 12,
              fontWeight: 600,
              cursor: 'pointer',
            }}
          >
            {opt.label}
          </button>
        );
      })}
    </div>
  );
}
```

5. **Create `TwoCol.tsx`**:
```tsx
import type { ReactNode } from 'react';
export function TwoCol({ children }: { children: ReactNode }) {
  return (
    <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10, marginBottom: 10 }}>
      {children}
    </div>
  );
}
```

6. **Create `AddChip.tsx`**:
```tsx
import type { ReactNode } from 'react';
export interface AddChipProps {
  children: ReactNode;
  onClick?: () => void;
  'aria-label'?: string;
}
export function AddChip({ children, onClick, ...rest }: AddChipProps) {
  return (
    <button
      type="button"
      onClick={onClick}
      aria-label={rest['aria-label']}
      style={{
        padding: '7px 12px',
        borderRadius: 999,
        border: '0.5px dashed rgba(255,255,255,0.15)',
        background: 'rgba(255,255,255,0.05)',
        color: '#fff',
        fontSize: 11,
        fontWeight: 600,
        cursor: 'pointer',
      }}
    >
      {children}
    </button>
  );
}
```

7. **Create `Pill.tsx`**. Discriminated props (tone OR muted OR default neutral). Match `<spacing_contract>` for all 3 modes. Use `color-mix(in oklab, {tone} 16%, transparent)` for tone bg.

8. **Create 5 jest specs** under `__tests__/primitives/`:
   - `FieldLabel.test.tsx`: renders children, small prop swaps font 11→10, htmlFor renders correctly
   - `TextInput.test.tsx`: renders input with value, onChange fires with new string value, mono prop swaps fontFamily, readOnly applies `readOnly` attribute
   - `NumInput.test.tsx`: empty input + allowNull=true fires onChange(null); empty + allowNull=false fires onChange(0); unit prop renders unit label; min/max attributes applied
   - `SegmentedControl.test.tsx`: renders all options as buttons; click fires onChange with correct value; aria-checked reflects active state
   - `Pill.test.tsx`: tone prop applies tone-colored bg; muted prop applies transparent bg; default is neutral white

Each spec uses `@testing-library/react` `render` + `screen` + `fireEvent`. Pattern matches `app/components/EmberGlass/rooms/__tests__/primitives/*.test.tsx`.
  </action>
  <verify>
    <automated>npm test -- app/components/EmberGlass/automations/__tests__/primitives/ && grep -l "className=" app/components/EmberGlass/automations/primitives/*.tsx | grep -v -E "(test|stories)" | grep -v '^$' | wc -l | grep -E '^\s*0\s*$'</automated>
  </verify>
  <done>
    - All 7 primitives compile cleanly
    - 5 tests files passing (>15 test cases)
    - Zero `className=` strings in primitive source files (inline-style discipline)
    - Visual values match `<spacing_contract>` verbatim
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land 3 advanced primitives (TypeTile, CronHint, IconBtn) + tests for TypeTile + CronHint</name>
  <files>app/components/EmberGlass/automations/primitives/TypeTile.tsx, app/components/EmberGlass/automations/primitives/CronHint.tsx, app/components/EmberGlass/automations/primitives/IconBtn.tsx, app/components/EmberGlass/automations/__tests__/primitives/TypeTile.test.tsx, app/components/EmberGlass/automations/__tests__/primitives/CronHint.test.tsx</files>
  <read_first>
    - `.planning/inbox/ember-glass-design/project/components/automations.jsx` lines 877-924 + 676-684 (verbatim source for TypeTile, CronHint, IconBtn)
    - `.planning/phases/180-automations-tab-full-editor/180-UI-SPEC.md` § Spacing — Component-Level Reference (TypeTile + CronHint + IconBtn rows)
    - `.planning/phases/180-automations-tab-full-editor/180-PATTERNS.md` "TypeTile" + "CronHint" sections (code blocks)
    - `.planning/phases/180-automations-tab-full-editor/180-CONTEXT.md` D-12 (TypeTile disabled = trigger read-only edit mode)
    - `<spacing_contract>` block in this plan
  </read_first>
  <behavior>
    - TypeTile: `icon: ReactNode`, `label: string`, `desc?: string`, `tone: string`, `selected: boolean`, `disabled?: boolean`, `onClick?: () => void`
    - TypeTile selected state: tone-colored bg (18% mix) + border (40% mix) + glow (25% mix)
    - TypeTile unselected: rgba(255,255,255,0.04) bg + 0.5px white border
    - TypeTile disabled (D-12): `opacity: 0.45`, `cursor: 'not-allowed'`, `pointerEvents: 'none'` — onClick must NOT fire
    - CronHint: `expr: string`; renders 5 segments; segments labeled exactly ['min', 'ora', 'giorno', 'mese', 'giorno sett.']
    - CronHint missing tokens render '—' placeholder
    - CronHint preserves segment order even with extra whitespace in expr
    - IconBtn: `children: ReactNode`, `onClick?: () => void`, `disabled?: boolean`, `'aria-label': string` (REQUIRED)
    - IconBtn disabled: color rgba(255,255,255,0.2), cursor not-allowed
  </behavior>
  <action>
1. **Create `TypeTile.tsx`**. Follow `<spacing_contract>` exactly. Code skeleton:
```tsx
import type { ReactNode } from 'react';

export interface TypeTileProps {
  icon: ReactNode;
  label: string;
  desc?: string;
  tone: string;
  selected: boolean;
  disabled?: boolean;
  onClick?: () => void;
  'aria-label'?: string;
}

export function TypeTile({
  icon, label, desc, tone, selected, disabled = false, onClick, ...rest
}: TypeTileProps) {
  const baseStyle = {
    padding: 10,
    borderRadius: 11,
    border: 'none',
    cursor: disabled ? ('not-allowed' as const) : ('pointer' as const),
    textAlign: 'left' as const,
    background: selected
      ? `color-mix(in oklab, ${tone} 18%, transparent)`
      : 'rgba(255,255,255,0.04)',
    boxShadow: selected
      ? `0 0 14px color-mix(in oklab, ${tone} 25%, transparent)`
      : 'none',
    color: '#fff',
    width: '100%',
    boxSizing: 'border-box' as const,
    ...(disabled ? { opacity: 0.45, pointerEvents: 'none' as const } : {}),
  };
  // border has to be merged after spread because of `as const` mismatch
  const borderStyle = selected
    ? `0.5px solid color-mix(in oklab, ${tone} 40%, transparent)`
    : '0.5px solid rgba(255,255,255,0.06)';

  return (
    <button
      type="button"
      onClick={disabled ? undefined : onClick}
      aria-pressed={selected}
      aria-disabled={disabled}
      aria-label={rest['aria-label']}
      style={{ ...baseStyle, border: borderStyle }}
    >
      <div
        style={{
          width: 26,
          height: 26,
          borderRadius: 7,
          marginBottom: 6,
          background: `color-mix(in oklab, ${tone} 20%, rgba(255,255,255,0.05))`,
          color: tone,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        {icon}
      </div>
      <div style={{ fontSize: 12, fontWeight: 600, lineHeight: 1.2 }}>{label}</div>
      {desc && (
        <div style={{ fontSize: 10, color: 'var(--text-2)', lineHeight: 1.3, marginTop: 2 }}>
          {desc}
        </div>
      )}
    </button>
  );
}
```

2. **Create `CronHint.tsx`**:
```tsx
const LABELS = ['min', 'ora', 'giorno', 'mese', 'giorno sett.'] as const;

export interface CronHintProps {
  expr: string;
}

export function CronHint({ expr }: CronHintProps) {
  const parts = expr.trim().split(/\s+/);
  const segments = LABELS.map((_, i) => parts[i] ?? '—');

  return (
    <div style={{ display: 'flex', gap: 4, marginTop: 8 }}>
      {LABELS.map((label, i) => (
        <div
          key={label}
          style={{
            flex: 1,
            padding: '4px 6px',
            borderRadius: 6,
            background: 'rgba(255,255,255,0.04)',
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontSize: 9,
              color: 'var(--text-2)',
              textTransform: 'uppercase',
              letterSpacing: 0.5,
            }}
          >
            {label}
          </div>
          <div
            style={{
              fontSize: 12,
              color: '#fff',
              fontFamily: 'ui-monospace, monospace',
              marginTop: 1,
            }}
          >
            {segments[i]}
          </div>
        </div>
      ))}
    </div>
  );
}
```

3. **Create `IconBtn.tsx`**:
```tsx
import type { ReactNode } from 'react';

export interface IconBtnProps {
  children: ReactNode;
  onClick?: () => void;
  disabled?: boolean;
  'aria-label': string; // REQUIRED for accessibility (D-19)
}

export function IconBtn({ children, onClick, disabled = false, ...rest }: IconBtnProps) {
  return (
    <button
      type="button"
      onClick={disabled ? undefined : onClick}
      aria-label={rest['aria-label']}
      aria-disabled={disabled}
      style={{
        width: 24,
        height: 24,
        borderRadius: 6,
        border: 'none',
        background: 'rgba(255,255,255,0.05)',
        color: disabled ? 'rgba(255,255,255,0.2)' : 'var(--text-2)',
        cursor: disabled ? 'not-allowed' : 'pointer',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        flexShrink: 0,
      }}
    >
      {children}
    </button>
  );
}
```

4. **Create `__tests__/primitives/TypeTile.test.tsx`**:
```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { TypeTile } from '../../primitives/TypeTile';

describe('TypeTile', () => {
  test('renders label and desc', () => {
    render(<TypeTile icon={<span>I</span>} label="Pianificazione" desc="Cron schedule" tone="#5eafff" selected={false} />);
    expect(screen.getByText('Pianificazione')).toBeInTheDocument();
    expect(screen.getByText('Cron schedule')).toBeInTheDocument();
  });
  test('selected state sets aria-pressed=true', () => {
    render(<TypeTile icon={<span>I</span>} label="X" tone="#5eafff" selected />);
    expect(screen.getByRole('button')).toHaveAttribute('aria-pressed', 'true');
  });
  test('onClick fires when not disabled', () => {
    const onClick = jest.fn();
    render(<TypeTile icon={<span>I</span>} label="X" tone="#5eafff" selected={false} onClick={onClick} />);
    fireEvent.click(screen.getByRole('button'));
    expect(onClick).toHaveBeenCalledTimes(1);
  });
  test('disabled prevents onClick (D-12 trigger read-only mode)', () => {
    const onClick = jest.fn();
    render(<TypeTile icon={<span>I</span>} label="X" tone="#5eafff" selected={false} onClick={onClick} disabled />);
    fireEvent.click(screen.getByRole('button'));
    expect(onClick).not.toHaveBeenCalled();
    expect(screen.getByRole('button')).toHaveAttribute('aria-disabled', 'true');
  });
  test('disabled applies opacity 0.45 + pointerEvents none', () => {
    render(<TypeTile icon={<span>I</span>} label="X" tone="#5eafff" selected={false} disabled />);
    const btn = screen.getByRole('button');
    expect(btn).toHaveStyle({ opacity: '0.45', pointerEvents: 'none', cursor: 'not-allowed' });
  });
});
```

5. **Create `__tests__/primitives/CronHint.test.tsx`**:
```tsx
import { render, screen } from '@testing-library/react';
import { CronHint } from '../../primitives/CronHint';

describe('CronHint', () => {
  test('renders 5 segments with Italian labels in lowercase', () => {
    const { container } = render(<CronHint expr="0 8 * * *" />);
    expect(container.textContent).toContain('min');
    expect(container.textContent).toContain('ora');
    expect(container.textContent).toContain('giorno');
    expect(container.textContent).toContain('mese');
    expect(container.textContent).toContain('giorno sett.');
  });
  test('parses standard cron "0 8 * * *" into [0, 8, *, *, *]', () => {
    const { container } = render(<CronHint expr="0 8 * * *" />);
    // value rows have monospace font-family
    const valueDivs = Array.from(container.querySelectorAll('div')).filter((d) =>
      (d as HTMLElement).style.fontFamily?.includes('ui-monospace')
    );
    expect(valueDivs.map((d) => d.textContent)).toEqual(['0', '8', '*', '*', '*']);
  });
  test('missing tokens render placeholder "—"', () => {
    const { container } = render(<CronHint expr="0 8" />);
    const valueDivs = Array.from(container.querySelectorAll('div')).filter((d) =>
      (d as HTMLElement).style.fontFamily?.includes('ui-monospace')
    );
    expect(valueDivs.map((d) => d.textContent)).toEqual(['0', '8', '—', '—', '—']);
  });
  test('extra whitespace tolerated (multiple spaces collapse)', () => {
    const { container } = render(<CronHint expr="*/5    *  *  *  *" />);
    const valueDivs = Array.from(container.querySelectorAll('div')).filter((d) =>
      (d as HTMLElement).style.fontFamily?.includes('ui-monospace')
    );
    expect(valueDivs.map((d) => d.textContent)).toEqual(['*/5', '*', '*', '*', '*']);
  });
});
```
  </action>
  <verify>
    <automated>npm test -- app/components/EmberGlass/automations/__tests__/primitives/TypeTile.test.tsx app/components/EmberGlass/automations/__tests__/primitives/CronHint.test.tsx && grep -E '^\s*const LABELS\s*=' app/components/EmberGlass/automations/primitives/CronHint.tsx | grep -c "'min', 'ora', 'giorno', 'mese', 'giorno sett.'"</automated>
  </verify>
  <done>
    - TypeTile, CronHint, IconBtn primitives exist
    - TypeTile.test.tsx covers selected, onClick, disabled, aria-pressed
    - CronHint.test.tsx covers 5-segment parse + missing-token fallback + Italian labels
    - All 10 primitives ready for downstream consumption
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Primitives are pure presentational; no I/O, no auth, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-03-01 | Tampering | TypeTile disabled state bypassed by JS click triggering onClick | mitigate | Implementation uses `onClick={disabled ? undefined : onClick}` AND `pointerEvents: none` AND `aria-disabled` — three layers prevent invocation. Test asserts onClick NOT called. |
| T-180-03-02 | Information Disclosure | CronHint renders user input verbatim — XSS risk via React children | accept | React's text-node escape handles. No `dangerouslySetInnerHTML`. Static analysis confirms zero `dangerouslySet*` in plan. |
| T-180-03-03 | Tampering | Pill `tone` prop accepts arbitrary string interpolated into `color-mix(in oklab, ${tone}...)` — could break CSS if tone contains `;` | accept | Tones are sourced ONLY from D-09 catalog (hardcoded hex/var names) — no user-controlled values reach Pill. ACTION_TYPES + CONDITION_TYPES + TRIGGER_TYPES are `as const` literals. |
</threat_model>

<verification>
- All 10 primitive files exist
- 7 jest specs (5 in Task 1 + 2 in Task 2) — total ~25 test cases
- `grep -rE 'className="[^"]*[a-z]' app/components/EmberGlass/automations/primitives/ | grep -v test` returns 0 (no Tailwind classes in primitives)
- `grep -c "ui-monospace" app/components/EmberGlass/automations/primitives/CronHint.tsx` returns ≥1
- `grep -c "rgba(255,255,255,0.05)" app/components/EmberGlass/automations/primitives/TextInput.tsx` returns ≥1 (matches bundle bg)
- TypeTile disabled test asserts `onClick` NOT called
</verification>

<success_criteria>
- 10 primitives compile, 7 jest specs pass
- Visual values match `<spacing_contract>` verbatim
- Inline-style discipline enforced (zero className for visual values)
- TypeTile supports D-12 disabled state for trigger read-only edit mode
- CronHint renders 5 Italian-labeled segments
</success_criteria>

<output>
After completion, create `.planning/phases/180-automations-tab-full-editor/180-03-SUMMARY.md` documenting:
- 10 primitive files (LOC each)
- 7 test files + total assertion count
- Any deviation from `<spacing_contract>` (none expected)
- Confirmation that no Tailwind classes leaked into primitive source
</output>
