---
phase: 180-automations-tab-full-editor
plan: 04
type: execute
wave: 3
depends_on: [180-02, 180-03]
files_modified:
  - app/components/EmberGlass/automations/forms/TriggerForms.tsx
  - app/components/EmberGlass/automations/sections/TriggerSection.tsx
  - app/components/EmberGlass/automations/__tests__/forms/TriggerForms.test.tsx
  - app/components/EmberGlass/automations/__tests__/sections/TriggerSection.test.tsx
autonomous: true
requirements: [AUTO-03]
tags: [phase-180, sections, trigger, D-08, D-12]

must_haves:
  truths:
    - "TriggerSection renders exactly 2 type tiles (schedule_cron + manual_api_call) per D-08 — NOT 5"
    - "ScheduleCronForm renders TextInput (mono variant) + CronHint 5-segment breakdown"
    - "ManualApiCallForm renders ONLY static info copy (Italian, bundle verbatim)"
    - "Edit mode (isNew=false) per D-12: tiles render with disabled prop; form fields readOnly; inline note 'Per cambiare il trigger, elimina e ricrea l'automazione.' visible above tile grid"
    - "Create mode (isNew=true): tiles fully interactive; form fields editable; no inline note"
    - "Switching trigger type calls onChange with defaultTrigger(newType) (Plan 02 factory)"
  artifacts:
    - path: app/components/EmberGlass/automations/sections/TriggerSection.tsx
      provides: "Section wrapper: 2-tile grid + per-type form + edit-mode read-only behavior"
      exports: ["TriggerSection"]
    - path: app/components/EmberGlass/automations/forms/TriggerForms.tsx
      provides: "ScheduleCronForm + ManualApiCallForm + TriggerForm dispatcher"
      exports: ["ScheduleCronForm", "ManualApiCallForm", "TriggerForm"]
  key_links:
    - from: app/components/EmberGlass/automations/sections/TriggerSection.tsx
      to: app/components/EmberGlass/automations/lib/automations-config.ts
      via: "import TRIGGER_TYPES, defaultTrigger"
      pattern: "from '\\.\\./lib/automations-config'"
    - from: app/components/EmberGlass/automations/forms/TriggerForms.tsx
      to: app/components/EmberGlass/automations/primitives/CronHint.tsx
      via: "import CronHint"
      pattern: "from '\\.\\./primitives/CronHint'"
---

<objective>
Land the Trigger tab section + 2 trigger forms (`ScheduleCronForm`, `ManualApiCallForm`) per CONTEXT D-08 / D-08a / D-08b / D-12. Bundle lines 325-406 verbatim, with the API-truth correction (2 trigger types, not 5) and the edit-mode read-only behavior (D-12).

Purpose: this is one of three section plans (Trigger / Conditions / Actions) that can run in parallel after Plans 02 + 03 land the lib + primitives. The trigger section is the smallest of the three (only 2 tiles + 2 forms + edit-mode disable handling).

Output: 2 source files + 2 jest specs covering AUTO-03 + D-12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/180-automations-tab-full-editor/180-CONTEXT.md
@.planning/phases/180-automations-tab-full-editor/180-RESEARCH.md
@.planning/phases/180-automations-tab-full-editor/180-PATTERNS.md
@.planning/phases/180-automations-tab-full-editor/180-UI-SPEC.md
@./CLAUDE.md
@./.planning/inbox/ember-glass-design/project/components/automations.jsx
@./app/components/EmberGlass/automations/types.ts
@./app/components/EmberGlass/automations/lib/automations-config.ts
@./app/components/EmberGlass/automations/primitives/TypeTile.tsx
@./app/components/EmberGlass/automations/primitives/TextInput.tsx
@./app/components/EmberGlass/automations/primitives/CronHint.tsx
@./app/components/EmberGlass/automations/primitives/FieldLabel.tsx
@./.planning/phases/180-automations-tab-full-editor/180-02-SUMMARY.md
@./.planning/phases/180-automations-tab-full-editor/180-03-SUMMARY.md

<section_layout>
<!-- UI-SPEC § Spacing — Section config panel + bundle line 325-345 -->

TriggerSection layout:
- Outer: no extra wrapper; section bodies render directly inside the editor's tab content area
- Edit-mode note (when !isNew): div { fontSize: 12, color: 'var(--text-2)', marginBottom: 10 }
- Tile grid: display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 8, marginBottom: 14
- Config panel (per-type form wrapper): padding: 14, borderRadius: 14, background: `color-mix(in oklab, ${tone} 8%, rgba(255,255,255,0.03))`, border: `0.5px solid color-mix(in oklab, ${tone} 20%, rgba(255,255,255,0.06))`
  Where {tone} comes from TRIGGER_TYPES.find(t => t.id === draft.trigger.type).tone
</section_layout>

<copy_strings>
<!-- UI-SPEC §Copywriting Contract — verbatim Italian -->

- Edit-mode note: "Per cambiare il trigger, elimina e ricrea l'automazione."
- ManualApiCallForm info copy: "Questa automazione si avvia solo quando viene invocata manualmente dall'app o via API."
- ScheduleCronForm field label: "Espressione cron"
- ScheduleCronForm placeholder: "0 8 * * *"
- ScheduleCronForm hint copy (under input + above CronHint): "Formato: min ora giorno mese giorno_sett."
- TriggerSection outer label (above tile grid): "Quando fire?" (bundle verbatim line 504 of UI-SPEC; unconventional Italian intentionally locked per CONTEXT D-02)
</copy_strings>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land TriggerForms.tsx (ScheduleCronForm + ManualApiCallForm + TriggerForm dispatcher) + jest spec</name>
  <files>app/components/EmberGlass/automations/forms/TriggerForms.tsx, app/components/EmberGlass/automations/__tests__/forms/TriggerForms.test.tsx</files>
  <read_first>
    - `.planning/inbox/ember-glass-design/project/components/automations.jsx` lines 347-397 (verbatim source for trigger forms — but ONLY the schedule_cron and manual_api_call branches per D-08)
    - `.planning/phases/180-automations-tab-full-editor/180-CONTEXT.md` D-08, D-08a, D-08b
    - `.planning/phases/180-automations-tab-full-editor/180-PATTERNS.md` (no specific section — use general inline-style pattern from primitives)
    - `<copy_strings>` block in this plan
    - `./app/components/EmberGlass/automations/primitives/TextInput.tsx` (just landed Plan 03)
    - `./app/components/EmberGlass/automations/primitives/CronHint.tsx` (just landed Plan 03)
    - `./app/components/EmberGlass/automations/primitives/FieldLabel.tsx`
  </read_first>
  <behavior>
    - ScheduleCronForm: renders FieldLabel "Espressione cron" + TextInput(mono, value=trigger.cron_expression, placeholder='0 8 * * *', readOnly={!isNew}) + small hint copy + CronHint(expr=trigger.cron_expression)
    - ScheduleCronForm onChange fires with `{ type: 'schedule_cron', cron_expression: newValue }`
    - ManualApiCallForm: renders ONLY a styled paragraph with the Italian info copy from `<copy_strings>`. No inputs, no fields.
    - TriggerForm dispatcher: discriminated switch on `trigger.type`; fallback returns null for unknown types (no assertNever — TS narrows to never via 2-member union, but runtime safety preferred)
    - All inputs respect `readOnly` prop when in edit mode
  </behavior>
  <action>
1. Create `app/components/EmberGlass/automations/forms/TriggerForms.tsx`:
```tsx
'use client';

import type { TriggerType, ScheduleCronTrigger, ManualApiCallTrigger } from '@/types/automations';
import { TextInput } from '../primitives/TextInput';
import { CronHint } from '../primitives/CronHint';
import { FieldLabel } from '../primitives/FieldLabel';

export interface TriggerFormProps<T extends TriggerType> {
  trigger: T;
  onChange: (next: T) => void;
  isNew: boolean;
}

export function ScheduleCronForm({
  trigger, onChange, isNew,
}: TriggerFormProps<ScheduleCronTrigger>) {
  return (
    <div>
      <FieldLabel htmlFor="trigger-cron">Espressione cron</FieldLabel>
      <TextInput
        id="trigger-cron"
        value={trigger.cron_expression}
        onChange={(v) => onChange({ type: 'schedule_cron', cron_expression: v })}
        placeholder="0 8 * * *"
        mono
        readOnly={!isNew}
        aria-label="Espressione cron"
      />
      <div
        style={{
          fontSize: 11,
          color: 'var(--text-2)',
          marginTop: 6,
          fontFamily: 'var(--font-body)',
        }}
      >
        Formato: min ora giorno mese giorno_sett.
      </div>
      <CronHint expr={trigger.cron_expression} />
    </div>
  );
}

export function ManualApiCallForm(_: TriggerFormProps<ManualApiCallTrigger>) {
  return (
    <div
      style={{
        fontSize: 12,
        color: 'var(--text-2)',
        lineHeight: 1.5,
        padding: 4,
      }}
    >
      Questa automazione si avvia solo quando viene invocata manualmente dall'app o via API.
    </div>
  );
}

export interface TriggerFormDispatchProps {
  trigger: TriggerType;
  onChange: (next: TriggerType) => void;
  isNew: boolean;
}

export function TriggerForm({ trigger, onChange, isNew }: TriggerFormDispatchProps) {
  switch (trigger.type) {
    case 'schedule_cron':
      return <ScheduleCronForm trigger={trigger} onChange={onChange} isNew={isNew} />;
    case 'manual_api_call':
      return <ManualApiCallForm trigger={trigger} onChange={onChange} isNew={isNew} />;
  }
  // TS narrows to never; runtime safety null
  return null;
}
```

2. Create `app/components/EmberGlass/automations/__tests__/forms/TriggerForms.test.tsx`:
```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { ScheduleCronForm, ManualApiCallForm, TriggerForm } from '../../forms/TriggerForms';

describe('ScheduleCronForm', () => {
  test('renders cron TextInput with mono font + value + CronHint', () => {
    render(
      <ScheduleCronForm
        trigger={{ type: 'schedule_cron', cron_expression: '0 8 * * *' }}
        onChange={() => {}}
        isNew
      />
    );
    const input = screen.getByLabelText(/Espressione cron/i) as HTMLInputElement;
    expect(input.value).toBe('0 8 * * *');
    expect(input).not.toHaveAttribute('readonly');
    // CronHint renders 5 segment labels
    expect(screen.getByText('min')).toBeInTheDocument();
    expect(screen.getByText('giorno sett.')).toBeInTheDocument();
  });
  test('onChange fires with full ScheduleCronTrigger shape', () => {
    const onChange = jest.fn();
    render(
      <ScheduleCronForm
        trigger={{ type: 'schedule_cron', cron_expression: '0 8 * * *' }}
        onChange={onChange}
        isNew
      />
    );
    fireEvent.change(screen.getByLabelText(/Espressione cron/i), { target: { value: '*/5 * * * *' } });
    expect(onChange).toHaveBeenCalledWith({ type: 'schedule_cron', cron_expression: '*/5 * * * *' });
  });
  test('isNew=false makes TextInput readOnly (D-12)', () => {
    render(
      <ScheduleCronForm
        trigger={{ type: 'schedule_cron', cron_expression: '0 8 * * *' }}
        onChange={() => {}}
        isNew={false}
      />
    );
    expect(screen.getByLabelText(/Espressione cron/i)).toHaveAttribute('readonly');
  });
  test('renders Italian hint copy', () => {
    render(
      <ScheduleCronForm
        trigger={{ type: 'schedule_cron', cron_expression: '0 8 * * *' }}
        onChange={() => {}}
        isNew
      />
    );
    expect(screen.getByText(/Formato: min ora giorno mese giorno_sett\./)).toBeInTheDocument();
  });
});

describe('ManualApiCallForm', () => {
  test('renders Italian info copy verbatim, no inputs', () => {
    render(<ManualApiCallForm trigger={{ type: 'manual_api_call' }} onChange={() => {}} isNew />);
    expect(
      screen.getByText(
        /Questa automazione si avvia solo quando viene invocata manualmente dall'app o via API\./
      )
    ).toBeInTheDocument();
    expect(screen.queryByRole('textbox')).not.toBeInTheDocument();
  });
});

describe('TriggerForm dispatcher', () => {
  test('schedule_cron renders ScheduleCronForm', () => {
    render(
      <TriggerForm
        trigger={{ type: 'schedule_cron', cron_expression: '0 8 * * *' }}
        onChange={() => {}}
        isNew
      />
    );
    expect(screen.getByLabelText(/Espressione cron/i)).toBeInTheDocument();
  });
  test('manual_api_call renders ManualApiCallForm', () => {
    render(<TriggerForm trigger={{ type: 'manual_api_call' }} onChange={() => {}} isNew />);
    expect(screen.getByText(/invocata manualmente/)).toBeInTheDocument();
  });
});
```
  </action>
  <verify>
    <automated>npm test -- app/components/EmberGlass/automations/__tests__/forms/TriggerForms.test.tsx</automated>
  </verify>
  <done>
    - 3 form components exported (ScheduleCronForm, ManualApiCallForm, TriggerForm)
    - >7 test cases passing
    - All Italian strings match `<copy_strings>` verbatim
    - readOnly prop honored in edit mode
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land TriggerSection.tsx (2-tile picker + edit-mode disable + config panel) + jest spec</name>
  <files>app/components/EmberGlass/automations/sections/TriggerSection.tsx, app/components/EmberGlass/automations/__tests__/sections/TriggerSection.test.tsx</files>
  <read_first>
    - `.planning/inbox/ember-glass-design/project/components/automations.jsx` lines 325-345 (verbatim source for TriggerSection wrapper)
    - `.planning/phases/180-automations-tab-full-editor/180-CONTEXT.md` D-08, D-12 (edit-mode read-only)
    - `.planning/phases/180-automations-tab-full-editor/180-UI-SPEC.md` § Section config panel + § State machine — TriggerSection tiles
    - `<section_layout>` + `<copy_strings>` blocks in this plan
    - `./app/components/EmberGlass/automations/primitives/TypeTile.tsx` (Plan 03) — disabled prop contract
    - `./app/components/EmberGlass/automations/lib/automations-config.ts` (Plan 02) — TRIGGER_TYPES + defaultTrigger
  </read_first>
  <behavior>
    - TriggerSection renders exactly 2 TypeTile components (matches TRIGGER_TYPES.length)
    - In create mode (isNew=true): tiles are fully interactive; clicking a tile NOT currently selected calls onTriggerChange(defaultTrigger(newType))
    - In edit mode (isNew=false): tiles render with `disabled` prop; clicking does NOT fire onTriggerChange; inline note "Per cambiare il trigger, elimina e ricrea l'automazione." renders ABOVE the tile grid
    - Config panel renders below the tile grid; uses tone-colored bg+border based on currently-selected trigger type's tone (lookup TRIGGER_TYPES)
    - TriggerForm (Task 1) renders inside the config panel with the current trigger + onChange + isNew props
    - When draft.trigger is null (shouldn't happen — emptyDraft sets manual_api_call default — but defensively), no config panel renders
  </behavior>
  <action>
1. Create `app/components/EmberGlass/automations/sections/TriggerSection.tsx`:
```tsx
'use client';

import type { TriggerType } from '@/types/automations';
import { TRIGGER_TYPES, defaultTrigger } from '../lib/automations-config';
import { TypeTile } from '../primitives/TypeTile';
import { TriggerForm } from '../forms/TriggerForms';

export interface TriggerSectionProps {
  trigger: TriggerType | null;
  onChange: (next: TriggerType) => void;
  isNew: boolean;
}

export function TriggerSection({ trigger, onChange, isNew }: TriggerSectionProps) {
  const selectedTone = trigger
    ? TRIGGER_TYPES.find((t) => t.id === trigger.type)?.tone ?? 'var(--text-2)'
    : 'var(--text-2)';

  return (
    <div>
      {!isNew && (
        <div
          style={{
            fontSize: 12,
            color: 'var(--text-2)',
            marginBottom: 10,
          }}
        >
          Per cambiare il trigger, elimina e ricrea l'automazione.
        </div>
      )}
      <div
        style={{
          display: 'grid',
          gridTemplateColumns: '1fr 1fr',
          gap: 8,
          marginBottom: 14,
        }}
      >
        {TRIGGER_TYPES.map((t) => {
          const Icon = t.Icon;
          return (
            <TypeTile
              key={t.id}
              icon={<Icon size={13} />}
              label={t.label}
              desc={t.desc}
              tone={t.tone}
              selected={trigger?.type === t.id}
              disabled={!isNew}
              onClick={
                isNew
                  ? () => {
                      // Only fire when switching to a different type
                      if (trigger?.type !== t.id) {
                        onChange(defaultTrigger(t.id));
                      }
                    }
                  : undefined
              }
              aria-label={t.label}
            />
          );
        })}
      </div>
      {trigger && (
        <div
          style={{
            padding: 14,
            borderRadius: 14,
            background: `color-mix(in oklab, ${selectedTone} 8%, rgba(255,255,255,0.03))`,
            border: `0.5px solid color-mix(in oklab, ${selectedTone} 20%, rgba(255,255,255,0.06))`,
          }}
        >
          <TriggerForm trigger={trigger} onChange={onChange} isNew={isNew} />
        </div>
      )}
    </div>
  );
}
```

2. Create `app/components/EmberGlass/automations/__tests__/sections/TriggerSection.test.tsx`:
```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { TriggerSection } from '../../sections/TriggerSection';

describe('TriggerSection (AUTO-03 + D-08 + D-12)', () => {
  test('renders exactly 2 type tiles (D-08 — schedule_cron + manual_api_call only)', () => {
    render(
      <TriggerSection
        trigger={{ type: 'manual_api_call' }}
        onChange={() => {}}
        isNew
      />
    );
    expect(screen.getAllByRole('button')).toHaveLength(2);
    expect(screen.getByLabelText(/Pianificazione/)).toBeInTheDocument();
    expect(screen.getByLabelText(/Manuale/)).toBeInTheDocument();
  });

  test('create mode: clicking unselected tile fires onChange with defaultTrigger', () => {
    const onChange = jest.fn();
    render(
      <TriggerSection
        trigger={{ type: 'manual_api_call' }}
        onChange={onChange}
        isNew
      />
    );
    fireEvent.click(screen.getByLabelText(/Pianificazione/));
    expect(onChange).toHaveBeenCalledWith({ type: 'schedule_cron', cron_expression: '0 8 * * *' });
  });

  test('create mode: clicking already-selected tile does NOT fire onChange', () => {
    const onChange = jest.fn();
    render(
      <TriggerSection
        trigger={{ type: 'manual_api_call' }}
        onChange={onChange}
        isNew
      />
    );
    fireEvent.click(screen.getByLabelText(/Manuale/));
    expect(onChange).not.toHaveBeenCalled();
  });

  test('edit mode (isNew=false): tiles disabled, click does NOT fire onChange (D-12)', () => {
    const onChange = jest.fn();
    render(
      <TriggerSection
        trigger={{ type: 'schedule_cron', cron_expression: '0 8 * * *' }}
        onChange={onChange}
        isNew={false}
      />
    );
    const cronTile = screen.getByLabelText(/Pianificazione/);
    expect(cronTile).toHaveAttribute('aria-disabled', 'true');
    fireEvent.click(cronTile);
    expect(onChange).not.toHaveBeenCalled();
  });

  test('edit mode: inline note appears above tile grid (D-12)', () => {
    render(
      <TriggerSection
        trigger={{ type: 'schedule_cron', cron_expression: '0 8 * * *' }}
        onChange={() => {}}
        isNew={false}
      />
    );
    expect(
      screen.getByText("Per cambiare il trigger, elimina e ricrea l'automazione.")
    ).toBeInTheDocument();
  });

  test('create mode: inline note does NOT render', () => {
    render(
      <TriggerSection
        trigger={{ type: 'manual_api_call' }}
        onChange={() => {}}
        isNew
      />
    );
    expect(
      screen.queryByText("Per cambiare il trigger, elimina e ricrea l'automazione.")
    ).not.toBeInTheDocument();
  });

  test('config panel renders TriggerForm matching the selected trigger type', () => {
    const { rerender } = render(
      <TriggerSection
        trigger={{ type: 'schedule_cron', cron_expression: '0 8 * * *' }}
        onChange={() => {}}
        isNew
      />
    );
    expect(screen.getByLabelText(/Espressione cron/)).toBeInTheDocument();
    rerender(
      <TriggerSection
        trigger={{ type: 'manual_api_call' }}
        onChange={() => {}}
        isNew
      />
    );
    expect(screen.getByText(/invocata manualmente/)).toBeInTheDocument();
    expect(screen.queryByLabelText(/Espressione cron/)).not.toBeInTheDocument();
  });
});
```
  </action>
  <verify>
    <automated>npm test -- app/components/EmberGlass/automations/__tests__/sections/TriggerSection.test.tsx</automated>
  </verify>
  <done>
    - TriggerSection exports 1 component
    - 7 test cases passing covering AUTO-03 + D-08 + D-12
    - Create mode + edit mode behavior split correctly
    - 2 tiles always (never 5 — D-08 lock)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → API | Trigger object built from controlled catalog (TRIGGER_TYPES + defaultTrigger); cron_expression is the only user-controlled string sent to API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-04-01 | Tampering | User crafts cron string with shell metacharacters | accept | Backend treats cron as a string (executed by croniter, not shell). Frontend renders verbatim in CronHint. No client-side validation needed. |
| T-180-04-02 | Tampering | Edit-mode tile click bypassed via JS (aria-disabled is advisory) | mitigate | Three layers: (1) onClick={isNew ? handler : undefined} so handler is structurally absent in edit mode; (2) TypeTile sets pointerEvents:none when disabled; (3) test asserts onChange NOT called. |
| T-180-04-03 | DoS | Frontend never invents the 3 sensor-trigger types as triggers | mitigate | TRIGGER_TYPES catalog is `as const` 2-element tuple. UI cannot render a 3rd tile. Backend would 422 on save anyway. D-08c locks this. |
</threat_model>

<verification>
- TriggerSection.tsx + TriggerForms.tsx exist
- Two tests files passing (~10 test cases combined)
- `grep -c 'sensor_state_change\|sensor_threshold\|netatmo_temperature_threshold' app/components/EmberGlass/automations/sections/TriggerSection.tsx app/components/EmberGlass/automations/forms/TriggerForms.tsx` returns 0 (no sensor triggers in trigger picker — D-08 lock)
- `grep -c "Per cambiare il trigger, elimina e ricrea l'automazione" app/components/EmberGlass/automations/sections/TriggerSection.tsx` returns 1
- Edit-mode test asserts onChange not called when tile clicked
</verification>

<success_criteria>
- TriggerSection renders 2 tiles (matches D-08 — never 5)
- Edit-mode read-only behavior (D-12) enforced at 3 levels: TypeTile disabled, TextInput readOnly, inline note visible
- Italian copy strings verbatim per UI-SPEC
- ScheduleCronForm + CronHint integration confirmed
- ManualApiCallForm renders ONLY info copy (no inputs)
</success_criteria>

<output>
After completion, create `.planning/phases/180-automations-tab-full-editor/180-04-SUMMARY.md` documenting:
- 2 source files (LOC each)
- 2 test files + assertion count
- Confirmation of D-12 read-only behavior across all 3 layers
- Any deviation from `<copy_strings>` (none expected)
</output>
