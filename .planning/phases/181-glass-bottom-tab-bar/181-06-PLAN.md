---
phase: 181-glass-bottom-tab-bar
plan: 06
type: execute
wave: 4
depends_on: [05]
files_modified:
  - tests/smoke/bottom-tab-bar.spec.ts
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]
validation_map:
  181-06-01: NAV-04  # Safe-area inset CSS contract at 375x812
  181-06-02: NAV-02  # Active tab tinting + accent color resolution
  181-06-03: NAV-03  # Bar hides under open Sheet, returns on close
  181-06-04: NAV-01  # Desktop centered 480px pill at 1280x800
  181-06-05: NAV-01,NAV-02,NAV-03,NAV-04  # No console errors during the spec
must_haves:
  truths:
    - "tests/smoke/bottom-tab-bar.spec.ts exists and runs under existing Playwright config"
    - "Spec reuses collectConsoleErrors + dismissVersionEnforcerIfPresent + dismissWhatsNewModalIfPresent + primeDashboardForSheetTest helpers from rooms-tab.spec.ts"
    - "Spec asserts data-bottom-tab=\"true\" element is visible on /, /stanze, /automazioni, /altro"
    - "Spec asserts computed bottom == '8px' at 375x812 (env=0 in headless Chromium per RESEARCH Pattern 9)"
    - "Spec asserts CSS source string contains 'env(safe-area-inset-bottom)' (NAV-04 contract)"
    - "Spec asserts width=480 + center-aligned at 1280x800 (NAV-01 desktop)"
    - "Spec asserts active tab color !== inactive grey when navigating /stanze (NAV-02)"
    - "Spec asserts bar slides off-screen when a sheet opens (NAV-03)"
    - "Spec gates console errors via collectConsoleErrors helper"
  artifacts:
    - path: "tests/smoke/bottom-tab-bar.spec.ts"
      provides: "Playwright smoke spec — at least 5 grouped tests covering NAV-01..04 + console-error gate"
      min_lines: 150
  key_links:
    - from: "tests/smoke/bottom-tab-bar.spec.ts"
      to: "tests/smoke/rooms-tab.spec.ts"
      via: "verbatim helper copy (collectConsoleErrors + dismiss helpers)"
      pattern: "collectConsoleErrors"
threat_model:
  - id: T-181-01
    category: I
    component: smoke spec (test code)
    disposition: accept
    mitigation: "Test code; no production surface."
  - id: T-181-02
    category: T
    component: smoke spec
    disposition: accept
    mitigation: "Test code; not user-facing."
  - id: T-181-03
    category: E
    component: smoke spec
    disposition: accept
    mitigation: "Test code; not user-facing."
---

<objective>
Ship the Phase 181 Playwright smoke spec — `tests/smoke/bottom-tab-bar.spec.ts` — that exercises NAV-01..04 end-to-end against a running Next dev server. The spec covers (1) safe-area inset CSS contract at 375x812 (NAV-04), (2) active-tab tinting on `/stanze` (NAV-02), (3) hide-on-sheet-open (NAV-03), (4) desktop-centered 480px pill at 1280x800 (NAV-01), and (5) console-error gate over the whole journey. Helpers (`collectConsoleErrors`, `dismissVersionEnforcerIfPresent`, `dismissWhatsNewModalIfPresent`, `primeDashboardForSheetTest`) are lifted verbatim from `tests/smoke/rooms-tab.spec.ts:33-127` per CONTEXT D-15 + PATTERNS.md.

Purpose: Final verification gate before phase sign-off. Headless Chromium does NOT simulate iOS home-indicator inset (env=0); the spec asserts the CSS contract (`bottom: '8px'`) and the source string presence (`env(safe-area-inset-bottom)`). Real-device verification is documented in VALIDATION.md §Manual-Only Verifications and is intentionally out of scope here.
Output: Single Playwright spec file under `tests/smoke/` (NOT `tests/playwright/` — RESEARCH Pattern 9 corrects that CONTEXT typo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/181-glass-bottom-tab-bar/181-CONTEXT.md
@.planning/phases/181-glass-bottom-tab-bar/181-PATTERNS.md
@.planning/phases/181-glass-bottom-tab-bar/181-UI-SPEC.md
@.planning/phases/181-glass-bottom-tab-bar/181-VALIDATION.md
@./CLAUDE.md

@tests/smoke/rooms-tab.spec.ts
@tests/smoke/sheet-primitive.spec.ts
@playwright.config.ts

<interfaces>
Helpers to lift VERBATIM from tests/smoke/rooms-tab.spec.ts:
- `collectConsoleErrors(page: Page): { errors: string[]; cleanup: () => void }` (lines 33-45)
- `dismissVersionEnforcerIfPresent(page: Page): Promise<void>` (lines 53-70)
- `dismissWhatsNewModalIfPresent(page: Page): Promise<void>` (lines 83-97)
- `primeDashboardForSheetTest(page: Page): Promise<void>` (lines 105-127)

Spec target shape (CONTEXT D-15 → 7 cases; mirror rooms-tab.spec.ts:252-411 describe pattern).

Critical path: tests/smoke/  (NOT tests/playwright/)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 181-06-01: Create tests/smoke/bottom-tab-bar.spec.ts with 5+ grouped tests + console-error gate</name>
  <files>
    tests/smoke/bottom-tab-bar.spec.ts
  </files>
  <read_first>
    - tests/smoke/rooms-tab.spec.ts (FULL file — copy helpers verbatim from lines 33-127; mirror describe shape from lines 252-411)
    - tests/smoke/sheet-primitive.spec.ts (sheet-open helper — for NAV-03 hide test, find an existing pattern that opens a Phase 175 Sheet from / and reuse its open-trigger sequence; OR open the Phase 178 StoveSheet via dashboard click as PATTERNS.md suggests)
    - playwright.config.ts (confirm webServer / baseURL conventions)
    - .planning/phases/181-glass-bottom-tab-bar/181-CONTEXT.md (D-15 — 7 spec cases)
    - .planning/phases/181-glass-bottom-tab-bar/181-PATTERNS.md (`tests/smoke/bottom-tab-bar.spec.ts` section — full scaffold)
    - .planning/phases/181-glass-bottom-tab-bar/181-UI-SPEC.md (lines ~671-678 — Playwright verification scope, Approach A)
    - .planning/phases/181-glass-bottom-tab-bar/181-VALIDATION.md (per-task verification map for 181-06-01..05)
    - .planning/phases/181-glass-bottom-tab-bar/181-RESEARCH.md (Pattern 9 — file path is tests/smoke/ not tests/playwright/)
  </read_first>
  <action>
Create `tests/smoke/bottom-tab-bar.spec.ts` (NOT `tests/playwright/...`). Copy the helper functions verbatim from `tests/smoke/rooms-tab.spec.ts` lines 33-127, then build the test.describe block per the structure below. Mirror the rooms-tab.spec.ts:252-411 organization style.

Spec scaffold (assemble the helper sources by reading rooms-tab.spec.ts first; substitute the helper bodies verbatim):

```ts
import { test, expect, type Page } from '@playwright/test';

/**
 * Phase 181 — Bottom Tab Bar smoke (NAV-01..04).
 *
 * Headless Chromium does NOT simulate iOS home-indicator inset
 * (`env(safe-area-inset-bottom)` returns 0 — RESEARCH Pattern 9 / Pitfall 4).
 * The spec asserts the CSS contract (`bottom: '8px'`) plus the source string
 * presence of `env(safe-area-inset-bottom)`. Real-device 34px inset
 * verification is documented in 181-VALIDATION.md §Manual-Only Verifications.
 *
 * Helpers below are LIFTED VERBATIM from tests/smoke/rooms-tab.spec.ts:33-127.
 */

// ---------------------------------------------------------------------------
// Helpers (verbatim from rooms-tab.spec.ts)
// ---------------------------------------------------------------------------

function collectConsoleErrors(page: Page): { errors: string[]; cleanup: () => void } {
  // ... copy verbatim from rooms-tab.spec.ts:33-45 ...
}

async function dismissVersionEnforcerIfPresent(page: Page): Promise<void> {
  // ... copy verbatim from rooms-tab.spec.ts:53-70 ...
}

async function dismissWhatsNewModalIfPresent(page: Page): Promise<void> {
  // ... copy verbatim from rooms-tab.spec.ts:83-97 ...
}

async function primeDashboardForSheetTest(page: Page): Promise<void> {
  // ... copy verbatim from rooms-tab.spec.ts:105-127 ...
}

// ---------------------------------------------------------------------------
// Spec
// ---------------------------------------------------------------------------

test.describe('Phase 181 — Bottom Tab Bar (NAV-01..04)', () => {
  test.beforeEach(async ({ page }) => {
    await primeDashboardForSheetTest(page);
    await page.setViewportSize({ width: 375, height: 812 });
    await page.goto('/');
    await page.waitForLoadState('domcontentloaded');
    await dismissVersionEnforcerIfPresent(page);
    await dismissWhatsNewModalIfPresent(page);
  });

  test('NAV-04 safe-area inset CSS contract at 375x812 (computed bottom + source string)', async ({ page }) => {
    const bar = page.locator('[data-bottom-tab="true"]');
    await expect(bar).toBeVisible();

    const computed = await bar.evaluate((el) => {
      const r = el.getBoundingClientRect();
      const cs = getComputedStyle(el);
      return { bottomRect: r.bottom, computedBottom: cs.bottom };
    });

    // env() returns 0 in headless Chromium → calc(8px + 0) = 8px.
    expect(computed.computedBottom).toBe('8px');

    // Bar's bottom edge is within the viewport bottom minus 8px (allow 2px float).
    expect(Math.abs(computed.bottomRect - (812 - 8))).toBeLessThan(2);

    // Source CSS string MUST contain env(safe-area-inset-bottom) (NAV-04 contract).
    // Read either from the inline style attribute or from the computed style on iOS.
    const inlineBottom = await bar.evaluate((el) => (el as HTMLElement).style.bottom);
    expect(inlineBottom).toContain('env(safe-area-inset-bottom)');
  });

  test('NAV-02 active tab tints on /stanze with accent (color !== inactive grey)', async ({ page }) => {
    const stanzeLink = page.locator('[data-bottom-tab="true"]').getByRole('link', { name: /stanze/i });
    await stanzeLink.click();
    await expect(page).toHaveURL(/\/stanze/);

    const active = page.locator('[data-bottom-tab="true"] [aria-current="page"]').first();
    await expect(active).toBeVisible();

    const color = await active.evaluate((el) => getComputedStyle(el).color);
    // var(--accent) resolves to RGB; assert non-empty + not the inactive 55% white.
    expect(color).not.toBe('rgba(255, 255, 255, 0.55)');
    expect(color).not.toBe('');
  });

  test('NAV-02 navigating to /altro renders Esci row', async ({ page }) => {
    const altroLink = page.locator('[data-bottom-tab="true"]').getByRole('link', { name: /altro/i });
    await altroLink.click();
    await expect(page).toHaveURL(/\/altro/);
    await expect(page.getByRole('link', { name: /esci/i })).toBeVisible();
  });

  test('NAV-01 desktop-centered 480px pill at 1280x800', async ({ page }) => {
    await page.setViewportSize({ width: 1280, height: 800 });
    // Allow the @media (min-width: 640px) rule to apply after resize.
    await page.waitForTimeout(100);
    const bar = page.locator('[data-bottom-tab="true"]');
    const box = await bar.boundingBox();
    expect(box).not.toBeNull();
    expect(box!.width).toBe(480);
    // Centered: x + width/2 ≈ viewportWidth/2 ± 4px.
    expect(Math.abs(box!.x + box!.width / 2 - 1280 / 2)).toBeLessThan(4);
  });

  test('NAV-03 bar hides under an open sheet', async ({ page }) => {
    // Open a sheet from the dashboard. The exact open-trigger depends on the
    // current dashboard composition (e.g., StoveCard click opens Phase 178 StoveSheet).
    // Reuse the open-sequence pattern from tests/smoke/sheet-primitive.spec.ts or
    // rooms-tab.spec.ts (whichever opens a Phase 178+ sheet from /).
    //
    // After opening, the body[data-sheet-open="true"] selector + globals.css rule
    // (Plan 01 D-09) slides the bar off-screen via translateY(140%).
    //
    // Read the existing sheet-open pattern in tests/smoke/sheet-primitive.spec.ts
    // before completing this test. If no existing pattern opens a sheet from /,
    // assert the augmentation directly: programmatically set the body attribute
    // via page.evaluate and confirm the bar is no longer in the visible viewport.

    const bar = page.locator('[data-bottom-tab="true"]');
    const beforeBox = await bar.boundingBox();
    expect(beforeBox).not.toBeNull();
    expect(beforeBox!.y).toBeLessThan(812);

    // Programmatic body attribute set — same as a real Phase 175 Sheet opening.
    await page.evaluate(() => {
      document.body.dataset.sheetOpen = 'true';
    });

    // Wait for the .3s slide-out transition.
    await page.waitForTimeout(400);

    const afterBox = await bar.boundingBox();
    // The bar slides translateY(140%) off-screen; bounding box top is below
    // viewport bottom (or boundingBox returns null if fully off-screen).
    if (afterBox !== null) {
      expect(afterBox.y).toBeGreaterThan(812 - 10);
    }

    // Cleanup: clear the attribute so subsequent tests start fresh.
    await page.evaluate(() => {
      delete document.body.dataset.sheetOpen;
    });
  });

  test('NAV-01..04 console-error gate over /altro navigation', async ({ page }) => {
    const { errors, cleanup } = collectConsoleErrors(page);
    await page.goto('/altro');
    await page.waitForLoadState('domcontentloaded');
    await dismissVersionEnforcerIfPresent(page);
    cleanup();
    expect(errors).toEqual([]);
  });
});
```

Important implementation notes:
- The helper bodies must be COPIED VERBATIM from `tests/smoke/rooms-tab.spec.ts` (lines 33-45, 53-70, 83-97, 105-127). Read that file first; do not reimplement from memory.
- If `primeDashboardForSheetTest` is the wrong helper for `/` (it may be tuned to a different page), use whatever `rooms-tab.spec.ts` uses for its `goto('/')` priming sequence. Match the spec's existing patterns exactly.
- For the NAV-03 hide test, the programmatic `body.dataset.sheetOpen = 'true'` shortcut is used because opening a real sheet end-to-end is fragile (depends on dashboard state, device wiring, etc.). The Plan 01 + Plan 05 architecture guarantees the CSS rule + selector hook combo work — this test verifies the contract the spec depends on.
- File path is `tests/smoke/bottom-tab-bar.spec.ts` — NOT `tests/playwright/...`. RESEARCH Pattern 9 corrects the CONTEXT typo.
- Total spec ~150-200 LOC including helpers.
  </action>
  <verify>
    <automated>npx playwright test tests/smoke/bottom-tab-bar.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/smoke/bottom-tab-bar.spec.ts` exists.
    - `grep -c "test(" tests/smoke/bottom-tab-bar.spec.ts` returns `>= 5`.
    - `grep -q "data-bottom-tab=\"true\"" tests/smoke/bottom-tab-bar.spec.ts`.
    - `grep -q "collectConsoleErrors" tests/smoke/bottom-tab-bar.spec.ts` (helper present).
    - `grep -q "dismissVersionEnforcerIfPresent" tests/smoke/bottom-tab-bar.spec.ts`.
    - `grep -q "375" tests/smoke/bottom-tab-bar.spec.ts` AND `grep -q "812" tests/smoke/bottom-tab-bar.spec.ts` (mobile viewport).
    - `grep -q "1280" tests/smoke/bottom-tab-bar.spec.ts` AND `grep -q "800" tests/smoke/bottom-tab-bar.spec.ts` (desktop viewport).
    - `grep -q "env(safe-area-inset-bottom)" tests/smoke/bottom-tab-bar.spec.ts` (NAV-04 source-string assertion).
    - `grep -q "data-sheet-open" tests/smoke/bottom-tab-bar.spec.ts` (NAV-03 attribute interaction).
    - `grep -q "/altro" tests/smoke/bottom-tab-bar.spec.ts` (Altro navigation test).
    - `npx playwright test tests/smoke/bottom-tab-bar.spec.ts --reporter=list` exits `0` with all tests passing (or, if the dev server fixture is unavailable in the worker, the executor retries with `--workers=1` or documents the environment gap in the SUMMARY).
  </acceptance_criteria>
  <done>
    Smoke spec exists in `tests/smoke/`, uses verbatim rooms-tab helpers, covers all 5 listed NAV verifications + console-error gate, and runs green under `npx playwright test tests/smoke/bottom-tab-bar.spec.ts`.
  </done>
</task>

<task type="auto">
  <name>Task 181-06-02: Final scoped test pass + UI smoke at 375px and 1280px breakpoints</name>
  <files>
    (no files modified — gate-only task)
  </files>
  <read_first>
    - tests/smoke/bottom-tab-bar.spec.ts (post-Task 181-06-01 state)
    - .planning/phases/181-glass-bottom-tab-bar/181-VALIDATION.md (Sampling Rate section)
    - ./CLAUDE.md rule 8 (scoped commands only)
  </read_first>
  <action>
Run the final scoped verification suite for Phase 181 before sign-off:

1. `npm run test:components` — broad component-level pass (all changed files in app/components/EmberGlass/, app/components/layout/).
2. `npm run test:pages` — broad page-level pass (covers app/altro/__tests__/page.test.tsx).
3. `npx playwright test tests/smoke/bottom-tab-bar.spec.ts --reporter=list` — the new smoke.

Document in the Plan 06 SUMMARY:
- Pass/fail counts for each invocation.
- The output of the spec at both 375x812 and 1280x800 viewport assertions (the spec exercises both — confirm both passed).
- Any console-error noise observed (the `collectConsoleErrors` helper hard-fails if any are seen; document if a known transient was filtered upstream by `dismissVersionEnforcerIfPresent`).

Do NOT use `npm test` (CLAUDE.md rule 8). Do NOT install new packages (rule 4). Do NOT run `npm run build` (rule 4).

If any spec fails: STOP. Surface the failure in the SUMMARY and request a remediation plan before phase sign-off.
  </action>
  <verify>
    <automated>npm run test:components && npm run test:pages && npx playwright test tests/smoke/bottom-tab-bar.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:components` exits `0`.
    - `npm run test:pages` exits `0`.
    - `npx playwright test tests/smoke/bottom-tab-bar.spec.ts --reporter=list` exits `0`.
    - Aggregate output shows `0 failed` for each suite.
    - The Plan 06 SUMMARY records pass counts + viewport-breakpoint coverage notes.
  </acceptance_criteria>
  <done>
    All three scoped suites green; phase verification gate passes; SUMMARY records pass counts and breakpoint coverage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke spec | Test code; no production surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-01 | I | smoke spec | accept | Test code only. |
| T-181-02 | T | smoke spec | accept | Test code only. |
| T-181-03 | E | smoke spec | accept | Test code only. |
</threat_model>

<verification>
- `npx playwright test tests/smoke/bottom-tab-bar.spec.ts` exits `0` with all listed assertions passing.
- `npm run test:components` and `npm run test:pages` continue to exit `0` (no regression from the layout swap).
- Console-error gate green over `/altro` navigation.
</verification>

<success_criteria>
- tests/smoke/bottom-tab-bar.spec.ts ships with 5+ grouped tests covering NAV-01..04 + console-error gate.
- Helpers are verbatim copies from rooms-tab.spec.ts (no drift from established convention).
- All scoped Jest + Playwright suites green.
- Phase 181 ready for sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/181-glass-bottom-tab-bar/181-06-SUMMARY.md` per template.
</output>


## Truths — Decisions Addressed

- D-15: Playwright smoke spec covering NAV-01..04 + sheet-hide + desktop-center + console-errors
