---
phase: 182-design-system-reference-page-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/debug/design-system-v2/page.tsx
  - app/debug/design-system-v2/sections/Section01Hue.tsx
  - app/debug/design-system-v2/sections/Section02Ambient.tsx
  - app/debug/design-system-v2/sections/Section03Tokens.tsx
  - app/debug/design-system-v2/sections/Section04GlassSurface.tsx
  - app/debug/design-system-v2/sections/Section05Press.tsx
  - app/debug/design-system-v2/sections/Section06Sheet.tsx
  - app/debug/design-system-v2/sections/Section07Splash.tsx
autonomous: true
requirements: [DSREF-03]

must_haves:
  truths:
    - "Existing /debug/design-system-v2 page still renders 7 numbered sections with stable IDs sec-01-heading..sec-07-heading"
    - "Existing accent picker still writes --accent live and persists to localStorage (DSREF-03 invariant)"
    - "Existing Playwright specs accent-picker.spec.ts and ambient-persist.spec.ts pass without edits"
    - "Existing Jest spec page.test.tsx passes without edits"
    - "Reconciliation: CONTEXT.md D-10 superseded — see <reconciliation> block in objective"
  artifacts:
    - path: "app/debug/design-system-v2/page.tsx"
      provides: "Thin orchestrator that imports + renders Section01Hue..Section07Splash in order"
      contains: "import { Section01Hue }"
    - path: "app/debug/design-system-v2/sections/Section01Hue.tsx"
      provides: "Verbatim extract of existing sec-01 (HUE picker)"
      contains: "id=\"sec-01-heading\""
    - path: "app/debug/design-system-v2/sections/Section02Ambient.tsx"
      provides: "Verbatim extract of existing sec-02 (AMBIENT toggle)"
      contains: "id=\"sec-02-heading\""
    - path: "app/debug/design-system-v2/sections/Section03Tokens.tsx"
      provides: "Verbatim extract of existing sec-03 (TOKENS oklch swatch grid). Will be extended in Plan 05."
      contains: "id=\"sec-03-heading\""
    - path: "app/debug/design-system-v2/sections/Section04GlassSurface.tsx"
      provides: "Verbatim extract of existing sec-04 (GLASS SURFACE DEMO)"
      contains: "id=\"sec-04-heading\""
    - path: "app/debug/design-system-v2/sections/Section05Press.tsx"
      provides: "Verbatim extract of existing sec-05 (PRESS PRIMITIVE demo)"
      contains: "id=\"sec-05-heading\""
    - path: "app/debug/design-system-v2/sections/Section06Sheet.tsx"
      provides: "Verbatim extract of existing sec-06 (SHEET preview demo)"
      contains: "id=\"sec-06-heading\""
    - path: "app/debug/design-system-v2/sections/Section07Splash.tsx"
      provides: "Verbatim extract of existing sec-07 (SPLASH replay)"
      contains: "id=\"sec-07-heading\""
  key_links:
    - from: "app/debug/design-system-v2/page.tsx"
      to: "app/debug/design-system-v2/sections/Section01Hue.tsx"
      via: "default import + render"
      pattern: "import \\{ Section01Hue \\}"
    - from: "Section01Hue.tsx"
      to: "document.documentElement.style"
      via: "setAccent helper"
      pattern: "setProperty\\('--accent'"
---

<objective>
<reconciliation>
Section ordering reconciliation (orchestrator override of CONTEXT.md D-10):
- CONTEXT.md D-10 was authored before the page reached its current 7-section form. The actual page has 7 sections: 01/HUE, 02/AMBIENT, 03/TOKENS, 04/GLASS-SURFACE, 05/PRESS, 06/SHEET, 07/SPLASH.
- tests/smoke/press-primitive.spec.ts (Phase 175 contract) and accent-picker.spec.ts / ambient-persist.spec.ts (Phase 174) key off `sec-NN-heading` IDs. Renumbering or dropping these sections would break locked test contracts.
- Therefore: Phase 182 PRESERVES all 7 existing sections (extracting each to `Section0XYyy.tsx` verbatim) and ADDS new sections as 08/CARDS, 09/SHEET, 10/DEMO. CONTEXT.md D-10's '05/06/07/08' numbering and D-26's eyebrow strings ('05 / TOKENS', '06 / CARDS', etc.) are SUPERSEDED by this reconciliation. Tokens stay at section 03 (existing); the new copy strings are '08 / CARDS', '09 / SHEET', '10 / DEMO'.
</reconciliation>

Decompose the existing 667-LOC `app/debug/design-system-v2/page.tsx` into 7 per-section files under `app/debug/design-system-v2/sections/` (one per existing numbered section) and trim `page.tsx` to a thin orchestrator that imports + renders them in order. This is a pure file-move + import-wiring operation. No behavior changes. The existing accent picker keeps its `--accent` write path (DSREF-03 invariant). Existing test suites (`tests/smoke/accent-picker.spec.ts`, `tests/smoke/ambient-persist.spec.ts`, `tests/smoke/press-primitive.spec.ts`, `app/debug/design-system-v2/__tests__/page.test.tsx`) MUST stay green with zero edits.

Per the orchestrator's `<research_reconciliation>` block: the page already has 7 sections numbered 01-07 (HUE / AMBIENT / TOKENS / GLASS-SURFACE / PRESS / SHEET / SPLASH). DO NOT renumber, DO NOT drop any section. Existing tests assert against `sec-01-heading` and `sec-02-heading` IDs and against headings like "Ember Glass" — these MUST survive verbatim.

Purpose: Establishes the section-decomposition foundation that Plans 05-08 extend. Without this plan, page.tsx becomes unmaintainable when Sections 08/09/10 are added.
Output: 7 new section component files, 1 trimmed orchestrator page, all existing tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-design-system-reference-page-v2/182-CONTEXT.md
@.planning/phases/182-design-system-reference-page-v2/182-RESEARCH.md
@.planning/phases/182-design-system-reference-page-v2/182-PATTERNS.md
@app/debug/design-system-v2/page.tsx
@app/debug/design-system-v2/__tests__/page.test.tsx
@tests/smoke/accent-picker.spec.ts
@tests/smoke/ambient-persist.spec.ts

<interfaces>
<!-- The current page.tsx exports default DesignSystemV2Page. After this plan, page.tsx
     remains the default export but delegates body to 7 named-export section components. -->

From app/components/EmberGlass/index.ts (existing barrel — used as-is):
```typescript
export { Pressable, usePressed } from './Pressable';
export type { PressableProps } from './Pressable';
export { Sheet } from './Sheet';
export { default as AmbientBg } from './AmbientBg';
export { Splash } from './Splash';
export { SplashGate } from './SplashGate';
```

From app/debug/design-system-v2/page.tsx (existing — sources the verbatim extract):
```typescript
// State helpers extracted from current page.tsx lines 31-95:
const ACCENT_PRESETS = {
  copper: 'oklch(0.68 0.17 45)',
  rose:   'oklch(0.68 0.17 0)',
  violet: 'oklch(0.65 0.17 290)',
  blue:   'oklch(0.65 0.14 230)',
  green:  'oklch(0.68 0.12 150)',
  amber:  'oklch(0.76 0.15 75)',
} as const;
type HueName = keyof typeof ACCENT_PRESETS;
const HUE_DISPLAY_NAMES: Record<HueName, string> = { copper:'Copper', rose:'Rose', violet:'Violet', blue:'Blue', green:'Green', amber:'Amber' };
function setAccent(value: string): void { ... }
function setAmbient(on: boolean): void { ... }

// State currently held in DesignSystemV2Page (lines 71-76):
const [activeHue, setActiveHue] = useState<HueName>('copper');
const [ambientOn, setAmbientOn] = useState<boolean>(false);
const [sheetOpen, setSheetOpen] = useState<boolean>(false);
const [replayKey, setReplayKey] = useState<number>(0);
```

Section line ranges in the CURRENT page.tsx (verified 2026-05-03 by direct grep on `aria-labelledby`):
- Section 01 (HUE):           lines 161-219  → Section01Hue.tsx
- Section 02 (AMBIENT):       lines 222-292  → Section02Ambient.tsx
- Section 03 (TOKENS):        lines 295-361  → Section03Tokens.tsx
- Section 04 (GLASS SURFACE): lines 364-423  → Section04GlassSurface.tsx
- Section 05 (PRESS):         lines 426-513  → Section05Press.tsx
- Section 06 (SHEET):         lines 517-593  → Section06Sheet.tsx
- Section 07 (SPLASH):        lines 599-664  → Section07Splash.tsx
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the 7 section files by extracting JSX + state from page.tsx verbatim</name>
  <read_first>
    - app/debug/design-system-v2/page.tsx (full read — source of all 7 verbatim extracts; 667 LOC)
    - app/debug/design-system-v2/__tests__/page.test.tsx (lines 1-138 — confirms which IDs/strings tests rely on)
    - tests/smoke/accent-picker.spec.ts (full — confirms button aria-label "Set accent to {Name}" and oklch values must survive verbatim)
    - tests/smoke/ambient-persist.spec.ts (full — confirms ambient toggle JS contract)
    - tests/smoke/press-primitive.spec.ts (full — confirms PRESS section assertions still apply)
    - app/components/EmberGlass/index.ts (lines 1-52 — barrel imports needed by sections)
  </read_first>
  <files>
    app/debug/design-system-v2/sections/Section01Hue.tsx (NEW),
    app/debug/design-system-v2/sections/Section02Ambient.tsx (NEW),
    app/debug/design-system-v2/sections/Section03Tokens.tsx (NEW),
    app/debug/design-system-v2/sections/Section04GlassSurface.tsx (NEW),
    app/debug/design-system-v2/sections/Section05Press.tsx (NEW),
    app/debug/design-system-v2/sections/Section06Sheet.tsx (NEW),
    app/debug/design-system-v2/sections/Section07Splash.tsx (NEW)
  </files>
  <action>
    Create 7 new files under `app/debug/design-system-v2/sections/`. Each file is a verbatim extract of one numbered section from `app/debug/design-system-v2/page.tsx`. Per D-01 (CONTEXT.md) and D-03 (every section is `'use client'`).

    **General template for every Section file:**
    ```tsx
    'use client';
    import React, { useState, useEffect } from 'react';
    // ...component-specific imports from @/app/components/EmberGlass

    // ...module-level helpers and constants (extracted from page.tsx top scope, see per-file list below)

    export function SectionXxYyy(): React.ReactElement {
      // ...local useState + useEffect (extracted from DesignSystemV2Page body)
      return (
        // ...JSX verbatim from page.tsx for this section's <section> block
      );
    }
    ```

    **Per-file extract details — verbatim copy with these mechanical edits:**

    **1. `Section01Hue.tsx` — extract page.tsx <section aria-labelledby="sec-01-heading"> at lines 161-219.**
       - Imports: `import React, { useState, useEffect } from 'react'; import { Pressable } from '@/app/components/EmberGlass';`
       - Module-level (copy verbatim from page.tsx lines 29-58, including the AUDIT-EXCEPTION comment): `ACCENT_PRESETS`, `HueName` type, `HUE_DISPLAY_NAMES`, `setAccent()` function. Per D-25/D-26 keep all comments + Italian/English copy strings exactly.
       - Component body extracts from page.tsx lines 71, 81-95, 97-100: `const [activeHue, setActiveHue] = useState<HueName>('copper');` plus the `useEffect` localStorage hydration block AND `onSwatchClick` handler. Only extract the activeHue-related parts of the existing useEffect (the ambient lines 90-91 belong to Section02Ambient — see file 2).
       - JSX: copy the entire `<section aria-labelledby="sec-01-heading" ...>...</section>` block from page.tsx 161-219, character-for-character. Keep `id="sec-01-heading"`, the `01 / HUE` eyebrow, "Tinte accento" h2, all 6 swatch buttons with their `aria-label="Set accent to {HUE_DISPLAY_NAMES[hue]}"` and `aria-pressed={activeHue === hue}` attributes (test contract — accent-picker.spec.ts:19 + page.test.tsx:43-46).

    **2. `Section02Ambient.tsx` — extract page.tsx <section aria-labelledby="sec-02-heading"> at lines 222-292.**
       - Imports: `import React, { useState, useEffect } from 'react';` (no EmberGlass import needed — this section uses raw `<button role="switch">`).
       - Module-level helper `setAmbient` extracted from page.tsx lines 60-68 verbatim (keep T-174-03-04 try/catch comment).
       - Component body: `const [ambientOn, setAmbientOn] = useState<boolean>(false);` + a `useEffect` that reads `localStorage.getItem('ember-glass-ambient')` and sets ambientOn to true if persisted (split this off the original combined useEffect in page.tsx lines 81-95 — keep only the ambient-related lines 90-91 wrapped in their own useEffect with its own try/catch). Plus `onAmbientToggle` extracted from lines 102-106.
       - JSX: copy the entire `<section aria-labelledby="sec-02-heading" ...>...</section>` block from page.tsx 222-292 verbatim. Keep `id="sec-02-heading"`, `02 / AMBIENT` eyebrow, "Glow ambient" h2, the `<button role="switch" aria-checked={ambientOn}>` element exactly.

    **3. `Section03Tokens.tsx` — extract page.tsx <section aria-labelledby="sec-03-heading"> at lines 295-361.**
       - Imports: `import React from 'react';` (server-renderable; existing block is purely declarative).
       - Component body: zero state for now (no useState; this section is a static token grid). Plan 05 will extend this with typography/spacing/shadow tiles.
       - JSX: copy the entire `<section aria-labelledby="sec-03-heading" ...>...</section>` block from page.tsx 295-361 verbatim. Keep `id="sec-03-heading"`, `03 / TOKENS` eyebrow, the existing oklch swatch grid + neutral tones table.

    **4. `Section04GlassSurface.tsx` — extract page.tsx <section aria-labelledby="sec-04-heading"> at lines 364-423.**
       - Imports: `import React from 'react';` plus any EmberGlass imports the section uses (audit page.tsx imports at line 27 — likely none beyond what's already in scope at top; if `<GlassCard>` etc. is used, import from `@/app/components/EmberGlass`).
       - Component body: zero state.
       - JSX: copy the entire `<section aria-labelledby="sec-04-heading" ...>...</section>` block from page.tsx 364-423 verbatim. Keep `id="sec-04-heading"` and `04 / GLASS` (or whatever exact eyebrow string is in the source — copy character-for-character).

    **5. `Section05Press.tsx` — extract page.tsx <section aria-labelledby="sec-05-heading"> at lines 426-513.**
       - Imports: `'use client';` (this section has interactive Pressable demos with state). `import React, { useState } from 'react'; import { Pressable } from '@/app/components/EmberGlass';` (audit the source for any additional imports used by the press-primitive demo).
       - Component body: any local useState used by the press demo (copy from page.tsx — likely small; e.g. demonstration toggles).
       - JSX: copy the entire `<section aria-labelledby="sec-05-heading" ...>...</section>` block from page.tsx 426-513 verbatim. Keep `id="sec-05-heading"` and `05 / PRESS` eyebrow.

    **6. `Section06Sheet.tsx` — extract page.tsx <section aria-labelledby="sec-06-heading"> at lines 517-593.**
       - Imports: `'use client';` `import React, { useState } from 'react'; import { Sheet } from '@/app/components/EmberGlass';`
       - Component body: `const [sheetOpen, setSheetOpen] = useState<boolean>(false);` (extracted from page.tsx line 73).
       - JSX: copy the entire `<section aria-labelledby="sec-06-heading" ...>...</section>` block from page.tsx 517-593 verbatim. Keep `id="sec-06-heading"`, `06 / SHEET` eyebrow, the demo Sheet preview.

    **7. `Section07Splash.tsx` — extract page.tsx <section aria-labelledby="sec-07-heading"> at lines 599-664.**
       - Imports: `'use client';` `import React, { useState } from 'react'; import { SplashGate } from '@/app/components/EmberGlass';`
       - Component body: `const [replayKey, setReplayKey] = useState<number>(0);` (extracted from page.tsx line 76).
       - JSX: copy the entire `<section aria-labelledby="sec-07-heading" ...>...</section>` block from page.tsx 599-664 verbatim. Keep `id="sec-07-heading"`, `07 / SPLASH` eyebrow, the replay-splash button + SplashGate forceShow render keyed on `replayKey`.

    **Critical invariants (per D-02, D-03, D-25, D-26):**
    - DO NOT change inline styles. Every `style={{ ... }}` block is copied character-for-character.
    - DO NOT change `aria-label`, `aria-pressed`, `aria-checked`, `id`, or `aria-labelledby` attributes — existing tests key off these.
    - DO NOT change the visible Italian copy strings (e.g. "Tinte accento", "Glow ambient").
    - DO NOT introduce Tailwind classes for color/font/spacing in any extracted section (D-02).
    - DO NOT add new behavior — pure mechanical extract.
  </action>
  <verify>
    <automated>npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 7 new files exist: `ls app/debug/design-system-v2/sections/Section0{1..7}*.tsx | wc -l` returns `7`
    - Each new file declares 'use client' (Sections 01, 02, 05, 06, 07 require it; Sections 03 and 04 may omit it). Run `grep -L "'use client'" app/debug/design-system-v2/sections/Section0{1,2,5,6,7}*.tsx` returns empty (all 5 have it).
    - Each new file exports a named React component matching its filename: `grep -c "^export function Section01Hue" app/debug/design-system-v2/sections/Section01Hue.tsx` returns `1` (and same for Section02Ambient, Section03Tokens, Section04GlassSurface, Section05Press, Section06Sheet, Section07Splash).
    - Section IDs preserved verbatim: `grep -h 'id="sec-0' app/debug/design-system-v2/sections/Section0*.tsx | sort -u` returns 7 lines containing `id="sec-01-heading"` through `id="sec-07-heading"`.
    - Section eyebrow strings preserved verbatim: `grep -h '/ HUE\|/ AMBIENT\|/ TOKENS\|/ PRESS\|/ SHEET\|/ SPLASH' app/debug/design-system-v2/sections/*.tsx | wc -l` returns 6 or more (no eyebrow renamed).
    - Accent picker write path preserved: `grep -c "setProperty('--accent'" app/debug/design-system-v2/sections/Section01Hue.tsx` returns at least `1`.
    - Localstorage ambient write preserved: `grep -c "ember-glass-ambient" app/debug/design-system-v2/sections/Section02Ambient.tsx` returns at least `1`.
    - All 6 swatch buttons preserved: `grep -c "Set accent to" app/debug/design-system-v2/sections/Section01Hue.tsx` returns at least `1` (the dynamic interpolation pattern produces one literal string source).
  </acceptance_criteria>
  <done>The 7 section component files exist as verbatim extracts of the corresponding sections from page.tsx; no behavior changed; all module-level helpers (ACCENT_PRESETS, setAccent, setAmbient) live in the appropriate section file.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Trim page.tsx to a section orchestrator that imports + renders Section01..Section07</name>
  <read_first>
    - app/debug/design-system-v2/page.tsx (full — current 667 LOC source we are trimming)
    - app/debug/design-system-v2/sections/Section01Hue.tsx (created in Task 1 — confirms named export)
    - app/debug/design-system-v2/sections/Section02Ambient.tsx (created in Task 1)
    - app/debug/design-system-v2/sections/Section03Tokens.tsx (created in Task 1)
    - app/debug/design-system-v2/sections/Section04GlassSurface.tsx (created in Task 1)
    - app/debug/design-system-v2/sections/Section05Press.tsx (created in Task 1)
    - app/debug/design-system-v2/sections/Section06Sheet.tsx (created in Task 1)
    - app/debug/design-system-v2/sections/Section07Splash.tsx (created in Task 1)
    - app/debug/design-system-v2/__tests__/page.test.tsx (lines 22-138 — Jest test still mounts <DesignSystemV2Page /> and asserts swatch + ambient + h1 behavior)
  </read_first>
  <files>app/debug/design-system-v2/page.tsx</files>
  <action>
    Replace the body of `app/debug/design-system-v2/page.tsx` with a thin orchestrator. The file MUST still default-export `DesignSystemV2Page`. Per D-01 the orchestrator targets ~80 LOC.

    **Exact target file content (replace lines 1-667 entirely with this — adjust the page header copy block to match the existing one verbatim from current lines 119-156):**

    ```tsx
    'use client';

    /**
     * /debug/design-system-v2 — Phase 174 + Phase 182 (D-01)
     *
     * Section orchestrator. Decomposed into per-section files under `./sections/`.
     * Each Section0X component owns its own state, helpers, and JSX verbatim from
     * the original Phase-174 single-file page (sections 01-07) — Phase 182 trims
     * page.tsx to import + render order only. Phase 182 will add Sections 08-10.
     *
     * Section 01 (HUE) writes `--accent` and persists to localStorage; the recolor
     * invariant for DSREF-03 is unchanged. Existing test contracts (accent-picker
     * Playwright spec, ambient-persist Playwright spec, page.test.tsx Jest spec) all
     * key off section IDs `sec-01-heading`..`sec-07-heading` and aria-labels that
     * survive verbatim through the extraction.
     */

    import React from 'react';
    import { Section01Hue } from './sections/Section01Hue';
    import { Section02Ambient } from './sections/Section02Ambient';
    import { Section03Tokens } from './sections/Section03Tokens';
    import { Section04GlassSurface } from './sections/Section04GlassSurface';
    import { Section05Press } from './sections/Section05Press';
    import { Section06Sheet } from './sections/Section06Sheet';
    import { Section07Splash } from './sections/Section07Splash';

    export default function DesignSystemV2Page(): React.ReactElement {
      return (
        <main
          style={{
            maxWidth: 1240,
            margin: '0 auto',
            padding: 'var(--pad-card)',
            position: 'relative',
            zIndex: 1,
          }}
        >
          {/* Page header — verbatim from Phase 174 page.tsx lines 119-156 */}
          <header style={{ marginBottom: 32 }}>
            <p
              style={{
                fontFamily: 'var(--font-body)',
                fontSize: 12,
                fontWeight: 600,
                letterSpacing: '1.2px',
                textTransform: 'uppercase',
                color: 'var(--text-2)',
              }}
            >
              DESIGN SYSTEM · v2
            </p>
            <h1
              style={{
                fontFamily: 'var(--font-display)',
                fontSize: 40,
                fontWeight: 600,
                lineHeight: 1.05,
                letterSpacing: '-1px',
                color: 'var(--text-1)',
                margin: 0,
              }}
            >
              Ember Glass
            </h1>
            <p
              style={{
                fontFamily: 'var(--font-body)',
                fontSize: 16,
                fontWeight: 400,
                color: 'var(--text-2)',
                marginTop: 8,
              }}
            >
              Riferimento token e picker live · Phase 174
            </p>
          </header>

          <hr style={{ border: 0, borderTop: '0.5px solid var(--glass-border)', margin: '24px 0' }} />

          <Section01Hue />
          <Section02Ambient />
          <Section03Tokens />
          <Section04GlassSurface />
          <Section05Press />
          <Section06Sheet />
          <Section07Splash />
        </main>
      );
    }
    ```

    Note: keep the `<h1>` text "Ember Glass" — the Playwright spec (`accent-picker.spec.ts:16`) waits for `getByRole('heading', { level: 1, name: /Ember Glass/i })` and the Jest tests for "h1 text" check the same string. DO NOT rename it. Keep the `position: relative; zIndex: 1` on `<main>` and the `--pad-card` padding token.

    Per D-02 and rules #3/#4: do not delete the file, do not run `npm install`, do not run `npm run build`. Pure edit-in-place.
  </action>
  <verify>
    <automated>npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `app/debug/design-system-v2/page.tsx` exists and is at most 110 lines (orchestrator target ~80; allow header verbatim block + imports). Run: `wc -l app/debug/design-system-v2/page.tsx` returns `<= 110`.
    - File still default-exports `DesignSystemV2Page`: `grep -c "export default function DesignSystemV2Page" app/debug/design-system-v2/page.tsx` returns `1`.
    - All 7 sections imported: `grep -c "^import { Section0[1-7]" app/debug/design-system-v2/page.tsx` returns `7`.
    - All 7 sections rendered: `grep -E -c "<Section0[1-7]" app/debug/design-system-v2/page.tsx` returns `7`.
    - `<h1>` text "Ember Glass" preserved: `grep -c "Ember Glass" app/debug/design-system-v2/page.tsx` returns at least `1`.
    - No state declared at the page level: `grep -c "useState\|useEffect" app/debug/design-system-v2/page.tsx` returns `0` (state moved to section files).
    - No ACCENT_PRESETS or setAccent/setAmbient at page level: `grep -c "ACCENT_PRESETS\|setAccent\|setAmbient" app/debug/design-system-v2/page.tsx` returns `0`.
    - Page-test stays green: `npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx` exits 0.
    - Existing Playwright specs reference IDs that still exist: `grep -c 'sec-01-heading\|sec-02-heading' app/debug/design-system-v2/sections/*.tsx` returns at least `2`.
  </acceptance_criteria>
  <done>page.tsx is a thin orchestrator (≤110 LOC) that imports + renders the 7 Section files in order; the Jest spec passes without edits; the existing Playwright suites' subjects (heading "Ember Glass", swatches, ambient toggle, sec-0X-heading IDs) are preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → localStorage | Phase 174 already crosses this boundary; Phase 182 inherits. No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-01-01 | Tampering | localStorage `ember-glass-accent` | accept | Inherits Phase 174 T-174-03-04: try/catch wraps reads/writes; tampered value silently falls back to defaults. No new code path. |

No new threat surface in this plan. Pure file move + import wiring.
</threat_model>

<verification>
- `npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx` exits 0 (Phase 174 Jest contract preserved).
- `grep -c "id=\"sec-0[1-7]-heading\"" app/debug/design-system-v2/sections/*.tsx` returns at least 7.
- `wc -l app/debug/design-system-v2/page.tsx` returns ≤ 110.
- Manual verification deferred to Plan 09 Playwright spec (covers cross-section presence after all sections land).
</verification>

<success_criteria>
- Plan delivers DSREF-03 invariant preservation: accent picker (Section01Hue) still writes `--accent` and persists to localStorage with no behavior change.
- 7 section files exist, page.tsx trimmed to orchestrator, all existing Jest + Playwright assertions pass without edits.
- Foundation laid for Plans 05-08 (extending Section03Tokens; adding Sections 08/09/10).
</success_criteria>

<output>
After completion, create `.planning/phases/182-design-system-reference-page-v2/182-01-SUMMARY.md`
</output>
