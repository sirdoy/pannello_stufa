---
phase: 182-design-system-reference-page-v2
plan: 06
type: execute
wave: 2
depends_on: [182-01-PLAN, 182-02-PLAN, 182-04-PLAN]
files_modified:
  - app/debug/design-system-v2/sections/Section08CardPrimitives.tsx
  - app/debug/design-system-v2/page.tsx
autonomous: true
requirements: [DSREF-01, DSREF-02]

must_haves:
  truths:
    - "Section 08 (CARDS) renders with eyebrow '08 / CARDS', title 'Primitive carta', and description 'Componenti delle dashboard card'"
    - "Section 08 includes 8 primitive sub-blocks: GlassCard, CardHead, StatusDot, InlineToggle, CircBtn, MiniStat, FlameViz, PlayingBars"
    - "Each sub-block displays the primitive name (Outfit 24/600), one-line Italian description (Inter 16/400), live interactive sample, and a CodeSnippet with copy-paste-ready JSX"
    - "page.tsx orchestrator renders <Section08CardPrimitives /> after Section07Splash"
    - "Live samples consume var(--accent) so they recolor when the user picks a hue (DSREF-03)"
  artifacts:
    - path: "app/debug/design-system-v2/sections/Section08CardPrimitives.tsx"
      provides: "Section 08 with 8 card-primitive samples"
      contains: "id=\"sec-08-heading\""
    - path: "app/debug/design-system-v2/page.tsx"
      provides: "Orchestrator now renders Section08CardPrimitives after Section07Splash"
      contains: "<Section08CardPrimitives"
  key_links:
    - from: "Section08CardPrimitives.tsx"
      to: "@/app/components/EmberGlass"
      via: "barrel imports for 8 card primitives"
      pattern: "import \\{[^}]*GlassCard"
    - from: "Section08CardPrimitives.tsx"
      to: "./CodeSnippet"
      via: "CodeSnippet primitive (Plan 04)"
      pattern: "import \\{ CodeSnippet \\}"
---

<objective>
_Section numbering reconciled per Plan 01 reconciliation block — sections 08/09/10 supersede CONTEXT.md D-10's 05/06/07/08._

Build `Section08CardPrimitives.tsx` — a new section showcasing 8 EmberGlass card primitives with live samples and copy-paste JSX snippets. Per CONTEXT D-10 (with the orchestrator's research_reconciliation override): this section is numbered `08 / CARDS` (NOT 06/CARDS as in CONTEXT.md, because the existing page already has sections 01-07 numbered with `id="sec-07-heading"`). Per D-26 copy strings: eyebrow "08 / CARDS", title "Primitive carta", description "Componenti delle dashboard card".

Each of the 8 primitive sub-blocks follows the D-11 fixed layout: name (Outfit 24/600) → description (Inter 16/400, marginTop:4) → [gap:16] → live sample → [gap:12] → CodeSnippet. Sub-blocks separated by `<hr style={{...}}>`.

Wire the new section into `page.tsx` orchestrator after `<Section07Splash />`.

Purpose: Implements DSREF-01 card-primitive samples and DSREF-02 copy-paste JSX snippets for 8 of the 13 SC-#1 primitives. Contributes to DSREF-03 by exposing primitives that consume `var(--accent)`.
Output: One new file (~350-450 LOC) + one edit to page.tsx (3 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-design-system-reference-page-v2/182-CONTEXT.md
@.planning/phases/182-design-system-reference-page-v2/182-RESEARCH.md
@.planning/phases/182-design-system-reference-page-v2/182-PATTERNS.md
@app/components/EmberGlass/GlassCard.tsx
@app/components/EmberGlass/CardHead.tsx
@app/components/EmberGlass/StatusDot.tsx
@app/components/EmberGlass/InlineToggle.tsx
@app/components/EmberGlass/MiniStat.tsx
@app/components/EmberGlass/FlameViz.tsx
@app/components/EmberGlass/PlayingBars.tsx
@app/components/EmberGlass/cards/CircBtn.tsx
@app/debug/design-system-v2/sections/CodeSnippet.tsx
@app/debug/design-system-v2/page.tsx

<interfaces>
<!-- Primitive APIs (verified — RESEARCH §EmberGlass Primitive API Reference): -->
GlassCard:        { children, tone?, onOpen?, style?, 'data-testid'? }
CardHead:         { Icon, label, tone, right? }              // all required except `right`
StatusDot:        { on, color? }                              // color defaults var(--accent)
InlineToggle:     { on, color?, onChange: (e: MouseEvent) }   // onChange is MouseEvent NOT boolean
CircBtn (Plan 02):{ Icon, onClick, primary?, tone }           // tone required
MiniStat:         { label, value, bar }                       // bar 0..1
FlameViz:         { on, intensity? }                          // intensity defaults 0.6
PlayingBars:      {}                                          // no props

<!-- CodeSnippet (Plan 04): -->
CodeSnippet:      { code: string }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Section08CardPrimitives.tsx with 8 primitive sub-blocks</name>
  <read_first>
    - app/components/EmberGlass/GlassCard.tsx (lines 1-end — confirm tone/onOpen API)
    - app/components/EmberGlass/CardHead.tsx (lines 1-end — confirm Icon/label/tone)
    - app/components/EmberGlass/StatusDot.tsx (lines 1-end — confirm on/color)
    - app/components/EmberGlass/InlineToggle.tsx (lines 1-end — confirm onChange MouseEvent signature)
    - app/components/EmberGlass/MiniStat.tsx (lines 1-end — confirm label/value/bar)
    - app/components/EmberGlass/FlameViz.tsx (lines 1-end — confirm on/intensity)
    - app/components/EmberGlass/PlayingBars.tsx (lines 1-end — confirm zero-prop)
    - app/components/EmberGlass/cards/CircBtn.tsx (created in Plan 02 — confirm Icon/onClick/primary/tone)
    - app/debug/design-system-v2/sections/CodeSnippet.tsx (created in Plan 04 — confirm `code` prop)
    - .planning/phases/182-design-system-reference-page-v2/182-PATTERNS.md (Section06CardPrimitives.tsx pattern + Pitfall 4 InlineToggle.onChange MouseEvent)
  </read_first>
  <files>app/debug/design-system-v2/sections/Section08CardPrimitives.tsx (NEW)</files>
  <action>
    Create `app/debug/design-system-v2/sections/Section08CardPrimitives.tsx`. Per D-03 'use client'. Per D-02 inline-style + var(--token). Per D-25/D-26 Italian visible copy.

    **File header:**
    ```tsx
    'use client';
    /**
     * Section08CardPrimitives — Phase 182 (DSREF-01, DSREF-02)
     *
     * 8 card primitive samples per SC-#1: GlassCard, CardHead, StatusDot,
     * InlineToggle, CircBtn, MiniStat, FlameViz, PlayingBars. Each sub-block
     * follows the D-11 fixed layout: name → description → live sample →
     * CodeSnippet. Inline-style + var(--token) discipline (D-02).
     *
     * Section number 08 per the orchestrator's research_reconciliation override
     * (existing page sections are 01-07; CONTEXT.md D-10 said "06/CARDS" but
     * those IDs collide with the existing sec-06-heading SHEET section).
     */
    import React, { useState } from 'react';
    import {
      GlassCard,
      CardHead,
      StatusDot,
      InlineToggle,
      CircBtn,
      MiniStat,
      FlameViz,
      PlayingBars,
    } from '@/app/components/EmberGlass';
    import { Flame, Plus, Minus } from 'lucide-react';
    import { CodeSnippet } from './CodeSnippet';

    export function Section08CardPrimitives(): React.ReactElement {
      // Isolated useState per stateful primitive (do NOT share state across primitives).
      const [toggleOn, setToggleOn] = useState(false);
      const [statusOn, setStatusOn] = useState(true);
      const [flameOn, setFlameOn] = useState(true);
      const [circBtnClicks, setCircBtnClicks] = useState(0);

      return (
        <section aria-labelledby="sec-08-heading" style={{ marginBottom: 48 }}>
          <p
            style={{
              fontFamily: 'var(--font-body)',
              fontSize: 12,
              fontWeight: 600,
              letterSpacing: '1.2px',
              textTransform: 'uppercase',
              color: 'var(--text-2)',
            }}
          >
            08 / CARDS
          </p>
          <h2
            id="sec-08-heading"
            style={{
              fontFamily: 'var(--font-display)',
              fontSize: 24,
              fontWeight: 600,
              lineHeight: 1.2,
              color: 'var(--text-1)',
              margin: '4px 0 8px',
            }}
          >
            Primitive carta
          </h2>
          <p
            style={{
              fontFamily: 'var(--font-body)',
              fontSize: 16,
              fontWeight: 400,
              color: 'var(--text-2)',
              marginBottom: 24,
            }}
          >
            Componenti delle dashboard card
          </p>

          {/* All sub-blocks follow the same D-11 layout. Iterate through the 8 primitives. */}

          {/* === 1. GlassCard === */}
          <SubBlock
            name="GlassCard"
            description="Superficie 1:1 vetro, riceve tone wash e gestisce press scale via Pressable interno."
            sample={
              <GlassCard tone="var(--accent)" onOpen={() => undefined} style={{ width: 140, height: 140 }}>
                <CardHead Icon={Flame} label="Stufa" tone="var(--accent)" />
              </GlassCard>
            }
            code={'<GlassCard tone="var(--accent)" onOpen={() => openSheet()}>\n  <CardHead Icon={Flame} label="Stufa" tone="var(--accent)" />\n</GlassCard>'}
          />

          {/* === 2. CardHead === */}
          <SubBlock
            name="CardHead"
            description="Riga superiore card: icon tile colorato + label e slot destro opzionale."
            sample={
              <div style={{ width: 220 }}>
                <CardHead Icon={Flame} label="Stufa" tone="var(--accent)" />
              </div>
            }
            code={'<CardHead Icon={Flame} label="Stufa" tone="var(--accent)" />'}
          />

          {/* === 3. StatusDot === */}
          <SubBlock
            name="StatusDot"
            description="Pallino di stato 8x8, glow attivo quando on. Color default var(--accent)."
            sample={
              <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
                <button
                  type="button"
                  aria-label={statusOn ? 'Set status off' : 'Set status on'}
                  onClick={() => setStatusOn((v) => !v)}
                  style={{ background: 'none', border: 'none', padding: 0, cursor: 'pointer' }}
                >
                  <StatusDot on={statusOn} />
                </button>
                <span style={{ fontFamily: 'var(--font-body)', fontSize: 12, color: 'var(--text-2)' }}>
                  Click per togglare ({statusOn ? 'on' : 'off'})
                </span>
              </div>
            }
            code={'<StatusDot on={true} color="var(--accent)" />'}
          />

          {/* === 4. InlineToggle === */}
          <SubBlock
            name="InlineToggle"
            description="Switch inline 32x18, onChange riceve MouseEvent (NON boolean)."
            sample={
              <InlineToggle
                on={toggleOn}
                color="var(--accent)"
                onChange={(e) => {
                  e.stopPropagation();
                  setToggleOn((prev) => !prev);
                }}
              />
            }
            code={'<InlineToggle\n  on={on}\n  color="var(--accent)"\n  onChange={(e) => { e.stopPropagation(); setOn(prev => !prev); }}\n/>'}
          />

          {/* === 5. CircBtn === */}
          <SubBlock
            name="CircBtn"
            description="Pulsante circolare 34x34. Variante primary colorata da var(--accent)."
            sample={
              <div style={{ display: 'flex', gap: 12, alignItems: 'center' }}>
                <CircBtn Icon={Plus} onClick={() => setCircBtnClicks((n) => n + 1)} primary tone="var(--accent)" />
                <CircBtn Icon={Minus} onClick={() => setCircBtnClicks((n) => n + 1)} tone="var(--accent)" />
                <span style={{ fontFamily: 'var(--font-body)', fontSize: 12, color: 'var(--text-2)' }}>
                  click count: {circBtnClicks}
                </span>
              </div>
            }
            code={'<CircBtn Icon={Plus} primary tone="var(--accent)" onClick={() => bump()} />\n<CircBtn Icon={Minus} tone="var(--accent)" onClick={() => bump()} />'}
          />

          {/* === 6. MiniStat === */}
          <SubBlock
            name="MiniStat"
            description="Stat compatta con label, valore e barra 0..1."
            sample={
              <div style={{ width: 140 }}>
                <MiniStat label="Potenza" value="3 / 5" bar={0.6} />
              </div>
            }
            code={'<MiniStat label="Potenza" value="3 / 5" bar={0.6} />'}
          />

          {/* === 7. FlameViz === */}
          <SubBlock
            name="FlameViz"
            description="Animazione fiamma 1:1 per la stufa, intensity 0..1 (default 0.6)."
            sample={
              <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
                <button
                  type="button"
                  aria-label={flameOn ? 'Spegni fiamma' : 'Accendi fiamma'}
                  onClick={() => setFlameOn((v) => !v)}
                  style={{ background: 'none', border: 'none', padding: 0, cursor: 'pointer' }}
                >
                  <FlameViz on={flameOn} intensity={0.7} />
                </button>
                <span style={{ fontFamily: 'var(--font-body)', fontSize: 12, color: 'var(--text-2)' }}>
                  Click per togglare ({flameOn ? 'on' : 'off'})
                </span>
              </div>
            }
            code={'<FlameViz on={isAccesa} intensity={0.7} />'}
          />

          {/* === 8. PlayingBars === */}
          <SubBlock
            name="PlayingBars"
            description="Indicatore audio 3 barre animate (Sonos)."
            sample={<PlayingBars />}
            code={'<PlayingBars />'}
            isLast
          />
        </section>
      );
    }

    /**
     * Internal sub-block component — applies the D-11 fixed layout.
     * Not exported (page-internal pattern only).
     */
    interface SubBlockProps {
      name: string;
      description: string;
      sample: React.ReactNode;
      code: string;
      isLast?: boolean;
    }

    function SubBlock({ name, description, sample, code, isLast }: SubBlockProps): React.ReactElement {
      return (
        <>
          <div>
            <h3
              style={{
                fontFamily: 'var(--font-display)',
                fontSize: 24,
                fontWeight: 600,
                lineHeight: 1.2,
                color: 'var(--text-1)',
                margin: 0,
              }}
            >
              {name}
            </h3>
            <p
              style={{
                fontFamily: 'var(--font-body)',
                fontSize: 16,
                fontWeight: 400,
                color: 'var(--text-2)',
                marginTop: 4,
              }}
            >
              {description}
            </p>
            <div style={{ marginTop: 16 }}>{sample}</div>
            <div style={{ marginTop: 12 }}>
              <CodeSnippet code={code} />
            </div>
          </div>
          {!isLast && (
            <hr style={{ border: 0, borderTop: '0.5px solid var(--glass-border)', margin: '24px 0' }} />
          )}
        </>
      );
    }
    ```

    **Critical pitfalls to avoid (per RESEARCH + PATTERNS):**
    - InlineToggle's `onChange` is `(e: MouseEvent<HTMLButtonElement>) => void` — NOT boolean. The sample MUST destructure `e` and use `setToggleOn((prev) => !prev)` (Pitfall 4).
    - Pass `tone="var(--accent)"` to CircBtn primary so the recolor invariant works (DSREF-03).
    - Each sample uses ISOLATED useState — do NOT share toggleOn across primitives.
    - GlassCard's baseStyle has `aspectRatio: '1/1'` — `width: 140` will resolve height to 140 too; the sample sets both for clarity.
    - Pass `Icon` prop (capitalized) to CardHead and CircBtn — lucide-react components are passed as-is.
    - DO NOT wrap CardHead in another `<button>` — it has its own click affordance via parent.

    Per CLAUDE.md rule #4: do not run `npm install`/`npm run build`. Per rule #8: scoped test:pages only.
  </action>
  <verify>
    <automated>npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f app/debug/design-system-v2/sections/Section08CardPrimitives.tsx`.
    - Starts with `'use client';`: `head -1 app/debug/design-system-v2/sections/Section08CardPrimitives.tsx` returns `'use client';`.
    - Section ID `sec-08-heading`: `grep -c 'id="sec-08-heading"' app/debug/design-system-v2/sections/Section08CardPrimitives.tsx` returns at least `1`.
    - Eyebrow `08 / CARDS`: `grep -c '08 / CARDS' app/debug/design-system-v2/sections/Section08CardPrimitives.tsx` returns `1`.
    - Italian title and description present: `grep -c 'Primitive carta\|Componenti delle dashboard card' app/debug/design-system-v2/sections/Section08CardPrimitives.tsx` returns at least `2`.
    - All 8 primitives sampled: `grep -c '<GlassCard\|<CardHead\|<StatusDot\|<InlineToggle\|<CircBtn\|<MiniStat\|<FlameViz\|<PlayingBars' app/debug/design-system-v2/sections/Section08CardPrimitives.tsx` returns at least `8`.
    - All 8 primitive name labels in sub-block names: `grep -E -c "name=\"(GlassCard|CardHead|StatusDot|InlineToggle|CircBtn|MiniStat|FlameViz|PlayingBars)\"" app/debug/design-system-v2/sections/Section08CardPrimitives.tsx` returns `8`.
    - var(--accent) referenced for live recolor: `grep -c 'var(--accent)' app/debug/design-system-v2/sections/Section08CardPrimitives.tsx` returns at least `4`.
    - Imports CodeSnippet: `grep -c "import { CodeSnippet } from './CodeSnippet'" app/debug/design-system-v2/sections/Section08CardPrimitives.tsx` returns `1`.
    - InlineToggle onChange uses MouseEvent destructuring (NOT boolean): `grep -c 'onChange={(e)' app/debug/design-system-v2/sections/Section08CardPrimitives.tsx` returns at least `1`.
    - Page-test passes: `npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Section08CardPrimitives.tsx renders all 8 SC-#1 card primitives with live samples, JSX snippets, and Italian descriptions. Inline-style discipline preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Section08CardPrimitives into page.tsx orchestrator</name>
  <read_first>
    - app/debug/design-system-v2/page.tsx (current after Plan 01 — orchestrator with Section01..Section07)
    - app/debug/design-system-v2/sections/Section08CardPrimitives.tsx (created in Task 1)
  </read_first>
  <files>app/debug/design-system-v2/page.tsx</files>
  <action>
    Edit `app/debug/design-system-v2/page.tsx` (the orchestrator from Plan 01). Append two changes:

    1. After the existing import line `import { Section07Splash } from './sections/Section07Splash';`, add:
       ```tsx
       import { Section08CardPrimitives } from './sections/Section08CardPrimitives';
       ```

    2. After the JSX line `<Section07Splash />`, add:
       ```tsx
       <Section08CardPrimitives />
       ```

    Do NOT modify any other line in page.tsx. Do NOT remove existing imports or sections.

    Per CLAUDE.md rule #3: edit existing file, do not create a duplicate.
  </action>
  <verify>
    <automated>npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports Section08CardPrimitives: `grep -c "import { Section08CardPrimitives } from './sections/Section08CardPrimitives'" app/debug/design-system-v2/page.tsx` returns `1`.
    - page.tsx renders the section after Section07Splash: line number of `<Section08CardPrimitives />` > line number of `<Section07Splash />` (both must exist). Verify: `grep -n "<Section07Splash\|<Section08CardPrimitives" app/debug/design-system-v2/page.tsx` returns 2 lines, with Section08 line number greater than Section07.
    - All 7 prior sections still imported: `grep -c "^import { Section0[1-7]" app/debug/design-system-v2/page.tsx` returns `7`.
    - Page Jest test green: `npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Orchestrator now renders 8 sections in order: Section01..Section07 (Phase 174 carry-forward) + new Section08CardPrimitives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component prop boundary | Live samples consume only var(--token) values + bundle-verbatim AUDIT-EXCEPTION literals. No untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-06-01 | Information Disclosure | Live samples render | accept | All sample data is hardcoded; no PII or secrets. Page is /debug/** Auth0-gated. |

No new threat surface. Inherits Phase 174 client-side dev page model.
</threat_model>

<verification>
- `npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx` exits 0.
- 8 of the 13 SC-#1 primitives now have live samples + copy-paste-ready snippets on the reference page.
</verification>

<success_criteria>
- DSREF-01 card-primitive layer satisfied: 8 of 13 primitives sampled.
- DSREF-02 satisfied for those 8 primitives: each has a CodeSnippet with realistic JSX.
- DSREF-03 propagated: each accent-touching primitive uses `var(--accent)`.
</success_criteria>

<output>
After completion, create `.planning/phases/182-design-system-reference-page-v2/182-06-SUMMARY.md`
</output>
