---
phase: 182-design-system-reference-page-v2
plan: 07
type: execute
wave: 2
depends_on: [182-01-PLAN, 182-03-PLAN, 182-04-PLAN]
files_modified:
  - app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx
  - app/debug/design-system-v2/page.tsx
autonomous: true
requirements: [DSREF-01, DSREF-02]

must_haves:
  truths:
    - "Section 09 (SHEET) renders with eyebrow '09 / SHEET', title 'Primitive sheet', and description 'Componenti dei pannelli a comparsa'"
    - "Section 09 includes 7 primitive sub-blocks: SheetRow, Stepper, Slider, BigSlider, RadialDial, SheetBtn, QuickActionButton"
    - "Each sub-block has live interactive sample with isolated useState + CodeSnippet"
    - "page.tsx orchestrator renders <Section09SheetPrimitives /> after Section08CardPrimitives"
    - "Live samples consume var(--accent) so they recolor with the accent picker (DSREF-03)"
  artifacts:
    - path: "app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx"
      provides: "Section 09 with 7 sheet-primitive samples"
      contains: "id=\"sec-09-heading\""
    - path: "app/debug/design-system-v2/page.tsx"
      provides: "Orchestrator now renders Section09SheetPrimitives"
      contains: "<Section09SheetPrimitives"
  key_links:
    - from: "Section09SheetPrimitives.tsx"
      to: "@/app/components/EmberGlass"
      via: "barrel imports for 7 sheet primitives"
      pattern: "import \\{[^}]*BigSlider"
---

<objective>
_Section numbering reconciled per Plan 01 reconciliation block — sections 08/09/10 supersede CONTEXT.md D-10's 05/06/07/08._

Build `Section09SheetPrimitives.tsx` — a new section showcasing 7 sheet primitives. Per orchestrator's research_reconciliation override: section number is `09 / SHEET` (NOT 07/SHEET PRIMITIVES as in CONTEXT.md, because existing page IDs sec-01..sec-07 are reserved for the Phase 174 sections). Per D-26: eyebrow "09 / SHEET", title "Primitive sheet", description "Componenti dei pannelli a comparsa".

Each sub-block follows the D-11 fixed layout (name → description → live sample → CodeSnippet) and uses isolated `useState` per stateful primitive. Per CD-01: RadialDial sub-block gets `marginTop: 24` (extra breathing room — 220×220 SVG).

Wire the new section into `page.tsx` after `<Section08CardPrimitives />`.

Purpose: Implements the remaining 5 of the 13 SC-#1 primitives (SheetRow, Stepper, Slider, BigSlider, RadialDial, SheetBtn, QuickActionButton — 7 total here). DSREF-01 + DSREF-02.
Output: One new file (~400-500 LOC) + page.tsx import + render edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-design-system-reference-page-v2/182-CONTEXT.md
@.planning/phases/182-design-system-reference-page-v2/182-RESEARCH.md
@.planning/phases/182-design-system-reference-page-v2/182-PATTERNS.md
@app/components/EmberGlass/sheets/primitives/SheetRow.tsx
@app/components/EmberGlass/sheets/primitives/Stepper.tsx
@app/components/EmberGlass/sheets/primitives/Slider.tsx
@app/components/EmberGlass/sheets/primitives/BigSlider.tsx
@app/components/EmberGlass/sheets/primitives/RadialDial.tsx
@app/components/EmberGlass/sheets/primitives/SheetBtn.tsx
@app/components/EmberGlass/sheets/primitives/QuickActionButton.tsx
@app/debug/design-system-v2/sections/CodeSnippet.tsx
@app/debug/design-system-v2/page.tsx

<interfaces>
<!-- Sheet primitive APIs (verified via RESEARCH §EmberGlass Primitive API Reference > Sheet Primitives): -->
SheetRow:           { label, value?, children? }
Stepper:            { value, min, max, onChange: (n: number) => void }
Slider:             { value, min, max, onChange: (n: number) => void, color? }       // color defaults var(--accent)
BigSlider (Plan03): { value, onChange: (n: number) => void, color? }                 // 0..100, color defaults var(--accent)
RadialDial:         { value, min, max, onChange: (n: number) => void, color, label } // color REQUIRED
SheetBtn:           { Icon, label, onClick? }
QuickActionButton:  { active: boolean, onClick: () => void, label }                  // active drives visual state
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Section09SheetPrimitives.tsx with 7 sheet-primitive sub-blocks</name>
  <read_first>
    - app/components/EmberGlass/sheets/primitives/SheetRow.tsx (full — confirm label/value/children)
    - app/components/EmberGlass/sheets/primitives/Stepper.tsx (full — confirm value/min/max/onChange)
    - app/components/EmberGlass/sheets/primitives/Slider.tsx (full — confirm value/min/max/onChange/color)
    - app/components/EmberGlass/sheets/primitives/BigSlider.tsx (created in Plan 03 — confirm value/onChange/color)
    - app/components/EmberGlass/sheets/primitives/RadialDial.tsx (full — confirm value/min/max/onChange/color/label, color REQUIRED)
    - app/components/EmberGlass/sheets/primitives/SheetBtn.tsx (full — confirm Icon/label/onClick)
    - app/components/EmberGlass/sheets/primitives/QuickActionButton.tsx (full — confirm active/onClick/label)
    - app/debug/design-system-v2/sections/CodeSnippet.tsx (Plan 04 — confirm code prop)
    - .planning/phases/182-design-system-reference-page-v2/182-PATTERNS.md (Section07SheetPrimitives.tsx pattern)
  </read_first>
  <files>app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx (NEW)</files>
  <action>
    Create `app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx` per D-03 'use client', D-02 inline-style, D-25/D-26 Italian copy.

    **Exact file content:**
    ```tsx
    'use client';
    /**
     * Section09SheetPrimitives — Phase 182 (DSREF-01, DSREF-02)
     *
     * 7 sheet primitive samples: SheetRow, Stepper, Slider, BigSlider, RadialDial,
     * SheetBtn, QuickActionButton. Each sub-block follows D-11 layout with
     * isolated useState per stateful primitive. Inline-style + var(--token).
     *
     * Section number 09 per orchestrator's research_reconciliation override.
     */
    import React, { useState } from 'react';
    import {
      SheetRow,
      Stepper,
      Slider,
      BigSlider,
      RadialDial,
      SheetBtn,
      QuickActionButton,
    } from '@/app/components/EmberGlass';
    import { Settings } from 'lucide-react';
    import { CodeSnippet } from './CodeSnippet';

    export function Section09SheetPrimitives(): React.ReactElement {
      // Isolated useState per stateful primitive.
      const [stepVal, setStepVal] = useState(3);
      const [sliderVal, setSliderVal] = useState(40);
      const [bigSliderVal, setBigSliderVal] = useState(72);
      const [dialVal, setDialVal] = useState(22);
      const [qabActive, setQabActive] = useState(false);

      return (
        <section aria-labelledby="sec-09-heading" style={{ marginBottom: 48 }}>
          <p style={{ fontFamily: 'var(--font-body)', fontSize: 12, fontWeight: 600, letterSpacing: '1.2px', textTransform: 'uppercase', color: 'var(--text-2)' }}>
            09 / SHEET
          </p>
          <h2 id="sec-09-heading" style={{ fontFamily: 'var(--font-display)', fontSize: 24, fontWeight: 600, lineHeight: 1.2, color: 'var(--text-1)', margin: '4px 0 8px' }}>
            Primitive sheet
          </h2>
          <p style={{ fontFamily: 'var(--font-body)', fontSize: 16, fontWeight: 400, color: 'var(--text-2)', marginBottom: 24 }}>
            Componenti dei pannelli a comparsa
          </p>

          {/* === 1. SheetRow === */}
          <SubBlock
            name="SheetRow"
            description="Riga sheet con label, value opzionale e slot children destro."
            sample={
              <SheetRow label="Stato" value="Acceso">
                <span style={{ fontFamily: 'var(--font-body)', fontSize: 13, color: 'var(--text-2)' }}>3 / 5</span>
              </SheetRow>
            }
            code={'<SheetRow label="Stato" value="Acceso">\n  <span>3 / 5</span>\n</SheetRow>'}
          />

          {/* === 2. Stepper === */}
          <SubBlock
            name="Stepper"
            description="Stepper +/- discreto, onChange emette numero raw."
            sample={<Stepper value={stepVal} min={1} max={5} onChange={setStepVal} />}
            code={'<Stepper value={pwr} min={1} max={5} onChange={setPwr} />'}
          />

          {/* === 3. Slider === */}
          <SubBlock
            name="Slider"
            description="Slider compatto 140px, default color var(--accent)."
            sample={<Slider value={sliderVal} min={0} max={100} onChange={setSliderVal} />}
            code={'<Slider value={vol} min={0} max={100} onChange={setVol} color="var(--accent)" />'}
          />

          {/* === 4. BigSlider === */}
          <SubBlock
            name="BigSlider"
            description="Slider 72px con gradient color-mix var(--accent), label percentuale 28/600."
            sample={
              <div style={{ width: '100%', maxWidth: 360 }}>
                <BigSlider value={bigSliderVal} onChange={setBigSliderVal} />
              </div>
            }
            code={'<BigSlider value={brightness} onChange={setBrightness} color="var(--accent)" />'}
          />

          {/* === 5. RadialDial — extra breathing per CD-01 === */}
          <SubBlock
            name="RadialDial"
            description="Dial 220x220, valore centrale 68/600, color prop obbligatoria."
            sample={
              <div style={{ display: 'flex', justifyContent: 'center', marginTop: 8 }}>
                <RadialDial value={dialVal} min={7} max={30} onChange={setDialVal} color="var(--accent)" label="°C" />
              </div>
            }
            code={'<RadialDial value={target} min={7} max={30} onChange={setTarget} color="var(--accent)" label="°C" />'}
            extraTopGap
          />

          {/* === 6. SheetBtn === */}
          <SubBlock
            name="SheetBtn"
            description="Pulsante sheet con icon + label 14/500."
            sample={<SheetBtn Icon={Settings} label="Impostazioni" onClick={() => undefined} />}
            code={'<SheetBtn Icon={Settings} label="Impostazioni" onClick={() => navigate("/settings")} />'}
          />

          {/* === 7. QuickActionButton === */}
          <SubBlock
            name="QuickActionButton"
            description="Toggle quick-action; active drive lo stato visuale yellow."
            sample={
              <QuickActionButton
                active={qabActive}
                onClick={() => setQabActive((p) => !p)}
                label={qabActive ? 'Timer attivo' : 'Timer'}
              />
            }
            code={'<QuickActionButton active={timerOn} onClick={() => setTimerOn(p => !p)} label="Timer" />'}
            isLast
          />
        </section>
      );
    }

    interface SubBlockProps {
      name: string;
      description: string;
      sample: React.ReactNode;
      code: string;
      isLast?: boolean;
      extraTopGap?: boolean;
    }

    function SubBlock({ name, description, sample, code, isLast, extraTopGap }: SubBlockProps): React.ReactElement {
      return (
        <>
          <div>
            <h3 style={{ fontFamily: 'var(--font-display)', fontSize: 24, fontWeight: 600, lineHeight: 1.2, color: 'var(--text-1)', margin: 0 }}>
              {name}
            </h3>
            <p style={{ fontFamily: 'var(--font-body)', fontSize: 16, fontWeight: 400, color: 'var(--text-2)', marginTop: 4 }}>
              {description}
            </p>
            <div style={{ marginTop: extraTopGap ? 24 : 16 }}>{sample}</div>
            <div style={{ marginTop: 12 }}>
              <CodeSnippet code={code} />
            </div>
          </div>
          {!isLast && (
            <hr style={{ border: 0, borderTop: '0.5px solid var(--glass-border)', margin: '24px 0' }} />
          )}
        </>
      );
    }
    ```

    **Critical correctness notes:**
    - RadialDial's `color` prop is REQUIRED (not optional) — pass `"var(--accent)"`.
    - QuickActionButton's `active` prop is boolean — drives visual state. Use isolated `useState(false)`.
    - BigSlider was added in Plan 03; if Plan 03 hasn't merged yet, the import will fail — confirm Plan 03 dependency.
    - Stepper's `onChange` receives a `number`, NOT a wrapped event.
    - Slider, BigSlider, RadialDial all use `var(--accent)` — that's the DSREF-03 recolor invariant Plan 09 asserts.

    Per CLAUDE.md rule #4: do not run `npm install`/`npm run build`. Per rule #8: scoped test:pages only.
  </action>
  <verify>
    <automated>npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx`.
    - Starts with `'use client';`.
    - Section ID `sec-09-heading`: `grep -c 'id="sec-09-heading"' app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx` returns at least `1`.
    - Eyebrow `09 / SHEET` (not 07): `grep -c '09 / SHEET' app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx` returns `1`.
    - Italian title + description: `grep -c 'Primitive sheet\|Componenti dei pannelli a comparsa' app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx` returns at least `2`.
    - All 7 primitives sampled (live JSX usage): `grep -E -c "<(SheetRow|Stepper|Slider|BigSlider|RadialDial|SheetBtn|QuickActionButton)\b" app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx` returns at least `7`.
    - All 7 sub-block names present: `grep -E -c "name=\"(SheetRow|Stepper|Slider|BigSlider|RadialDial|SheetBtn|QuickActionButton)\"" app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx` returns `7`.
    - `var(--accent)` referenced in sample area + code text: `grep -c 'var(--accent)' app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx` returns at least `3`.
    - RadialDial passes `color="var(--accent)"`: `grep -c 'color="var(--accent)" label' app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx` returns at least `1`.
    - Imports CodeSnippet: `grep -c "import { CodeSnippet }" app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx` returns `1`.
    - Page-test passes: `npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Section09SheetPrimitives.tsx renders all 7 sheet primitives with live samples + JSX snippets + Italian descriptions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Section09SheetPrimitives into page.tsx orchestrator</name>
  <read_first>
    - app/debug/design-system-v2/page.tsx (current — orchestrator with Section01..Section08 after Plan 06)
    - app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx (created in Task 1)
  </read_first>
  <files>app/debug/design-system-v2/page.tsx</files>
  <action>
    Edit `app/debug/design-system-v2/page.tsx` (orchestrator). Two changes:

    1. After `import { Section08CardPrimitives } from './sections/Section08CardPrimitives';` add:
       ```tsx
       import { Section09SheetPrimitives } from './sections/Section09SheetPrimitives';
       ```

    2. After `<Section08CardPrimitives />` add:
       ```tsx
       <Section09SheetPrimitives />
       ```

    No other lines changed. Per rule #3: edit existing file in place.
  </action>
  <verify>
    <automated>npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Import line present: `grep -c "import { Section09SheetPrimitives }" app/debug/design-system-v2/page.tsx` returns `1`.
    - Render line present: `grep -c '<Section09SheetPrimitives' app/debug/design-system-v2/page.tsx` returns `1`.
    - Render order Section08 → Section09: `grep -n '<Section08CardPrimitives\|<Section09SheetPrimitives' app/debug/design-system-v2/page.tsx | sort -t: -k1n | head -2` shows Section08 first, Section09 second.
    - Page-test passes: `npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Orchestrator now renders 9 sections in order: 7 Phase 174 sections + Section08CardPrimitives + Section09SheetPrimitives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component prop boundary | Live samples consume only var(--token) values. No untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-07-01 | Information Disclosure | Live samples render | accept | Hardcoded sample data only. /debug/** Auth0-gated. |

No new threat surface.
</threat_model>

<verification>
- `npm run test:pages -- app/debug/design-system-v2/__tests__/page.test.tsx` exits 0.
- 7 of the 13 SC-#1 primitives now have live samples + copy-paste-ready snippets.
</verification>

<success_criteria>
- DSREF-01 sheet-primitive layer satisfied (7 primitives sampled).
- DSREF-02 satisfied for those 7 primitives.
- DSREF-03 propagated: Slider, BigSlider, RadialDial all consume var(--accent).
</success_criteria>

<output>
After completion, create `.planning/phases/182-design-system-reference-page-v2/182-07-SUMMARY.md`
</output>
