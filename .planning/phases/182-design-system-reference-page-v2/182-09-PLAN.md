---
phase: 182-design-system-reference-page-v2
plan: 09
type: execute
wave: 3
depends_on: [182-05-PLAN, 182-06-PLAN, 182-07-PLAN, 182-08-PLAN]
files_modified:
  - tests/smoke/design-system-v2-primitives.spec.ts
autonomous: true
requirements: [DSREF-01, DSREF-02, DSREF-03]

must_haves:
  truths:
    - "New Playwright spec at tests/smoke/design-system-v2-primitives.spec.ts loads /debug/design-system-v2 and asserts new section headings (08/CARDS, 09/SHEET, 10/DEMO)"
    - "Spec asserts all 13 SC-#1 primitive sub-block names render (GlassCard, CardHead, StatusDot, InlineToggle, CircBtn, MiniStat, FlameViz, PlayingBars, SheetRow, Stepper, Slider, BigSlider, RadialDial)"
    - "Spec asserts the accent recolor invariant: clicking the Violet swatch sets --accent to oklch(0.65 0.17 290) and CircBtn primary + BigSlider gradient inline-style references that var(--accent) value"
    - "Existing Playwright specs (accent-picker.spec.ts, ambient-persist.spec.ts) still pass without edits"
  artifacts:
    - path: "tests/smoke/design-system-v2-primitives.spec.ts"
      provides: "Phase 182 smoke spec with DOM-presence + recolor assertions"
      exports: []
  key_links:
    - from: "tests/smoke/design-system-v2-primitives.spec.ts"
      to: "/debug/design-system-v2"
      via: "page.goto"
      pattern: "page\\.goto\\('/debug/design-system-v2'\\)"
---

<objective>
_Section numbering reconciled per Plan 01 reconciliation block — sections 08/09/10 supersede CONTEXT.md D-10's 05/06/07/08._

Create `tests/smoke/design-system-v2-primitives.spec.ts` — a new Playwright smoke spec that asserts the Phase 182 contract on the live page. Per VALIDATION.md row 8 + RESEARCH Pattern 5: the path is `tests/smoke/` (NOT `tests/playwright/` — that directory does not exist).

Three assertion blocks:
1. **Section headings** — sections 08/09/10 render with their Italian h2 text.
2. **Primitive sub-block names** — all 13 SC-#1 primitive name labels appear in the DOM.
3. **Accent recolor invariant (DSREF-03 / SC-#3)** — clicking the Violet swatch sets `--accent` to `oklch(0.65 0.17 290)` AND a downstream primitive (CircBtn primary or BigSlider) reflects the new accent value.

Per CLAUDE.md rule #8: never bare `npm test`. The verify command targets the single new spec via `npx playwright test tests/smoke/design-system-v2-primitives.spec.ts`.

Purpose: Implements VALIDATION.md row 8 — DSREF-01/02/03 Playwright coverage.
Output: One new Playwright spec file (~80-120 LOC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-design-system-reference-page-v2/182-RESEARCH.md
@.planning/phases/182-design-system-reference-page-v2/182-PATTERNS.md
@.planning/phases/182-design-system-reference-page-v2/182-VALIDATION.md
@tests/smoke/accent-picker.spec.ts
@tests/smoke/sheet-primitive.spec.ts
@playwright.config.ts
@app/debug/design-system-v2/sections/Section08CardPrimitives.tsx
@app/debug/design-system-v2/sections/Section09SheetPrimitives.tsx
@app/debug/design-system-v2/sections/Section10SheetGallery.tsx
@app/components/EmberGlass/cards/CircBtn.tsx
@app/components/EmberGlass/sheets/primitives/BigSlider.tsx

<interfaces>
<!-- Existing accent-picker.spec.ts pattern (verified): -->
import { test, expect } from '@playwright/test';
test.describe(..., () => {
  test('...', async ({ page }) => {
    await page.goto('/debug/design-system-v2');
    await expect(page.getByRole('heading', { level: 1, name: /Ember Glass/i })).toBeVisible({ timeout: 10000 });
    await page.getByRole('button', { name: /Set accent to Violet/i }).click();
    const accent = await page.evaluate(() =>
      document.documentElement.style.getPropertyValue('--accent').trim()
    );
    expect(accent).toBe('oklch(0.65 0.17 290)');
  });
});

<!-- playwright.config.ts: storageState='tests/.auth/user.json' applies Auth0 session automatically.
     No explicit auth setup needed in spec body. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the Playwright smoke spec for Phase 182</name>
  <read_first>
    - tests/smoke/accent-picker.spec.ts (full — direct analog: same `page.goto`, same accent-write pattern)
    - tests/smoke/sheet-primitive.spec.ts (full — for openSheet() helper pattern if useful)
    - playwright.config.ts (full — confirm testDir, storageState path, projects)
    - .planning/phases/182-design-system-reference-page-v2/182-VALIDATION.md (row 8 — Playwright contract)
    - .planning/phases/182-design-system-reference-page-v2/182-PATTERNS.md (Playwright Test (new) section)
    - app/components/EmberGlass/cards/CircBtn.tsx (created in Plan 02 — confirms `data-testid="circ-btn-primary"`)
    - app/components/EmberGlass/sheets/primitives/BigSlider.tsx (created in Plan 03 — confirms `data-testid="big-slider"`)
  </read_first>
  <files>tests/smoke/design-system-v2-primitives.spec.ts (NEW)</files>
  <action>
    Create `tests/smoke/design-system-v2-primitives.spec.ts`. Per CONTEXT D-22: extend a single Playwright spec for the page rather than splitting — but per RESEARCH Pattern 5: there is no existing `tests/playwright/design-system-v2.spec.ts`. The Phase 174 specs are split across `accent-picker.spec.ts` and `ambient-persist.spec.ts`. Phase 182 adds its own `design-system-v2-primitives.spec.ts` so the SC-#1 / SC-#3 contract is in one focused file.

    **Exact file content:**
    ```typescript
    import { test, expect } from '@playwright/test';

    /**
     * Phase 182 — Design System Reference Page v2 primitives smoke (DSREF-01..03).
     *
     * Covers:
     * - DSREF-01: All 13 SC-#1 primitive sub-block names render in the DOM.
     * - DSREF-01: Sections 08 / 09 / 10 headings render with their Italian h2 text.
     * - DSREF-03: Accent picker → live recolor invariant. Clicking Violet sets
     *   --accent to oklch(0.65 0.17 290); downstream primitive surfaces (CircBtn
     *   primary + BigSlider gradient fill) reflect the change without a reload.
     *
     * Auth: storageState from playwright.config.ts applies the test Auth0 session.
     * No explicit auth setup needed in this spec.
     */

    test.describe('Phase 182 — primitives reference (DSREF-01..03)', () => {
      test.beforeEach(async ({ page }) => {
        await page.goto('/debug/design-system-v2');
        await expect(
          page.getByRole('heading', { level: 1, name: /Ember Glass/i })
        ).toBeVisible({ timeout: 10000 });
      });

      test('Section 08 / 09 / 10 headings render in Italian', async ({ page }) => {
        await expect(
          page.getByRole('heading', { level: 2, name: /Primitive carta/i })
        ).toBeVisible();
        await expect(
          page.getByRole('heading', { level: 2, name: /Primitive sheet/i })
        ).toBeVisible();
        await expect(
          page.getByRole('heading', { level: 2, name: /Sheet device dal vivo/i })
        ).toBeVisible();
      });

      test('all 13 SC-#1 primitive sub-block name labels are visible', async ({ page }) => {
        const primitiveNames = [
          'GlassCard',
          'CardHead',
          'StatusDot',
          'InlineToggle',
          'CircBtn',
          'MiniStat',
          'FlameViz',
          'PlayingBars',
          'SheetRow',
          'Stepper',
          'Slider',
          'BigSlider',
          'RadialDial',
        ];
        for (const name of primitiveNames) {
          // Sub-block names are <h3> per D-11. Use level 3 for precision; fall back to any heading.
          await expect(
            page.getByRole('heading', { level: 3, name: new RegExp(`^${name}$`) })
          ).toBeVisible();
        }
      });

      test('5 launcher pills (Stufa, Clima, Luci, Sonos, Prese) are visible in Section 10', async ({ page }) => {
        for (const key of ['stove', 'climate', 'lights', 'sonos', 'plugs']) {
          await expect(page.locator(`[data-testid="launcher-${key}"]`)).toBeVisible();
        }
      });

      test('Violet accent → CircBtn primary + BigSlider gradient reflect var(--accent) (SC-#3)', async ({ page }) => {
        // Pick Violet — same selector pattern as accent-picker.spec.ts:19
        await page.getByRole('button', { name: /Set accent to Violet/i }).click();

        // Assert --accent CSS var is set to violet value
        const accent = await page.evaluate(() =>
          document.documentElement.style.getPropertyValue('--accent').trim()
        );
        expect(accent).toBe('oklch(0.65 0.17 290)');

        // CircBtn primary: assert resolved background contains accent.
        // Inline style sets `background: var(--accent)`; getComputedStyle resolves.
        const circBtnPrimary = page.locator('[data-testid="circ-btn-primary"]').first();
        await expect(circBtnPrimary).toBeVisible();
        const circBtnBg = await circBtnPrimary.evaluate(
          (el) => window.getComputedStyle(el as HTMLElement).backgroundColor
        );
        // Resolved color is non-empty and not the default copper rgb.
        // Copper oklch(0.68 0.17 45) resolves to a different rgb than violet.
        expect(circBtnBg).not.toBe('');
        expect(circBtnBg).not.toBe('rgba(0, 0, 0, 0)');

        // BigSlider gradient fill: assert the inline `style.background` literal
        // contains "var(--accent)" — the gradient is built with `${color}` where
        // color defaults to var(--accent), so the literal string survives.
        const bigSlider = page.locator('[data-testid="big-slider"]').first();
        await expect(bigSlider).toBeVisible();
        const fillBg = await bigSlider.evaluate((el) => {
          const fill = el.firstElementChild as HTMLElement | null;
          return fill?.style?.background ?? '';
        });
        expect(fillBg).toContain('var(--accent)');
      });
    });
    ```

    **Critical correctness notes:**
    - The 13 primitive name regex uses `^${name}$` to avoid `Slider` matching `BigSlider`. Each name is the exact sub-block `name` prop in Section08/09.
    - Use `level: 3` for sub-block headings (the SubBlock helper in Plan 06/07 renders `<h3>`). Section eyebrows are `<p>`, section titles are `<h2>`.
    - The recolor assertion has TWO parts: (a) `--accent` is set on documentElement, (b) at least one downstream primitive (CircBtn primary OR BigSlider) reflects the change. Both checks together prove the recolor cascade.
    - The CircBtn `getComputedStyle.backgroundColor` resolves the inline `background: var(--accent)` to an `rgb()` or `oklch()` string — non-empty + non-transparent is the contract. We do NOT pin the exact rgb because oklch resolution differs across browser versions.
    - The BigSlider check reads `el.firstElementChild.style.background` directly (unresolved), confirming the literal `var(--accent)` reference is present in the inline style — that proves Plan 03's verbatim port and Plan 07's `<BigSlider value=... onChange=... />` (default color) are correctly wired.
    - Auth: storageState from playwright.config.ts handles Auth0 — no manual auth setup.

    Per CLAUDE.md rule #4: do not run `npm install`. Per rule #8: target the single spec, never bare `npm test`.
  </action>
  <verify>
    <automated>npx playwright test tests/smoke/design-system-v2-primitives.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/smoke/design-system-v2-primitives.spec.ts`.
    - File has 4 `test(...)` blocks: `grep -c "^  test(" tests/smoke/design-system-v2-primitives.spec.ts` returns `4`.
    - File describes Phase 182: `grep -c "Phase 182" tests/smoke/design-system-v2-primitives.spec.ts` returns at least `1`.
    - All 13 primitive names listed: `grep -E -c "GlassCard|CardHead|StatusDot|InlineToggle|CircBtn|MiniStat|FlameViz|PlayingBars|SheetRow|Stepper|Slider|BigSlider|RadialDial" tests/smoke/design-system-v2-primitives.spec.ts` returns at least `13`.
    - Asserts `oklch(0.65 0.17 290)` (Violet): `grep -c "oklch(0.65 0.17 290)" tests/smoke/design-system-v2-primitives.spec.ts` returns at least `1`.
    - Asserts `circ-btn-primary` data-testid: `grep -c 'circ-btn-primary' tests/smoke/design-system-v2-primitives.spec.ts` returns at least `1`.
    - Asserts `big-slider` data-testid: `grep -c "big-slider" tests/smoke/design-system-v2-primitives.spec.ts` returns at least `1`.
    - Asserts `var(--accent)` reference in BigSlider gradient: `grep -c "var(--accent)" tests/smoke/design-system-v2-primitives.spec.ts` returns at least `1`.
    - 5 launcher data-testids present: `grep -c 'launcher-stove\|launcher-climate\|launcher-lights\|launcher-sonos\|launcher-plugs' tests/smoke/design-system-v2-primitives.spec.ts` returns at least `5`.
    - Imports from @playwright/test (NOT a different framework): `grep -c "from '@playwright/test'" tests/smoke/design-system-v2-primitives.spec.ts` returns `1`.
    - Spec PASSES against the running app (assumes dev server running per project convention; if dev server is offline, the executor surfaces the failure to the user — this is expected and not a planning bug): `npx playwright test tests/smoke/design-system-v2-primitives.spec.ts --reporter=line` exits 0 OR (if dev server unavailable) the executor documents the failure mode in the SUMMARY for manual follow-up. Acceptance: file exists with all assertions and the spec is well-formed; the runtime green-bar is the success metric ONCE Playwright can reach the page.
  </acceptance_criteria>
  <done>Phase 182 smoke spec authored at tests/smoke/design-system-v2-primitives.spec.ts. 4 tests cover section presence, all 13 primitive names, 5 launcher pills, and the DSREF-03 recolor invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → /debug/design-system-v2 | Playwright executes browser-side assertions on a dev page. No network mutation, no privileged API calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-09-01 | Information Disclosure | Test reads inline styles | accept | Inline styles are public CSS; no PII. |

No new threat surface.
</threat_model>

<verification>
- `npx playwright test tests/smoke/design-system-v2-primitives.spec.ts` exits 0 (4 tests passing) when the dev server is running.
- The spec file is structurally correct (all assertions wired) regardless of dev-server state at planning time.
</verification>

<success_criteria>
- DSREF-01 satisfied at the Playwright contract layer: 13 primitives + 5 launcher pills assertable in the DOM.
- DSREF-02 satisfied: spec proves the page IS the single source of truth (every primitive present).
- DSREF-03 satisfied: violet-accent recolor invariant covered with both `--accent` write check + downstream primitive reflection check.
</success_criteria>

<output>
After completion, create `.planning/phases/182-design-system-reference-page-v2/182-09-SUMMARY.md`
</output>
