---
phase: 183-v20-hygiene-cleanup
plan: 05
type: execute
wave: 2
depends_on: [183-01, 183-02, 183-03, 183-04]
files_modified:
  - .planning/phases/174-ember-glass-tokens-foundations/174-VALIDATION.md
  - .planning/phases/176-post-auth0-splash-animation/176-VALIDATION.md
  - .planning/phases/178-per-device-modal-sheets/178-VALIDATION.md
  - .planning/phases/179-rooms-tab-redesign/179-VALIDATION.md
  - .planning/phases/180-automations-tab-full-editor/180-VALIDATION.md
  - .planning/phases/181-glass-bottom-tab-bar/181-VALIDATION.md
  - .planning/phases/182-design-system-reference-page-v2/182-VALIDATION.md
autonomous: true
requirements: []
requirements_addressed: []
must_haves:
  truths:
    - "All 7 v20.0 phases (174, 176, 178, 179, 180, 181, 182) have VALIDATION.md frontmatter `status` flipped from `draft` to a terminal value (e.g. `complete` or `ready`)"
    - "Where applicable, `wave_0_complete: true` per the auditor's findings"
    - "/gsd-validate-phase commits per phase (audit trail entry retained)"
  artifacts:
    - path: ".planning/phases/{174,176,178,179,180,181,182}*/[0-9]*-VALIDATION.md"
      provides: "Normalized validation artifact per Nyquist convention"
      contains: "status: " # but NOT `status: draft`
  key_links:
    - from: "VALIDATION.md frontmatter"
      to: "milestone audit (gsd-audit-milestone)"
      via: "non-draft terminal status enables audit aggregator to recognize Nyquist compliance"
      pattern: "^status: (complete|ready|final)"
---

<objective>
Run `/gsd-validate-phase` against each of the 7 v20.0 phases whose VALIDATION.md frontmatter is still `status: draft` despite passing verification. The workflow normalizes the frontmatter atomically and commits an audit trail entry per phase.

Purpose: ROADMAP Phase 183 Success Criterion #5. The audit notes 7 phases as `partial` Nyquist coverage purely due to `status: draft` drift; the underlying coverage is complete. This plan flips that drift.

**Important convention finding (RESEARCH §Pattern 3 + §Assumptions A2):** the ROADMAP phrasing "draft → final" is approximate. The actual project convention is `complete` (terminal, post-verification) or `ready` (terminal, pre-execution). DO NOT hand-edit frontmatter to `final` — let `/gsd-validate-phase` choose the appropriate terminal value per phase.
Output: 7 VALIDATION.md files updated; ~7-14 commits (workflow may produce two commits per phase: `test(phase-N)` if test gaps were filled + `docs(phase-N)` for the validation strategy update).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v20.0-MILESTONE-AUDIT.md
@.planning/phases/183-v20-hygiene-cleanup/183-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run /gsd-validate-phase against all 7 phases sequentially</name>
  <read_first>
    - .planning/phases/183-v20-hygiene-cleanup/183-RESEARCH.md (§Architecture Patterns Pattern 3 — workflow steps; §Common Pitfalls Pitfall §4 — commit fan-out is expected; §Assumptions A1-A2 — terminal status is `complete`/`ready`, not `final`)
    - .planning/v20.0-MILESTONE-AUDIT.md frontmatter `scores.nyquist` block (lists the 7 partial_phases: 174, 176, 178, 179, 180, 181, 182)
    - For each phase, glance at the existing VALIDATION.md to confirm State A (file exists) per workflow detection
  </read_first>
  <action>
    Run `/gsd-validate-phase {N}` once per phase, in this order (smallest phase number first):

    1. `/gsd-validate-phase 174`
    2. `/gsd-validate-phase 176`
    3. `/gsd-validate-phase 178`
    4. `/gsd-validate-phase 179`
    5. `/gsd-validate-phase 180`
    6. `/gsd-validate-phase 181`
    7. `/gsd-validate-phase 182`

    The workflow:
    - Detects State A (existing VALIDATION.md → audit/normalize path)
    - Performs gap analysis against the phase's PLAN/SUMMARY artifacts
    - Optionally spawns `gsd-nyquist-auditor` if test gaps are detected; otherwise skips test work
    - Writes/updates VALIDATION.md frontmatter atomically (status flips from `draft` to a terminal value chosen by the workflow)
    - Commits separately per phase (`test(phase-N)` for any test additions + `docs(phase-N)` for VALIDATION.md)

    **Per-invocation behavior:**
    - Do NOT pass `--no-commit` — the commit fan-out is intentional (RESEARCH §Pitfall §4); milestone audit traceability requires per-phase docs commits.
    - Do NOT hand-edit VALIDATION.md frontmatter as a shortcut (RESEARCH §Anti-Patterns) — bypasses gap-fill.
    - Do NOT chain via `&&` shell composition — run each invocation as its own /gsd-validate-phase call so any failure halts the sequence visibly.
    - If a workflow run produces unexpected gaps that require new tests, accept the test work that the workflow generates (don't suppress it). Phase 183's scope explicitly includes whatever the auditor needs to flip status legitimately.

    **After each invocation:** confirm the target VALIDATION.md no longer has `status: draft` before moving to the next phase. If a run fails or status doesn't flip, STOP and report — do NOT continue to the next phase.

    **Final verification (after all 7 runs):**
    ```bash
    for p in 174 176 178 179 180 181 182; do
      vf=$(ls .planning/phases/${p}*/[0-9]*-VALIDATION.md 2>/dev/null | head -1)
      status=$(awk '/^status:/ {print $2; exit}' "$vf" 2>/dev/null)
      echo "Phase $p: status=$status"
    done
    ```
    Expected: every phase reports a non-`draft` terminal value (e.g., `complete`, `ready`). If any phase still shows `draft`, the workflow run for that phase didn't complete — investigate.
  </action>
  <verify>
    <automated>for p in 174 176 178 179 180 181 182; do vf=$(ls .planning/phases/${p}*/[0-9]*-VALIDATION.md 2>/dev/null | head -1); status=$(awk '/^status:/ {print $2; exit}' "$vf" 2>/dev/null); echo "Phase $p: status=$status"; done | grep -c "draft" | grep -q "^0$" && echo "All 7 phases non-draft ✓"</automated>
  </verify>
  <acceptance_criteria>
    - For each phase in {174, 176, 178, 179, 180, 181, 182}: `awk '/^status:/ {print $2; exit}' .planning/phases/${phase}*/[0-9]*-VALIDATION.md` outputs a value that is NOT `draft`
    - The 7-iteration verification loop above produces zero `draft` lines
    - `git log --oneline -50` shows new commits with messages prefixed `docs(phase-174)`, `docs(phase-176)`, ... `docs(phase-182)` (or `test(phase-N)` if gaps existed) — at least 7 new docs commits
    - Phase 175 + 177 VALIDATION.md UNCHANGED (they were already compliant per audit; not in this plan's `files_modified`)
  </acceptance_criteria>
  <done>All 7 partial-Nyquist phases have terminal-status VALIDATION.md frontmatter; commits land for milestone audit traceability.</done>
</task>

</tasks>

<verification>
- 7 VALIDATION.md files normalized (status no longer `draft`)
- Phase 175 + Phase 177 VALIDATION.md untouched (already compliant)
- Per CLAUDE.md Rule 8: workflow's internal commits use the workflow's own commit conventions; no bare `npm test` invoked from this plan
- Per CLAUDE.md Rule 4: workflow does NOT call `npm run build` or `npm install`
- Wave 2 dependency rationale: runs AFTER Wave 1 (plans 01-04) so any test changes from `/gsd-validate-phase` execute against the cleaned tree (post-orphan-deletion, post-console.error, post-doc-flips). If `/gsd-validate-phase` triggers `npm run test:changed` internally and the tree had stale orphans, results could be misleading.
</verification>

<success_criteria>
- [ ] 174-VALIDATION.md `status:` is not `draft`
- [ ] 176-VALIDATION.md `status:` is not `draft`
- [ ] 178-VALIDATION.md `status:` is not `draft`
- [ ] 179-VALIDATION.md `status:` is not `draft`
- [ ] 180-VALIDATION.md `status:` is not `draft`
- [ ] 181-VALIDATION.md `status:` is not `draft`
- [ ] 182-VALIDATION.md `status:` is not `draft`
- [ ] Phase 175 + 177 VALIDATION.md unchanged (not touched by this plan)
- [ ] Per-phase docs commits visible in `git log` for traceability
</success_criteria>

<output>
After completion, create `.planning/phases/183-v20-hygiene-cleanup/183-05-SUMMARY.md` documenting:
- 7 VALIDATION.md files normalized (one per phase); commit hashes for the per-phase docs commits
- Final terminal-status value chosen by the workflow per phase (most likely `complete` for code-verified phases, `ready` if any are pre-execution — let workflow decide)
- Note: ROADMAP wording "draft → final" is approximate; actual values are `complete`/`ready` per project convention (RESEARCH §Pattern 3)
</output>
