---
phase: 35-micro-interactions
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - app/components/ui/Badge.js
  - app/components/ui/Tabs.js
  - app/components/ui/Accordion.js
autonomous: true

must_haves:
  truths:
    - "Badges respond instantly to hover/focus"
    - "Tab indicator slides smoothly between tabs with subtle bounce"
    - "Accordion content reveals smoothly"
  artifacts:
    - path: "app/components/ui/Badge.js"
      provides: "Badge with fast animation tokens"
      contains: "var(--duration-fast)"
    - path: "app/components/ui/Tabs.js"
      provides: "Tabs with spring physics on indicator"
      contains: "ease-spring-subtle"
    - path: "app/components/ui/Accordion.js"
      provides: "Accordion with animation tokens"
      contains: "var(--duration-"
  key_links:
    - from: "app/components/ui/Tabs.js"
      to: "app/globals.css"
      via: "CSS custom properties"
      pattern: "--ease-spring-subtle"
---

<objective>
Update Badge, Tabs, and Accordion components to use the new animation token system, adding spring physics to the tab indicator for polished feel.

Purpose: Complete animation token adoption across secondary interactive components
Output: Updated Badge, Tabs, Accordion with animation tokens
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-micro-interactions/35-01-SUMMARY.md
@.planning/phases/35-micro-interactions/35-CONTEXT.md
@app/components/ui/Badge.js
@app/components/ui/Tabs.js
@app/components/ui/Accordion.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Badge with fast animation token</name>
  <files>app/components/ui/Badge.js</files>
  <action>
Find 'duration-200' and replace with:
```javascript
'duration-[var(--duration-fast)]',
```

Badge is a small UI element - use fast duration for snappy feedback on hover/focus states.

Keep all existing functionality - only update timing values.
  </action>
  <verify>
Grep Badge.js for "duration-fast" - should exist.
Run `npm test -- --testPathPattern=Badge` - tests should pass.
  </verify>
  <done>
Badge uses --duration-fast for snappy hover/focus transitions
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Tabs with animation tokens and spring indicator</name>
  <files>app/components/ui/Tabs.js</files>
  <action>
Find 'duration-200' and 'duration-300' occurrences and replace:
- Tab trigger hover: 'duration-[var(--duration-fast)]' (for responsive hover)
- Sliding indicator: 'duration-[var(--duration-smooth)]' + 'ease-[var(--ease-spring-subtle)]' (for polished slide effect)

The sliding indicator should have subtle spring physics - this is where the polish shines.

Keep all existing functionality - only update timing values.
  </action>
  <verify>
Grep Tabs.js for "duration-smooth" - should exist.
Grep Tabs.js for "ease-spring-subtle" - should exist.
Run `npm test -- --testPathPattern=Tabs` - tests should pass.
  </verify>
  <done>
Tabs uses --duration-fast for hover and --duration-smooth with --ease-spring-subtle for sliding indicator
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Accordion with animation tokens</name>
  <files>app/components/ui/Accordion.js</files>
  <action>
Find 'duration-200' and 'duration-300' occurrences and replace:
- Trigger hover: 'duration-[var(--duration-fast)]'
- Chevron rotation: 'duration-[var(--duration-smooth)]' (keep existing or use smooth)
- Content animation already uses @keyframes accordion-down/up which reference CSS - no change needed there

Keep all existing functionality - only update timing values.
  </action>
  <verify>
Grep Accordion.js for "duration-fast" - should exist.
Run `npm test -- --testPathPattern=Accordion` - tests should pass.
  </verify>
  <done>
Accordion uses --duration-fast for trigger hover, chevron uses animation tokens
  </done>
</task>

</tasks>

<verification>
1. Badge has fast hover transitions
2. Tabs sliding indicator has spring physics
3. Accordion trigger has responsive hover
4. All updated components still pass existing tests
</verification>

<success_criteria>
- 3 components updated with animation tokens
- Tabs indicator has spring easing
- All existing tests pass
- Components render correctly in browser
</success_criteria>

<output>
After completion, create `.planning/phases/35-micro-interactions/35-03-SUMMARY.md`
</output>
