---
phase: 51-e2e-test-improvements
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [package.json]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "playwright.config.ts import of dotenv/config resolves without module-not-found error"
    - "E2E tests can run locally after npm install"
  artifacts:
    - path: "package.json"
      provides: "dotenv devDependency declaration"
      contains: "dotenv"
  key_links:
    - from: "playwright.config.ts"
      to: "package.json"
      via: "import 'dotenv/config' resolves to node_modules/dotenv"
      pattern: "dotenv"
---

<objective>
Add missing dotenv dependency to package.json devDependencies.

Purpose: playwright.config.ts line 2 imports `dotenv/config` but dotenv is not listed in package.json, blocking all local E2E test execution. CI works because GitHub Actions provides env vars directly, but developers cannot run tests locally.

Output: package.json with dotenv in devDependencies.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-e2e-test-improvements/51-VERIFICATION.md
@playwright.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dotenv to package.json devDependencies</name>
  <files>package.json</files>
  <action>
    Add `"dotenv": "^16.4.5"` to the `devDependencies` section of package.json.

    Insert it in alphabetical order within devDependencies (after "cypress" or similar d-prefixed entries, before any e-prefixed entries).

    Do NOT run `npm install` — project rules prohibit it. The user will run it manually after plan execution.

    This fixes the blocker identified in 51-VERIFICATION.md: `import 'dotenv/config'` on line 2 of playwright.config.ts fails with "Cannot find module 'dotenv/config'" because the package was never declared.
  </action>
  <verify>
    Run: `node -e "const pkg = require('./package.json'); const v = pkg.devDependencies?.dotenv; if (v) console.log('OK: dotenv', v); else { console.error('MISSING'); process.exit(1); }"`

    Expected output: `OK: dotenv ^16.4.5`
  </verify>
  <done>
    package.json devDependencies contains "dotenv": "^16.4.5". After user runs `npm install`, `import 'dotenv/config'` in playwright.config.ts resolves successfully.
  </done>
</task>

</tasks>

<verification>
1. `node -e "console.log(require('./package.json').devDependencies.dotenv)"` outputs `^16.4.5`
2. After user runs `npm install`: `node -e "require('dotenv/config')"` exits without error
</verification>

<success_criteria>
- dotenv is declared in package.json devDependencies with version ^16.4.5
- No other files modified (minimal gap closure)
- playwright.config.ts unchanged (it already has the correct import)
</success_criteria>

<output>
After completion, create `.planning/phases/51-e2e-test-improvements/51-04-SUMMARY.md`
</output>
