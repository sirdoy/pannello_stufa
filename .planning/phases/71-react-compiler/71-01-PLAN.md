---
phase: 71-react-compiler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - package.json
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "User benefits from auto-memoization: React Compiler is enabled and transforms components at build time"
    - "User sees no regressions: all 3,700+ existing tests pass after compiler enablement"
    - "User benefits from healthcheck: npx react-compiler-healthcheck output is reviewed and documented before enablement"
    - "Non-compliant components are individually opted out with 'use no memo' rather than blocking the compiler globally"
  artifacts:
    - path: "next.config.ts"
      provides: "React Compiler enabled via reactCompiler: true"
      contains: "reactCompiler: true"
    - path: "package.json"
      provides: "babel-plugin-react-compiler added to devDependencies"
      contains: "babel-plugin-react-compiler"
  key_links:
    - from: "next.config.ts"
      to: "babel-plugin-react-compiler"
      via: "Next.js 16 built-in integration reads devDependency"
      pattern: "reactCompiler.*true"
---

<objective>
Enable React Compiler auto-memoization across the entire codebase, replacing manual useMemo/useCallback overhead with compiler-inferred memoization. Run the healthcheck first to identify non-compliant files, enable the compiler globally, verify all tests pass, and opt out any problematic components with "use no memo".

Purpose: React Compiler 1.0 provides build-time auto-memoization that is more precise than manual useMemo/useCallback (can memoize after early returns). This phase enables it globally for the 277 useMemo/useCallback calls across 80 files, reducing re-render overhead without code changes. Isolated in its own phase for clean regression attribution.

Output: React Compiler enabled in next.config.ts, healthcheck results documented, all tests green, any non-compliant files opted out with "use no memo".
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-react-compiler/71-RESEARCH.md
@next.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run healthcheck, add compiler dependency, and enable React Compiler</name>
  <files>next.config.ts, package.json</files>
  <action>
    **Step 1 — Run healthcheck (COMP-03):**
    Run `npx react-compiler-healthcheck@latest` from the project root. Capture the full output. Document:
    - How many components compiled successfully out of total (`X out of Y`)
    - Whether StrictMode was detected (expected: yes, project has `reactStrictMode: true`)
    - Whether incompatible libraries were found (expected: none — Firebase, Radix, Recharts are compiler-safe)
    - List any files that failed compilation — these are candidates for `"use no memo"` in Task 2

    **Step 2 — Add babel-plugin-react-compiler to package.json (devDependency):**
    Edit `package.json` to add `"babel-plugin-react-compiler": "^1.0.0"` to the `devDependencies` section. Keep alphabetical order. Do NOT run `npm install` (per project rules). The dependency will be installed when the user next runs `npm install`.

    **Step 3 — Enable React Compiler in next.config.ts:**
    Add `reactCompiler: true` to the `nextConfig` object in `next.config.ts`, immediately after the existing `reactStrictMode: true` line. This is a single-line addition:

    ```typescript
    const nextConfig: NextConfig = {
      reactStrictMode: true,
      reactCompiler: true,   // Phase 71: auto-memoization via React Compiler 1.0
      // ... rest unchanged
    };
    ```

    Do NOT change any other configuration. The Serwist and bundle-analyzer wrappers remain unchanged. Next.js 16 handles the compiler integration natively — it uses SWC optimization to only apply the Babel compiler to files with JSX or hooks.
  </action>
  <verify>
    1. `grep "reactCompiler" next.config.ts` shows `reactCompiler: true`
    2. `grep "babel-plugin-react-compiler" package.json` shows the dependency in devDependencies
    3. Healthcheck output is captured and reviewed (no action needed if all components compile)
  </verify>
  <done>
    - Healthcheck output reviewed and documented (COMP-03 satisfied)
    - `babel-plugin-react-compiler` added to package.json devDependencies
    - `reactCompiler: true` added to next.config.ts
    - No other configuration changes made
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and opt out non-compliant components</name>
  <files>Any file that needs "use no memo" opt-out (determined at runtime)</files>
  <action>
    **Step 1 — Run the full test suite:**
    Run `npm test` and capture the output. The React Compiler operates at build time, but Jest uses Babel transforms — the compiler may affect test behavior if tests rely on referential equality of memoized values.

    **Step 2 — If ALL tests pass (expected path):**
    Document that all 3,700+ tests pass. COMP-02 is satisfied. No opt-outs needed.

    **Step 3 — If any tests FAIL (fallback path):**
    For each failing test file:
    1. Identify the component/hook being tested
    2. Check if the failure is due to changed memoization identity (common: `toBe()` assertions on callback props) or a genuine Rules of React violation
    3. If referential equality test: the test is fragile — consider if it's worth fixing the test vs. opting out the component
    4. If Rules of React violation: add `"use no memo"` as the FIRST statement inside the function body of the affected component/hook

    ```typescript
    export default function AffectedComponent() {
      "use no memo"; // TODO(phase-71): Rules of React violation — fix and remove opt-out
      // ... rest of component unchanged
    }
    ```

    5. Re-run `npm test` after each batch of opt-outs until ALL tests pass
    6. Maximum 3 retry cycles. If tests still fail after 3 rounds, document the remaining failures.

    **IMPORTANT:** Do NOT remove any existing `useMemo` or `useCallback` calls. The compiler coexists with manual memoization. Cleanup is deferred to a future phase for clean regression attribution.

    **Step 3 — Capture post-compiler test results:**
    Document the final test result: total tests, passed, failed, skipped. This proves COMP-02 (no regressions).
  </action>
  <verify>
    1. `npm test` exits with code 0 (all tests pass)
    2. If any `"use no memo"` opt-outs were added, each is documented with the reason
    3. No existing useMemo/useCallback calls were removed
  </verify>
  <done>
    - All 3,700+ tests pass with React Compiler enabled (COMP-02 satisfied)
    - Any non-compliant components are opted out with `"use no memo"` and documented
    - No manual memoization removed (cleanup deferred)
    - COMP-01 satisfied: compiler auto-memoizes all compliant components
  </done>
</task>

</tasks>

<verification>
1. `grep "reactCompiler: true" next.config.ts` — compiler enabled
2. `grep "babel-plugin-react-compiler" package.json` — dependency present
3. `npm test` passes (all 3,700+ tests green)
4. Healthcheck output documented in SUMMARY.md
5. Any `"use no memo"` opt-outs documented with reasons
</verification>

<success_criteria>
- React Compiler is enabled globally via `reactCompiler: true` in next.config.ts
- babel-plugin-react-compiler is listed in package.json devDependencies
- npx react-compiler-healthcheck output reviewed and documented
- All existing tests pass (3,700+) — zero regressions
- Any non-compliant components individually opted out (not blocking compiler globally)
- No existing useMemo/useCallback calls removed (deferred for regression attribution)
</success_criteria>

<output>
After completion, create `.planning/phases/71-react-compiler/71-01-SUMMARY.md`
</output>
