---
phase: 77-camera-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types/netatmoProxy.ts
  - lib/netatmoProxy.ts
  - __tests__/lib/netatmoProxy-camera.test.ts
autonomous: true
requirements: [CAM-01, CAM-02, CAM-03, CAM-04, CAM-05, CAM-06]

must_haves:
  truths:
    - "Camera proxy types exist for all 6 proxy endpoints"
    - "Convenience wrappers compile and call correct proxy paths"
  artifacts:
    - path: "types/netatmoProxy.ts"
      provides: "Camera proxy response types"
      contains: "CameraStatusResponse"
    - path: "lib/netatmoProxy.ts"
      provides: "Camera convenience wrappers"
      exports: ["getProxyCameraStatus", "getProxyCameraStream", "getProxyCameraSnapshot", "proxySetCameraMonitoring", "getProxyCameraEvents", "getProxyCameraEventSnapshot"]
    - path: "__tests__/lib/netatmoProxy-camera.test.ts"
      provides: "Unit tests for camera convenience wrappers"
  key_links:
    - from: "lib/netatmoProxy.ts"
      to: "types/netatmoProxy.ts"
      via: "import camera types"
      pattern: "import.*Camera.*from.*netatmoProxy"
---

<objective>
Extend the Netatmo proxy client with camera-specific TypeScript types and convenience wrappers for all 6 camera proxy endpoints.

Purpose: Provide typed client functions that API routes (Plan 02) will call, following the same pattern established in Phase 75 for energy endpoints.
Output: Camera types in types/netatmoProxy.ts, 6 convenience wrappers in lib/netatmoProxy.ts, tests.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-camera-migration/77-CONTEXT.md
@docs/api/netatmo.md

<interfaces>
<!-- Existing proxy client functions to extend -->

From lib/netatmoProxy.ts:
```typescript
export async function netatmoProxyGet<T>(endpoint: string, options?: { timeout?: number }): Promise<T>;
export async function netatmoProxyPost<T>(endpoint: string, body: Record<string, unknown>, options?: { timeout?: number }): Promise<T>;
```

From types/netatmoProxy.ts:
```typescript
export type DataFreshness = 'LIVE' | 'STALE' | 'UNREACHABLE';
export interface ProxyControlResponse { status: string; time_exec: number; time_server: number; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add camera proxy types to types/netatmoProxy.ts</name>
  <files>types/netatmoProxy.ts</files>
  <behavior>
    - CameraStatus interface has camera_id, name, device_type, status, sd_status, alim_status, firmware, is_local (all nullable except camera_id)
    - CameraStatusResponse has cameras: CameraStatus[] and data_freshness: DataFreshness
    - StreamUrls has high, medium, low string fields
    - CameraStreamResponse has camera_id, vpn_streams: StreamUrls, is_local: boolean, local_streams?: StreamUrls
    - CameraSnapshotUrlResponse has camera_id and snapshot_url strings
    - SetMonitoringRequest has monitoring: "on" | "off"
    - SetMonitoringResponse has camera_id, monitoring: "on" | "off", status: "applied"
    - CameraEvent has event_id, camera_id, event_type, timestamp, message (nullable), snapshot_url (nullable), person_id (nullable)
    - CameraEventsResponse has events: CameraEvent[] and count: number
  </behavior>
  <action>
    Add a new "CAMERA TYPES" section to types/netatmoProxy.ts (after the ROOM MEASURE TYPES section) with all camera proxy types exactly matching the proxy API docs at docs/api/netatmo.md (Security Camera section). Use the same coding style as existing types in the file (JSDoc comments, export interface). All fields per the proxy docs — note these are proxy field names (event_id, event_type, timestamp) NOT the old Netatmo API field names (id, type, time). This is a locked decision from CONTEXT.md.
  </action>
  <verify>
    <automated>npx tsc --noEmit types/netatmoProxy.ts 2>&1 | head -20</automated>
  </verify>
  <done>All 9 camera type interfaces/types exported from types/netatmoProxy.ts with zero tsc errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add camera convenience wrappers + tests</name>
  <files>lib/netatmoProxy.ts, __tests__/lib/netatmoProxy-camera.test.ts</files>
  <behavior>
    - getProxyCameraStatus() calls GET /camera/status, returns CameraStatusResponse
    - getProxyCameraStream(cameraId) calls GET /camera/{cameraId}/stream, returns CameraStreamResponse
    - getProxyCameraSnapshot(cameraId) calls GET /camera/{cameraId}/snapshot, returns CameraSnapshotUrlResponse
    - proxySetCameraMonitoring(cameraId, body) calls POST /camera/{cameraId}/monitoring, returns SetMonitoringResponse
    - getProxyCameraEvents(hours?) calls GET /camera/events?hours={hours}, returns CameraEventsResponse
    - getProxyCameraEventSnapshot(eventId) calls GET /camera/events/{eventId}/snapshot — returns raw Response (binary), NOT parsed JSON
    - All wrappers use netatmoProxyGet<T> or netatmoProxyPost<T> except getProxyCameraEventSnapshot which uses raw fetch (binary endpoint)
  </behavior>
  <action>
    1. RED: Create __tests__/lib/netatmoProxy-camera.test.ts with tests for each of the 6 convenience wrappers. Mock netatmoProxyGet and netatmoProxyPost using jest.mock. For getProxyCameraEventSnapshot, mock global fetch (it does raw binary fetch, not JSON). Test that correct endpoints are called with correct parameters.

    2. GREEN: Add 6 convenience wrappers to lib/netatmoProxy.ts after the existing energy wrappers section. Follow the same pattern as getProxyHomestatus/proxySetRoomThermpoint. Add a new comment section "// CAMERA WRAPPERS". Import the new camera types from types/netatmoProxy.

    For getProxyCameraEventSnapshot: This is a binary endpoint. It must NOT use netatmoProxyGet (which parses JSON). Instead, do a raw fetch with X-API-Key header and return the Response object directly. The API route will stream it to the client. Use the same env var check + AbortController pattern as netatmoProxyGet but return response (not response.json()).

    For getProxyCameraEvents: Accept optional `hours` parameter (number). If provided, append `?hours=${hours}` to the endpoint path.

    For wrappers taking cameraId: Use template literal to build path e.g. `/camera/${cameraId}/stream`.

    Double assertion pattern for proxySetCameraMonitoring body: `body as unknown as Record<string, unknown>` — same as proxySetRoomThermpoint.

    3. REFACTOR if needed.
  </action>
  <verify>
    <automated>npx jest __tests__/lib/netatmoProxy-camera.test.ts --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <done>6 convenience wrappers exported, all tests passing, zero tsc errors</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx jest __tests__/lib/netatmoProxy-camera.test.ts` all tests green
- All 9 camera types exported from types/netatmoProxy.ts
- All 6 wrappers exported from lib/netatmoProxy.ts
</verification>

<success_criteria>
- Camera proxy types match docs/api/netatmo.md Security Camera section exactly
- Convenience wrappers follow same pattern as energy wrappers (Phase 75/76)
- getProxyCameraEventSnapshot returns raw Response (not parsed JSON)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/77-camera-migration/77-01-SUMMARY.md`
</output>
