---
phase: 77-camera-migration
plan: 03
type: execute
wave: 3
depends_on: ["77-02"]
files_modified:
  - lib/netatmoCameraApi.ts
  - app/components/devices/camera/CameraCard.tsx
  - app/(pages)/camera/CameraDashboard.tsx
  - app/(pages)/camera/events/CameraEventsPage.tsx
  - app/components/devices/camera/EventPreviewModal.tsx
  - __tests__/app/(pages)/camera/CameraDashboard.test.tsx
autonomous: true
requirements: [CAM-01, CAM-02, CAM-03, CAM-04, CAM-05, CAM-06]

must_haves:
  truths:
    - "CameraCard loads camera status from /camera/status route"
    - "CameraCard shows live stream using proxy stream URLs"
    - "CameraDashboard fetches status and events separately"
    - "CameraEventsPage uses proxy event field names (event_id, event_type, timestamp)"
    - "No OAuth re-authorization UI remains in camera components"
    - "netatmoCameraApi.ts only retains display helpers"
  artifacts:
    - path: "lib/netatmoCameraApi.ts"
      provides: "Display helpers only (getCameraTypeName, getEventTypeName, getEventIcon, getSubTypeName, getSubTypeIcon)"
    - path: "app/components/devices/camera/CameraCard.tsx"
      provides: "Camera card using proxy routes"
    - path: "app/(pages)/camera/CameraDashboard.tsx"
      provides: "Camera dashboard using split proxy fetches"
    - path: "app/(pages)/camera/events/CameraEventsPage.tsx"
      provides: "Events page using proxy event field names"
  key_links:
    - from: "app/components/devices/camera/CameraCard.tsx"
      to: "lib/routes.ts"
      via: "CAMERA_ROUTES.status"
      pattern: "CAMERA_ROUTES\\.status"
    - from: "app/(pages)/camera/CameraDashboard.tsx"
      to: "lib/routes.ts"
      via: "CAMERA_ROUTES.status and CAMERA_ROUTES.allEvents"
      pattern: "CAMERA_ROUTES\\.(status|allEvents)"
    - from: "app/(pages)/camera/events/CameraEventsPage.tsx"
      to: "lib/routes.ts"
      via: "CAMERA_ROUTES.allEvents"
      pattern: "CAMERA_ROUTES\\.allEvents"
---

<objective>
Update all camera frontend components to use proxy routes and field names, remove OAuth re-authorization UI, and clean up netatmoCameraApi.ts by deleting all API/URL functions (keeping only display helpers).

Purpose: Complete the camera migration by updating consumers. Components adapt to proxy schema (event_id not id, event_type not type, timestamp not time). OAuth flow is replaced by standard error handling.
Output: All camera UI components work with proxy data, netatmoCameraApi.ts reduced to display helpers only.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-camera-migration/77-CONTEXT.md
@.planning/phases/77-camera-migration/77-01-SUMMARY.md
@.planning/phases/77-camera-migration/77-02-SUMMARY.md

<interfaces>
<!-- Updated CAMERA_ROUTES from Plan 02 -->

From lib/routes.ts:
```typescript
export const CAMERA_ROUTES = {
  status: `${API_BASE}/netatmo/camera/status`,
  allEvents: `${API_BASE}/netatmo/camera/events`,
  stream: (cameraId: string): string => `...`,
  snapshot: (cameraId: string): string => `...`,
  monitoring: `${API_BASE}/netatmo/camera/monitoring`,
  eventSnapshot: (eventId: string): string => `...`,
  list: `${API_BASE}/netatmo/camera`,  // backward compat
} as const;
```

<!-- Proxy response shapes (from Plan 01 types) -->

From types/netatmoProxy.ts:
```typescript
interface CameraStatus {
  camera_id: string | null; name: string | null; device_type: string | null;
  status: string | null; sd_status: string | null; alim_status: string | null;
  firmware: string | null; is_local: boolean | null;
}
interface CameraStatusResponse { cameras: CameraStatus[]; data_freshness: DataFreshness; }
interface CameraEvent {
  event_id: string; camera_id: string;
  event_type: "movement" | "person" | "sound";
  timestamp: number; message: string | null;
  snapshot_url: string | null; person_id: string | null;
}
interface CameraEventsResponse { events: CameraEvent[]; count: number; }
interface CameraStreamResponse { camera_id: string; vpn_streams: StreamUrls; is_local: boolean; local_streams?: StreamUrls; }
```

<!-- Display helpers to KEEP from netatmoCameraApi.ts -->
```typescript
function getCameraTypeName(type: string): string;
function getEventTypeName(type: string): string;
function getEventIcon(type: string): string;
function getSubTypeName(subType: number): string | null;
function getSubTypeIcon(subType: number): string | null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up netatmoCameraApi.ts — keep display helpers only</name>
  <files>lib/netatmoCameraApi.ts</files>
  <action>
    Per CONTEXT.md locked decision, delete all API functions and URL helpers from lib/netatmoCameraApi.ts. Keep ONLY display helpers.

    **DELETE these functions:**
    - getCamerasData, getCameraEvents, getEventsUntil (API endpoints)
    - makeRequest (HTTP client)
    - parseCameras, parsePersons, parseEvents (data parsers)
    - getSnapshotUrl, getLiveStreamUrl (URL constructors — proxy provides URLs directly)
    - getEventSnapshotUrl (URL constructor — proxy provides snapshot_url field)
    - getEventVideoUrl, getEventVideoThumbnail, getEventVideoDownloadUrl (URL constructors)

    **DELETE these types (no longer needed):**
    - NetatmoCamera, NetatmoPerson, NetatmoEvent, NetatmoCameraHome (old Netatmo API shapes)
    - ParsedCamera, ParsedPerson, ParsedEvent (old parsed shapes)
    - RequestOptions, NetatmoApiResponse (HTTP client types)
    - NETATMO_API_BASE constant

    **KEEP these functions:**
    - getCameraTypeName(type: string): string
    - getEventTypeName(type: string): string
    - getEventIcon(type: string): string
    - getSubTypeName(subType: number): string | null
    - getSubTypeIcon(subType: number): string | null

    **Update exports:**
    - Default export: object with { getCameraTypeName, getEventTypeName, getEventIcon, getSubTypeName, getSubTypeIcon }
    - Named exports: same 5 functions
    - Remove all deleted functions from export lists

    **Update file header comment** to reflect it's now "Camera Display Helpers" not "Camera API Wrapper".
  </action>
  <verify>
    <automated>npx tsc --noEmit lib/netatmoCameraApi.ts 2>&1 | head -10</automated>
  </verify>
  <done>netatmoCameraApi.ts contains only 5 display helper functions, no API calls, no URL constructors, no old Netatmo types</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend components to use proxy routes and field names</name>
  <files>
    app/components/devices/camera/CameraCard.tsx,
    app/(pages)/camera/CameraDashboard.tsx,
    app/(pages)/camera/events/CameraEventsPage.tsx,
    app/components/devices/camera/EventPreviewModal.tsx,
    __tests__/app/(pages)/camera/CameraDashboard.test.tsx
  </files>
  <action>
    Update all 4 camera components and 1 test file. The key changes across all components:

    **A. Field name mapping (per CONTEXT.md locked decision — components adapt to proxy schema):**
    - `camera.id` -> `camera.camera_id`
    - `camera.type` -> `camera.device_type`
    - `event.id` -> `event.event_id`
    - `event.type` -> `event.event_type`
    - `event.time` -> `event.timestamp`
    - Event snapshot: use `event.snapshot_url` directly from proxy (not `getEventSnapshotUrl()`)
    - Camera status: `camera.status` stays same (proxy uses same field name)

    **B. Fetch endpoints (per CONTEXT.md — split fetches):**
    - Status: `CAMERA_ROUTES.status` (not `CAMERA_ROUTES.list`)
    - Events: `CAMERA_ROUTES.allEvents`
    - Snapshot: `CAMERA_ROUTES.snapshot(camera.camera_id)`
    - Stream: `CAMERA_ROUTES.stream(camera.camera_id)`

    **C. OAuth removal (per CONTEXT.md locked decision):**
    - Remove `needsReauth` state, `handleReauthorize` function, all `setNeedsReauth` calls
    - Remove `data.reconnect` checks
    - Remove `NETATMO_ROUTES` import (only used for OAuth disconnect)
    - Remove all "invalid access token" / "access_denied" / "insufficient scope" string checks
    - Remove the "Riautorizza Netatmo" button and related DeviceCard/Section blocks
    - Replace with standard error display (the error boundary + proxy RFC 9457 errors handle failures)

    **D. Staleness (per CONTEXT.md — pass data_freshness to frontend):**
    - CameraCard: After fetching status, check `data.data_freshness`. If `UNREACHABLE`, show staleness indicator and disable live/snapshot controls. Reuse the visual pattern from thermostat (e.g. a subtle banner or badge).

    **E. Live stream URL (per CONTEXT.md — proxy provides quality-tiered URLs):**
    - CameraCard and CameraDashboard currently call `NETATMO_CAMERA_API.getLiveStreamUrl(camera)` which builds URL from vpn_url
    - Replace with: fetch `CAMERA_ROUTES.stream(camera.camera_id)`, get `vpn_streams.high` (or `local_streams.high` if is_local and local_streams present)
    - Pass the stream URL to HlsPlayer `src` prop

    **Per component:**

    **1. CameraCard.tsx:**
    - Remove `NETATMO_CAMERA_API` import entirely (no longer needed for getLiveStreamUrl)
    - Import `getCameraTypeName` from `@/lib/netatmoCameraApi` (named import)
    - Remove `needsReauth` state, `handleReauthorize`, reauth block, `NETATMO_ROUTES` import
    - Update `fetchCameras()` to call `CAMERA_ROUTES.status`, parse `data.cameras` (uses `camera_id` as key)
    - Add `data_freshness` state, set from `data.data_freshness`
    - Update `fetchSnapshot()` to use `CAMERA_ROUTES.snapshot(camera.camera_id)`
    - For live mode: fetch `CAMERA_ROUTES.stream(selectedCameraId)` to get stream URL, then pass to HlsPlayer
    - Add `streamUrl` state, fetch when entering live mode
    - Update all `camera.id` references to `camera.camera_id`
    - Update `camera.type` to `camera.device_type` in getCameraTypeName call
    - If `!connected` (error, not reauth), show a standard error card with retry button (no OAuth redirect)

    **2. CameraDashboard.tsx:**
    - Remove `NETATMO_CAMERA_API` default import; import only `{ getCameraTypeName, getEventTypeName, getEventIcon, getSubTypeName, getSubTypeIcon }` as named imports
    - Remove `ParsedCamera`, `ParsedEvent` type imports (use inline types or proxy types)
    - Remove `needsReauth`, `handleReauthorize`, reauth block, `NETATMO_ROUTES` import
    - Split fetchData: first fetch `CAMERA_ROUTES.status`, then fetch `CAMERA_ROUTES.allEvents`
    - Update all `camera.id` -> `camera.camera_id`, `camera.type` -> `camera.device_type`
    - Update all `event.id` -> `event.event_id`, `event.type` -> `event.event_type`, `event.time` -> `event.timestamp`
    - Use `event.snapshot_url` directly for event thumbnails (not getEventSnapshotUrl)
    - For live stream: fetch stream URL from `CAMERA_ROUTES.stream(camera.camera_id)` and pass to HlsPlayer
    - Remove `event.video_id`, `event.video_status`, `event.snapshot`, `event.vignette` references (proxy events don't have these fields)
    - Event preview: pass simplified event to EventPreviewModal

    **3. CameraEventsPage.tsx:**
    - Remove `ParsedCamera`, `ParsedEvent` type imports
    - Remove `NETATMO_CAMERA_API` default import; import only `{ getEventTypeName, getEventIcon, getSubTypeName, getSubTypeIcon }` named imports
    - fetchData: call `CAMERA_ROUTES.allEvents` — response has `{ events, count }` directly
    - Need cameras for filter and display: fetch `CAMERA_ROUTES.status` to get camera list for the filter buttons
    - Update all event field references: `event.event_id`, `event.event_type`, `event.timestamp`, `event.snapshot_url`
    - Update camera field references: `camera.camera_id`, `camera.name`
    - Remove `event.video_id`, `event.video_status` references — proxy events don't have video fields
    - Remove `event.sub_type` references — proxy events use `event_type` which covers all types
    - Use `event.snapshot_url` directly in img src (not getEventSnapshotUrl)

    **4. EventPreviewModal.tsx:**
    - Remove `ParsedEvent`, `ParsedCamera` type imports from netatmoCameraApi
    - Import `{ getEventTypeName, getEventIcon }` named imports only
    - Update props: event type uses proxy fields (event_id, event_type, timestamp, message, snapshot_url)
    - Remove video-related code: `getEventVideoUrl`, `getEventVideoThumbnail`, `getEventVideoDownloadUrl`, `downloadHlsVideo` import, download button, video player in modal
    - Video playback is out of scope per REQUIREMENTS.md ("Camera recording playback — Proxy provides events + snapshots, video playback not in scope")
    - Modal shows: event snapshot (from `event.snapshot_url` or via `CAMERA_ROUTES.eventSnapshot(event.event_id)` for full-size), event type, timestamp, message
    - Keep the full-size snapshot view using the binary endpoint: `CAMERA_ROUTES.eventSnapshot(event.event_id)` as img src for the detail view
    - Remove `event.time` -> use `event.timestamp`
    - Remove `event.type` -> use `event.event_type`
    - Remove `event.id` -> use `event.event_id`

    **5. CameraDashboard.test.tsx:**
    - Update mock data to use proxy field names: `camera_id` not `id`, `device_type` not `type`, etc.
    - Update mock fetch responses to match new route structure (separate status and events calls)
    - Remove any OAuth/reauth test cases
    - Ensure test still passes after component changes
  </action>
  <verify>
    <automated>npx jest __tests__/app/\\(pages\\)/camera/CameraDashboard.test.tsx --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <done>
    - All 4 camera components use CAMERA_ROUTES.status (not .list) and proxy field names
    - No imports of requireNetatmoToken, NETATMO_ROUTES, or ParsedCamera/ParsedEvent from netatmoCameraApi
    - No OAuth re-authorization UI (needsReauth, handleReauthorize, "Riautorizza Netatmo" button)
    - Event fields use event_id, event_type, timestamp (not id, type, time)
    - Camera fields use camera_id, device_type (not id, type)
    - CameraDashboard test passes with proxy mock data
    - Video download/playback removed from EventPreviewModal (out of scope)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx jest __tests__/app/\\(pages\\)/camera/ --no-coverage` passes
- `grep -r "requireNetatmoToken\|NETATMO_CAMERA_API\.get\|NETATMO_CAMERA_API\.parse\|NETATMO_CAMERA_API\.getSnapshot\|NETATMO_CAMERA_API\.getLiveStream\|NETATMO_CAMERA_API\.getEvent" app/` returns no matches
- `grep -r "needsReauth\|handleReauthorize\|Riautorizza" app/components/devices/camera/ app/\\(pages\\)/camera/` returns no matches
- `grep -r "event\.id\b\|event\.type\b\|event\.time\b" app/components/devices/camera/ app/\\(pages\\)/camera/` returns no matches (old field names gone)
</verification>

<success_criteria>
- Camera components load data from proxy routes, not direct Netatmo API
- Proxy field names used everywhere (event_id, event_type, timestamp, camera_id, device_type)
- OAuth re-authorization flow completely removed
- netatmoCameraApi.ts is a small display-helpers-only module
- Existing CameraDashboard test updated and passing
</success_criteria>

<output>
After completion, create `.planning/phases/77-camera-migration/77-03-SUMMARY.md`
</output>
