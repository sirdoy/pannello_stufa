---
phase: 81-fix-stovesync-debug-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/netatmo/StoveSyncPanel.tsx
  - __tests__/components/StoveSyncPanel.test.tsx
  - app/api/netatmo/disconnect/route.ts
  - app/api/netatmo/stove-sync/route.ts
  - app/thermostat/page.tsx
  - app/thermostat/page.test.tsx
  - app/settings/thermostat/page.tsx
  - __tests__/app/settings/thermostat/page.test.tsx
  - lib/routes.ts
  - app/debug/components/tabs/NetatmoTab.tsx
  - app/debug/api/components/tabs/NetatmoTab.tsx
  - lib/coordinationNotificationThrottle.ts
autonomous: true
requirements: [CLEAN-02]

must_haves:
  truths:
    - "Debug panel Netatmo tab loads without any 404 errors from deleted endpoints"
    - "Thermostat page renders without StoveSyncPanel and without disconnect button"
    - "Settings thermostat page renders without StoveSyncPanel"
    - "NETATMO_ROUTES no longer contains stoveSync or disconnect keys"
    - "Debug panel shows proxy-era endpoints (valves, camera/status, schedules)"
  artifacts:
    - path: "lib/routes.ts"
      provides: "NETATMO_ROUTES without stoveSync and disconnect"
      contains: "NETATMO_ROUTES"
    - path: "app/debug/components/tabs/NetatmoTab.tsx"
      provides: "Updated debug tab with proxy endpoints"
      contains: "valves"
    - path: "app/debug/api/components/tabs/NetatmoTab.tsx"
      provides: "Updated debug/api tab with proxy endpoints"
      contains: "valves"
  key_links:
    - from: "app/debug/components/tabs/NetatmoTab.tsx"
      to: "/api/netatmo/health"
      via: "fetchGetEndpoint for connectionStatus"
      pattern: "health.*provider_status|data_freshness"
---

<objective>
Remove all references to deleted API routes (stove-sync, devices, devices-temperatures, debug, disconnect), delete StoveSyncPanel and its test, clean up debug panel NetatmoTab to show proxy-era endpoints, and fix stale JSDoc.

Purpose: Close CLEAN-02 gap -- the last cleanup item from the v10.0 Netatmo API Migration audit.
Output: Clean codebase with no 404 references to deleted routes, debug panel reflecting proxy-era endpoints.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-fix-stovesync-debug-cleanup/81-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/routes.ts (NETATMO_ROUTES to modify):
```typescript
export const NETATMO_ROUTES = {
  disconnect: `${API_BASE}/netatmo/disconnect`,      // DELETE THIS
  homesData: `${API_BASE}/netatmo/homesdata`,
  homeStatus: `${API_BASE}/netatmo/homestatus`,
  schedules: `${API_BASE}/netatmo/schedules`,
  switchHomeSchedule: `${API_BASE}/netatmo/switchhomeschedule`,
  setRoomThermpoint: `${API_BASE}/netatmo/setroomthermpoint`,
  setThermMode: `${API_BASE}/netatmo/setthermmode`,
  calibrate: `${API_BASE}/netatmo/calibrate`,
  stoveSync: `${API_BASE}/netatmo/stove-sync`,       // DELETE THIS
} as const;
```

From app/debug/components/ApiTab (component interface used in NetatmoTab):
```typescript
<EndpointCard name={string} url={string} externalUrl?={string} response={any} loading={boolean} timing={number} onRefresh={()=>void} onCopyUrl={()=>void} isCopied={boolean} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete StoveSyncPanel, disconnect route, and clean consumers</name>
  <files>
    app/components/netatmo/StoveSyncPanel.tsx,
    __tests__/components/StoveSyncPanel.test.tsx,
    app/api/netatmo/disconnect/route.ts,
    app/thermostat/page.tsx,
    app/thermostat/page.test.tsx,
    app/settings/thermostat/page.tsx,
    __tests__/app/settings/thermostat/page.test.tsx,
    lib/routes.ts
  </files>
  <action>
1. **Delete files entirely:**
   - `app/components/netatmo/StoveSyncPanel.tsx` (600 LOC, obsolete)
   - `__tests__/components/StoveSyncPanel.test.tsx`
   - `app/api/netatmo/disconnect/route.ts` (dead OAuth route, proxy handles auth)
   - Check if `app/api/netatmo/stove-sync/` directory exists and delete it too (the route StoveSyncPanel was calling)

2. **Edit `lib/routes.ts`:**
   - Remove `disconnect: \`${API_BASE}/netatmo/disconnect\`` from NETATMO_ROUTES
   - Remove `stoveSync: \`${API_BASE}/netatmo/stove-sync\`` from NETATMO_ROUTES
   - Remove the comment `// Stove-valve sync (StoveSyncPanel settings UI)` above stoveSync
   - Remove the comment `// Auth & topology` above disconnect (keep homesData under a suitable comment like `// Topology`)

3. **Edit `app/thermostat/page.tsx`:**
   - Remove `import StoveSyncPanel from '@/app/components/netatmo/StoveSyncPanel';`
   - Remove the `<StoveSyncPanel onSyncComplete={fetchStatus} />` JSX block and its wrapping `<div className="mb-6">` (around line 577-579)
   - Remove the "Riconnetti Account" button block (lines ~299-310) that calls `NETATMO_ROUTES.disconnect` -- this entire button with its onClick handler. Keep the "Riprova" button.
   - If `NETATMO_ROUTES` import is no longer needed after removing disconnect usage, check other usages in the file before removing the import

4. **Edit `app/thermostat/page.test.tsx`:**
   - Remove the `jest.mock('@/app/components/netatmo/StoveSyncPanel', ...)` block (lines 75-79)
   - Verify no test assertions reference "Stove Sync Panel" text

5. **Edit `app/settings/thermostat/page.tsx`:**
   - Remove `import StoveSyncPanel from '@/app/components/netatmo/StoveSyncPanel';`
   - Remove `<StoveSyncPanel />` JSX (line 56). If PidAutomationPanel is the only child left, keep the wrapping div with `space-y-6`

6. **Edit `__tests__/app/settings/thermostat/page.test.tsx`:**
   - Remove the `jest.mock('@/app/components/netatmo/StoveSyncPanel', ...)` block
   - Remove any test assertions referencing StoveSyncPanel or "stove-sync-panel" testid
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npx tsc --noEmit 2>&1 | head -30 && npm test -- --testPathPattern="thermostat" --silent 2>&1 | tail -20</automated>
  </verify>
  <done>StoveSyncPanel.tsx, its test, disconnect route, and stove-sync route are deleted. thermostat page and settings page render without them. NETATMO_ROUTES has no stoveSync or disconnect keys. All thermostat tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Clean debug NetatmoTab endpoints and stale JSDoc</name>
  <files>
    app/debug/components/tabs/NetatmoTab.tsx,
    app/debug/api/components/tabs/NetatmoTab.tsx,
    lib/coordinationNotificationThrottle.ts
  </files>
  <action>
1. **Edit both `NetatmoTab.tsx` files identically** (app/debug/components/tabs/ AND app/debug/api/components/tabs/):

   a. **Remove 3 dead GET endpoint calls from `fetchAllGetEndpoints`:**
      - Remove `fetchGetEndpoint('devices', '/api/netatmo/devices');`
      - Remove `fetchGetEndpoint('devicesTemps', '/api/netatmo/devices-temperatures');`
      - Remove `fetchGetEndpoint('debug', '/api/netatmo/debug');`

   b. **Add 3 proxy-era GET endpoint calls to `fetchAllGetEndpoints`:**
      - Add `fetchGetEndpoint('valves', '/api/netatmo/valves');`
      - Add `fetchGetEndpoint('cameraStatus', '/api/netatmo/camera/status');`
      - Add `fetchGetEndpoint('schedules', '/api/netatmo/schedules');`

   c. **Remove 3 dead EndpointCard JSX blocks:**
      - Remove the "Devices" EndpointCard (url="/api/netatmo/devices")
      - Remove the "Devices Temperatures" EndpointCard (url="/api/netatmo/devices-temperatures")
      - Remove the "Debug Info" EndpointCard (url="/api/netatmo/debug")

   d. **Add 3 proxy-era EndpointCard JSX blocks** (after existing cards, before closing `</div>`):
      - Valves card: name="Valves", url="/api/netatmo/valves", response key `valves`, loading key `valves`
      - Camera Status card: name="Camera Status", url="/api/netatmo/camera/status", response key `cameraStatus`, loading key `cameraStatus`
      - Schedules card: name="Schedules", url="/api/netatmo/schedules", response key `schedules`, loading key `schedules`
      - No `externalUrl` prop on any of these (they are proxy endpoints, not Netatmo Cloud)

   e. **Fix connectionStatus to use /health response instead of deleted /debug:**
      - Change the `if (name === 'debug' && data.connection)` check in `fetchGetEndpoint` to:
        `if (name === 'health' && data.provider_status)` then `setConnectionStatus(data.provider_status === 'ok' ? 'connected' : 'disconnected');`
      - This derives connection status from the proxy health endpoint's `provider_status` field

   f. **Remove stale `externalUrl` props** pointing to `api.netatmo.com` on existing cards:
      - Remove `externalUrl` prop from "Homes Data" card
      - Remove `externalUrl` prop from "Home Status" card
      - Update their `onCopyUrl` to copy the local URL instead of the api.netatmo.com URL
      - Update `isCopied` to match the local URL
      - Also remove `externalUrl` from PostEndpointCard for "Set Therm Mode" and "Set Room Therm Point"
      - Update their `onCopyUrl` and `isCopied` to use local URLs

2. **Edit `lib/coordinationNotificationThrottle.ts`:**
   - Replace the module-level JSDoc (lines 1-22) with updated documentation:
     ```
     /**
      * Coordination Notification Throttle Service
      *
      * Uses Firebase RTDB for persistent throttle state.
      * Falls back to in-memory throttle on Firebase errors.
      *
      * Design:
      * - DIFFERENT from rateLimiter which is per-notification-type
      * - This service is GLOBAL per user (one window for all coordination events)
      * - Graceful fallback on Firebase errors
      * - Does NOT send notifications - only decides if sending is allowed
      *
      * Usage Pattern:
      * 1. Check shouldSendCoordinationNotification(userId)
      * 2. If allowed: send notification, then recordNotificationSent(userId)
      * 3. If not allowed: skip notification but ALWAYS log event to Firestore
      *
      * Per 08-CONTEXT.md:
      * "Global throttle - max 1 notification total every 30 minutes across all coordination events"
      */
     ```
     Key changes: Remove "Feature-flagged throttle with persistent fallback" line, remove "When USE_PERSISTENT_RATE_LIMITER=true, uses Firebase RTDB" line, remove "When false or Firebase fails, falls back to in-memory throttle" line, remove "Feature flag controls implementation (persistent vs in-memory)" line. Replace with direct description of current behavior.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npx tsc --noEmit 2>&1 | head -20 && grep -c "netatmo/devices\|devices-temperatures\|netatmo/debug\|USE_PERSISTENT_RATE_LIMITER" app/debug/components/tabs/NetatmoTab.tsx app/debug/api/components/tabs/NetatmoTab.tsx lib/coordinationNotificationThrottle.ts; echo "Expected: 0 matches per file"</automated>
  </verify>
  <done>Both NetatmoTab variants show only valid proxy-era endpoints (health, homesdata, homestatus, valves, camera/status, schedules). Connection status badge derives from /health provider_status. No externalUrl props point to api.netatmo.com. coordinationNotificationThrottle JSDoc no longer references USE_PERSISTENT_RATE_LIMITER.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm test -- --testPathPattern="thermostat" --silent` passes
3. No grep hits for deleted routes: `grep -r "netatmo/devices\|devices-temperatures\|netatmo/debug\|stove-sync\|StoveSyncPanel\|NETATMO_ROUTES.disconnect\|USE_PERSISTENT_RATE_LIMITER" app/ lib/ __tests__/ --include="*.ts" --include="*.tsx" | grep -v ".planning/" | grep -v "node_modules/"` returns empty
4. Both NetatmoTab files contain "valves", "camera/status", "schedules" endpoint cards
</verification>

<success_criteria>
- StoveSyncPanel.tsx and its test file are deleted
- disconnect route and stove-sync route directories are deleted
- NETATMO_ROUTES has no stoveSync or disconnect keys
- Thermostat page renders without StoveSyncPanel or "Riconnetti" button
- Settings thermostat page renders without StoveSyncPanel
- Debug panel NetatmoTab shows 6 GET endpoints: health, homesdata, homestatus, valves, camera/status, schedules
- Connection status badge reads from /health provider_status
- No stale api.netatmo.com externalUrl props remain
- coordinationNotificationThrottle JSDoc describes current behavior (no USE_PERSISTENT_RATE_LIMITER)
- TypeScript compilation passes, thermostat tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/81-fix-stovesync-debug-cleanup/81-01-SUMMARY.md`
</output>
