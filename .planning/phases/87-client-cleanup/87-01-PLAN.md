---
phase: 87-client-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/fritzbox/fritzboxErrors.ts
  - lib/fritzbox/index.ts
  - lib/fritzbox/fritzboxClient.ts
  - lib/netatmoProxy.ts
autonomous: true
requirements:
  - API-10

must_haves:
  truths:
    - "No unused exports exist in Fritz!Box or Netatmo wrapper modules"
    - "envValidator.ts references only HA_API_URL and HA_API_KEY (no old Netatmo env vars)"
    - "All existing tests pass after any dead export removal"
  artifacts:
    - path: "lib/fritzbox/index.ts"
      provides: "Fritz!Box barrel exports — only live exports remain"
    - path: "lib/fritzbox/fritzboxErrors.ts"
      provides: "Fritz!Box error codes — no JWT-related factories"
  key_links:
    - from: "lib/fritzbox/index.ts"
      to: "app/api/fritzbox/*/route.ts"
      via: "barrel re-exports consumed by route files"
      pattern: "from '@/lib/fritzbox'"
    - from: "lib/netatmoProxy.ts"
      to: "app/api/netatmo/*/route.ts"
      via: "direct imports from wrapper module"
      pattern: "from '@/lib/netatmoProxy'"
---

<objective>
Verify dead exports in Fritz!Box and Netatmo wrapper modules using knip, and remove any unused exports found.

Purpose: Confirm that the in-place migrations in Phases 85-86 left no orphaned exports in the provider wrapper modules. This is the code-side cleanup of API-10.
Output: Clean wrapper modules with zero unused exports, confirmed by knip and test suite.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-client-cleanup/87-CONTEXT.md
@.planning/phases/87-client-cleanup/87-RESEARCH.md

<interfaces>
<!-- Fritz!Box barrel exports (lib/fritzbox/index.ts) -->
export { fritzboxClient } from './fritzboxClient';
export { getCachedData, invalidateCache, CACHE_TTL_MS } from './fritzboxCache';
export { checkRateLimitFritzBox, FRITZBOX_RATE_LIMIT } from './fritzboxRateLimiter';
export { FRITZBOX_ERROR_CODES } from './fritzboxErrors';
export { logDeviceEvent, getDeviceEvents, getDeviceStates, updateDeviceStates } from './deviceEventLogger';

<!-- Fritz!Box error codes (lib/fritzbox/fritzboxErrors.ts) -->
export const FRITZBOX_ERROR_CODES = {
  TR064_NOT_ENABLED: ERROR_CODES.TR064_NOT_ENABLED,
  FRITZBOX_TIMEOUT: ERROR_CODES.FRITZBOX_TIMEOUT,
  FRITZBOX_NOT_CONFIGURED: ERROR_CODES.FRITZBOX_NOT_CONFIGURED,
} as const;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run knip dead export analysis and remove unused exports</name>
  <files>lib/fritzbox/fritzboxErrors.ts, lib/fritzbox/index.ts, lib/fritzbox/fritzboxClient.ts, lib/netatmoProxy.ts</files>
  <read_first>
    lib/fritzbox/index.ts
    lib/fritzbox/fritzboxErrors.ts
    lib/fritzbox/fritzboxClient.ts
    lib/netatmoProxy.ts
    lib/envValidator.ts
    __tests__/lib/envValidator.test.ts
  </read_first>
  <action>
1. Run `npx knip --include exports` to detect unused exports across the codebase.
2. Parse the knip output for any unused exports in these files:
   - `lib/fritzbox/fritzboxErrors.ts`
   - `lib/fritzbox/index.ts`
   - `lib/fritzbox/fritzboxClient.ts`
   - `lib/netatmoProxy.ts`
3. If knip reports unused exports:
   - Remove the unused export from the source file
   - Remove the corresponding barrel re-export from `lib/fritzbox/index.ts` if applicable
   - Do NOT delete entire files — only remove specific unused exports
4. If knip reports zero unused exports in these files, no code changes needed — task is a verification pass only.
5. Verify `lib/envValidator.ts` does NOT contain strings `NETATMO_PROXY_URL` or `NETATMO_PROXY_API_KEY` or `NETATMO_API_KEY` — it should only reference `HA_API_URL` and `HA_API_KEY`. Do NOT modify envValidator.ts (it is already clean per research).
6. Run `npm test -- --testPathPattern="envValidator|netatmoProxy|fritzbox" --no-coverage` to confirm no regressions.
7. Run `npx tsc --noEmit` to confirm zero type errors.

IMPORTANT: Do NOT delete `lib/fritzbox/fritzboxClient.ts`, `lib/fritzbox/index.ts`, `lib/fritzbox/fritzboxErrors.ts`, or `lib/netatmoProxy.ts` — they are active wrapper modules used by 27+ route files.
  </action>
  <verify>
    <automated>npx knip --include exports 2>&1 | grep -E "fritzbox|netatmoProxy" || echo "NO_UNUSED_EXPORTS_FOUND"; npm test -- --testPathPattern="envValidator|netatmoProxy|fritzbox" --no-coverage; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx knip --include exports` reports zero unused exports in lib/fritzbox/ and lib/netatmoProxy.ts
    - `lib/envValidator.ts` contains `HA_API_URL` and `HA_API_KEY` (grep confirms)
    - `lib/envValidator.ts` does NOT contain `NETATMO_PROXY_URL` or `NETATMO_PROXY_API_KEY` (grep confirms)
    - `npm test -- --testPathPattern="envValidator|netatmoProxy|fritzbox" --no-coverage` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All Fritz!Box and Netatmo wrapper modules have zero unused exports. envValidator confirmed clean. Tests and tsc pass.</done>
</task>

</tasks>

<verification>
- `npx knip --include exports` shows no unused exports in fritzbox/ or netatmoProxy.ts
- `npm test -- --testPathPattern="envValidator|netatmoProxy|fritzbox" --no-coverage` passes
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Zero unused exports in provider wrapper modules (confirmed by knip)
- envValidator.ts references only HA_API_URL/HA_API_KEY
- All tests pass, zero tsc errors
</success_criteria>

<output>
After completion, create `.planning/phases/87-client-cleanup/87-01-SUMMARY.md`
</output>
