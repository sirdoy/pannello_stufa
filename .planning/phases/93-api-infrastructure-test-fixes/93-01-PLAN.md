---
phase: 93-api-infrastructure-test-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/middleware.ts
  - lib/changelogService.ts
autonomous: true
requirements:
  - TFIX-01
  - TFIX-02

must_haves:
  truths:
    - "withIdempotency middleware caches and returns idempotent results via Firebase"
    - "syncVersionHistoryToFirebase logs completion message after syncing"
    - "saveVersionToFirebase logs confirmation after saving a version"
  artifacts:
    - path: "lib/core/middleware.ts"
      provides: "Static imports for firebase/database and @/lib/firebase used by withIdempotency"
      contains: "import { ref, get, set } from 'firebase/database'"
    - path: "lib/changelogService.ts"
      provides: "Console.log calls in saveVersionToFirebase and syncVersionHistoryToFirebase"
      contains: "VERSION_HISTORY sincronizzato con Firebase"
  key_links:
    - from: "lib/core/__tests__/middleware.test.ts"
      to: "lib/core/middleware.ts"
      via: "jest.mock intercepts static imports"
      pattern: "import.*ref.*get.*set.*from.*firebase/database"
    - from: "lib/__tests__/changelogService.test.ts"
      to: "lib/changelogService.ts"
      via: "console.log spy assertions"
      pattern: "console\\.log.*VERSION_HISTORY"
---

<objective>
Fix middleware.test.ts (TFIX-01) and changelogService.test.ts (TFIX-02) by updating source code.

Purpose: TFIX-01 fails because withIdempotency uses dynamic `await import()` which bypasses Jest's static mock registry. TFIX-02 fails because two changelog functions are missing console.log calls that tests assert on.
Output: 3 middleware tests + 4 changelog tests passing
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/93-api-infrastructure-test-fixes/93-CONTEXT.md
@.planning/phases/93-api-infrastructure-test-fixes/93-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert dynamic imports to static in withIdempotency (TFIX-01)</name>
  <files>lib/core/middleware.ts</files>
  <read_first>
    - lib/core/middleware.ts (full file — understand existing imports at top and dynamic imports on lines 282-283)
    - lib/core/__tests__/middleware.test.ts (understand mock setup and failing assertions)
  </read_first>
  <action>
In `lib/core/middleware.ts`, add two static import lines near the top of the file (after the existing imports around line 24-25):

```typescript
import { ref, get, set } from 'firebase/database';
import { db } from '@/lib/firebase';
```

Then inside the `withIdempotency` function body, remove these two dynamic import lines (currently around lines 282-283):

```typescript
// DELETE these two lines:
const { ref, get, set } = await import('firebase/database');
const { db } = await import('@/lib/firebase');
```

The rest of the function body remains unchanged — it already uses `ref`, `get`, `set`, and `db` correctly. The only change is WHERE these symbols are resolved (top-level static import vs. runtime dynamic import).

Root cause: Jest's `jest.mock()` intercepts static `import` statements via its module registry, but dynamic `await import()` inside function bodies may bypass the mock. Converting to static imports ensures the test's `jest.mock('firebase/database')` and `jest.mock('@/lib/firebase')` properly intercept these modules.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npx jest lib/core/__tests__/middleware.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/middleware.ts contains `import { ref, get, set } from 'firebase/database';` as a top-level import (not inside a function)
    - lib/core/middleware.ts contains `import { db } from '@/lib/firebase';` as a top-level import
    - lib/core/middleware.ts does NOT contain `await import('firebase/database')` anywhere
    - lib/core/middleware.ts does NOT contain `await import('@/lib/firebase')` anywhere
    - `npx jest lib/core/__tests__/middleware.test.ts --no-coverage` exits with code 0
    - All 3 withIdempotency tests pass: "executes handler and caches result", "still returns response even if cache write fails", "uses 1-hour TTL"
  </acceptance_criteria>
  <done>All 3 withIdempotency tests in middleware.test.ts pass. Static imports replace dynamic imports.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing console.log calls to changelogService (TFIX-02)</name>
  <files>lib/changelogService.ts</files>
  <read_first>
    - lib/changelogService.ts (full file — see saveVersionToFirebase and syncVersionHistoryToFirebase)
    - lib/__tests__/changelogService.test.ts (understand all console.log assertions)
  </read_first>
  <action>
In `lib/changelogService.ts`, add two missing `console.log` calls:

1. In `saveVersionToFirebase` (around line 20-38): Add after the `await set(versionRef, {...})` call and before the `} catch`:
```typescript
console.log(`Versione ${version} salvata su Firebase`);
```

2. In `syncVersionHistoryToFirebase` (around line 93-111): Add after the `for` loop completes and before the `} catch`:
```typescript
console.log('VERSION_HISTORY sincronizzato con Firebase');
```

These are the exact strings the tests assert on. No logic changes — just adding diagnostic logging that was removed during prior refactoring.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npx jest lib/__tests__/changelogService.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/changelogService.ts `saveVersionToFirebase` contains `console.log(`Versione ${version} salvata su Firebase`)`
    - lib/changelogService.ts `syncVersionHistoryToFirebase` contains `console.log('VERSION_HISTORY sincronizzato con Firebase')`
    - `npx jest lib/__tests__/changelogService.test.ts --no-coverage` exits with code 0
    - All 4 syncVersionHistoryToFirebase tests pass (sync, types, empty, continues on error)
    - The saveVersionToFirebase test asserting `console.log('Versione 1.2.3 salvata su Firebase')` also passes
  </acceptance_criteria>
  <done>All changelogService tests pass. Both saveVersionToFirebase and syncVersionHistoryToFirebase emit the expected log messages.</done>
</task>

</tasks>

<verification>
Run both test suites together:
```bash
npx jest lib/core/__tests__/middleware.test.ts lib/__tests__/changelogService.test.ts --no-coverage
```
All tests pass with 0 failures.
</verification>

<success_criteria>
- TFIX-01: All 3 withIdempotency tests in middleware.test.ts pass
- TFIX-02: All 4 syncVersionHistoryToFirebase tests + saveVersionToFirebase log test pass in changelogService.test.ts
- No other tests broken (verified by running full suite at phase end)
</success_criteria>

<output>
After completion, create `.planning/phases/93-api-infrastructure-test-fixes/93-01-SUMMARY.md`
</output>
