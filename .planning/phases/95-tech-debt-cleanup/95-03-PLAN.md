---
phase: 95-tech-debt-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.local
autonomous: true
requirements:
  - DEBT-02

must_haves:
  truths:
    - "No HOMEASSISTANT_* or NETATMO_CLIENT/REDIRECT env vars remain in .env.local"
    - "Active env vars (HA_API_URL, HA_API_KEY, etc.) are untouched"
    - "No source code references the deleted env vars"
  artifacts:
    - path: ".env.local"
      provides: "Clean environment configuration with only active vars"
      contains: "no HOMEASSISTANT_API_URL, HOMEASSISTANT_USER, HOMEASSISTANT_PASSWORD, NETATMO_CLIENT_ID, NETATMO_CLIENT_SECRET, NETATMO_REDIRECT_URI, NEXT_PUBLIC_NETATMO_CLIENT_ID, NEXT_PUBLIC_NETATMO_REDIRECT_URI"
  key_links: []
---

<objective>
Remove the 8 stale environment variables from .env.local that were left over from the pre-v11.0 Netatmo OAuth and HomeAssistant direct connection architecture. These vars have zero references in source code and are dead configuration.

Purpose: Eliminate dead configuration that could confuse future maintainers or cause accidental use of deprecated auth flows.
Output: .env.local edited with 8 lines removed.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-tech-debt-cleanup/95-CONTEXT.md
@.planning/phases/95-tech-debt-cleanup/95-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 8 stale env vars from .env.local</name>
  <files>.env.local</files>
  <read_first>
    .env.local
  </read_first>
  <action>
Delete the following 8 lines (key=value pairs) from `.env.local`:

1. `HOMEASSISTANT_API_URL="..."` — pre-v11.0 HA direct connection URL
2. `HOMEASSISTANT_USER="..."` — pre-v11.0 HA auth username
3. `HOMEASSISTANT_PASSWORD="..."` — pre-v11.0 HA auth password
4. `NETATMO_CLIENT_ID="..."` — pre-v10.0 Netatmo OAuth client ID
5. `NETATMO_CLIENT_SECRET="..."` — pre-v10.0 Netatmo OAuth client secret
6. `NETATMO_REDIRECT_URI="..."` — pre-v10.0 Netatmo OAuth redirect URI
7. `NEXT_PUBLIC_NETATMO_CLIENT_ID="..."` — pre-v10.0 Netatmo OAuth client-side ID
8. `NEXT_PUBLIC_NETATMO_REDIRECT_URI="..."` — pre-v10.0 Netatmo OAuth client-side redirect

Do NOT touch any other env vars. Specifically preserve:
- `HA_API_URL` — active shared client URL
- `HA_API_KEY` — active shared client API key
- All `NEXT_PUBLIC_*` vars except the two Netatmo ones listed above
- All Firebase, Auth0, and other active service vars

Do NOT edit `.env.example` (already clean per research).
Do NOT edit `.planning/` or `CHANGELOG.md` historical references.
Do NOT touch `NETATMO_ROUTES`, `NETATMO_CAMERA_API`, or `ERROR_CODES.NETATMO_*` — these are TypeScript constants, not env vars.

Also remove any blank lines or comments that were specifically associated with the deleted vars (e.g., a `# HomeAssistant` section header if it only contained these vars).
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -c "HOMEASSISTANT_\|NETATMO_CLIENT\|NETATMO_REDIRECT\|NEXT_PUBLIC_NETATMO" .env.local; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep "HOMEASSISTANT_API_URL" .env.local returns no matches
    - grep "HOMEASSISTANT_USER" .env.local returns no matches
    - grep "HOMEASSISTANT_PASSWORD" .env.local returns no matches
    - grep "NETATMO_CLIENT_ID" .env.local returns no matches
    - grep "NETATMO_CLIENT_SECRET" .env.local returns no matches
    - grep "NETATMO_REDIRECT_URI" .env.local returns no matches
    - grep "NEXT_PUBLIC_NETATMO_CLIENT_ID" .env.local returns no matches
    - grep "NEXT_PUBLIC_NETATMO_REDIRECT_URI" .env.local returns no matches
    - grep "HA_API_URL" .env.local returns a match (active var preserved)
    - grep "HA_API_KEY" .env.local returns a match (active var preserved)
  </acceptance_criteria>
  <done>All 8 stale env vars removed from .env.local, all active vars preserved</done>
</task>

</tasks>

<verification>
1. `grep -E "HOMEASSISTANT_|NETATMO_CLIENT|NETATMO_REDIRECT|NEXT_PUBLIC_NETATMO" .env.local` returns empty (zero matches)
2. `grep "HA_API_URL" .env.local` returns a match (active var intact)
3. `grep "HA_API_KEY" .env.local` returns a match (active var intact)
</verification>

<success_criteria>
- Zero stale HOMEASSISTANT_*/NETATMO_* env vars in .env.local
- All active env vars untouched
- DEBT-02 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/95-tech-debt-cleanup/95-03-SUMMARY.md`
</output>
