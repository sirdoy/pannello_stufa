---
phase: 96-polling-simplification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/devices/thermostat/ThermostatCard.tsx
  - app/components/devices/lights/hooks/useLightsData.ts
  - app/components/devices/network/hooks/useNetworkData.ts
  - app/components/devices/raspi/hooks/useRaspiData.ts
  - app/components/devices/raspi/hooks/useRaspiFullData.ts
  - lib/hooks/useDeviceStaleness.ts
autonomous: true
requirements:
  - POLL-04
  - POLL-05
  - POLL-06
  - POLL-07
  - POLL-08

must_haves:
  truths:
    - "ThermostatCard polls at 60s instead of 30s"
    - "LightsCard polls at 60s instead of 30s"
    - "NetworkCard polls at 60s visible / 5min hidden instead of 30s/5min"
    - "RaspiCard and RaspiFullData poll at 60s visible / 5min hidden instead of 30s/5min"
    - "useDeviceStaleness checks every 60s instead of 5s"
  artifacts:
    - path: "app/components/devices/thermostat/ThermostatCard.tsx"
      provides: "Thermostat polling at 60s"
      contains: "interval: topology ? 60000 : null"
    - path: "app/components/devices/lights/hooks/useLightsData.ts"
      provides: "Lights polling at 60s"
      contains: "interval: connected ? 60000 : null"
    - path: "app/components/devices/network/hooks/useNetworkData.ts"
      provides: "Network polling at 60s/5min"
      contains: "isVisible ? 60000 : 300000"
    - path: "app/components/devices/raspi/hooks/useRaspiData.ts"
      provides: "Raspi card polling at 60s/5min"
      contains: "isVisible ? 60000 : 300000"
    - path: "app/components/devices/raspi/hooks/useRaspiFullData.ts"
      provides: "Raspi full data polling at 60s/5min"
      contains: "isVisible ? 60000 : 300000"
    - path: "lib/hooks/useDeviceStaleness.ts"
      provides: "Staleness check at 60s"
      contains: "setInterval(fetchStaleness, 60000)"
  key_links:
    - from: "app/components/devices/thermostat/ThermostatCard.tsx"
      to: "lib/hooks/useAdaptivePolling.ts"
      via: "useAdaptivePolling call"
      pattern: "interval: topology \\? 60000"
    - from: "lib/hooks/useDeviceStaleness.ts"
      to: "lib/pwa/stalenessDetector.ts"
      via: "getDeviceStaleness call at 60s"
      pattern: "setInterval\\(fetchStaleness, 60000\\)"
---

<objective>
Change all remaining device hook polling intervals from 30s to 60s (thermostat, lights, network, raspi) and change useDeviceStaleness from 5s to 60s.

Purpose: Unify all device polling to 60-second intervals, reducing API call volume by 50% for non-stove devices and 92% for staleness checks (from 5s to 60s).

Output: Six files edited with interval constant changes, plus one comment update.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-polling-simplification/96-CONTEXT.md

<interfaces>
<!-- useAdaptivePolling signature (from lib/hooks/useAdaptivePolling.ts) -->
```typescript
export interface UseAdaptivePollingOptions {
  callback: () => void | Promise<void>;
  interval: number | null;
  alwaysActive?: boolean;
  immediate?: boolean;
  initialDelay?: number;
}
export function useAdaptivePolling(options: UseAdaptivePollingOptions): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all device hook polling intervals from 30s to 60s</name>
  <files>
    app/components/devices/thermostat/ThermostatCard.tsx,
    app/components/devices/lights/hooks/useLightsData.ts,
    app/components/devices/network/hooks/useNetworkData.ts,
    app/components/devices/raspi/hooks/useRaspiData.ts,
    app/components/devices/raspi/hooks/useRaspiFullData.ts
  </files>
  <read_first>
    app/components/devices/thermostat/ThermostatCard.tsx,
    app/components/devices/lights/hooks/useLightsData.ts,
    app/components/devices/network/hooks/useNetworkData.ts,
    app/components/devices/raspi/hooks/useRaspiData.ts,
    app/components/devices/raspi/hooks/useRaspiFullData.ts
  </read_first>
  <action>
**In `app/components/devices/thermostat/ThermostatCard.tsx` (line 109):**
- Change `interval: topology ? 30000 : null,` to `interval: topology ? 60000 : null,`
- Update the comment on line 105 from `// Poll status every 30 seconds` to `// Poll status every 60 seconds`
- PRESERVE `initialDelay: 50,` exactly as-is

**In `app/components/devices/lights/hooks/useLightsData.ts` (line 235):**
- Change `interval: connected ? 30000 : null,` to `interval: connected ? 60000 : null,`
- Update the comment on line 231 from `// Poll data every 30 seconds` to `// Poll data every 60 seconds`
- PRESERVE `initialDelay: 100,` exactly as-is

**In `app/components/devices/network/hooks/useNetworkData.ts` (line 94):**
- Change `const interval = isVisible ? 30000 : 300000;` to `const interval = isVisible ? 60000 : 300000;`
- Update the comment on line 92 from `// Visibility tracking for adaptive polling` — no change needed (it is generic)
- Update the `SPARKLINE_MAX_POINTS` comment on line 34 from `// 1h of data at 30s polling interval` to `// 2h of data at 60s polling interval`
- Keep `SPARKLINE_MAX_POINTS = 120` unchanged (2h of history is fine, more data is not harmful)

**In `app/components/devices/raspi/hooks/useRaspiData.ts` (line 46):**
- Change `const interval = isVisible ? 30000 : 300000;` to `const interval = isVisible ? 60000 : 300000;`

**In `app/components/devices/raspi/hooks/useRaspiFullData.ts` (line 42):**
- Change `const interval = isVisible ? 30000 : 300000;` to `const interval = isVisible ? 60000 : 300000;`
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -n "30000" app/components/devices/thermostat/ThermostatCard.tsx app/components/devices/lights/hooks/useLightsData.ts app/components/devices/network/hooks/useNetworkData.ts app/components/devices/raspi/hooks/useRaspiData.ts app/components/devices/raspi/hooks/useRaspiFullData.ts; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - ThermostatCard.tsx contains `interval: topology ? 60000 : null`
    - ThermostatCard.tsx does NOT contain `30000`
    - ThermostatCard.tsx contains `initialDelay: 50` (preserved)
    - useLightsData.ts contains `interval: connected ? 60000 : null`
    - useLightsData.ts does NOT contain `30000`
    - useLightsData.ts contains `initialDelay: 100` (preserved)
    - useNetworkData.ts contains `isVisible ? 60000 : 300000`
    - useNetworkData.ts does NOT contain `30000`
    - useNetworkData.ts contains `// 2h of data at 60s polling interval`
    - useRaspiData.ts contains `isVisible ? 60000 : 300000`
    - useRaspiData.ts does NOT contain `30000`
    - useRaspiFullData.ts contains `isVisible ? 60000 : 300000`
    - useRaspiFullData.ts does NOT contain `30000`
    - grep for `30000` across all 5 files returns no results
  </acceptance_criteria>
  <done>All five device hooks poll at 60s visible intervals, initialDelay stagger values preserved, SPARKLINE_MAX_POINTS comment updated</done>
</task>

<task type="auto">
  <name>Task 2: Update useDeviceStaleness from 5s to 60s interval</name>
  <files>
    lib/hooks/useDeviceStaleness.ts
  </files>
  <read_first>
    lib/hooks/useDeviceStaleness.ts
  </read_first>
  <action>
**In `lib/hooks/useDeviceStaleness.ts` (line 63):**
- Change `const intervalId = setInterval(fetchStaleness, 5000);` to `const intervalId = setInterval(fetchStaleness, 60000);`
- Update the JSDoc comment at the top: change `Polls every 5 seconds` to `Polls every 60 seconds`
- Update the inline comment on line 62: change `// Poll every 5 seconds while visible` to `// Poll every 60 seconds while visible`
- Keep the `useVisibility()` guard and the `isVisible` dependency — no structural changes
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -n "5000\|setInterval" lib/hooks/useDeviceStaleness.ts</automated>
  </verify>
  <acceptance_criteria>
    - useDeviceStaleness.ts contains `setInterval(fetchStaleness, 60000)`
    - useDeviceStaleness.ts does NOT contain `5000`
    - useDeviceStaleness.ts contains `Polls every 60 seconds`
    - useDeviceStaleness.ts contains `// Poll every 60 seconds while visible`
  </acceptance_criteria>
  <done>useDeviceStaleness polls at 60s instead of 5s, aligned with device polling cadence</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -rn "30000" app/components/devices/thermostat/ThermostatCard.tsx app/components/devices/lights/hooks/useLightsData.ts app/components/devices/network/hooks/useNetworkData.ts app/components/devices/raspi/hooks/useRaspiData.ts app/components/devices/raspi/hooks/useRaspiFullData.ts` — no results
2. `grep "5000" lib/hooks/useDeviceStaleness.ts` — no results
3. `grep "60000" app/components/devices/thermostat/ThermostatCard.tsx app/components/devices/lights/hooks/useLightsData.ts app/components/devices/network/hooks/useNetworkData.ts app/components/devices/raspi/hooks/useRaspiData.ts app/components/devices/raspi/hooks/useRaspiFullData.ts lib/hooks/useDeviceStaleness.ts` — all 6 files have 60000
4. `npm test -- --testPathPattern="useNetworkData" --no-coverage` — passes
</verification>

<success_criteria>
- All 5 device hooks use 60000ms visible interval (was 30000ms)
- useDeviceStaleness uses 60000ms interval (was 5000ms)
- All initialDelay stagger values preserved unchanged
- SPARKLINE_MAX_POINTS remains 120 with updated comment
- Existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/96-polling-simplification/96-02-SUMMARY.md`
</output>
