---
phase: 98-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/devices/raspi/hooks/__tests__/useRaspiFullData.test.ts
  - tests/smoke/page-loads.spec.ts
  - app/components/devices/stove/StoveCard.tsx
  - app/stove/page.tsx
  - app/components/devices/lights/hooks/useLightsData.ts
  - app/stove/components/StovePageBanners.tsx
  - .planning/phases/96-polling-simplification/96-01-SUMMARY.md
  - .planning/phases/96-polling-simplification/96-02-SUMMARY.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "useRaspiFullData test asserts 60000ms for visible interval, matching the implementation"
    - "Playwright smoke test file has no uncommitted changes"
    - "No JSDoc in stove or lights files references Firebase or 30s intervals"
    - "96-01 and 96-02 SUMMARY files have populated requirements_completed arrays"
  artifacts:
    - path: "app/components/devices/raspi/hooks/__tests__/useRaspiFullData.test.ts"
      provides: "Corrected polling interval assertion"
      contains: "toBe(60000)"
    - path: "tests/smoke/page-loads.spec.ts"
      provides: "Committed Playwright test fixes"
    - path: "app/components/devices/stove/StoveCard.tsx"
      provides: "Updated JSDoc without Firebase reference"
    - path: "app/stove/page.tsx"
      provides: "Updated JSDoc without Firebase reference"
    - path: "app/components/devices/lights/hooks/useLightsData.ts"
      provides: "Updated JSDoc with 60s interval"
    - path: "app/stove/components/StovePageBanners.tsx"
      provides: "Updated JSDoc without Firebase connection status reference"
    - path: ".planning/phases/96-polling-simplification/96-01-SUMMARY.md"
      provides: "Populated requirements_completed"
    - path: ".planning/phases/96-polling-simplification/96-02-SUMMARY.md"
      provides: "Populated requirements_completed"
  key_links: []
---

<objective>
Fix all integration gaps and tech debt identified by the v12.0 milestone audit.

Purpose: Close the v12.0 milestone by fixing a stale test assertion, committing Playwright test fixes, cleaning stale JSDoc references, and populating SUMMARY frontmatter.
Output: All 7 audit items resolved, milestone ready for completion.
</objective>

<execution_context>
@/Users/federicomanfredi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/federicomanfredi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v12.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale test assertion and commit Playwright changes</name>
  <files>
    app/components/devices/raspi/hooks/__tests__/useRaspiFullData.test.ts
    tests/smoke/page-loads.spec.ts
  </files>
  <read_first>
    app/components/devices/raspi/hooks/__tests__/useRaspiFullData.test.ts
    app/components/devices/raspi/hooks/useRaspiFullData.ts
    tests/smoke/page-loads.spec.ts
  </read_first>
  <action>
1. In `useRaspiFullData.test.ts`:
   - Line 183: Change test description from "30s visible, 300s hidden" to "60s visible, 300s hidden"
   - Line 191: Change `toBe(30000)` to `toBe(60000)`

2. Stage and include the already-modified `tests/smoke/page-loads.spec.ts` (working tree has selector fixes, axe-core filter, waitUntil changes from audit session — these are correct and must be preserved).

Both files should be staged together for commit in the plan's final commit.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && npx jest app/components/devices/raspi/hooks/__tests__/useRaspiFullData.test.ts --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "60s visible" useRaspiFullData.test.ts returns a match on the test description
    - grep "toBe(60000)" useRaspiFullData.test.ts returns a match on line 191
    - grep "toBe(30000)" useRaspiFullData.test.ts returns NO matches
    - git diff tests/smoke/page-loads.spec.ts shows no changes (file is committed)
  </acceptance_criteria>
  <done>useRaspiFullData test passes with 60000ms assertion; Playwright test file committed</done>
</task>

<task type="auto">
  <name>Task 2: Clean stale JSDoc references and populate SUMMARY frontmatter</name>
  <files>
    app/components/devices/stove/StoveCard.tsx
    app/stove/page.tsx
    app/components/devices/lights/hooks/useLightsData.ts
    app/stove/components/StovePageBanners.tsx
    .planning/phases/96-polling-simplification/96-01-SUMMARY.md
    .planning/phases/96-polling-simplification/96-02-SUMMARY.md
  </files>
  <read_first>
    app/components/devices/stove/StoveCard.tsx
    app/stove/page.tsx
    app/components/devices/lights/hooks/useLightsData.ts
    app/stove/components/StovePageBanners.tsx
    .planning/phases/96-polling-simplification/96-01-SUMMARY.md
    .planning/phases/96-polling-simplification/96-02-SUMMARY.md
  </read_first>
  <action>
1. `StoveCard.tsx` line 26: Change `- useStoveData: All state management + polling + Firebase` to `- useStoveData: All state management + polling via useAdaptivePolling`

2. `app/stove/page.tsx` line 33: Change `// Reuse StoveCard hooks - SAME state management, polling, Firebase` to `// Reuse StoveCard hooks - SAME state management and polling via useAdaptivePolling`

3. `useLightsData.ts` line 5: Change `- Polling via useAdaptivePolling (30s interval)` to `- Polling via useAdaptivePolling (60s interval)`

4. `StovePageBanners.tsx` lines 7-8: Change:
   ```
    * - Firebase connection status
    * - Pending commands queue
   ```
   to:
   ```
    * - Staleness status
    * - Pending commands queue
   ```

5. `96-01-SUMMARY.md`: Add `requirements_completed: [POLL-01, POLL-02, POLL-03, POLL-08]` to the frontmatter (after the `tags` line). These are the requirements 96-01 addressed: stove polling (POLL-01), Firebase removal (POLL-02), sync-external-state removal (POLL-03), and useDeviceStaleness rewrite (POLL-08).

6. `96-02-SUMMARY.md`: Add `requirements_completed: [POLL-04, POLL-05, POLL-06, POLL-07]` to the frontmatter (after the `tags` line). These are the requirements 96-02 addressed: thermostat (POLL-04), lights (POLL-05), network (POLL-06), and raspi (POLL-07) polling updates.
  </action>
  <verify>
    <automated>cd /Users/federicomanfredi/Sites/localhost/pannello-stufa && grep -n "Firebase" app/components/devices/stove/StoveCard.tsx app/stove/page.tsx app/stove/components/StovePageBanners.tsx 2>/dev/null; echo "---"; grep "30s" app/components/devices/lights/hooks/useLightsData.ts 2>/dev/null; echo "---"; grep "requirements_completed" .planning/phases/96-polling-simplification/96-01-SUMMARY.md .planning/phases/96-polling-simplification/96-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Firebase" across StoveCard.tsx, stove/page.tsx, StovePageBanners.tsx returns NO matches
    - grep "30s" useLightsData.ts returns NO matches
    - grep "useAdaptivePolling" StoveCard.tsx returns a match in the JSDoc
    - grep "useAdaptivePolling" app/stove/page.tsx returns a match in the comment
    - grep "60s interval" useLightsData.ts returns a match
    - grep "Staleness status" StovePageBanners.tsx returns a match
    - grep "requirements_completed" 96-01-SUMMARY.md returns line containing POLL-01, POLL-02, POLL-03, POLL-08
    - grep "requirements_completed" 96-02-SUMMARY.md returns line containing POLL-04, POLL-05, POLL-06, POLL-07
  </acceptance_criteria>
  <done>All stale JSDoc cleaned; both SUMMARY files have populated requirements_completed arrays</done>
</task>

</tasks>

<verification>
1. `npx jest useRaspiFullData.test.ts --no-coverage` passes (60000ms assertion)
2. `git diff tests/smoke/page-loads.spec.ts` is empty (committed)
3. `grep -r "Firebase" app/components/devices/stove/StoveCard.tsx app/stove/page.tsx app/stove/components/StovePageBanners.tsx` returns nothing
4. `grep "30s" app/components/devices/lights/hooks/useLightsData.ts` returns nothing
5. `grep "requirements_completed" .planning/phases/96-polling-simplification/96-0{1,2}-SUMMARY.md` shows populated arrays
</verification>

<success_criteria>
All 7 v12.0 audit items resolved: stale test fixed, Playwright committed, 4 JSDoc comments updated, 2 SUMMARY frontmatter populated. Milestone ready for completion.
</success_criteria>

<output>
After completion, create `.planning/phases/98-gap-closure/98-01-SUMMARY.md`
</output>
