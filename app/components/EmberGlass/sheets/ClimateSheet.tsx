'use client';
// TODO Plan 178-05: replace stub with real ClimateSheet implementation per UI-SPEC + PATTERNS.
/** Stub — Plan 178-05 ships the real implementation. */
export function ClimateSheet() {
  return <div data-testid="climate-sheet-stub" />;
}
