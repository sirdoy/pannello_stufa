'use client';
// TODO Plan 178-06: replace stub with real LightsSheet implementation per UI-SPEC + PATTERNS.
/** Stub — Plan 178-06 ships the real implementation. */
export function LightsSheet() {
  return <div data-testid="lights-sheet-stub" />;
}
