'use client';
// TODO Plan 178-08: replace stub with real PlugsSheet implementation per UI-SPEC + PATTERNS.
/** Stub — Plan 178-08 ships the real implementation. */
export function PlugsSheet() {
  return <div data-testid="plugs-sheet-stub" />;
}
