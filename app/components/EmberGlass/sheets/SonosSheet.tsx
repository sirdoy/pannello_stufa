'use client';
// TODO Plan 178-07: replace stub with real SonosSheet implementation per UI-SPEC + PATTERNS.
/** Stub — Plan 178-07 ships the real implementation. */
export function SonosSheet() {
  return <div data-testid="sonos-sheet-stub" />;
}
