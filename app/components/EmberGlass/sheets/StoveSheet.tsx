'use client';
// TODO Plan 178-04: replace stub with real StoveSheet implementation per UI-SPEC + PATTERNS.
/** Stub — Plan 178-04 ships the real implementation. */
export function StoveSheet() {
  return <div data-testid="stove-sheet-stub" />;
}
