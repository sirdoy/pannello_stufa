// TODO Plan 178-01: replace stub with verbatim port of bundle sheets.jsx:299-306 (`quickBtn`).
// Stub exists ONLY so the sheets barrel (Plan 178-02) compiles in parallel-wave worktree.
export interface QuickActionButtonProps {
  active: boolean;
  onClick: () => void;
  label: string;
}

export function QuickActionButton(_props: QuickActionButtonProps) {
  return null;
}
