// TODO Plan 178-01: replace stub with verbatim port of bundle sheets.jsx:536-579.
// Stub exists ONLY so the sheets barrel (Plan 178-02) compiles in parallel-wave worktree.
export interface RadialDialProps {
  value: number;
  min: number;
  max: number;
  onChange: (next: number) => void;
  color: string;
  label: string;
}

export function RadialDial(_props: RadialDialProps) {
  return null;
}
