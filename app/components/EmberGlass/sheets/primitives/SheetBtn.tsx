import type { LucideIcon } from 'lucide-react';

// TODO Plan 178-01: replace stub with verbatim port of bundle sheets.jsx:581-592.
// Stub exists ONLY so the sheets barrel (Plan 178-02) compiles in parallel-wave worktree.
export interface SheetBtnProps {
  Icon: LucideIcon;
  label: string;
  onClick?: () => void;
}

export function SheetBtn(_props: SheetBtnProps) {
  return null;
}
