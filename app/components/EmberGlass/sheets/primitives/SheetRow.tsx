import type { ReactNode } from 'react';

// TODO Plan 178-01: replace stub with verbatim port of bundle sheets.jsx:469-482.
// Stub exists ONLY so the sheets barrel (Plan 178-02) compiles in parallel-wave worktree;
// Plan 178-01 ships the real implementation. Signature must match SheetRowProps below.
export interface SheetRowProps {
  label: string;
  value?: string;
  children?: ReactNode;
}

export function SheetRow(_props: SheetRowProps) {
  return null;
}
