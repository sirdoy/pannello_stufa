// TODO Plan 178-01: replace stub with verbatim port of bundle sheets.jsx:502-513.
// Stub exists ONLY so the sheets barrel (Plan 178-02) compiles in parallel-wave worktree.
export interface SliderProps {
  value: number;
  min: number;
  max: number;
  onChange: (next: number) => void;
  color?: string;
}

export function Slider(_props: SliderProps) {
  return null;
}
