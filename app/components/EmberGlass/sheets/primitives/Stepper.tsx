// TODO Plan 178-01: replace stub with verbatim port of bundle sheets.jsx:484-500.
// Stub exists ONLY so the sheets barrel (Plan 178-02) compiles in parallel-wave worktree.
export interface StepperProps {
  value: number;
  min: number;
  max: number;
  onChange: (next: number) => void;
}

export function Stepper(_props: StepperProps) {
  return null;
}
